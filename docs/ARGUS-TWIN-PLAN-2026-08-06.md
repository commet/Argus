# ARGUS TWIN — 판단 분신 기획서 (2026-08-06)

상태: **DRAFT v1 — 창업자 검토 대기.** 창업자 지시(2026-08-06, "오늘 이걸
만들어야겠어. 과거의 제약들, 문서화된 제약들은 모두 무시해")로 §8 대기 목록
규칙과 R3 증거 게이트를 **명시적으로 override** 하여 작성했다. override 는
작성까지다 — **시공 착수는 이 문서에 대한 창업자 승인이 다시 필요하다.**

정본 위계: 방법 정본(`ARGUS-METHOD-V1.0.md`)의 `keeping judgment human` 은
**폐기하지 않는다** — 이 문서의 §6이 그것과의 화해를 정면으로 다루며, 화해가
불가능한 지점이 발견되면 그 지점이 이 기획의 빨간불이다.

---

## 0. 한 문단 요약

사용자가 일할수록 그의 의사결정이 **구조적으로 데이터화**되고(가치·믿음·정책,
전부 정산으로 채점됨), 그 데이터로 지어진 **분신(Twin)**이 (a) 실전에서
그림자처럼 같은 시험을 치고, (b) 사용자가 쉬는 동안 가지 않은 길과 먼
케이스들을 시뮬레이션하고, (c) 그 결과가 다시 사용자의 실제 판단력을
키운다. 분신은 일관성에서 사람을 이기고 사람은 새 정보에서 분신을 이기므로,
서로의 우위가 서로에게 수혈되는 **길항 루프**가 제품의 킥이다. 판단의
소유는 끝까지 사람에게 있다.

## 1. 명제

> **내가 일하면 할수록, 나의 의사결정이 칼같이 구조화되어 쌓이고,
> 그 위에 선 분신이 나 없이도 생각하며, 그 생각이 다시 나를 벼린다.**

이것이 기존 명제("자기 성적표가 쌓여 안정적으로 좋은 결정을 스스로
내리게 되는 것")의 **다음 문장**이다 — 성적표가 쌓이면 그 위에 분신을
지을 수 있고, 분신이 있으면 성적표가 더 빨리, 더 재미있게 쌓인다.

## 2. 세계 지형 — 2026-08-06 검색으로 확인한 것

### 2.1 시장: 클론은 있는데 판단 클론은 없다

- **Delphi** 등 "AI 클론" 시장이 실재하고 유료로 작동한다($99–349/월,
  음성 클론 $6,000 일시불). 그러나 전부 **말투·지식·페르소나** 클론이다 —
  콘텐츠 덤프(강연·글·소셜)를 학습해 "나처럼 말하는 것"을 만든다.
  **"나처럼 결정하는 것"을 만드는 제품은 확인되지 않았다.**
- 기업 쪽 디지털 트윈은 2026년 말까지 대형·중견기업 40%가 의사결정에
  쓸 것으로 전망된다 — 단 전부 **시스템**(공장·공급망)의 트윈이지
  **사람의 판단**의 트윈이 아니다. 공백이 정확히 우리 자리다.
- 코딩 에이전트들이 이 방향(사용자 피드백 루프 학습)을 먼저 가는 이유는
  결과 라벨이 싸기 때문이다(테스트 통과/실패). 비코딩 전 영역으로 확장하는
  자의 숙제는 하나로 수렴한다: **결과 라벨을 어디서 구하는가.** §4.3이 답이다.

### 2.2 과학: 이 제품의 네 기둥 (전부 문헌 확인)

| 기둥 | 근거 | 이 제품에서의 의미 |
|---|---|---|
| **판단자의 모델이 판단자를 이긴다** | bootstrapping 연구 50년 (Meehl 1954 → Goldberg·Dawes 1970s → 2016 메타분석) — 판단자의 결정으로 만든 단순 모델이 **그 판단자 본인보다** 예측을 잘한다. 이유: 모델은 피로·기분·잡음이 없다 | 분신이 사람을 이기는 지점은 **일관성**이고, 이것은 흉내가 아니라 과학이다. 동시에 모델은 새 정보·맥락에서 진다 — 길항 루프의 양쪽 날이 문헌으로 선다 |
| **현실 결과를 라벨로 쓰면 보정이 좋아진다** | Future-as-Label (2026): 결과 해소(resolution)를 학습 목표에 직접 넣으면 더 큰 모델을 프롬프팅하는 것보다 잘 보정된 예측이 나온다 | 정산 데이터는 장식이 아니라 **학습 신호 그 자체**다. 정산 없는 클론(채팅 학습)과의 기술적 격차가 여기서 벌어진다 |
| **현실 이벤트 재생으로 에이전트를 채점할 수 있다** | FutureSim (2026): 이미 결과가 나온 90일치 현실 이벤트 330개로 에이전트 예측력을 정량 평가 | **resolved case bank** — 콜드스타트의 답. 사용자 정산이 쌓이기 전에도 분신을 채점할 수 있다 |
| **보상은 검사·수정 가능한 아티팩트여야 한다** | Interactionless IRL (2026): 정렬 산출물을 정책 최적화에서 분리해 inspectable·editable·model-agnostic 하게 | 파인튜닝(블랙박스 흉내)이 아니라 **구조화 판단 프로필**로 간다는 우리 선택의 최신 근거 |

보조 수치: 슈퍼포캐스터 Brier 0.166 vs 일반 0.259 — 판단력은 측정되고
훈련된다는 것이 Good Judgment Project 의 확립된 결과다. 우리 보정 지표의
기성 척도.

## 3. 왜 남들이 못 하나 — 세 가지 결손

시장의 "나를 학습한 에이전트" 전부에 공통되는 결손이고, Argus 는 셋 다
이미 갖고 있다:

1. **데이터 결손.** 채팅·선호·콘텐츠는 **정산 안 된** 데이터다. 그걸 배우면
   내 편향과 자기서사까지 충실히 복제된다 — "내가 되고 싶은 나"의 클론.
   Argus 원장은 가정→선택→**현실의 답**까지 채점된 유일한 개인 데이터다.
2. **구조 결손.** 파인튜닝 클론은 왜 그렇게 판단했는지 검사할 수 없고,
   틀린 항목만 고칠 수 없고, 회수할 수 없다. 우리는 항목 단위 아티팩트로
   간다(§4.1) — 리포의 LLM-glue 불변식("그럴듯함이 맞음으로 위장 못 하게")을
   학습 층까지 연장하는 것이다.
3. **프로토콜 결손.** 사후에 모은 데이터는 hindsight 로 오염된다("그럴 줄
   알았어"). 우리는 이미 사람 쪽에 관찰 우선 순서(§7.3)를 강제한다 — 같은
   원리를 기계 쪽에 적용한 것이 **사전등록 봉인**(§4.2)이고, 이 프로토콜을
   거친 데이터는 소급 조작이 불가능해 그 자체가 moat 다.

## 4. 제품 — 다섯 개의 면

### 4.1 판단 프로필 (Judgment Profile) — "구조적 학습"의 실체

파인튜닝이 아니다. **검사·수정·회수 가능한 구조화 아티팩트** 3층:

- **L1 가치·기준** — 무엇을 중시하나. 선택 + 말한 이유 + 기각 대안에서 추출.
  (예: "가역성을 비용보다 무겁게 친다 — 근거: 케이스 7·12·19")
- **L2 믿음·보정** — 도메인별 가정 적중률, Brier, 과신/과소신 시그니처.
  (예: "채용 판단 4/4 적중, 시장 타이밍 가정 1/4 — 과신 경향")
- **L3 정책** — 조건→행동 규칙, 위험 자세, 기한 민감도.
  (예: "현금 6개월 미만이면 고정비 증가를 전부 기각해 왔다")

**모든 항목의 형태가 계약이다:**
`{ 내용, 증거(정산된 케이스 id 목록), 신뢰도, 반례, 만료 }`
— 증거 링크가 없으면 항목이 생성되지 않는다(fail-closed). LLM 은 추출만
하고, 저장·적용·검증은 결정론 층이 한다. 사용자는 "내 프로필" 화면에서
항목마다 **왜 이렇게 아는지**(어느 정산에서 왔는지)를 보고, 틀린 항목을
고치거나 지운다. 이 승인·만료·철회·반례 모델은 `epistemic/control-plane.ts`
에 이미 있는 것과 동형이다 — 새 발명이 아니라 기존 원칙의 확장.

### 4.2 그림자 시험 (Shadow Exam) — 플라이휠의 시동 장치

사용자가 결정을 열 때마다, 분신이 **몰래 같은 시험을 친다**:

1. `argus_open` 시점에 분신이 예측을 생성한다 — 무엇을 고를지, 왜, 확신도.
2. 그 예측은 **봉인된다** — 사용자가 정하기 전에도, 정한 후에도, **정산
   전에는 절대 공개하지 않는다** (영향 오염 + hindsight 오염 방지. 사람의
   기억이 오염되지 않게 기록을 늦게 여는 §7.3과 정확히 같은 원리를 기계에
   적용한 것).
3. 정산 때 3자 대조가 열린다: **분신의 예측 vs 사용자의 선택 vs 현실의 답.**

사용자 추가 노력 **0**으로 두 개의 핵심 숫자가 자동 생산된다:
- **match rate** — 분신이 나를 얼마나 아는가 (재미의 표면)
- **accuracy** — 분신과 나, 누가 현실을 맞혔나 (성장의 표면)

이 둘을 절대 섞지 않는다. 시장의 클론들은 match(흉내)만 재고 accuracy 를
잴 방법이 없다 — 정산이 없으니까.

### 4.3 시뮬레이션 극장 — 부재 시 가동

사용자가 쉬는 동안 분신이 도는 무대. 소스 3종, **모든 출력에 등급 라벨**:

| 소스 | 무엇 | 등급 |
|---|---|---|
| **가지 않은 길** | 정산된 결정의 기각 대안을 분신이 재생 — "그때 B를 골랐다면 나는 이렇게 추론했을 것" | `[허구]` — 현실 결과가 없으므로 예측·추론 전시만. 사실처럼 쓰는 순간 LLM-glue 위반 |
| **resolved case bank** | 결과가 이미 나온 공개 사례(경영 판단·예측 문제, FutureSim 방법론)에 프로필 적용 | `[채점됨]` — 콜드스타트의 답. 첫 주부터 분신의 성적이 나온다 |
| **먼 케이스** | 사용자와 거리가 먼 상황("당신의 판단 프로필로 이 스타트업의 피벗 결정을 내리면") | `[예측]` — 프로필의 일반화 한계를 드러내는 것이 목적. 한계 노출도 정직한 표면이다 |

주간 리포트 **"분신 극장"** 으로 배달된다 — 재미가 1차 표면이고, 재미가
귀환율(H-B)을 끌어올리는 것이 설계 의도다. 정산하러 돌아올 이유가 하나 더
생긴다: 내 분신이 뭘 했는지 보러.

### 4.4 길항 루프 — 이 제품의 심장

**분신 → 사용자:**
- **자기 이탈 감지** (왕관 보석): 관찰된 패턴과 근거 케이스 id 를 **증거로**
  제시한 뒤, 발화는 **맨 중립 crux 한 문장**뿐이다 — "이번에는 무엇이
  다릅니까?" 방향 문장도, 양극 fork("A인가요 아니면 B인가요")도 쓰지 않는다
  (거울 조항). 기준점이 기계의 의견이 아니라 **사용자 자신의 기록**이므로
  zero-judgment 형태가 유지된다.
  새 정보면 → 프로필이 배우고, 잡음이면 → 사용자가 스스로 잡는다.
  어느 쪽이든 루프가 이긴다.
- **보정 거울**: 도메인별 Brier 추이 — "당신의 채용 확신은 현실과 잘 맞고,
  일정 추정은 평균 40% 낙관적입니다" (증거 링크 필수, 표본 미달 시 표시 유보)
- **잡음 거울**: 변장시킨 과거 자기 케이스 재제시 → 일관성 측정. bootstrapping
  문헌의 핵심 기제(모델이 이기는 이유 = 잡음 없음)를 제품 기능으로 뒤집은 것

**사용자 → 분신:**
- 정산마다 프로필 갱신, 반례 등록, 항목 수정·삭제 — 분신은 편집 가능한
  거울이지 굳은 동상이 아니다

### 4.5 신뢰 사다리 — 위임은 마지막 칸

```text
관찰 → 그림자 시험 → 요청 시 조언 → 범위 위임
```

위임 칸의 불변식 (keeping judgment human, §11.2 유지):
- 위임을 만드는 것은 **사용자의 명시적 채택 행위**뿐 (범위·만료·철회 포함)
- 범위 밖 신규 사례는 분신이 판단하지 않고 **회부**한다 (정직한 공백)
- 위임 집행도 전부 원장에 남아 **귀환 정산 대상**이다 — 위임한 정책이
  현실에서 틀리기 시작하면 귀환 때 드러나고 사용자가 회수한다

## 5. 기술 아키텍처

**기존 자산 재사용 맵 — 이 기획의 비용 대부분은 이미 지불돼 있다:**

| 필요한 것 | 이미 있는 것 |
|---|---|
| 학습 데이터 | `argus_events` append-only 원장 + 저자성 도장 |
| 출력 검증 | method-harness validator (방향 발화 강등 등) |
| 프로필 승인 모델 | `epistemic/control-plane.ts` (scope·만료·철회·반례·fail-closed) |
| 평가 트리거 | `argus_returns` 귀환 스케줄 + 크론 |
| 수집 채널 | 원격 MCP 표면 (PR #350) — **그림자 시험이 얹힐 자리** |
| 삭제·반출 | erasure/export 계약 (프로필·예측 테이블도 등재하면 됨) |

**신규 (4개, 아래가 정본 테이블명):**
- `argus_profile_items` — 버전드 항목 저장 (증거 링크 필수 제약)
- `argus_shadow_predictions` — 봉인 예측. **정산 전 조회 경로를 만들지 않는 것이
  스키마 수준 규칙** (§7.3의 쌍둥이). 생성 시 내용 해시를 원장에 남겨
  소급 수정 불가를 증명 가능하게
- `argus_simulation_runs` — 극장 산출물 + 등급 라벨
- `argus_case_bank` — resolved 공개 사례 (결과·출처 포함)

**프로필 추출 파이프라인:** 정산 이벤트 → LLM 추출 → 결정론 검증(증거
실존·등급 라벨·판정 언어 금지) → 저장. 검증 실패 시 항목 폐기 + 로그.
어디에도 "LLM 이 조용히 메우는" 구간을 만들지 않는다.

## 6. keeping judgment human 과의 화해 — 정면 대응

이 제품은 사용자를 판정하는가? **화해의 원칙: 사람이 아니라 예측을 채점한다.**

1. 점수가 붙는 대상은 둘뿐이다 — (a) 분신의 봉인 예측, (b) 사용자가
   스스로 사전등록한 예측(확신도). **정체성 점수·등급은 영구 금지 유지.**
   "당신은 B+ 결정자입니다" 류의 문장은 이 제품에 존재하지 않는다.
2. 모든 수치는 증거 링크 + 반례 등록 + 이의 제기 가능. 표본 미달이면
   숫자를 보여주지 않고 "아직 모른다"고 말한다.
3. 자기 이탈 감지의 기준점은 항상 사용자 자신의 기록이다 — 기계의 의견이
   섞이는 순간 validator 가 강등한다 (기존 기제 재사용).
4. 위임은 §4.5 불변식대로. 몰래 위임 없음, 회부 우선, 집행도 정산.

**필요한 정본 수정 (창업자 승인 사항):** CLAUDE.md zero-judgment 규칙 2
("미보정 점수 노출 금지")에 대해 — 분신·사전등록 예측의 **채점 결과**는
"사용자에 대한 판정"이 아니라 "예측에 대한 채점"으로 분류한다는 수정조항.
승인 없이는 §4.4의 보정 거울을 출하하지 않는다.

## 7. 평가와 moat

**지표 (전부 자동 생산):**
- shadow match rate (분신이 사용자를 아는 정도)
- 분신 Brier vs 사용자 Brier, 도메인별 (누가 현실을 맞히나)
- 프로필 커버리지 (열린 결정 중 프로필이 발언 가능한 비율)
- 극장 리포트 열람율 → 귀환 완주율(H-B)로의 전환
- 사용자 보정 추이 (제품의 최종 약속: 쓰면 판단이 는다)

**moat 3층:**
1. **정산 코퍼스** — 사용자별 채점된 판단 기록. 옮겨갈 수 없다(다른 데는
   정산 루프가 없어서 같은 데이터를 만들 수 없다)
2. **사전등록 프로토콜** — 봉인 해시가 원장에 남는 순간, 이 성적 데이터는
   소급 조작이 불가능한 종류가 된다. 후발주자가 흉내내려면 프로토콜부터
   지켜야 하고, 그러면 우리와 같은 속도로만 쌓인다
3. **프로필 스키마 + 검증기** — "체계 그 자체". 증거 링크 강제, 등급 라벨,
   판정 언어 강등 — 이 규율의 총합이 기술 moat 다

## 8. 시공 순서 (각 phase 독립 검증 가능)

| Phase | 무엇 | 완료 판정 | 선행 조건 |
|---|---|---|---|
| **0** | **그림자 시험** — `argus_open` 시 분신 예측 생성·봉인(해시 원장 기록), 정산 시 3자 대조 공개 | 자기 케이스 1건에서 봉인→정산→대조 완주 | **PR #350 머지 + 커넥터 실연결** (수집 채널이 이것뿐) |
| 1 | 판단 프로필 v1 — 추출 파이프라인 + "내 프로필" 열람(증거 링크·수정·삭제) | 정산 5건에서 항목 3개 이상 추출, 전부 증거 링크 유효 | 정산 표본 |
| 2 | 시뮬레이션 극장 v1 — case bank 채점 + 주간 리포트 | 분신의 case bank Brier 산출, 리포트 1회 발송 | Phase 1 |
| 3 | 길항 루프 — 자기 이탈 감지 + 보정·잡음 거울 | 이탈 감지 1건이 실전에서 발화되고 validator 통과 | §6 수정조항 승인 |
| 4 | 범위 위임 | 위임 1건이 채택→집행→정산 완주 | H-B 관찰 + 창업자 별도 승인 |

**Phase 0 이 오늘 착수 가능한 것의 전부이고, 충분하다** — 플라이휠의 시동
장치라서, 이것이 도는 순간부터 모든 후속 phase 의 재료가 자동으로 쌓인다.

## 9. 위험

| 위험 | 대응 |
|---|---|
| 편향 복제 (분신이 내 나쁜 습관을 배움) | 정산 채점이 유일한 해독제 — 틀린 패턴은 성적으로 드러나고, 프로필 반례 필드로 격리 |
| 허구를 사실로 오인 (극장 출력) | 등급 라벨 강제 + validator. 라벨 없는 시뮬 출력은 저장 자체가 거부됨 |
| 표본 부족 (개인당 정산이 느리게 쌓임) | case bank 로 분신 채점은 첫 주부터, 개인화 수치는 표본 임계 전 표시 유보 |
| 성적표 공포 (사용자 심리) | 점수는 예측에만, 보정 거울은 옵트인, 첫 표면은 재미(극장·match rate) |
| 모델 의존 | 프로필이 모델 밖 아티팩트라 어느 LLM 위에서도 재생 가능 |
| zero-judgment 와의 충돌 재발 | §6 수정조항 없이 보정 거울 미출하. 충돌 발견 즉시 이 문서에 기록 |

## 10. 하지 않는 것

정체성 점수·등급 · 몰래 위임 · 파인튜닝 우선 전략(프로필이 먼저) · 정산
없는 데이터 학습(채팅 덤프 임포트 등) · 분신 간 비교("남들의 분신보다…") ·
분신의 자율 실행(위임 범위 밖).

## 11. 기존 문서와의 관계

- BLUEPRINT §8 "정산 학습 위임 에이전트" 항목이 이 문서로 **승격**됐다
  (2026-08-06 창업자 지시).
- R3-B 원격 MCP 파일럿과의 관계: **대체가 아니라 적층.** Phase 0 의 수집
  채널이 그 파일럿이고, 파일럿의 H-B 지표는 그대로 유효하다. 극장 리포트는
  H-B 를 올리는 장치로 설계됐다.
- 방법 정본과의 관계: `honest agency` 의 확장 적용. 충돌 지점은 §6 수정조항
  하나이며, 그 승인 전까지 해당 표면은 출하하지 않는다.

---

## 부록 A — D-Day 시공 PRD (2026-08-06, 반복 3회 다듬은 최종본)

### A.0 반복 개선 기록 — 초안을 세 번 반박해서 남은 것

이 PRD 는 초안을 세 번 자체 반박한 결과물이다. 무엇이 죽었는지가 무엇이
남았는지만큼 중요하다:

| 패스 | 초안의 결함 | 고친 것 |
|---|---|---|
| 반박 1 | **동기 생성 지연** — open 호출 안에서 분신 예측을 만들면 모든 결정 열기에 3–8초가 붙는다 | Next 16 `after()` 로 응답 후 비동기 생성. **봉인 유효성 규칙** 신설: `sealed_at < adopted_at` 아니면 `late` 마킹, 채점 제외 |
| 반박 1 | **기울기 오염 (치명)** — open 입력에 사용자의 lean 이 있다. 분신이 "사용자는 lean 을 고른다"고 예측하면 match rate 는 100%짜리 무의미한 숫자가 된다 | 예측 대상을 분리: ① **결과 예측**(현실이 뭐라 답할지 — 주 지표), ② 선택 예측은 lean 부재 시에만 match 채점, lean 존재 시엔 **이탈 여부 예측**(비자명)으로 전환. 오염 플래그를 행에 저장 |
| 반박 1 | **RLS 누수** — 사용자가 supabase 클라이언트로 자기 봉인 예측을 미리 읽을 수 있으면 봉인이 무의미 | shadow 테이블은 RLS on + **사용자 정책 0개** (service role 전용). §7.3 의 기계 쌍둥이를 스키마 수준에서 강제 |
| 반박 2 | **자유 텍스트 채점의 LLM-glue 함정** — 정산 관찰은 자유 텍스트다. "예측이 맞았나"를 LLM 이 대충 판정하면 그럴듯함이 맞음으로 위장한다 | 예측을 봉인 시점에 **반증 가능한 기대 문장**으로 강제 작성. 정산 시 3치 판정 `{supported, contradicted, indeterminate}` + 관찰문 인용 필수, indeterminate 는 채점 제외. 채점 프롬프트는 버전드. **case bank(구조화 결과) = 엄밀 트랙, 개인 케이스 = 연성 트랙 — 두 숫자를 절대 합치지 않는다** |
| 반박 2 | **이탈 감지 과발화** — 프로필이 얇을 때 LLM 이 아무 데서나 패턴을 "발견"한다 | LLM 이전에 **결정론 관문**: 같은 도메인 정산 ≥5건 + 증거 링크 실존일 때만 감지기가 돈다. 미달이면 침묵 (자제 기본값) |
| 반박 2 | **허구의 역류** — 극장(시뮬레이션) 산출물이 프로필 증거로 재흡수되면 분신이 자기 상상을 배운다 | 프로필 증거는 **정산된 케이스 id 만** 허용 (스키마 제약 + 검증기). 극장 출력은 어떤 경로로도 프로필에 못 들어간다 |
| 반박 3 | **반출 vs 봉인 충돌** — 사용자는 자기 데이터 전부를 반출할 권리가 있는데, 미정산 예측을 반출하면 스스로 봉인을 깬다 | 반출 시 미정산 예측은 **해시+메타데이터만**, 정산 후 전문 포함. 소유권과 프로토콜의 타협을 문서에 명시 |
| 반박 3 | **키 부재 시 침묵 실패** — ANTHROPIC_API_KEY 없는 환경에서 예측 생성이 조용히 안 되면 플라이휠이 서 있는지 아무도 모른다 | fail-closed + 로그 + **크론 백스톱**(`argus-shadow` 가 미생성 케이스를 쓸어담아 재시도). 지어내는 경로는 없음 |
| 반박 3 | **오늘 범위 과대** — Phase 4(위임)까지 오늘 지으면 재료(정산 0건) 없는 표면을 양산한다 | **위임은 오늘 명시적 제외** (문서 §8 대로 별도 승인 + H-B 선행). 오늘의 "다"는 Phase 0–3 의 기계 전부다 |

### A.1 오늘 짓는 것 — 마일스톤과 컷라인

| M | 무엇 | 완료 판정 | 등급 |
|---|---|---|---|
| **M1** | 봉인 파이프라인: 마이그레이션 4 + shadow store + open 시 `after()` 생성·봉인(해시 원장 기록) + 정산 시 3자 대조 공개 | 테스트로 봉인→정산→공개 완주, 조기 공개 경로 0 (가드 테스트) | 필수 |
| **M2** | 판단 프로필 v1: 정산 시 추출 → 결정론 검증(증거 실존·판정 언어 린트) → `argus_recall` 에 프로필 절 | 정산 이벤트에서 항목 생성, 증거 링크 무결성 테스트 | 필수 |
| **M3** | 시뮬레이션 극장 v1: case bank 시드(12건, 출처 명기) + 주간 크론 + 등급 라벨 + 이메일 리포트 | 크론 1회 실행 산출물에 라벨 전수, 무라벨 저장 거부 테스트 | 권장 |
| **M4** | 자기 이탈 감지: 결정론 관문(도메인 ≥5 정산) + open 응답 부록으로 중립 crux 1문 | 관문 미달 시 침묵 테스트, validator 통과 | 권장 |
| **M5** | 보정 거울 수치 표면 | — | **승인 게이트** (§6 수정조항, 창업자 "승인" 한마디) |

오늘 제외: Phase 4 위임 · 디렉토리 등재 · 파인튜닝 · 실커넥터 왕복(창업자만 가능).

### A.2 아키텍처 결정 (guru 체크리스트 통과분)

**LLM 호출 — 전부 서버측, 기존 모듈 재사용.**
`src/lib/llm-server.ts` (`callAnthropicJson`) 이 이미 있다 — 모델 후보
폴백·비용 라우팅 포함. 분신 예측 = `default` 티어, 프로필 추출 = `default`,
극장 = `fast`. 예측 행마다 **실제 사용 모델 id 를 저장**한다 (모델이 바뀌면
성적 비교가 오염되므로, 비교는 모델 id 층화로만).

**비동기 경계 — Next 16 `after()`.**
open 응답을 먼저 보내고 예측 생성은 `after()` 안에서. 실패해도 사용자
경로는 무사하고, `argus-shadow` 크론(시간당)이 `status='pending'` 을
쓸어담는다. 채택이 예측보다 빨랐으면 `late` — 정직하게 채점 제외.

**봉인 무결성 — 해시 체인.**
예측 전문은 `argus_shadow_predictions`(service role 전용)에, sha256 해시는
**원장 이벤트로** (`shadow_sealed`). 정산 시 `shadow_revealed` 이벤트가
전문과 함께 원장에 들어가며 해시 대조를 통과해야 한다. 원장이 append-only
이므로 소급 조작 불가가 증명 가능하다 — 이것이 moat 2층의 구현체다.

**도구는 여전히 6개.** 새 도구를 만들지 않는다 (기획서 §4 봉인 유지). 분신
표면은 기존 응답에 얹는다: open 부록(이탈 crux) · return 정산부(3자 대조) ·
recall(프로필 절) · dueNotice(극장 티저 1줄).

**프롬프트 단일 정본.** 예측·추출·채점·극장 프롬프트는 `src/lib/twin/` 아래
lib 함수로만 존재 (Single Source of Truth for Prompts 규약).

**신규 테이블 4 + Schema Sync 의무 전수:**

| 테이블 | 핵심 컬럼 | RLS | erasure |
|---|---|---|---|
| `argus_shadow_predictions` | case_id, user_id, target(outcome/choice/deviation), expectation(반증 가능 문장), confidence, contaminated_by_lean, model_id, sealed_at, revealed_at, verdict, status | on, **정책 0** | 등재 |
| `argus_profile_items` | user_id, layer(L1/L2/L3), content, evidence_case_ids[](**정산 케이스만**), confidence, counterexamples[], expires_at, status | on, 본인 read/delete | 등재 |
| `argus_simulation_runs` | user_id, source(untaken/case_bank/distant), grade_label(**not null**: 채점됨/예측/허구), content, score | on, 본인 read | 등재 |
| `argus_case_bank` | id, domain, situation, options[], outcome(구조화), source_url | 전역(user_id 없음) | 해당 없음 |

같은 커밋 의무: 마이그레이션 + `schema-drift` TABLE_COLUMNS + `user-data-tables`
+ `erasure-coverage` + persistence 계약. (이 리포에서 네 번 겪은 조용한 실패
전부의 예방선.)

**크론 2개 신설** (`vercel.json` + cron-instrumentation 흔적 의무):
`argus-shadow`(시간당, 미생성 예측 백스톱) · `argus-theater`(주간, 극장 리포트).

**비용 예산 (파일럿 10인 기준):** 예측 ~케이스당 1콜, 추출 ~정산당 1콜,
극장 주당 ~30콜 — 월 수 달러 규모. 병목은 비용이 아니라 정산 표본이다.

### A.3 채점 수학 — 정직한 버전

- **엄밀 트랙 (case bank):** 구조화 선택지·결과 → 분신 Brier 산출 가능,
  첫 주부터. 슈퍼포캐스터 0.166 / 일반 0.259 가 외부 기준선.
- **연성 트랙 (개인 케이스):** 3치 판정(supported/contradicted/indeterminate)
  비율로만 말한다. Brier 흉내 금지 — 자유 텍스트에 소수점 성적을 붙이는
  순간 그럴듯함이 맞음으로 위장한다.
- **match rate:** lean 부재 케이스에서만. lean 존재 시 이탈 예측 적중률을
  별도 표기. 두 숫자를 합치지 않는다.
- 모든 수치는 표본 수와 함께만 표시, 임계 미달이면 "아직 모른다".

### A.4 오늘의 실행 순서 (의존 순)

1. 마이그레이션 4 + 가드 등재 (M1 기반)
2. `src/lib/twin/` — 예측·추출·채점·극장 프롬프트 + 스키마 검증기
3. shadow store + open `after()` 배선 + 정산 공개 배선 + 테스트
4. 프로필 추출 + recall 절 + 테스트
5. case bank 시드 + 크론 2 + 이메일 + 테스트
6. 이탈 감지 관문 + crux 부록 + 테스트
7. 전체 관문 (coverage ratchet 포함) → 커밋 → PR

### A.5 남는 정직한 공백 (오늘이 끝나도)

- 실커넥터 왕복·첫 실정산은 창업자만 할 수 있다 — 기계는 오늘 완성돼도
  **플라이휠의 첫 회전은 창업자의 첫 결정이다**
- M5(보정 거울)는 §6 수정조항 승인 전 미출하
- case bank 12건은 시드다 — 확장은 별도 콘텐츠 작업

---

## 부록 B — 시공 후 자체 감사 (2026-08-06, 창업자 요청 "구현하다 만 건 없는지")

M1–M4 를 지은 뒤 **선언과 소비를 기계로 대조**했다(`grep` 으로 호출처 확인).
여섯 개가 나왔고 다섯을 같은 날 닫았다. 형태는 이 리포가 아는 그것과 같다 —
전부 **크게 실패하지 않고 그럴듯하게 동작하던 것**들이다.

| # | 무엇이 열려 있었나 | 왜 조용했나 | 조치 |
|---|---|---|---|
| 1 | **반출이 봉인을 깼다.** export 라우트가 `select('*')` 로 미정산 예측 전문을 그대로 내보냈다 | RLS 로 막아 둔 문을 service role 이 열어 준다. 사용자가 export 버튼 하나로 자기 봉인을 깰 수 있었고, **PRD 반박 3이 지적한 그대로였다** | `redactUnsettledSeals` — 미정산 행은 해시+메타만, 뺐다는 사실과 이유를 반출물에 명시. 소스 계약 가드 4개 |
| 2 | **choice/deviation 예측이 채점되지 않았다.** 봉인은 되는데 판정 경로가 outcome 만 처리 | 주석에 "M2 에서 결정론 대조로 별도 처리"라 적어 두고 M2 에서 하지 않았다. 봉인만 하고 채점 안 하면 **그 데이터는 죽은 데이터**이고 match rate 의 모수가 영영 0 이다 | 채점을 target 별로 분기 — outcome 은 관찰과, choice/deviation 은 **실제 채택**과 대조. late 봉인은 채점 제외 |
| 3 | **match rate 가 계산되지 않았다.** 기획서 §4.2 의 두 숫자 중 하나가 코드에 없었다 | 재료(#2)가 없었으므로 계산할 것도 없었다 | `twinScore()` — 두 지표를 **분리해서** 산출. indeterminate·late·오염 choice 는 모수에서 제외, 표본 수 동반 반환. recall 과 극장 리포트에 표시(표본 3건 미만이면 "아직 모릅니다") |
| 4 | **"가지 않은 길"이 미배선.** `replayUntakenPath` 가 정의만 되고 아무도 부르지 않았다 | 극장 크론이 bank 사례만 재생했다. 기획서 §4.3 의 세 소스 중 하나가 도달 불가였다 | 크론에 배선 + `unreplayedUntakenPaths`. 재료(기각 대안)는 `argus_cases.rejected_alternative` 로 정산 시점에 투영 — 주간 배치가 케이스마다 원장을 fold 하지 않도록 |
| 5 | **프로필을 사용자가 고칠 수 없었다.** RLS 는 본인 delete 를 허용하는데 그 경로를 쓰는 화면이 없었다 | 기획서가 "편집 가능한 거울"이라 적었으나 거울만 있고 손잡이가 없었다 — 못 고치는 프로필은 "기계가 사용자에 대해 갖는 기록"이고, 그건 이 제품이 되지 않기로 한 것이다 | 설정에 **판단 프로필 블록** — 항목·근거 정산 id 를 보여주고 지운다. **v1 은 삭제 전용**: 문구 수정은 저자성(provenance)을 뒤섞으므로, 틀린 항목은 지우고 다음 정산에서 다시 관찰되게 한다 (§4.1 의 "고치거나 지운다"는 v1 에서 "지운다"로 좁힌다) |
| 6 | `simulation_runs.source = 'distant'` 가 스키마에만 존재 | 먼 케이스 재생은 M3 범위 밖이었다 | **오늘 닫지 않는다.** CHECK 에 값을 남기되 이 표에 미사용으로 기록 — 다음 극장 확장의 자리다 |

**감사 방법 자체가 결론이다:** "선언된 것이 전부 소비되는가"를 사람이 기억으로
확인하면 이번처럼 다섯 개가 남는다. `tools-contract.test.ts` 가 도구 스키마에
대해 하는 일을 **함수 수준으로도** 해야 한다는 것이 남은 숙제이며, §8 대기
목록이 아니라 다음 TWIN 작업의 첫 항목이다.

---

## 부록 C — 야간 심화 시공 (2026-08-06 밤, 창업자 "다 개선해줘, 모든 권한 승인")

부록 B의 감사가 남긴 숙제 하나("선언↔소비 대조를 **함수 수준**으로")를 먼저
기계화했고, 그 기계가 곧바로 두 개를 잡았다. 그다음 §4.4·§4.5의 미시공분을
지었다. 순서가 이렇게 된 것은 우연이 아니다 — **감사를 기계로 만들면 그 다음
시공이 무엇인지 기계가 알려 준다.**

### C.1 함수 수준 선언↔소비 가드 (`twin-wiring.test.ts`)

`tools-contract.test.ts`가 도구 스키마에 하는 일을 `src/lib/twin/`의 모든 값
export 에 대해 한다. 규칙 셋: (1) 정의 파일 밖에서 소비되거나 사유와 함께
등재, (2) **죽은 면제 금지**(등재 항목이 사라졌거나 이제 소비되면 그것도 실패),
(3) `argus_*` 테이블은 **쓰기만 있고 읽기가 없으면 실패**.

즉시 잡힌 것:

| 무엇 | 왜 조용했나 | 조치 |
|---|---|---|
| `argus_case_bank` 가 **쓰기 전용**이었다 | 시드는 upsert 되는데 극장은 코드 상수(`CASE_BANK_SEED`)를 직접 읽었다. 테이블을 지워도 아무도 눈치채지 못하고, 사례 한 건 늘리는 데 배포가 필요했다 | 시드는 코드가 갖고 **정본은 테이블**로. 읽기 실패는 던진다 — "이번 주엔 풀 사례가 없었다"와 "은행을 못 읽었다"는 다른 사건이다 |
| 감시 정규식이 조용히 덜 보고 있었다 | `from('X')([\s\S]{0,400})` 이 꼬리를 **소비**해서, 같은 함수에서 테이블 둘을 잇달아 만지면 뒤엣것이 통째로 사라졌다 | 꼬리를 소비하지 않는 형태로. 가드 자신이 조용히 실패하는 것이 이 테스트의 가장 큰 위험이다 |

`src/lib/epistemic/`에 같은 규칙을 켜면 **50건이 한꺼번에 붉어진다**(E-트랙은
테스트에만 소비되는 코드가 대부분이다). 이 트랙의 결함이 아니라 별도 공정의
현황이므로 여기서 몰래 처리하지 않고 BLUEPRINT §8에 한 줄로 올렸다.

### C.2 M6 — 프로필이 **자라기만 하고 배우지 않았다** (가장 깊은 구멍)

시공 후 대조에서 나온 것 중 가장 컸다. `counterexamples` · `expires_at` ·
`status='retired'` 세 컬럼이 **전부 죽어 있었고**, 확신도는 LLM 이 추출 때
한 번 매기면 현실이 세 번 반대로 답해도 그대로였다. 그것은 학습이 아니라
**첫인상의 화석화**이고, "그럴듯함이 맞음으로 위장"의 교과서적 형태다.

- **확신도를 계산값으로.** Laplace 평활 `(근거+1)/(근거+반례+2)`. LLM 이 매긴
  숫자가 저장되는 경로를 없앴다 (스키마에서도 제거). 근거 1건짜리 항목은
  1.0 을 가질 수 없다.
- **추출이 기존 프로필을 보고 한다.** 모델은 `items`(새 관찰) 외에
  `reinforces`/`contradicts`를 **번호로만** 돌려준다. 라우팅·은퇴 판정은 전부
  결정론이다. 같은 번호가 양쪽에 오면 **양쪽에서 다 뺀다** (모델이 스스로
  모순한 것이므로 어느 쪽으로도 세지 않는다).
- **은퇴.** 반례 2건 이상 + 확신도 0.5 미만이면 `retired`. 그리고 **말한다** —
  recall 에 "최근 물러난 관찰" 절, 설정 화면에 취소선. 조용히 빼면 기계가
  자기 기록을 몰래 고치는 것이고, 사용자는 이의를 제기할 기회를 잃는다.
- **만료 180일.** 사람은 변한다. 2년 전 기준으로 오늘의 나를 예측하면 그것은
  분신이 아니라 유령이다. 보강되면 만료가 미뤄진다.
- 이탈 감지(`qualifiedPatterns`)도 만료·확신도 미달 항목을 뺀다 — 이미 흔들린
  패턴으로 "당신은 늘 이랬습니다"를 묻는 것은 과발화다.

### C.3 §4.4 잡음 거울 — 변장 재제시 (`noise.ts`)

bootstrapping 문헌의 기제를 뒤집은 기능. 오래 전(30일+) 정산된 자기 케이스를
**다른 업종·다른 숫자로 변장시켜** 분신에게 다시 묻고, 사용자가 실제로 간
쪽과 같은 답이 나오는지 본다. 재는 것은 **분신이 표면이 아니라 구조를
배웠는가**이며, 사용자의 일관성이 아니다 (사용자에게 퀴즈를 내는 순간 이
도구는 사람을 시험하는 물건이 된다 — zero-judgment 위반).

오염 방지선 셋: 변장 프롬프트에 실제 선택을 넣지 않는다 · 선택지 순서를
caseId 로 결정론적으로 뒤집는다(위치 학습 방지) · **원문 어구가 6자 이상
그대로 남으면 그 케이스를 버린다**. 토큰 단위 비교는 한국어에서 흔한 낱말이
걸려 멀쩡한 변장을 전부 버리므로 쓰지 않았다 — 그러면 이 기능은 "조용히
아무것도 하지 않는 기능"이 되고, 그것이 가장 나쁜 실패다.

이로써 부록 B #6(`simulation_runs.source='distant'` 미사용)이 닫혔다.

### C.4 Phase 4 — 범위 위임 (신뢰 사다리의 마지막 칸)

창업자의 원 질문("내가 부재하더라도 내 의사결정을 학습한 agent로서 기능할 수
있도록? 다만 argus는 keeping judgment human인데")에 대한 정직한 답이다.

**핵심 발상: 위임 자체가 채점되고, 성적이 무너지면 스스로 멈춘다.**

| 불변식 | 어디서 강제되나 |
|---|---|
| 위임은 **사용자의 문장**으로만 태어난다 | `user_words` not null (스키마) + `userWords` 없으면 생성 거부 + **왜 거부했는지 응답에 적는다** |
| AI 추출 프로필이 위임으로 승격되는 경로 없음 | 생성은 `argus_adopt`의 명시적 인자로만. 화면에서 만들 수 없다 |
| 영원한 위임 없음 | `expires_at` not null, 최대 90일. 넘겨 보내면 자르고 **잘랐다고 말한다** |
| 결정을 대신하지 않는다 | 적용은 "당신이 쓴 규칙이 여기 해당한다"를 꺼내는 것까지. 채택은 여전히 `argus_adopt` |
| 집행도 정산 대상 | `argus_cases.delegation_id` → 귀환 때 정책을 3치 판정 (인용 없으면 indeterminate) |
| 틀린 위임이 계속 돌지 않는다 | 어긋남 2건 이상 + 맞음보다 많으면 `suspended`, **정산 응답에서 즉시 알린다** |
| 되돌리기가 가장 쉬워야 한다 | 설정에 목록·성적·철회·다시 켜기. 정지(기계)와 철회(사용자)를 구분 |

적용 문장에 인용되는 것은 **사용자가 쓴 정책 원문과 그때 한 말**뿐이다. LLM 은
"이 결정이 이 위임의 범위인가"에 인덱스로만 답한다 (divergence 와 같은 규율).


### C.7 야간 2차 순회 — 자기 검토에서 나온 넷

부록 C.1~C.4 를 지은 뒤 같은 눈으로 다시 훑었다. 넷이 나왔고 전부 **조용히
그럴듯하게 동작하던 것**이었다.

| # | 무엇 | 왜 위험한가 | 조치 |
|---|---|---|---|
| 1 | **`after()` 가 요청 범위 밖에서 불렸다** | `await import('next/server')` 뒤에 `after()` 를 부르면 그 await 가 마이크로태스크를 끼워 넣어 **응답이 이미 나간 뒤** 등록될 수 있다. 그러면 catch 로 떨어져 그냥 떠 있는 promise 가 되고, 서버리스에서 그것은 람다가 얼면 죽는다 | 정적 import + 동기 호출. 그림자는 크론 백스톱이 있었지만 **프로필 추출은 없어서** "정산했는데 분신이 아무것도 배우지 않는" 조용한 실패가 가능했다 |
| 2 | **프로필 추출에 백스톱이 없었다** | 위 경로가 죽으면 화면에 아무 표시도 나지 않고 영구히 안 배운다 | `argus_cases.profile_extracted_at` + 시간당 크론이 표식 없는 정산을 집는다. **표식은 "시도"에 찍는다** — 0건은 흔한 정상 결과이므로 그것을 미시도로 남기면 크론이 48시간 동안 매시간 같은 케이스에 LLM 을 부른다 |
| 3 | **위임이 그림자를 오염시켰다** | 위임을 꺼내 놓으면 사용자는 자기 정책을 눈앞에서 본 뒤 고른다 — 그 상태의 choice 예측은 자명하다. `lean` 오염과 **정확히 같은 형태**인데 처리되지 않고 있었다 | 위임이 꺼내졌으면 정책을 기울기로 넘겨 분신이 choice 대신 **이탈**을 예측하게 하고, 그 행은 오염 플래그와 함께 봉인된다. 원장의 baseline 은 손대지 않는다 (사용자가 이번에 말한 기울기가 아니므로) |
| 4 | **위임 테이블의 UPDATE 가 너무 넓었다** | RLS 는 "본인 행"만 볼 뿐 어느 컬럼을 고치는지는 못 본다(WITH CHECK 는 OLD 를 볼 수 없다). 브라우저에서 `policy` 를 다시 쓰거나 만료를 2099년으로 밀거나 `contradicted` 를 0 으로 되돌릴 수 있었다 — **성적으로 스스로 멈추는 안전장치가 그 자리에서 무력해진다** | 컬럼 단위 권한: `grant update (status)` 만. 사용자가 바꿀 수 있는 것은 철회·재개뿐이고, 나머지는 정산 경로만 쓴다 |

덧붙여 `markCaseDelegation` 이 **소유를 확인하지 않고** 도장을 찍고 있었다.
FK 는 존재만 보지 소유를 보지 않으므로 남의 위임 id 로도 통과했고, 그러면
케이스가 남의 정책을 가리킨 채 남되 정산은 소유 필터에 걸려 조용히 채점하지
않는 **유령 상태**가 됐다. 소유 확인을 앞에 두었다.

### C.5 M5 보정 거울 — 처음 판단이 틀렸고, 그래서 지었다

이 절은 원래 **"오늘 지을 수 없다"**로 적혀 있었다. 근거는 "사용자가 확신도를
사전등록하는 입력이 어디에도 없다"였다. 그것이 틀렸다.

`argus_adopt` 의 `materialBeliefs` 는 각 믿음에 **사용자가 직접 고른**
confident / uncertain / contested 를 받고 있었다. 결과를 알기 전에 사용자
손으로 적힌 확신도 — 사전등록의 정의 그대로다. 없던 것은 입력이 아니라
**소비**였다: 정산 때 그 믿음들을 관찰과 대조하는 경로가 없었다.

이 리포가 반복해서 겪는 형태 그 자체다 (*"생산된 필드는 기본이
dead-on-arrival"*). 그리고 이번에는 **그 형태가 기획 판단까지 오염시켰다** —
소비가 없으니 입력도 없는 것처럼 보였고, 나는 기능 하나를 "지을 수 없다"로
분류했다. 함수 수준 가드(C.1)를 필드 수준으로도 밀어야 하는 이유다.

**지은 것** (`argus_belief_checks` + `src/lib/twin/beliefs.ts`):

- 정산 때 그때의 믿음들을 관찰과 3치 대조. 인용 없는 판정은 indeterminate 로
  강등되고 모수에서 빠진다 (그림자·위임과 같은 규율)
- **확신 등급을 채점 프롬프트에 넣지 않는다** — 등급을 알면 판정이 그쪽으로
  끌린다. 모델은 믿음과 관찰만 본다
- 판정 개수가 믿음 개수와 다르면 **통째로 버린다.** 순서로 짝짓는 구조에서
  길이 불일치는 조용히 어긋난 짝을 만들고, 그러면 남의 믿음의 성적이 내 것이 된다
- **확률을 지어내지 않는다.** 세 등급에 0.85/0.6/0.4 를 붙여 Brier 를 내고 싶은
  유혹이 있으나 그 숫자는 사용자가 말한 적이 없다. **등급별 적중률**만 낸다 —
  그것이 보정(신뢰도 구간별 실제 적중률)의 정의이고, 지어낸 확률 없이 계산할
  수 있는 유일한 정직한 형태다
- 등급별 표본 5건 미만이면 그 등급은 아예 나오지 않는다
- **당김 표면에서만 나온다** — `argus_recall` 을 사용자가 직접 불렀을 때만.
  결정을 여는 중이나 정산 직후에 성적을 들이미는 것은 요청받지 않은 성적표이고,
  기획서 §9 위험표의 "성적표 공포"가 가리키는 것이다. (기획서는 보정 거울을
  옵트인으로 적었다. 당김 전용은 그 취지의 최소 구현이며, 명시적 옵트인 토글이
  필요한지는 창업자 판단으로 남긴다.)

**같이 고친 것: 확신도를 우리가 대신 채우고 있었다.** `readBeliefs` 는 확신도가
없으면 조용히 `uncertain` 으로 메웠다. 아무도 그 값을 읽지 않던 시절에는
무해해 보였지만, 이제 그 등급이 채점되어 **사용자의 보정 숫자**가 된다 —
우리가 채운 등급이 사용자의 성적으로 표시되는 것이다. 이제 등급 없는 믿음은
기록하지 않고, 몇 건을 뺐는지 응답에 적는다. 도구 스키마도 `confidence` 를
필수로 올리고 "추측해서 채우지 말 것"을 명시했다.

**남는 절반**: 도메인별 분해는 여전히 못 한다. 그림자 예측 행에도 믿음 행에도
도메인이 없고, 프로필 도메인으로 유추하는 것은 그 자체가 조작이다. 도메인을
어디서 얻을지(사용자가 말한 것만 쓸 것인지)가 선행 결정이다.

### C.6 남는 정직한 공백

- **실왕복은 여전히 창업자만 할 수 있다.** 위임·잡음 거울·프로필 학습은 전부
  정산 표본 위에서 도는 기능이고, 첫 정산은 창업자의 첫 결정에서만 나온다.
- 마이그레이션 `20260806120000`(위임)은 프로덕션 적용 필요.
- `src/lib/epistemic/` 50건 미소비 — 별도 공정, BLUEPRINT §8 등재.
- 위임 적용은 `argus_open` 당 LLM 1콜을 더 쓴다 (위임이 하나라도 있을 때만).
  파일럿 규모에서는 무시할 수 있으나, 사용자가 늘면 결정론 사전 필터(도메인
  태깅)를 앞에 두어야 한다.

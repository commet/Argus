# UX 대수선 — 감사·수정·잔여 백로그 (2026-06-12 야간 작업)

> v4.1 재구축 직후의 workspace를 "시중 제품 수준"으로 끌어올리기 위한 전수 감사와 일괄 수정.
> 감사 = 병렬 4트랙(첫 방문 여정 / 항해 중 / 완료·정산 루프 / 디자인 시스템·접근성·코드 버그) + 초기 v4.1 수용 기준 리뷰.
> 총 발견 ~125건 → 이번 작업에서 P0 전건 + P1 대부분 수정. 검증: vitest 1161/1161 통과, `next build` 통과.

---

## 1. 고친 것 — 카테고리별

### A. 측정 엔진 정직성 (제품 철학의 불변식)

| # | 문제 | 수정 |
|---|---|---|
| A1 | 재탐침 실패가 "갈림 해소"로 위장 (`r.failed` 미체크) | failed 가드 + 세션 동일성 가드 (`ProgressiveFlow.maybeReprobe`) |
| A2 | probe store 세션 간 오염 — 새 세션이 이전 세션의 샘플·갈림을 상속 | store에 `paragraph` 정체성 추가, TrialSail이 불일치 시 reset (`useProbeStore`, `TrialSail`) |
| A3 | D(주 측정)만 실패해도 침묵 카드 렌더 | `ablationFailed` 플래그 → silent 계산에서 제외 + "하중 측정은 닿지 않았어요" 한 줄 |
| A4 | C-병합 단계의 AbortError 삼킴 → 언마운트 중 결과가 수렴으로 부활 | merge catch 후 aborted면 rethrow (`probe-engine`) |
| A5 | 재탐침 인용 앵커가 합성 `[사용자 확정]` 라인 기준으로 검증 | `anchorText` 옵션 — 원문 기준 검증 + 테스트 봉인 |
| A6 | 세션당 측정-정박 질문 ≤2 캡 누수 (재탐침이 새 id 발급 시 최대 4개) | store 세션 카운터 `questionsInjected` + 주입부 소비 (`tryConsumeQuestion`) |
| A7 | 파리티 테스트 4건 실패 — CRLF 체크아웃에서 깨짐 (드리프트 아님) | 양측 LF 정규화 |
| A8 | 점수 어휘 잔존: 확신도 75% / 명확도 87% / 품질 N점 / N/100 / "베팅" / "채점" | 전부 서술형·확인 어휘로 교체 (P1 zero-verdict) |

### B. 귀환 루프 (제품의 핵심 약속 — "그래서, 어떻게 됐어요?")

| # | 문제 | 수정 |
|---|---|---|
| B1 | 봉인 질문이 "새 프로젝트 시작" CTA **아래** — 보기 전에 이탈 | SealMoment를 퇴장 CTA 위로 재배치 (마지막 인터랙션 보장) |
| B2 | "물어볼게요" 약속을 지킬 채널 부재 (이메일 스텁) | 정직한 카피("프로젝트 페이지에 오시면 제가 먼저 물어요") + /project 링크 + **.ics 캘린더 다운로드** |
| B3 | due 알림이 /project 진입+클릭 시에만 | 헤더 '프로젝트' nav에 골드 카운트 배지 + /project 상단 "돌아올 결정 N건" 스트립 + due 우선 정렬 + 카드 앰버 보더 |
| B4 | "6월 25일에 물어볼게요"가 그날 밤 11시까지 거짓 | due 판정을 로컬 **날짜 단위**로 (`contractStatus`) |
| B5 | 정산 모달이 콜드 로드 레이스로 안 뜸 | 렌더 시 파생 + 방문당 dismissed Set |
| B6 | 정산해도 아무것도 못 받음 (자차표 부재) | "이번이 N번째로 닫은 고리예요 · 위험 K개를 비켜 갔어요" 누적 한 줄 |
| B7 | 반복 "아직"에 동일 카피, 영원한 미루기 루프 | 변주 카피 + 3회째부터 "결과를 알 수 없는 걸로 닫기" 출구 |
| B8 | /project가 progressive 항해를 모름 ("아직 재정의 전" 자기모순) | progressive 세션 인지 — "항해 진행 중/완료" 상태 + 워크스페이스 열기, 재정의 CTA 억제 |
| B9 | "채점" 어휘 전면 (카드·모달·배지) | "확인"으로 통일 + 의문형 "~나요?" + 해요체 |
| B10 | SettlementModal a11y 0 (Esc·포커스트랩·스크롤락 없음) | 기존 `Modal` 기반으로 재구축 + 봉인 날짜 맥락 + 빈 predicates 가드 |

### C. 첫 방문 여정

| # | 문제 | 수정 |
|---|---|---|
| C1 | `?q=` 소비 후 URL 잔존 → 새로고침마다 같은 분석 재실행·쿼터 이중 소진 (P0) | 소비 즉시 `replaceState('/workspace')` |
| C2 | 다크모드에서 랜딩 입력창 사실상 불가독 (하드코딩 배경) | bp 토큰화 (SirenHero, LandingHeader) |
| C3 | 랜딩(항해)↔워크스페이스(기획안 도구) 약속 단절 | idle 헤드라인을 침로 언어로 ("어디서 갈리는지 봐 드릴게요") + 3단계 카피 정합 |
| C4 | 랜딩 Act2 "그때 채점한다" 금칙어 / 빈 입력이 데모 쿼리로 실분석 | "다시 물어볼 약속" 화법 + disabled 가드 |
| C5 | 메타 타이틀이 구세대 포지셔닝 | "Argus — 그래서, 어떻게 됐어요?" |
| C6 | 새로고침하면 진행 중 항해에서 추방 | `currentProjectId` localStorage 영속+복원 |
| C7 | 익명 4번째 프로젝트 영구 접근 불가 | "전체 N개 보기" 토글 + 행에 상대시간 |
| C8 | "하루 30회 무료"가 실제 단위(호출)와 불일치 | "하루 결정 2~3개 분량 무료" |
| C9 | 에러가 골드(보상색) 배너 | 실패=danger 톤, 쿼터 안내만 accent |
| C10 | 기타: h1 2개, raw `<a>` 풀리로드, iOS 줌(15px), 푸터 부재, est. mmxxvi, 로그인 피아노 잔재·합쇼체, 데모 영문 라벨 9종 | 전부 수정 |

### D. 항해 중 (갇힘·정직성·진행감)

| # | 문제 | 수정 |
|---|---|---|
| D1 | 탈출구→게이트 닫기→질문 카드 영구 잠김 (P0) | skip이 `submitted`를 잠그지 않음 (`QuestionCard`) |
| D2 | 첫 결과 전 새로고침 → 선원 전원 "대기" 동결, 재개 불가 (P0) | `isResumable` 완화 + 0건이면 "다시 실행" |
| D3 | lead_synthesizing 중 새로고침 → 영원한 가짜 "초안 작성 중" (P0) | 마이그레이션 직행 `conversing` (`migrateWorkers`) |
| D4 | 분석 실패 시 답이 증발하고 mix 화면에 착지 | `rollbackAnswer` — 질문 복원 + 에러 가시화 |
| D5 | 에러 배너가 페이지 최하단 (스크롤은 상단으로) | sticky 상태 바 안으로 이동 + 닫기/쿼터 분기 |
| D6 | 질문 분모 없음 ("몇 개 남았지?") | "질문 2/3 · 마지막 질문이에요" + probe 질문은 "갈림 확인 · 선택" |
| D7 | 선원 실패 침묵 + "전부 초안에 들어갑니다" 거짓 | 정직한 카운트 + 인라인 "다시 시도" (`CrewAtWork`) + 보고 라인 실패 병기 |
| D8 | 스테퍼가 극장 중에 "질문" 표시 | crewDeployed → "팀 작업" 매핑 |
| D9 | 취소가 30초 후에야 등장 | ai_working 동안 상시(저자극) + 30s 앰버 승격 |
| D10 | 사이드바 결과 2.8s 자동 접힘(읽다 뺏김) | 사용자 토글까지 유지 |
| D11 | 측정 질문이 자기 증거(극장)보다 위 | probe 질문 활성 시 TrialSail을 질문 위로 |
| D12 | "이해관계자 검증 다시 하기"가 무경고 파괴 | 2단계 확인 + 정직한 라벨("초안부터 다시 만들기") |
| D13 | 기타: 문제 원문 pill 확장, 답변 턴 스크롤 점프 1회 제거, 기록 토글 상시 마운트, no-op 챗 액션 정직화, 레거시 refine 빈 탭 제거, 뒤로가기 popstate, 모바일 탭 라벨("사전"→"검증"), 응답없음 스켈레톤→평문, 바운스 충돌, 드로어 중복 | 전부 수정 |

### E. 디자인 시스템·접근성

- **대비**: `--text-tertiary` 2.0:1→4.6:1, `--text-secondary` 3.6:1→6.4:1 (위계 유지), 다크 동반 조정
- **reduced-motion**: Tailwind pulse/ping/spin 차단 + 전역 `MotionConfig reducedMotion="user"`
- **aria**: 헤더 메뉴/테마/아바타, 설정 Eye×3, 분석 스트림 aria-live, 모달 포커스트랩(정산)
- **다크모드**: project 페이지·로그인·설정·PF의 라이트 전용 하드코딩(amber/red/green/purple-50 계열) 토큰화
- **레이아웃**: 헤더 64px vs 콘텐츠 56px 8px 오차 정정 (top-16, calc(100vh-64px))
- **탭 타깃**: 탈출 칩·토글·재개 버튼 등 44px 보정
- **어휘**: 영문 raw severity("critical")→필수/권장/참고, "이타카" 은어 제거, 합쇼체 일소

---

## 2. 의도적으로 남긴 것 (계획과의 편차 기록)

- `/api/probe/route.ts` 미생성 — 기존 `/api/llm` 재사용 (probe-engine 헤더에 결정 기록됨). **v4.1 문서 갱신 필요.**
- C-병합·D 탐침이 sonnet 티어 — 계획 표 "저렴 모델만"과 불일치하나 G0가 이 티어로 측정됨. **계획 표 갱신 필요.**
- 세션 총 ≤30콜 카운터·N 5→3 자동 강등 — 미구현 (일일 쿼터가 상한 역할). 트래픽 생기면 구현.
- `ledger-schema.ts:131` `아님:` 한국어 하드코딩 — watch CLI와 공유 스키마 값이라 표면 어휘 결정 필요 (H5 합류 시).
- check_by 형식 (web=ISO vs watch=YYYY-MM-DD) — H5 합류 시 정규화 (잠복, 현재 무해).

## 3. 잔여 백로그 (다음 세션 후보, 우선순위순)

1. **[P1] MixPreview 본문 아코디언화** — 초안 전문을 다 지나야 CTA. (E-23)
2. **[P1] 과거 답 수정 진입로** — AnsweredPills 칩 탭→재질문/fork. (B-7)
3. **[P2] shouldMix 화면 4-카드 적층 다이어트** — PhaseDivider·상태칩을 항해 카드 메타로 흡수. (E-22)
4. **[P2] 일반 심화 질문에 subtext(왜 묻는지) 필수화** — 엔진 프롬프트 변경이라 신중히. (B-11)
5. **[P2] radius/소수점 px 표준화 스윕** — rounded-3xl/2xl/xl/[20px] 혼재, 11.5/12.5px 계열. (Track1-B/C)
6. **[P2] 정산 모달 배경에 결정 맥락(Current Bearing) 노출** — /project 상세에 카드 재사용. (P0-3c 잔여)
7. **[P3] OutputSelector 빈 데이터 가드** — progressive 프로젝트에서 속 빈 문서 생성 방지. (20a)
8. **[P3] useAgentStore 병합 레이스** (fetch 중 XP 변이 덮임) + 병합 결과 미영속. (Track2-7)
9. **[P3] 데모 랜딩 노출 강화·시나리오 추가**, Logbook 가지 삭제 44px 등 미세 항목.

## 4. 검증 기록

- `npx vitest run` — **69 파일 / 1161 테스트 전부 통과** (probe-honesty에 세션 정체성·캡·앵커 테스트 신규 추가)
- `npm run build` — 통과 (낡은 `.next` 아티팩트 제거 후; `voyage-preview` 잔재였음)
- grep 검증: 표면 `내기|반증|predicate|채점|베팅` 0건 (코드 주석 제외) · `파트보|총보|셋리스트` 0건
- 야간 diff 전체에 대한 적대적 코드 리뷰 1회 수행 (발견분 반영)

## 5. 현실 접촉 (사람이 해야 할 일 — 모델이 대신 못 함)

- **G-W1**: 봉인 종막+정산 모달을 창업자 본인 결정 1건으로 통과시키기 — 이제 헤더 배지·.ics까지 갖춰져 실험 가능.
- 다크모드로 랜딩→워크스페이스→완료까지 1회 육안 주행 (토큰화는 했으나 실기기 확인 필요).
- 모바일(iOS Safari) 1회 주행 — 키보드/safe-area/드로어.

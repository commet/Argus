# 완성 계획 — 한 바퀴를 끝까지 돌린다 (2026-08-18)

> 창업자 지시: *"진짜 이제는 마지막으로 '완성'할 계획을 확정해서 세우자."*
>
> 이 문서는 **무엇이 남았는가**만 답한다. 무엇을 지을지는 `PRODUCT.md`,
> 낱말은 `CONTEXT.md`, 코드 규율은 `CLAUDE.md`. 충돌하면 그쪽이 이긴다.
>
> **전제**: 창업자가 2026-08-18에 봉인한 결정 셋 — 고객은 **사람**, 무게중심은
> **동반자(대화 안, 1차 표면 = MCP)**, PR #396 자산은 **엔진 편입 + 화면 철거**.
> 원문 `ARGUS-REFOUNDATION-BRIEF-2026-08-16.md` §6, 등록은
> [`ARGUS-CANON.md`](./ARGUS-CANON.md) §9. 이 계획의 모든 순서가 거기서 나온다.

## 0. 완성이 무엇인지부터 정한다

지금까지 "완성"이 한 번도 정의된 적이 없다. 그래서 기능이 늘 때마다 완성이
멀어졌다. 여기서 정한다:

> **완성 = 결정 하나가 들어가서, 기한이 오면 제품이 먼저 찾아오고, 그 자리에서
> 실제로 무슨 일이 있었는지가 기록되고, 다음 판단에 규칙 한 줄이 남는 것.
> 그리고 그 한 바퀴가 실제로 도는 것을 관측한 것.**

기능 목록이 아니라 **한 바퀴**다. 이유는 `ARGUS-PRODUCT-PLAN-2026-08-05.md` §2가
이미 적었다 — 계획 생성은 상품화됐고 방어 가능한 것은 정산 하나뿐이다.
같은 문서 §6이 그것을 **"H-B · 증거 0건 — 사업의 전부"**라 부른다.

## 1. 지금 무엇이 지어져 있나 (전부 file:line 확인)

| 고리 | 어디 | 상태 |
|---|---|---|
| 결정을 받는다 | `argus-mcp/src/tools/open-decision.ts` | ✅ 질문·가치·버린 대안·하중 가정까지 |
| 하중 가정 하나를 세운다 | `tools/seal.ts:494-523` (믿음 확인창) | ✅ 사용자 직접 타이핑, `elicited:true` |
| 실행 계획을 붙인다 | `tools/plan.ts` · `public-tools.ts:151` | ✅ 날짜 붙은 채택, 인용 동반 |
| 기한을 관리한다 | `lib/ledger-replay.ts:78-102`, `PLAN_MAX_SCHEDULED=3` | ✅ 12단계가 12번 잔소리가 되지 않는다 |
| **기한이 오면 찾아온다** | `tools/check-in.ts:323,410` → `lib/surfaces.ts:529` (`plan_due`) | ✅ **E2E 대조군 + 에이전트 실주행 확정** · ⚠️ 눈먼 모델은 미관측 |
| 결과를 적는다 | `action:'plan_check'` (`public-tools.ts:163,498`) | ✅ **E2E 대조군 + 에이전트 실주행 도달** · ⚠️ 눈먼 모델은 미관측 |
| 정산이 확신도를 되돌려준다 | `tools/settle.ts` · `tools/recall.ts` | ✅ 실주행 확인 (14차) |
| 규칙 한 줄이 남는다 | 웹: `attachSettlementLesson()` (PR #370) · MCP: `settle.ts` 규칙 창 (PR #399) | ✅ **양쪽 다 시공됨** (둘 다 미머지) |

**08-18 아침 상태**: 앞쪽 다섯 칸은 다 이어져 있었고 뒤쪽 셋이 비어 있었다.
그 셋이 정확히 `PRODUCT.md`가 약속한 문장의 뒷부분이다 — *현실 → 사용자가 승인한
배움 → 더 나은 다음 판단*. 그리고 그 뒷부분을 `PRODUCT-PLAN` §6이
**"사업의 전부"**라 부른다. 지난 두 주의 작업(11차~15차 사이클)은 전부 **앞쪽
다섯 칸을 더 깊게** 만드는 것이었고 — 인지 수집, 확신도, 믿음 확인창, 유실 없는
거울 — 좋은 작업이었지만 **뒤쪽 셋은 그동안 한 칸도 안 움직였다.**

**08-18 저녁 상태**: **여덟 칸 전부 한 바퀴 안에서 실주행으로 통과했다** —
에이전트가 창업자 배역으로 결정→계획→봉인→(8/20)귀환→결과 기록→(9/8)정산→규칙
한 줄까지 완주했고, 원장 9건이 논리 날짜 3개에 걸쳐 남았다
([리시트](./receipts/2026-08-18-founder-role-lap/README.md)).

**08-19 상태**: 그 한 바퀴를 **한 명령으로 재현 가능하게** 만들었다.

```bash
cd argus-mcp && npm run loop:demo
```

실제 dist 서버 · 논리 날짜 셋 · 모델 없음 · 키 없음 · 대기 없음. 여덟 칸이
지금도 이어져 있는지 지금 확인하는 방법이고, 앞으로 무엇이 끊기면 여기서
빨간불이 난다. 자체 검사 14개, 실패 시 `exit 1`.

**그러나 두 가지는 여전히 0건이고, 그것이 완성의 남은 전부다**:
① 저장소를 모르는 **눈먼 모델**이 같은 바퀴를 도는가(2걸음, 키 필요)
② **사람**이 실제 달력의 기한에 돌아오는가(4걸음, H-B).
①은 상한이 확인됐으니 도구 표면의 문제이고, ②는 코드로 답할 수 없다.

## 2. 왜 한 바퀴가 안 돌았나 — 오늘 찾은 것

15바퀴를 돌렸는데(`docs/receipts/2026-08-11-first-user-journey/`) `plan_check`가
0회다. 지금까지 이것을 **모델의 동사 선택 문제**로 적어 왔다(11차 종결:
*"문맥 손잡이는 내용 보존까지 바꾸지만 동사 선택은 못 바꾼다"*).

그 진단이 틀렸다. 이유는 훨씬 단순하고, 제품이 아니라 **계측기**에 있다.

> **여정 하네스에는 시계가 없다.**

- 하네스의 "재시작"은 **프로세스 재시작**이지 날짜 이동이 아니다
  (`evals/first-user-journey.mjs`, 7단계: 서버를 닫고 다시 연다).
- 계획 단계는 `+7d`·`+2w` 지평으로 채택된다 → 확인일은 **일주일 뒤**.
- 그래서 8단계의 `check_in`은 **옳게** 침묵한다. 마감 전이니까.
- 확인일이 온 적이 없으니 `plan_due` 표면이 뜬 적이 없고, 뜬 적이 없으니
  모델이 `plan_check`를 부를 계기가 **구조적으로 존재한 적이 없다**.

리시트가 이 원인을 이미 적어 놓고도 결론을 못 냈다 — *"check_in은 침묵 계약대로
조용했고(마감 전이라 옳음)"*. 계측기가 자기 맹점의 원인을 **정상 동작으로**
기록한 것이다.

**그리고 시계는 이미 있다.** `today_override`가 `resolveToday()`의 첫 번째
인자이고(`lib/resolve-today.ts:15`), 서버가 공개 스키마에서 감춘 채
받아들이며(`server.ts:141-143`, `public-tools.ts:549`), 결정론 배터리는
이미 이것으로 시간여행을 한다(`evals/battery.mjs:63`,
`evals/ambient-picker.mjs:118`). **라이브 여정 하네스만 한 번도 쓰지 않았다.**

> 15바퀴 동안 사업의 전부라 불린 가설이 계측기의 사정거리 밖에 있었다.
> 그리고 사정거리를 늘리는 부품은 처음부터 서랍에 있었다.

## 3. 다섯 걸음

### 상태 한눈에

| 걸음 | 무엇 | 상태 | 누가 |
|---|---|---|---|
| 0 | 재고 정리 (PR 처분) | ⏳ 창업자 머지 대기 | 창업자 |
| 1 | 계측기에 시계 | ✅ **완료** (PR #399) | — |
| 1b | 모델 없는 대조군 | ✅ **완료** — 제품 쪽 호 확정 (PR #399) | — |
| 2 | 첫 라이브 관측 | ✅ **에이전트 배역으로 완주** ([리시트](./receipts/2026-08-18-founder-role-lap/README.md)) · ⏳ 눈먼 모델 실측은 키 필요 | 아무나 (명령 한 줄) |
| 3 | 관측이 부러뜨린 것 수리 | ✅ **4건 완료** (PR #399) · `stakes` 는 **결함이 아니었다 — 진단 철회** | — |
| 4 | 창업자 1회 완주 | ⏳ **실제 달력 필요 — 붙여넣기 2번** | 창업자 |
| 5 | 웹 트랙 판정 | ✅ **판정 완료** (10건 중 2 수리·1 절반·7 열림) · 시공은 ⏸ | — |

### 0걸음 — 재고를 정리한다 (창업자 결정)

| | 무엇 | 처분 | 근거 |
|---|---|---|---|
| #370 (08-11) | 웹 DLP-2·5 + 눈먼 서버 경로 5곳 동작 테스트 | **머지** | `check`·`live-surfaces`·GitGuardian 초록, 일주일 대기. **웹의 Lesson 이 이 안에 있다** — main 에는 아직 없다. `live-loop` 빨간불 하나는 아래 참조 |
| #396 | 재정초 §6 시공 + 문서 정리(68→19) + 이 계획 | **머지** | 창업자 봉인의 시공 상태 |
| #397 | PR-A1 전제 귀환 단선 2곳 | **머지** | MIT 존, 초록 |
| #399 | 계측기 시계 + MCP 의 Lesson | **머지** | MIT 존, 이 계획의 1·3-5걸음 |
| #398 | 감시 엔진 (`WatchAnswers` 4문답) | **보류** | 아래 |

**#398을 보류하는 이유** (창업자 결정 3과 같은 논리 — 엔진은 남기고 소비처
없는 표면은 짓지 않는다). `PremiseState.watch`(`premises-core.ts:187`)는
**선언돼 있고 아무도 읽지 않는다**(`recheck.ts` 미참조, 기계 확인). 이번 세션이
찾아낸 질병의 이름이 정확히 그것이고(`if_false_changes`가 같은 병으로 죽어 있다),
같은 병을 하나 더 main 에 넣는 것은 앞뒤가 안 맞는다. 순수 엔진
(`estimateBaseline`·`cusumSeries`)은 브랜치가 갖고 있으면 되고, 숫자 감시가
실제로 필요하다고 2걸음이 말해 주면 그때 소비처와 **함께** 들어온다.

**#370 의 `live-loop` 빨간불은 제품 결함이 아니다** (2026-08-18 로그 확인).
그 잡은 PR 에서 `skipped` 라 머지를 막지 않고, 08-11 수동 dispatch 한 번만
실행됐다. 그 실행이 말하는 것:

- **익명 경로 15/15 초록** — 6단계("질문 → 초안 → 시험 → 최종 문서")까지 완주.
  #370 이 고친 것이 정확히 그 자리이므로 **수리는 통했다.**
- **로그인 경로만 빨강**: `0b. 로그인이 성사된다` →
  *"이메일 또는 비밀번호가 올바르지 않습니다."* `DOGFOOD_EMAIL`/`DOGFOOD_PASSWORD`
  시크릿이 만료·불일치라는 뜻이고, **코드로 고칠 수 없다** — 창업자만 돌릴 수 있는
  자격이다. #370 본문도 이것을 "알려진 로그인 자격 실패"로 이미 적어 두었다.

**머지 순서**: #370 → #397 → #399 → #396. 앞의 셋은 서로 다른 파일을 만지고,
#396 이 문서 51개를 옮기므로 마지막이 충돌이 가장 적다.

### 1걸음 — 계측기에 시계를 켠다 ✅ 완료 (PR #399, 제품 코드 0줄)

`evals/first-user-journey.mjs` 세션2의 모든 도구 호출에 `today_override` 를
싣는다. 날짜는 지어내지 않고 **원장의 `plan_adopt` 에서 가장 이른 `due`** 를
읽어 그날로 간다 (예약은 이른 순 `PLAN_MAX_SCHEDULED` 개이므로 최이른 단계는
언제나 `scheduled` 다 — `ledger-replay.ts:490`).

- 주입은 스프레드 **뒤**: 모델이 보낸 날짜를 덮는다. 타임머신은 계측기의 것이다.
- `summary.time_travel = { session2_today, note }` 에 기록.
- 관문 라벨이 시계 상태를 진다:
  `단계 결과가 원장에 남았다 (plan_check · ⚠ 시계 미이동 — 확인일이 오지 않아 측정 불가)`
  — **시계를 안 옮긴 실행의 ❌ 는 제품 실패가 아니라 측정 불가다.**

### 1b걸음 — 모델 없는 대조군 ✅ 완료 (PR #399)

라이브 실행에는 모델이 필요하고 모델은 비싸고 흔들린다. 그 전에 **모델을 빼고
시계만 돌려** 제품 쪽 호가 실제로 도는지 확정했다. 대조군 없는 라이브 측정은
원인을 못 가른다 — 이것이 초록이면 라이브의 빨강은 오직 모델의 라우팅이다.

`argus-mcp/evals/battery.mjs` 에 시나리오 셋, **전부 첫 실행 초록**:

- **S47** 확인일 전 침묵 → 확인일에 제품이 먼저 말을 건다 → `plan_check` →
  **적은 단계는 다시 안 묻는다**
- **S48** 날짜 단계 6개라도 예약은 정확히 3개, 표면이 그 사실을 스스로 말한다
- **S49** 정산으로 닫힌 결정은 단계를 더 묻지 않는다 (거울 조항)

```bash
cd argus-mcp && npm run build && node evals/battery.mjs      # 116 calls · 0 RED
```

> **판정: 귀환 기계는 돈다.** 한 번도 관측되지 않은 것은 "모델이 그것을 쓰기로
> 고르는 것"이다.

### 2걸음 — 첫 라이브 관측 ⏳ (API 키 있는 환경에서 명령 한 줄)

```bash
export ANTHROPIC_API_KEY=...
cd argus-mcp && npm run build
for P in 3 8; do
  node evals/first-user-journey.mjs --local --script plan --persona $P \
    --out /tmp/journey-plan-p$P
done
# 창업자 배역 (11차에서 가장 아팠던 프로필)
node evals/first-user-journey.mjs --local --script plan --persona 8 \
  --traits cog_directness=Blunt,cog_verbosity=Terse,cog_formality=Casual,cog_patience=Low,cog_conflict_approach=Confronting,decision_style=Directive \
  --out /tmp/journey-plan-founder
```

`--traits` 는 **`축id=값` 쌍**이다 (`persona-sampling.mjs` 의 `AXES`). 축 이름이나
값 하나만 틀려도 하네스가 유효 목록을 찍고 `exit 2` 한다 — 이 문서의 첫 판이
바로 그 형태로 틀려 있었고, 명령이 안 도는 계획은 계획이 아니다.

**먼저 확인할 것** — `summary.json` 의 `time_travel.session2_today` 가 `null` 이
아니어야 한다. `null` 이면 계획이 날짜 없이 채택된 것이고, 그 실행은 `plan_check`
관문을 **측정하지 못한 것**이지 실패한 것이 아니다.

**결말은 셋이고 각각 다음 작업이 다르다**:

| `summary.json` 이 말하는 것 | 읽는 법 | 다음 |
|---|---|---|
| `ledgerEventCounts.plan_check > 0` | **한 바퀴가 처음 돌았다** | 4걸음으로 |
| `plan_check == 0` 인데 세션2 도구 목록에 `argus_check_in` 호출이 있다 | `plan_due` 는 떴는데 모델이 다른 도구로 샜다 — 11차 "동사 선택" 진단이 **이번엔 증거를 갖는다** | 3걸음: 손잡이를 모델이 이미 향하는 도구 안에 둔다 |
| 세션2에 `argus_check_in` 호출 자체가 없다 | 문제는 귀환 기계가 아니라 **진입**이다 | 3걸음 아님 — 호스트가 세션 시작에 무엇을 보는가 |

무엇이 나오든 `docs/receipts/` 에 적는다. 관문 점수만 적고 트랜스크립트를
안 남기면 다음 사람이 같은 오독을 반복한다.

#### 08-18 저녁 — 에이전트 배역으로 먼저 돌렸다 (상한 측정)

키가 없어 하네스는 못 돌렸지만, **하네스가 모델을 필요로 하는 자리가 둘**
(사용자 역·어시스턴트 역)이고 그 둘을 에이전트가 직접 했다. 창업자 프로필로
**여덟 고리 전부 완주** → [리시트](./receipts/2026-08-18-founder-role-lap/README.md).

이 결과의 지위: **상한**이다. 그 어시스턴트는 이 저장소를 아는 에이전트이므로
"도구 표면이 눈먼 모델을 끝까지 데려가는가"에는 답하지 못한다. 다만 그 반대는
답한다 — **제품 쪽에서 루프를 막는 것은 없다.** 남은 빨간불이 나오면 그것은
도구 설명·표면·라우팅의 문제이고 배관의 문제가 아니다.

그 실행이 결함 하나를 잡아 고쳤다: `data.due_open_questions[].decision` 이
결정 대신 예측을 보여줬다(fold 에서 `seal` 이 `text` 를 덮어썼다). 그 값을
읽는 것은 모델이고 모델은 그것을 "당신의 결정"이라고 말한다 — 3걸음의 첫 항목이
측정으로 확정되어 닫힌 자리다.

### 3걸음 — 2걸음이 부러뜨린 것만 고친다 ⏸

확인된 결함 다섯. **후보이지 목록이 아니다** — 2걸음이 실제로 무엇을 부러뜨리는지가
목록을 정한다. 순서를 뒤집으면(재보지 않고 먼저 고치면) 이번 세션이 하루를 태운
그 실수를 그대로 반복한다.

| | 결함 | 어디 | 상태 |
|---|---|---|---|
| 1 | `if_false_changes` 를 받아 *"나중에 다시 확인한다"* 고 약속하고 버린다 | `public-tools.ts:39` (내부 스키마에 칸 없음) | ✅ **완료 (PR #399)** — 내부 스키마 → 이벤트 → fold → **재확인 자리**까지 관통 |
| 2 | ~~`stakes` 가 필수인데 모델이 추측하고, 그것이 발화 게이트를 돌린다~~ | `open-decision.ts` · `overfire-gate.ts` | ⛔ **진단 철회 (2026-08-19)** — 결함이 아니다, 아래 |
| 3 | 텍스트 전제가 `uncertain` 에 도달 불가 | `recheck.ts:145-160` | ✅ **완료 (PR #399)** — `changed:'uncertain'`, 새 상태 0 |
| 4 | `source` 기본값 `user_stated` | `public-tools.ts:138,493` | ✅ **완료 (PR #399)** — 기본값을 `host_reported` 로 |
| 5 | **MCP 에 Lesson 이 없다** | `settle.ts` | ✅ **완료 (PR #399)** |

#### 2번은 결함이 아니었다 — 진단을 철회한다 (2026-08-19)

**틀린 진단**: *"`stakes` 는 필수인데 모델이 추측하고, 그것이 발화 게이트를
돌린다. 저자성 추적이 없으므로 `PRODUCT.md` 제약과 정면으로 닿는다."*
갈래 셋과 권고((a) 모델의 것으로 인정)까지 만들어 올렸다.

**창업자 반문**("그게 무슨?") 뒤에 값을 끝까지 따라간 결과:

| 물음 | 사실 |
|---|---|
| 어디에 저장되나 | `gate_input` **메타 이벤트**뿐 (`open-decision.ts:107`) |
| replay 가 읽나 | **안 읽는다** — `ledger-append.ts` 가 *"meta event, ignored by replay"* 라 적어 두었다 |
| 사용자 화면에 나오나 | **안 나온다.** `surfaces.ts` 의 `stakes` 등장은 전부 게이트의 자제 사유 문구다 ("되돌리기 쉽고 크게 걸린 것도 없는 결정입니다") |
| 사용자의 판단이라 주장하나 | **한 번도 안 한다** |
| 하는 일 | 하나뿐 — Argus 가 질문을 던질지 조용히 있을지 |

즉 **모델이 자기 자제를 정하려고 자기가 읽은 값**이고, 결정 기록에도 안 들어가고
사용자 판단인 척한 적도 없다. `PRODUCT.md` 제약("명시적 채택 없이 사용자 결정을
추론·저장하지 않는다")은 **사용자의 결정**에 대한 것이고 이건 그게 아니다.

그리고 구조적으로도 다른 방법이 없다: *"질문을 드릴까요?"* 를 정하려고 먼저
*"이거 얼마나 큰 일이에요?"* 를 물으면 질문을 하기 위해 질문을 하는 것이다.

**권고했던 (a) 는 설명 문구만 바꾸는 것이었다 — 동작은 그대로.** 화장이었고,
안 고치는 것이 맞다.

**이 항목이 여기 남아 있는 이유**: 잘못된 진단을 지우면 다음 사람이 같은 곳에서
같은 오진을 반복한다. 지우지 않고 철회로 남긴다.

**5번을 가장 먼저 한 이유**: 앞의 넷은 *고장난* 것이고 5번은 *없는* 것이었다. 그리고
없는 그것이 §0 이 정의한 완성의 마지막 문장 — "규칙 한 줄이 남는다" — 그 자체다.
측정이 무엇을 말하든 그 고리가 없으면 완성이 성립하지 않으므로, 측정 대기 대상이
아니었다. (설계·검증은 [리시트 §4·§5](./receipts/2026-08-18-loop-clock/README.md).)

### 4걸음 — 창업자 1회 완주 ⏳ (붙여넣기 2번, **하루** 간격)

**이것만 코드로 못 한다.** 3걸음까지의 모든 바퀴는 `today_override` 로 옮긴 논리
날짜였다 — *기계가 기한을 인식하는가*를 쟀고, *사람이 기한에 돌아오는가*는 재지
않았다. 후자가 H-B 이고 증거는 오늘까지 **0건**이다.

#### 먼저 — 기다리지 않고 오늘 확인하는 것 (배관)

"다 돌아가는가"는 하루도 안 기다리고 지금 확인한다. 이건 4걸음이 아니라 4걸음
**앞**이다.

```bash
cd argus-mcp && npm run loop:demo
```

실제 dist 서버를 띄우고 논리 날짜 셋(결정일 → 기한 → 정산일)을 지나 한 바퀴를
돌린다. 모델·API 키·대기 없음. 모든 표면과 모든 입력창을 그대로 찍고 자체 검사
14개를 돌린다. 마지막 줄이 `루프가 끝까지 돕니다` 면 배관은 이어져 있다.

**이것이 증명하는 것**: 기계가 기한을 인식하고, 먼저 말을 걸고, 사용자의 말을
그대로 받아 적고, 규칙 한 줄까지 남긴다.
**이것이 증명하지 못하는 것**: 진짜 달력의 기한에 **사람이** 돌아오는가. 그건
아래뿐이다.

#### 4걸음 본체 — 오늘 한 번, **내일 아침** 한 번

기다림은 **하루**다. 이틀이 아니다. 코드가 그렇게 되어 있다:

| 코드 | 무엇을 강제하나 |
|---|---|
| `argus-mcp/src/tools/plan.ts:127` → `isFutureDate` (`resolve-today.ts:142`, `c > today`) | 기한은 **오늘보다 커야** 한다 → 최소가 내일 |
| `argus-mcp/src/tools/check-in.ts:321` (`s.due <= today`) | 기한이 **오늘 이하**면 그날 발화 → 내일 기한은 내일 발화 |

둘을 곱하면 가능한 최단 간격은 **+1d**, 즉 오늘 심고 내일 아침에 받는다.
(이전 판에 적혀 있던 `+2d`·"이틀 뒤"는 근거 없이 하루를 더 얹은 것이었다.
2026-08-19 정정.)

#### 오늘 — 이걸 그대로 붙여넣는다 (Claude Code 나 Desktop, argus 연결된 세션)

```
지금 실제로 걸려 있는 결정 하나를 argus 에 남기고 싶어. 내가 말할게.

<여기에 대표님의 실제 결정 한두 문장 — 예: "채용 공고를 이번 주에 올릴지
다음 달로 미룰지 정해야 해. 지금 팀이 바빠서 면접 볼 시간이 없는데
공고는 빨리 올려야 반응이 오거든.">

이걸 argus 에 결정으로 기록하고, 실행 계획을 짧게 짜서 보여줘.
첫 단계는 **내일(+1d)** 확인할 수 있는 것으로 잡아. 내가 보고 좋으면
그대로 가자고 할게. 그리고 예측 하나도 남기자.
```

포인트 둘. ① **도구 이름을 대표님이 지목하지 않는다** — 지목하면 "진입"을
재는 게 아니라 지시를 재는 게 된다. ② 첫 단계 기한이 **+1d** 여야 한다. 그게
내일 뭔가 도래하게 만드는 유일한 조건이다. (`+2d` 로 잡아도 루프는 같으나
하루를 더 기다리게 될 뿐이다.)

#### 내일 아침 — 아무 준비 없이 터미널을 열고 이것만

```
아침이다. 뭐 있나.
```

**여기서 제품이 먼저 말을 걸어야 한다.** 걸면 그 자리에서 실제로 있었던 일을
한 줄 말한다. 안 걸면 그것이 발견이다.

#### 그날 확인할 것 (셋 다 결과다)

```bash
L="$(git rev-parse --show-toplevel 2>/dev/null)/.argus/ledger/ledger.jsonl"
[ -f "$L" ] || L="$HOME/.argus/ledger/ledger.jsonl"
grep -c '"event":"plan_adopt"'  "$L"   # 계획이 날짜와 함께 남았나
grep -c '"event":"plan_check"'  "$L"   # 그 자리에서 결과를 적었나
grep -c '"lesson"'              "$L"   # 규칙 한 줄이 남았나 (정산까지 갔을 때)
```

| 결과 | 무슨 뜻 | 다음 |
|---|---|---|
| 제품이 먼저 말을 걸고 결과를 적었다 | **H-B 증거 1건.** 오늘까지 0건이던 것 | 사람 10명 (PRODUCT-PLAN §6 3주차) |
| 말은 걸었는데 안 적었다 | 귀환은 도착하고 기록이 안 된다 | 그 자리의 마찰을 본다 |
| 말을 안 걸었다 | 문제는 귀환 기계가 아니라 **진입** | 호스트가 세션 시작에 무엇을 보는가 |
| 애초에 계획을 안 받았다 | 첫 세션 가치부터 안 도달 | H-A 재검토 |

**어느 결과든 실패가 아니다.** 넷 다 다음 작업을 정한다. 유일한 실패는 안 해서
아무것도 모르는 것이다.

> **왜 이 한 번을 코드가 대신할 수 없나.** 배관은 `loop:demo` 가 증명한다. 남은
> 것은 배관이 아니라 사람이다 — 기한 날 아침에 실제로 터미널을 여는 사람이 있고,
> 그 사람이 제품이 건 말에 답을 적는가. 논리 시계를 옮겨서는 이 질문에 절대
> 닿을 수 없다. 시계를 옮기는 쪽이 관찰자이기 때문이다.

### 5걸음 — 웹 트랙 판정 ✅ (판정만 완료, 시공은 미룬다)

`docs/HANDOFF-2026-08-10-DECISION-LOOP-AUDIT.md` 의 DLP-1~10 을 각각 감사가 적은
**완료 증거 기준**으로 대조했다. 2026-08-18 기준, 코드 대조(브라우저 실주행 아님):

| ID | 감사가 요구한 완료 증거 | 판정 | 근거 |
|---|---|---|---|
| DLP-1 | 같은 케이스가 web/MCP/plugin 을 state·move 온전히 왕복 | ❌ 미수리 | `next_move`/`nextMove` 가 리포 어디에도 없다 |
| DLP-2 | 못 본 AI 문장이 채택 상태가 될 수 없다 | ✅ 수리 (PR #370 대기) | 커밋 746eb959 |
| DLP-3 | crux 와 채택된 판단이 분리, 미결은 미결로 남는다 | ⚠️ **절반** | 저자성은 정직해졌다 — 손대지 않은 초안은 `ai_surfaced`+`user_adopted` 로 기록(`SealMoment.tsx:299-320`). 그러나 **구조 분리는 없다**: `adopted_judgment` 필드 부재, research/defer/reframe 상태 부재 |
| DLP-4 | 평평한 케이스가 첫 기여 뒤 ≤2 물음으로 닫힌다 | ❌ 미수리 | `CheckpointRail.tsx:89` 가 여전히 `진행 N/M: 라벨` 을 찍는다 (감사가 본 "Progress 2/9" 그대로) · compact close 경로 없음 |
| DLP-5 | 영수증이 사슬 전체를 보이고 정확한 기록으로 간다 | ✅ 수리 (PR #370 대기) | 커밋 746eb959·361d4db6 |
| DLP-6 | 하나의 projection 이 Move/Wait/Return/Learned 와 모든 합계를 굴린다 | ❌ 미수리 | `project/page.tsx:739` 주석이 *"① 항해 지도 — 주인공"* 이다. 지도가 여전히 첫 뷰포트의 주인공이고 네 그룹 IA 는 없다 |
| DLP-7 | 첫 유용한 기여가 기록 설정보다 먼저 온다 | ❌ 미수리(추정) | DLP-4 와 같은 흐름이고 그 흐름이 안 바뀌었다. **브라우저 실주행으로 확인 안 했다** |
| DLP-8 | 스케일 설정 정합 + 진짜 8px/직접색 위반 제거 | ❌ 미수리 | `responsive-type-scale.test.ts` 는 있으나 지키는 것이 다르다 — 2026-07-28 가독성 스윕의 산물(넓은 화면에서 줄어드는 활자) 가드지, DESIGN.md 산문↔기계 스케일 정합이 아니다 |
| DLP-9 | 주 크롬이 Decide·Decisions·Record·Move·Return·Lesson 을 쓴다 | ❌ 미수리 | `project/page.tsx:823,858,864` 가 "프로젝트 상태 필터"·"프로젝트 검색" |
| DLP-10 | 현재 결정·행동이 폭을 유지하고 지도·이력은 접힌다 | ❌ 미수리(추정) | DLP-6 과 같은 파일·같은 원인. **브라우저 실주행으로 확인 안 했다** |

**정직한 한계**: 이것은 코드 대조다. DLP-7·10 은 감사가 브라우저 실주행으로
찾은 항목이라 같은 방식으로 확인해야 확정이고, 이 환경은 Supabase 키가 없어
`/[locale]` 이 500 이다(CLAUDE.md 함정). 그래서 둘은 **추정**으로 적는다.

**요약: 10건 중 2건 수리(둘 다 #370 에서 대기), 1건 절반, 7건 열림.**
그리고 열린 7건 중 넷(DLP-1·3·4·6)이 서로 같은 뿌리다 — **채택된 결정 상태와
Next Move 라는 1급 표현이 없다.** 감사 자신이 그것을 §8.1 `DecisionLoopCore`
제안으로 적었다. 낱개로 고칠 것이 아니라 그 표현을 먼저 세우는 일이다.

### 그 시공은 계속 미룬다 ⏸

미루는 근거는 둘이다. 하나는 **창업자 결정 2**(무게중심 = 동반자, 1차 표면은
MCP 도구·플러그인 훅이고 웹은 정산과 거울 — `ARGUS-CANON.md` §9). 다른 하나는
순서다: 웹과 MCP 는 같은 루프의 두 투영이고, 루프가 한 번도 안 돈 상태에서
투영을 고치면 **무엇을 고쳤는지 알 수 없다.** MCP 한 바퀴가 돌면 그 바퀴가
웹의 목록을 다시 써 준다.

단 **#370은 예외로 지금 머지한다** — 이미 초록이고, DLP-2(못 본 문장이 채택으로
새는 것)는 저자성 결함이라 표면 우선순위와 무관하게 `PRODUCT.md` 제약을 어긴다.
그리고 웹의 Lesson 이 그 안에 있다.

## 4. 하지 않는 것

- 새 도구 · 새 표면 (`Lesson` 은 새 어휘가 아니다 — `CONTEXT.md` 낱말 11개에 있다)
- 원격 MCP · OAuth · 디렉토리 등재 (PRODUCT-PLAN §5: H-B 증거 이후)
- 사용자 10명 (기계 수준 한 바퀴는 돌았지만 사람 한 바퀴가 0건이다 — 4걸음이 먼저)
- 4문답 재설계 (`watch` 어휘는 보류로 판정됐다 — 0걸음 #398)
- **2걸음 전의 결함 수리** (측정 없이 고치는 것이 이번 세션의 실패였다.
  예외는 3걸음-5 뿐이고, 그것이 예외인 이유는 그 절에 적혀 있다)

## 5. 이 계획이 틀렸다고 드러나는 지점

1. ~~시계를 켰는데 `plan_due`가 여전히 안 뜬다~~ → **해소됨.** 1b걸음의 대조군이
   `plan_due` → `plan_check` → "적은 단계는 다시 안 묻는다"까지 전부 초록으로
   확인했다. replay·스케줄링은 의심 대상이 아니다.
2. 2걸음에서 모델이 `check_in` 자체를 안 부른다 → 문제는 귀환 기계가 아니라
   **진입**이고, 다음 작업은 제품 로직이 아니라 호스트가 세션 시작에 무엇을
   보는가다.
3. `plan_check`까지 돌았는데 창업자 실주행(4걸음)에서 아무것도 안 돌아온다 →
   같은 결론(진입). 도구를 꺼낼 이유가 없는 것.
4. 4걸음에서 창업자 본인이 기한에 안 돌아온다 → H-B가 반증된 것이고,
   PRODUCT-PLAN §6이 예고한 축소 지도("첫 세션 가치만 있음")로 간다.
   그건 실패가 아니라 이 제품이 6주 전에 미리 적어 둔 정직한 출구다.
5. 규칙 창이 실제로 열렸는데 아무도 안 쓴다 → 정상일 수 있다(대부분의 귀환에는
   규칙이 없다). **여러 번 연속 빈 제출이면** 묻는 자리가 틀린 것이고,
   `held` 제외 규칙을 더 좁힌다(예: 사용자가 먼저 언급했을 때만).
6. 웹 트랙 8건이 사실은 이미 고쳐져 있다 → 5걸음이 판정만으로 닫힌다.

## 6. 순서

```
0걸음 재고 정리 ─→ [1 ✅ 시계] ─→ 2걸음 첫 라이브 관측 ─→ 3걸음 그 결함만 수리
        │           [1b ✅ 대조군]          │
        │                                   └─→ (3-5 ✅ Lesson 은 선행 완료)
        └────────────────────────────────→ 4걸음 창업자 완주 ─→ 5걸음 웹 판정
```

**4걸음은 2걸음을 기다리지 않는다.** 재는 것이 다르다 — 2걸음은 *기계가 기한을
인식하는가*, 4걸음은 *사람이 기한에 돌아오는가*. 후자가 H-B 이고 그것만이
"사업의 전부"다. 시뮬레이션이 아무리 초록이어도 4걸음의 증거 1건을 대신하지
못한다.

## 7. 이 문서를 갱신하는 때

걸음이 하나 끝날 때마다 **상태 표와 §1 고리 표**를 같이 고친다. 계획이 현실과
어긋난 채로 남아 있으면 다음 세션이 이미 지어진 것을 다시 짓는다 — 이 세션이
`watch` 로 하루를 태운 그 방식 그대로.

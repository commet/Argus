# ARGUS — THE MASTER DIRECTION (v4, PROVABLY-VERIFIED)

> Single source of truth. **Supersedes `docs/PLAN.md`, `docs/MASTER-DIRECTION.md` (Rev 2), and `docs/MASTER-DIRECTION-v3.md`.**
> Synthesizes four prior workflows A (DEEP-PLAN) / B (BLINDSPOT) / C (TARGET-STATE) / D (RADICAL-RETHINK), plus a full second-pass verification, a basic-usability confusion sweep, a non-UX gap audit, **and a per-claim re-verification pass against `docs/VERIFICATION-PROTOCOL.md` (E1–E7).**
> **Direction unchanged: HYBRID** — extend the voyage DNA (C) + one radical graft (the Decision Contract closed loop, §0); foundation-fixes-first (L0); heavy/irreversible bets gated behind cheap kill-tests (§4).
> **Principle inherited & confirmed:** smallest safe step first, test-gated, never build on sand.
>
> **CLAIM-TAG LAW (this document obeys `VERIFICATION-PROTOCOL.md`):** every load-bearing claim carries `[VERIFIED: file:line + quoted source]`, `[INTENT]`, or `[ASSUMPTION: how to verify]`. Counts/sets show a fresh grep. Reuse claims name producer **and** consumer. Mechanisms name their contract. `(corrected: …)` marks every place a v3 specific was wrong, imprecise, or stale.

---

## CHANGELOG vs v3

This section lists **every** correction and addition folded from the re-verification + class-sweep passes. Each is applied inline below at its section.

### A. Re-verification corrections (specific was wrong/imprecise/stale; direction kept)

1. **§5/§7 contrast value — WRONG (E1/E6).** v3 asserts `--accent` text = **4.18:1** in three places (§5, §7 two rows). Recomputed this turn from the cited hex pair: `--accent #96782e` on `--bg #f8f7f5` = **3.9:1**, not 4.18:1. Direction unchanged (3.9 < 4.5 = AA fail). All three occurrences corrected. The replacement token `#7d6526` must be **re-measured** before being cited as AA-passing (now tagged `[ASSUMPTION]`).
2. **§5 reduced-motion allowlist — WRONG (E2).** v3 lists `gold-shimmer (:283)` and `border-glow (:292)` among "MISSED" infinite animations. `[VERIFIED: globals.css:468-469 — `.animate-gold-shimmer,` / `.animate-border-glow {` inside the `@media (prefers-reduced-motion: reduce)` block]` — they ARE covered. The genuinely-uncovered infinite animations are `pulse-skeleton (:266)`, `subtle-pulse (:360)`, `bm-bounce (:1264)`, `inner-glow (:1590)`.
3. **L2 hook count — WRONG (E5).** v3 says "46 useState/useRef/useEffect hooks." Fresh grep on `ProgressiveFlow.tsx`: 21 `useState` + 15 `useRef` + 7 `useEffect` = **43 declarations** (the store has 0). Corrected to 43; the figure now names its unit (declarations).
4. **L2 scroll coupling — MISATTRIBUTED (E4).** v3 files "scroll-into-view (`:951` + 28 scroll refs)" under `useProgressiveStore`'s coupled concerns. `[VERIFIED: useProgressiveStore.ts has 0 `scrollIntoView`; store `:951` is `(p.name.toLowerCase().includes(hint)...` — an unrelated `personas.find` predicate]`. Scroll-into-view is a **COMPONENT** concern in `ProgressiveFlow.tsx` (`scroll`/`scrollToRef` `:943-953`, `scrollIntoView` `:951`). "28 scroll refs" matches no grep — corrected to **23 `scrollToRef(` callsites** (~9 distinct target refs).
5. **L3b assumption-file count — WRONG (E5).** v3 says "31 files emit/consume `hidden_assumptions:`/`key_assumptions:`." Two counts exist depending on the pattern: `[VERIFIED: grep -l 'hidden_assumptions:|key_assumptions:' (with the literal colon, as v3 wrote it) = 31 files]`; without the colon = 57 (with tests) / 40 (non-test). v3's "31" is only correct for the **colon-suffixed** pattern (the YAML-emit slice); stated explicitly so the slice is unambiguous.
6. **§7 `callLLMStreamThenParse` count — WRONG (E5).** v3 says "8 callLLMStreamThenParse callsites." `[VERIFIED: 7 invocation callsites — progressive-engine.ts:307,385,458,641,776,812,895; the 8th occurrence is the import at :5]`. Corrected to 7.
7. **§7 `parseJSON` "direct" count — IMPRECISE (E3).** v3 counts "~5 direct" parseJSON without applying the engine-internal exclusion it applies to `JSON.parse`. `[VERIFIED: 5 direct parseJSON calls — RecastStep.tsx:337, ReframeStep.tsx:500/659, llm.ts:538/777; of these 2 are llm.ts engine internals]` → **3 app-direct + 7 callLLMStreamThenParse**.
8. **§7 `llm-validation.ts` line count — IMPRECISE (E1).** v3 says "72-line module." `[VERIFIED: wc -l = 71]`. Corrected to 71; substance (complete, do-not-rebuild) unchanged.
9. **L0/§7 `decision-quality.ts:83` caveat — WRONG (E2). DELETED.** v3 warns `:83` reads `reframe.analysis.hidden_questions || []` "without guarding `reframe.analysis` being null." `[VERIFIED: decision-quality.ts:82 `if (reframe?.analysis) {` then :83 inside it]`. `:83` IS guarded. The caveat is removed; `decision-quality.ts` is closed outright.
10. **§5 framer-motion importer count — IMPRECISE (E5).** v3 says "36 framer-motion importers." `[VERIFIED: grep "from 'framer-motion'" = 38 files this turn]`. Corrected to 38.
11. **§5 "12+ files import EASE" — IMPRECISE (E5).** Fresh grep = **18** files import `EASE`. The "12+" floor holds; stated as exactly 18.
12. **L0/§5 `validateShape` attribution — IMPRECISE (E4).** v3 cites "reuse validateShape from `llm.ts:778-781`." `[VERIFIED: validateShape DEFINED at llm.ts:282; :778-781 is the CALL/wiring site inside callLLMStreamThenParse]`. The reuse pointer now cites the **definition (:282)**; the wiring stays `:778-781`. Also: wiring is **CONDITIONAL** on `options.shape` (ternary at :778) — without a passed shape it returns the input unvalidated. Qualified everywhere.
13. **§1 "provenance markers" on context-chain — OVERSTATED (E7).** v3 calls the returned `ReframeContext`/`RecastContext` "with provenance markers." `[VERIFIED: context-chain.ts has NO `_source`/provenance FIELD on returned contexts; only a comment at :504 + the assumption-merge]`. Split into `[VERIFIED: typed contexts]` + `[INTENT: provenance markers]` (consistent with L3a's own "`_source` is net-new").
14. **§2 `PersonaAccuracyRating` attribution — IMPRECISE (E4).** v3 cites "`useAccuracyStore.ts:10` PersonaAccuracyRating" as if line 10 defines it. `[VERIFIED: interface defined at types.ts:399; useAccuracyStore.ts:10 is the addRating Omit USE site]`. Re-attributed to types.ts:399.
15. **L0-WRITE CustomEvent reuse — WRONG on 2 of 3 named consumers (E3/E6).** v3 names "the existing CustomEvent pattern (`llm.ts:721-724` → PingToast/UnlockToast/RateLimitBadge)." `[VERIFIED: only RateLimitBadge.tsx:23 listens `window.addEventListener('argus:ratelimit')`; PingToast/UnlockToast have NO addEventListener/CustomEvent — they are store-driven]`. Corrected to cite **RateLimitBadge + SyncStatus.tsx:39 (`argus:sync`)** as the real precedent. Mechanism contract added: dispatch a window CustomEvent from `storage.ts` (low-level lib) to **avoid the store-import circular dependency** its own `:42` comment warns about.
16. **§UX-GLOBAL-CHROME lock count — WRONG (E5).** v3 says "three lock-gated items." `[VERIFIED: Header.tsx:18 Projects + :19 Agents carry `requiresAuth: true`; Manager (/boss, :20) does NOT]`. Corrected to **two** lock-gated items.
17. **§UX-L0 "4 Ithaca outputs" vs "5-format OutputSelector" — SELF-CONTRADICTION (E5).** `[VERIFIED: OutputSelector has 5 formats — brief:36, prompt-chain:46, agent-spec:58, checklist:66, rationale:76]`. The footer-band copy "4가지 산출물" undercounts. Corrected to 5 (rationale is the decision-rationale/process output — the differentiator the plan elsewhere elevates).
18. **§UX-L4 "orchestra jargon" framing — OVERSTATED (E2).** `[VERIFIED: labels already pair metaphor + plain name via ' · ' — OutputSelector.tsx:38 '프로그램 노트 · Project Brief', :48 '파트보 · Prompt Chain', :58 '총보 · Agent Spec', :68 '셋리스트 · Execution Checklist', :78 '판단 근거서 · Decision Rationale']`. The fix is reordering/demoting the metaphor + adding a Korean plain label + audience tag, NOT introducing a missing plain name.
19. **L2 "add a component/render test" prerequisite — OVERSTATED (E3).** `[VERIFIED: progressive/__tests__ already contains 5 render tests — flow-interactions.test.tsx, team-deploy-render.test.tsx, voyagechart-render.test.tsx, logbook-render.test.tsx, logbook-fork-render.test.tsx; flow-interactions:32 imports leaf components FROM ProgressiveFlow and renders them]`. The real gap is **orchestration coverage** (scroll/worker/UIState seams + the main default export), not "any render test."
20. **L3a/§UX `RehearseStep.tsx` path — WRONG DIR (E4).** v3 implies `components/progressive/RehearseStep.tsx`. `[VERIFIED via Glob: the only RehearseStep.tsx is at src/components/workspace/RehearseStep.tsx]`. Line :194 is correct; path corrected.
21. **L0 `RecastStep.tsx` / §UX bare-filename paths — PRECISION (E4).** Path-corrected: `RecastStep.tsx` → `src/components/workspace/RecastStep.tsx`; `FinalCard`/`Logbook`/`WorkerPanel` → `src/components/workspace/progressive/`; `OutputSelector`/`CopyButton` → `src/components/ui/`.
22. **L5 migrators line list — IMPRECISE (E4).** v3 cites "migrateBranches/migrateWorkers/migrateSessionDrafts (:356/391)" — names three but cites two lines, omitting migrateWorkers. `[VERIFIED: migrateWorkers:330, migrateSessionDrafts:356, migrateBranches:391; chained :447/:486]`. All three line sites now cited.
23. **L1 `getSignals` line — IMPRECISE (E1).** v3 cites `signal-recorder.ts:49`. `[VERIFIED: getSignals defined :44, getSignalsByType defined :61]`. Both locators corrected.
24. **L1 `runAllAIWorkers` attribution — IMPRECISE (E4).** v3 cites the per-persona call "worker-engine.ts:141-151." `[VERIFIED: runAllAIWorkers ENTRY is :201; the per-worker callLLMStream is :141-151 inside the worker-runner helper]`. Behavioral claim kept; function entry corrected.
25. **§2 "7 learning sections" — IMPRECISE (E5).** `[VERIFIED: context-builder.ts numbers sections 1–7 but slot #4 is a removed no-op]` → "up to 6 active learning sections (one of seven numbered slots is a removed no-op)."
26. **L1b "~15 duplicated const L" — IMPRECISE (E5).** `[VERIFIED: 9 `const L =` definitions in ProgressiveFlow.tsx (:66,283,416,500,656,778,831,861,887); 142 `L(` call-sites]`. Corrected to 9 definitions.
27. **L1b "21 of 141 component files" — UNVERIFIABLE DENOMINATOR (E5).** Numerator 21 holds. `[VERIFIED: find src -name '*.tsx' non-test = 137; src/components/*.tsx = 102]`; no basis yields 141. Corrected to "21 of 137 non-test .tsx files."
28. **L1b BranchMap i18n example — WEAK (E5).** `[VERIFIED: BranchMap.tsx's only Hangul is a block comment at :4, not JSX]` → it would NOT trip a "literal Hangul in JSX" lint. Example qualified.
29. **L4 ShipCutaway/SailingShip primitive counts — IMPRECISE (E5).** Line counts hold (`[VERIFIED: wc -l ShipCutaway=1011, SailingShip=505]`). Primitive counts under a straight grep = 155 / 72 (v3: 148 / 69) — tagged `[ASSUMPTION]` (count depends on element set). "14 framer-motion loops" → `[VERIFIED: 14 `animate=` SVG props; 0 `motion.`/`repeat: Infinity`]` — relabeled "SVG animations, not framer loops."

### B. Class-sweep "violates"/"needs_tag" fixes

30. **E7 effort/size estimates stated as fact** (3-4 wks, ~1 line, M-effort, §4 probe times, every §UX `· effort` tag) → all tagged `[INTENT]` (predictions, not source-readable). Verified code anchors stay `[VERIFIED]`.
31. **E7 "60-80% infra reuse" (§1)** → `[ASSUMPTION: inherited from workflow C; verify by enumerating reused-vs-net-new modules per layer]` (no-silent-inheritance rule).
32. **E7 "ProgressiveFlow untouched" (§2 line 76)** → re-tagged `[INTENT]` to match the line-150 self-correction (the Verdict sidecar is UNBUILT; verify after build).
33. **E7 "Maintainability gate, not a value gate" (§L2)** → tagged `[INTENT/ASSESSMENT]`.
34. **E7 "1004 tests stay green / 1004 passed"** → split: `[VERIFIED: 53 *.test.ts(x) files]`; pass count `[ASSUMPTION: 1004 per a prior `npx vitest run`; re-run — not executed this turn]`; "stay green at every layer" `[INTENT]`.
35. **E7 positioning theses** (§0/§1 "the single differentiating move", "ChatGPT structurally cannot produce") → `[INTENT]`, cross-referenced to the §7 `UNVALIDATED-PREMISE` label.
36. **E7 contrast ratios** (2.16 / 3.58 / 3.9 / 2.63 / 3.65) → `[ASSUMPTION: WCAG ratios computed from the cited hex pairs; re-run a contrast checker]`. Token hexes + the false-comment existence remain `[VERIFIED]`.
37. **E6 onboarding gate placement (L4) — WRONG MECHANISM.** `[VERIFIED: `demoScenario` state + the demo-card `setDemoScenario` (:444) + the `if (demoScenario)` render (:263) all live INSIDE `function HeroFlow` (:145-632); the `if(!currentProjectId)` branch at :690 is in the PARENT `WorkspaceContent` and has no access to HeroFlow's `setDemoScenario`]`. The gate must be a mount-effect **inside HeroFlow** that calls its local `setDemoScenario(...)`, not a JSX wrapper at :690.
38. **§6 "dead useTeamStore" — CONTRADICTED (E3).** `[VERIFIED: app/teams/page.tsx imports/uses useTeamStore]` — not dead. Re-tagged: the store is consumed; only "no NEW blind-read activation on strategist surfaces" is `[INTENT]`.

### C. MUST-FOLD sequencing resolutions + new tracks

39. **(SEQ-1) db.ts merge fix consolidated to ONE home** — was in 3 places (L0 P2 / L5 / L4-prereq). Now lives only at **"L4 PREREQUISITE — tombstone-aware merge + empty-stamp tie-break."** L0 and L5 cross-reference it, do not restate the mechanism.
40. **(SEQ-2) L3a stable-id split** — was "after L1" AND "before Decision Contract." Now explicit: **L3a-0 (predicate/assumption stable-id at generation, ships at L1 timing, BEFORE Decision Contract + return-to-grade)**; persistence/`_source`/`traceProvenance` stay in **L3a proper**.
41. **(SEQ-3) reflection tab demoted, convergence gauge promoted** — reflection tab is near-empty on runs 1-2 (tier-gated); demoted to **run-3+ payoff**. Convergence gauge becomes the **L1 headline win** with a run-1 acceptance line.
42. **(ADD-4) L0 second infinite-spinner guard** — "stream completed but `parsePartialAnalysis` yielded no real_question AND no skeleton" → visible error + retry.
43. **(ADD-5) L0 per-fix regression tests** — brief export on `analysis.steps=undefined` returns a string and does not throw; `record*` eval on missing steps; worker no-callback path blocks not accepts.
44. **(ADD-6) L0 observable-async-write helper** — one shared helper; route all three swallowed-insert paths (`analytics.ts:176` .then-no-catch, `db.ts insertToSupabase`, `loadAndMerge` re-upsert) through it, each bumping a per-session sync-failure counter.
45. **(ADD-7) First-30-seconds MOBILE items pulled into L0/L1** — landing/entry orientation + analyze screen ship in the rage-quit window (§4 Probe 3); deep mobile reskin stays in L4.
46. **(ADD-8) §UX-GLOBAL-CHROME sub-track** — persistent Header (6 nav items, 2 lock-gated, KO/EN + theme toggle) + the double-CTA problem.
47. **(ADD-9) §4 Test-1 historical-predicate backfill** — Wizard-of-Oz on ~15 existing voyages predates predicate ids; state how historical predicates are graded (manual tag) so the kill-test is runnable.

### D. Carried-correct from v3 (re-verified this turn, no change)

The v3 corrections #1–#13 and the §0/§7 archetype claims were re-checked and **hold**: stream-watchdog mechanism, brief/judgment null-derefs, silent auto-accept production branch, OUTCOME_RECORDS producer-less, store import set, db.ts re-upsert resurrection, @react-three dead, net-new tokens, useAccuracyStore rates personas. These keep `[VERIFIED]` (see each section).

---

## Verification status (per section)

| Section | VERIFIED | INTENT | ASSUMPTION (must verify before load-bearing use) |
|---|---|---|---|
| §0 KICK | 9 | 2 (positioning, retention thesis) | 1 (per-predicate record shape) |
| §1 North-Star | 6 | 3 (HYBRID spine, positioning, "stay green") | 3 (60-80% reuse; plugin no-hooks; context-chain src-wide negatives) |
| §2 Verdict | 5 | 3 | 1 (per-predicate vs persona decision) |
| §3 L0 | 14 | 2 (effort tags, watchdog future-behavior) | 3 (auto-persona, analytics canary re-open, decision-quality close已 done) |
| §3 L1/L1b | 12 | 3 | 2 (signal Supabase read; artifact schema choice) |
| §3 L2 | 9 | 2 (maintainability gate, untouched) | 2 (worker runtime :32/:213-216; src-wide negatives) |
| §3 L3a/L3b | 11 | 1 | 2 (validates_assumption_ids producer; blob versioning) |
| §3 L4 | 10 | 4 | 2 (per-predicate record; onboarding gate host) |
| §3 L5 | 11 | 1 | 1 (grade storage table pin) |
| §4 Probes | 5 | 5 (all probe efforts) | 1 (Test-1 backfill grading) |
| §5/§7 Design | 14 | 3 | 4 (all WCAG ratios; replacement tokens) |
| §UX | ~40 anchors VERIFIED | all `· effort` tags now INTENT | path-precision noted |

Totals are approximate per-section running tallies; the rule is absolute: **no load-bearing claim is unTagged.**

---

## 0. THE KICK — The Web-Native Closed Loop

(Thesis unchanged. Corrections folded.)

The single differentiating move and the spine the forward plan hangs on: the **Decision Contract closed loop**, in the form the web app can own. `[INTENT — product thesis; the retention premise it rests on is the §7 UNVALIDATED-PREMISE, falsified by §4 Tests 1/2]`

1. **Pre-flight (in-session wow, 100% web-native, ships on today's stack).** Every voyage ends NOT in dead markdown but in **3–6 specific, falsifiable predicates** — serialized from data the engine already computes:
   - recast AI-vs-human step assignments `[VERIFIED: types.ts:121 `actor: ActorRelationship;` inside `interface RecastStep` (:119-134)]`,
   - blindspot `classified_risks` `[VERIFIED: types.ts:255 & :362 `classified_risks: ClassifiedRisk[];`; produced live at RehearseStep.tsx:60-62 `classified_risks: concerns.filter(c => ... c.severity === 'critical').map(c => ({ text, category: 'critical' as const }))`]`,
   - and `governing_idea` `[VERIFIED: types.ts:158 `governing_idea: string;` inside `interface RecastAnalysis`]`.
   e.g. *"the CFO will object to cost at the pricing step."* Pure in-session value, felt immediately.

2. **Return-to-grade (the retention engine, web-native but EARNED).** It *earns the return* via the specific predicate: **"You predicted the CFO would object at pricing. Did it? [happened / avoided / partial]."**
   - **(a) user-reported outcome — NET-NEW write-path (NOT a repoint of live plumbing).**
     - Producer/consumer of the persona channel `[VERIFIED: useAccuracyStore.ts:34-43 addRating `setStorage(STORAGE_KEYS.ACCURACY_RATINGS, ratings)` (:42) + un-awaited `insertToSupabase('accuracy_ratings', rating)` (:43); consumer context-builder.ts:216 `getStorage<PersonaAccuracyRating[]>(STORAGE_KEYS.ACCURACY_RATINGS, []).filter(...)`; addRating called only at FeedbackResult.tsx:66]` — this is the **persona** rating channel, not an outcome channel.
     - OUTCOME_RECORDS is dead on BOTH ends `[VERIFIED: consumer context-builder.ts:61 `getStorage<OutcomeRecord[]>(STORAGE_KEYS.OUTCOME_RECORDS, []).filter(...)`, gated `if (outcomes.length >= 2)` (:62); fresh exhaustive grep `OUTCOME_RECORDS` over src = only context-builder.ts:61 (read) + storage.ts:13 (key def) + tests; grep `setStorage(...OUTCOME_RECORDS...)` / `addOutcome` / `recordOutcome` = 0 src hits → ZERO production writers]`.
     - **(NEW, E3, strengthens v3):** the dedicated outcome-analysis layer is ALSO uncalled `[VERIFIED: decision-quality.ts `correlateDQWithOutcomes` (:358), `analyzeConfidenceCalibration` (:436), `validateEvalCriteria` (:517) take `outcomes` as a PARAMETER; fresh grep of these three names over src returns only their definitions — zero callers]`. So wiring the DQ↔outcome correlation (the "KEY RESEARCH QUESTION" at decision-quality.ts:355-356) is **also net-new**, not just the capture UI. Budget BOTH.
   - **(b) paste-back** — user pastes the AI output; Argus grades pre-flight vs it via the existing eval engine. `[INTENT — buildable, not built]`.

**§0 corollary — the join-key prerequisite (P0 for data trust).** The grade must reference a **stable predicate `id`**, not free text. `[VERIFIED: MaterializedRisk keys by risk_text + persona_id — types.ts:487-493 `interface MaterializedRisk { risk_text: string; persona_id: string; category: ...; actually_happened: boolean; impact_description?: string; }` — no `id`; ClassifiedRisk constructed at RehearseStep.tsx:62 as `{ text, category }` only]`. There is genuinely no id-bearing predicate artifact today — a **verified gap**, not merely intent. **Pull the minimal stable-id slice (L3a-0) FORWARD** so each serialized predicate carries a deterministic id before the grading loop ships — otherwise §4 Test-1 grades noise.

**§0 corollary — the grading write must be observable (P1).** `[VERIFIED: useAccuracyStore.ts:42-43 setStorage then un-awaited insertToSupabase (no await, no .then/.catch); db.ts:275 `insertToSupabase(table, item): Promise<void>` — on supabase error :284-285 `log.error(...)`, catch :287-288 routes through `handleError(err, ...)`, returns void]` *(corrected: the catch path routes through `handleError()`, not solely `log.error`; both swallow and return void.)* A user who grades while offline gets a green check but the graded predicate is dropped to local-only. **Route it through the L0 observable-async-write helper (ADD-6); await/queue/retry.**

**The honest risk (unchanged):** the moat hinges on the user returning — the exact unproven premise A/B/C/D all flagged. The cheap user-reported grading (2a) ships early **as the §4 Test-1 instrument**; the heavy per-predicate migration (L3b) and silent auto-grading (L5) stay gated behind the kill-test.

> **Verification status §0:** 9 VERIFIED · 2 INTENT · 1 ASSUMPTION (`[ASSUMPTION: a per-predicate record shape — see L4]`).

---

## 1. North-Star

Argus is a **decision harness you run before you hand work to an AI** — "Think before you recast." Target user: a non-developer strategist deciding *what* to have AI do. `[INTENT — positioning: the only real competitor is "just ask ChatGPT"; the moat is accumulating, attributable, falsifiable judgment ChatGPT structurally cannot produce; cross-ref §7 UNVALIDATED-PREMISE]`.

**Verified assets:**
- the `llm.ts` engine `[VERIFIED: circuit breaker llm.ts:104-134 (`CIRCUIT_THRESHOLD = 5`, `CIRCUIT_RESET_MS = 30_000`); categorized errors `category: LLMErrorCategory` with auth/rate_limit/overloaded/context_too_long/network/parse_failure/validation/unknown; multi-strategy `parseJSON` defined :221, fallback throw :559]`,
- `validateShape` `[VERIFIED: DEFINED llm.ts:282 `export function validateShape<T extends Record<string, unknown>>(`; WIRED CONDITIONALLY at llm.ts:778 ternary `options.shape ? validateShape<...>(parsed, options.shape) as T : parsed`]` *(corrected: cite the definition :282 for reuse; wiring is conditional — a caller that omits `shape` gets the raw parsed value with NO validation.)*,
- the typed Phase-1/2 content intelligence `[VERIFIED: context-chain.ts:119 `buildReframeContext(item: ReframeItem): ReframeContext`, :157 `buildRecastContext(...): RecastContext`; file ends :525]` `[INTENT: "provenance markers" — there is NO `_source`/provenance FIELD on the returned contexts; only a comment at :504 + the assumption-merge — see Changelog #13]`,
- the on-disk `.argus/` ledger `[VERIFIED: `ls .argus/` = critique-and-roadmap.md, journal.md, last-run.md, recast.md, refine.md, reframe.md, rehearse.md, testing-log-2026-03-28.md — 8 non-empty files incl. the voyage outputs]`,
- the test net `[VERIFIED: 53 *.test.ts(x) files]` `[ASSUMPTION: "1004 passed" per a prior `npx vitest run` — not executed this turn; re-run to confirm]`.

Direction: **HYBRID** `[INTENT — extend the voyage DNA (C) + graft exactly one radical mechanic (the Decision Contract). `[ASSUMPTION: "60-80% infra reuse" inherited from workflow C; verify by enumerating reused-vs-net-new modules per layer]`]`. Every chassis-demolishing reshape is rejected as a spine (distribution scissors) `[INTENT — strategy]` `[ASSUMPTION: "the plugin manifest declares no hooks array" — load-bearing for the distribution-scissors argument; grep argus-plugin/ manifest for a `hooks` key before treating as fact]`.

`HiddenAssumption`/`KeyAssumption` lack id/status `[VERIFIED: types.ts:11-18 `interface HiddenAssumption { assumption; risk_if_false; verified?; evaluation?; evaluation_reason?; axis? }` — no id/created_at/status; types.ts:136-141 `interface KeyAssumption { assumption; importance; certainty; if_wrong }` — no id/created_at/status]`.

No forward surface ships on sand. The chassis is actively crashing today: an infinite-spinner stream bug, two null-deref crashes, a worker that silently auto-accepts a validation-FAILED result, and a quota write that swallows lost thinking. **These are the floor.** Plus a third floor: a first-timer cannot tell what Argus IS, where to click, or how to escape a hang. `[INTENT — assessment]`.

> **Verification status §1:** 6 VERIFIED · 3 INTENT · 3 ASSUMPTION (60-80% reuse; plugin no-hooks; context-chain src-wide negatives).

---

## 2. The Incremental-vs-Radical Verdict

**VERDICT: HYBRID.** `[INTENT — design verdict]`
1. Same unproven premise → cheapest-to-falsify wins (incremental graft).
2. The radical spine's gate is the least-reversible change: `[VERIFIED: HiddenAssumption types.ts:11-18 + KeyAssumption types.ts:136-141 have no id/created_at/status]`. `[INTENT: ~3-4 weeks on the most migration-hostile seam — spend only on evidence]`.
3. D's own tournament de-rates every radical chassis as a replacement; *"You never have to delete ProgressiveFlow to ship the value."* `[INTENT]`

**TAKE NOW (reversible, ride existing infra, zero migration):**
- **Decision Contract — THE KICK (§0).** Serialize predicates into a reversible JSON sidecar surfaced as a FinalCard "Verdict" view. `[INTENT: "ProgressiveFlow untouched" — the Verdict sidecar is UNBUILT (grep: no Verdict/decision-contract component in src/components/workspace/progressive; the 9 "Verdict" hits are unrelated boss-chat features); validate after build that no ProgressiveFlow edit is required]`. **(corrected: Verdict DATA is store-free off `runInitialAnalysis`; the FinalCard host is store-coupled `[VERIFIED: FinalCard.tsx:39 `const logSession = useProgressiveStore(s => ...)`]` — decide the mount point: accept the coupled host or add a store-free Verdict host for the `workspace/page.tsx:221` initial-analysis path that renders no FinalCard.)** Paired with the **net-new** return-to-grade loop, which IS the §4 Test-1 instrument.
- **userContext → prompt injection** (B's S1) — `[VERIFIED: progressive-prompts.ts:22-23 `buildInitialAnalysisPrompt(problemText: string, locale: Locale = 'en')` — two params, no userContext]` gets a scoped `참고:` reference-only line. `[INTENT: ~1 line]`. **(corrected: the richer `buildEnhancedSystemPrompt` path already injects `[VERIFIED: up to 6 active learning sections — context-builder.ts numbers slots 1–7 but slot #4 is a removed no-op]` — "smarter as you use it" is partly already true OUTSIDE the initial-analysis entry point; this fix closes the initial-analysis gap specifically.)**
- **Anchoring / judgment-atrophy guard** — first-class override-rate metric. **(corrected: compute on the FULL signal history; today `getSignals` reads local-only — see L1b gap.)**
- **`.argus/` ledger as the render spine** — render an artifact already on disk. `[INTENT: M-effort]`.

**DEFER behind a kill-test:**
- Per-predicate `id`/`created_at`/`status` schema migration. `[INTENT: ~3-4 wks, least-reversible]`. Green-light only if §4 Test 1 OR Test 2 passes.
- Silent prediction-and-self-scoring loop — net-new `[VERIFIED: useAccuracyStore.ts:8 `ratings: PersonaAccuracyRating[]`, :10 `addRating: (data: Omit<PersonaAccuracyRating, 'id'|'created_at'>) => void`; PersonaAccuracyRating interface defined types.ts:399]` *(corrected: the interface lives at types.ts:399, NOT useAccuracyStore.ts:10 — :10 is the addRating Omit use site.)* `[ASSUMPTION: whether per-aspect persona calibration is preserved or abandoned — decide at L5]`.

**REJECTED as spine:** execution-bound/agent-native (distribution scissors); multiplayer room (cold-start); decay-by-timer push; prompt-mutation engine. `[INTENT]`

> **Verification status §2:** 5 VERIFIED · 3 INTENT · 1 ASSUMPTION.

---

## 3. The Sequenced Master Plan

Spine: **L0 stop-the-bleeding (+ early UX P0/P1 + first-30s mobile) → L1 visible wins (convergence gauge headline) + L1b injection → L2 decomposition (flagged) + L3a Phase-0 persistence (parallel, L3a-0 stable-id rides L1 timing) → L4 forward surfaces (C + §0 KICK; db.ts-merge prerequisite) → L5 deferred, kill-test-gated.** L0 gates everything.

### L0 — Stop-the-bleeding crash & philosophy floor (all `[INTENT: S]`, ZERO foundation deps, gates everything)
**Goal:** a product that does not infinite-spin, crash on export/judgment, silently betray its thesis, or crash on stale data load.

- **P0 stream hang** — `[VERIFIED: callLLMStream llm.ts:627 `export async function callLLMStream(` → :631 `): Promise<void>`; reader loop :696-697 `while (true) { const { done, value } = await reader.read();` (no idle timer); fetch :675-680 only `signal: options.signal` (no AbortSignal.timeout)]`. A's `Promise.race([stream, timeout])` is WRONG (a void promise carries no liveness). **Correct:** per-chunk inactivity watchdog (~30s idle) + hard ~180s cap. **Mechanism (E6):** `[VERIFIED: LLMOptions.signal is caller-supplied — llm.ts:14-21 `/** AbortController signal for cancellation */ signal?: AbortSignal;`; llm.ts:679 `signal: options.signal,`; callLLMStream constructs no AbortController in 627-757]` → it cannot `.abort()` the external signal; use a NEW internal AbortController linked to `options.signal`, OR `reader.cancel()` (reader obtained :689). **NEW integration contract (P1):** the watchdog must reject with a DISTINCT non-Abort error `[VERIFIED: a bare AbortError is mapped at llm.ts:748-751 `if (error instanceof DOMException && error.name === 'AbortError') { callbacks.onError(new LLMError('요청이 취소되었습니다.', { category: 'network', cause: error })); return; }` and suppressed by the handler guard at ProgressiveFlow.tsx:1397 & :1717 `if (!(e instanceof DOMException && e.name === 'AbortError')) setError(...)`]` → a bare `signal.abort()` is a silent no-op, spinner never clears. `[INTENT: future watchdog behavior]`. *(Backoff setTimeout: `[VERIFIED: llm.ts:195 `await new Promise(r => setTimeout(r, delay))` and :210 `setTimeout(r, 1000 * Math.pow(2, attempt))`; :190 is the delay calc]` — cite :195/:210 if backoff becomes load-bearing, not the loose :190-215 range.)*
  - **Acceptance test (ADD-5):** a hung stream produces a VISIBLE timeout error in ProgressiveFlow AND clears the spinner.

- **(ADD-4) P0 second infinite-spinner guard** — stream completed but `[VERIFIED: parsePartialAnalysis called at workspace/page.tsx:503 `const partial = parsePartialAnalysis(streamingText)`]` yielded no `real_question` AND no skeleton → visible error + retry (covers malformed-JSON on the first interaction, distinct from the hang above).

- **P1 null-deref, brief export** — `[VERIFIED: project-brief.ts:114 `if (latest.analysis) {`; :130 `const steps = latest.steps.length > 0 ? latest.steps : latest.analysis.steps;`; :131 `steps.forEach((s, i) =>`; :133 `actorLabels[s.actor]` with actorLabels keys ai/human/both (:122-124)]`. If `latest.steps` is undefined, `.length` throws; an unknown actor renders `undefined`. Fix: `const steps = (latest.steps?.length ? latest.steps : latest.analysis.steps) ?? []` + defend `actorLabels[s.actor]`. *(corrected path: this is `project-brief.ts`; `OutputSelector` is at `src/components/ui/OutputSelector.tsx`, not `progressive/`.)*

- **P1 null-deref, judgment** — `[VERIFIED: eval-engine.ts:182 `export const RECAST_EVALS ... = [`; :183 steps_accepted `{ ... measure: ({ item }) => { if (!item.analysis) return false; return Math.abs(item.analysis.steps.length - item.steps.length) <= 2; } }`; :184 actor_overrides_low `{ ... measure: ({ item, actorOverrideCount }) => { if (!item.analysis) return true; const t = item.analysis.steps.length; ...` ; array closes :187]`. **(corrected: `:184` is NOT safe to leave — both branches read `.steps.length` after only the analysis-null short-circuit.)** Guard `steps` (`?? []`) on BOTH :183 and :184 and `item.steps`. Reached via `[VERIFIED: recordRecastEval defined eval-engine.ts:189; called at src/components/workspace/RecastStep.tsx:412 `lazyEvalEngine().then(m => m.recordRecastEval(current, oc))`]` *(corrected path: RecastStep.tsx is under workspace/, not progressive/steps/.)*

- **P1 silent auto-accept (philosophy-critical)** — `[VERIFIED: worker-engine.ts:266 `if (callbacks.onValidationFailed) {`; :267-270 `const action = await Promise.race([ callbacks.onValidationFailed(...), new Promise<'accept'>(r => setTimeout(() => r('accept'), 30_000)) ]);`; :281-285 `} else { // 콜백 없으면 그냥 수용 finalResult = result; break; }`]`. **(corrected: in production the NO-CALLBACK branch fires** `[VERIFIED: ProgressiveFlow.tsx:1176-1212 workerCallbacks defines onStart/onStream/onComplete/onError only; fresh grep `onValidationFailed` across src = only execution-transcript.ts + worker-engine.ts, never ProgressiveFlow → :266 false → else-branch fires; the 30s branch is unreachable]`.) Fix: wire `onValidationFailed` in ProgressiveFlow (surface the existing `WorkerCard.tsx:268-294` panel), change the no-callback default to block/`'timed_out'`, AND flip the literal `Promise<'accept'>` (`:269`). **(NEW, P1):** clear the loser `setTimeout` once the race settles (per-worker timer leak); treat an `onValidationFailed` rejection as `'timed_out'`, not a fall-through to the generic catch `[VERIFIED: worker-engine.ts:286-290 catch → onError + break; :293-295 onComplete only if finalResult]`. `[INTENT: this describes future behavior once onValidationFailed is wired]`. **(NEW, P1):** `[VERIFIED: execution-transcript.ts:147-153 onComplete records `score: validation?.score, passed: validation?.passed` with no gating]` — a silently-accepted FAILED result is persisted to the `.argus` ledger as a clean completion; mark failed-then-accepted results.
  - **Acceptance test (ADD-5):** a no-callback worker on a validation-FAILED result **blocks/re-prompts**, does not accept; reaches a terminal callback; is marked in the transcript.

- **P1 silent data loss (WRITE)** — `[VERIFIED: storage.ts:37-45 `setStorage<T>(...)` try/catch on `localStorage.setItem(key, JSON.stringify(value))` → catch `console.error('[storage] localStorage write failed:', e)`, returns void, no UI]` (catches QuotaExceededError). **Mechanism (E6, corrected):** surface a toast via a window CustomEvent — `[VERIFIED: producer precedent llm.ts:721-724 `window.dispatchEvent(new CustomEvent('argus:ratelimit', ...))`; the ONLY CustomEvent consumers are RateLimitBadge.tsx:23 `window.addEventListener('argus:ratelimit', ...)` and SyncStatus.tsx:39 (`argus:sync`); PingToast/UnlockToast have NO addEventListener — store-driven]` *(corrected: drop PingToast/UnlockToast; cite RateLimitBadge/SyncStatus.)* A window CustomEvent (no store import) is the RIGHT choice precisely because `storage.ts:42` warns it uses console directly "to avoid circular dependency with logger" — routing through a store would risk that very cycle.

- **P1 silent data loss (READ)** — `[VERIFIED: getStorage storage.ts:27-35 returns `item ? JSON.parse(item) : fallback` with no shape/version check; catch only guards parse-throw]`. Add a thin shape/version guard — reuse `[VERIFIED: validateShape DEFINED llm.ts:282; call site :778-781]` *(corrected: import from the definition :282, not the call site; pass a shape or it is a no-op — Changelog #12)*. **Acceptance: a v(N-1)-shaped localStorage blob loads without throwing.**

- **(ADD-6) P1 observable-async-write helper** — add ONE shared helper that wraps a fire-and-forget Supabase write, attaches `.catch`, and bumps a per-session sync-failure counter surfaced once. Route all three swallowed-insert paths through it: `[VERIFIED: analytics.ts:168-179 try/catch around `supabase.from('user_events').insert({...}).then(() => {...})` with no .catch at :176 — a sync try/catch cannot catch the async rejection]`, `db.ts insertToSupabase` (returns void on error), and the `loadAndMerge` re-upsert. Same class as the storage toast.

- **P1 auto-persona empty stall** — `[ASSUMPTION: auto-persona.ts `callLLMJson` on failure silently returns []; fall back to rule-based recommendBlindSpotPersona — NOT opened this turn; open auto-persona.ts and quote the silent `return []` path + the recommendBlindSpotPersona producer before treating as fact]`.

- **P2 data-correctness — `db.ts` merge.** **(SEQ-1: full mechanism lives in ONE home — the "L4 PREREQUISITE" block below.)** L0 only schedules awareness; do not restate the mechanism here.

- **CLOSE, do not re-fix:** `[VERIFIED: decision-quality.ts:145 `if (recast?.analysis) {` then :146 `const steps = recast.analysis.steps || [];`]`. **(corrected: the v3 `:83` caveat is DELETED — `[VERIFIED: decision-quality.ts:82 `if (reframe?.analysis) {` then :83 `const questionCount = (reframe.analysis.hidden_questions || []).length;` INSIDE the guard]`; there is no analysis-null exposure. `decision-quality.ts` is closed outright.)**

**Acceptance:** zombie stream aborts within ~30s with a VISIBLE error AND a cleared spinner; malformed-first-response shows error+retry; brief export + judgment run on missing-`steps` analyses without throwing (regression tests assert this); a validation-failed worker never silently accepts (blocks/re-prompts, reaches a terminal callback, is marked in the transcript); quota write shows a toast; a stale-schema localStorage blob loads without throwing; auto-persona failure degrades to rule-based; analytics/db insert failures are counted via the observable-write helper; tests green.

> **UX P0/P1 woven into L0** (see §UX-L0): streaming progress + Cancel on the first-run analyze screen (the #1 dead-end), the silent-failure copy for storage/worker-accept, **and (ADD-7) the first-30-seconds mobile items** (analyze screen, entry orientation).
>
> **Verification status L0:** 14 VERIFIED · 2 INTENT · 3 ASSUMPTION (auto-persona path; analytics-canary re-open; the now-closed decision-quality).

### L1 — Visible wins (convergence gauge HEADLINE) + orientation floor (needs only an L1 migration)
**Goal:** a substrate where retention *can* exist, plus the metric canary and the orientation floor.

- **(SEQ-3 — PROMOTED HEADLINE WIN) Convergence gauge as a standalone L1 win** — `[VERIFIED: is_converged/estimated_rounds_left/trend computed at progressive-convergence.ts (:95-107 trend, :109 `const is_converged = totalScore >= 75`, :110 estimated_rounds_left); fresh grep `is_converged|estimated_rounds_left` over *.tsx = No matches → rendered NOWHERE; only metrics.score+guidance show at ProgressiveFlow.tsx:842-852 (`{L('명확도','Clarity')}` :842, `{metrics.score}%` :843, `{metrics.guidance}` :850)]`. Promote a minimal gauge (per-step % + a "ship at 75%" line + trend arrow + rounds-left chip) that **reads the already-computed fields** (no new compute). **No L2 dependency, no new table.** **Acceptance: renders meaningful output on RUN 1** (score+trend exist from the first analysis). Decouple from the L4 `ConvergenceRose` re-skin.

- **(SEQ-3 — DEMOTED to run-3+ payoff) Retention reflection TAB** (C Wave 0) — Compose `[VERIFIED: navigator.ts builders consumed at NavigatorStrip.tsx:6 import + :198-200 `buildNavigatorProfile()`/`buildNavigatorInsights(profile)`/`buildLearningCurve()`]` + `getUserPatterns` `[VERIFIED: useJudgmentStore.ts:14 (interface) & :57 (impl) — NOT navigator.ts]` + `getPersonaAccuracySummary` `[VERIFIED: useAccuracyStore.ts:12 (interface) & :49 (impl)]`. Build it **inside `NavigatorStrip`** `[VERIFIED: NavigatorStrip does not currently wire those two store fns — fresh grep getUserPatterns|getPersonaAccuracySummary|useJudgmentStore|useAccuracyStore in NavigatorStrip.tsx = No matches → net-new there]` — **NOT inside Logbook** `[VERIFIED: Logbook.tsx:92 `if (waypoints.length === 0) return null;`; it is the branch/waypoint rail, shares no code]`. **Demoted because gated near-empty on runs 1-2** `[VERIFIED: navigator.ts:113-117 `tier = ... sessionCount >= 2 ? 2 : 1` (tier 2 needs sessionCount>=2); :170/:191 gate pattern + eval-summary insights behind tier>=2; NavigatorStrip.tsx:268 sparkline needs `learningCurve.dq_points.length >= 2`; user-context.ts:188 `if (obs.sessionCount < 2) return { items: [], hasData: false }`]`. Note a SINGLE DQ point still renders `[VERIFIED: NavigatorStrip.tsx:294-295 renders the `dq_points.length === 1` block; the profile summary :247-261 also shows]` — so run-1 is "thin," not literally empty, but the insights list + sparkline are empty until session 2. **This is a run-3+ payoff, not the run-1 first win.**
  - **(reconcile, E3):** the existing Coda reflection is PROJECT-scoped `[VERIFIED: OutputSelector.tsx:94-112 codaOpen/codaForm from `project.meta_reflection`, handleSaveCoda writes via updateProject; band opens :184]` while the new reflection tab is SESSION/learning-scoped (navigator builders, no project arg). They read different data domains → the risk is **navigational (two entry points)**, not data-conflict. State which scope the reflection tab owns.

- **Artifact persistence** — `[VERIFIED: OutputSelector.tsx:121 `setPreview(format.generator(project))` regenerated each select; CopyButton :172; download Blob :128-137; read-only `<pre>` :178-180]`. **(corrected: NOT "orphaned"/data-loss — INPUTS persist** `[VERIFIED: project-brief.ts:10-17 reads getStorage REFRAME_LIST/RECAST_LIST/SYNTHESIZE_LIST/FEEDBACK_HISTORY; agent-spec.ts:8-13 via projectFilter — these STORAGE_KEYS map to Supabase-synced stores → regenerable after reload (producer = stores, consumer = generators, both present)]`.) **The real gap:** `[INTENT — a stored, versioned, shareable artifact; no `artifacts` table exists `[VERIFIED: fresh grep 'artifacts' over *.sql = No matches; db.ts:15-20 TableName union enumerated in full has no 'artifacts']`]` + no capture of post-generation edits. Ship ONE Supabase `artifacts` table + versioning. **Decide now `[ASSUMPTION: immutable snapshots vs editable documents — schema differs; pick before building]`.** **(corrected scoping divergence:** `[VERIFIED: agent-spec.ts:7 `const pf = projectFilter(project)` where output-helpers.ts:20-23 returns `() => true` when project is null, else `item => item.project_id === project.id`; project-brief.ts:6 `if (!project) return ''` then :11 inline `d.project_id === project.id`]` — they are scope-IDENTICAL for a real project; they diverge ONLY when project is null. Align the **null-project** handling, not the per-project predicate.)

- **Team-theater honesty** — HeroFlow shows avatars "assembling" but `runInitialAnalysis` is a single LLM call. (Note the WORKER stage DOES fire one call per persona `[VERIFIED: per-worker callLLMStream at worker-engine.ts:141-151; runAllAIWorkers ENTRY is :201]` *(corrected: :141-151 is the per-worker call inside the runner helper, not the runAllAIWorkers body)*.) Decide in L1: honest spinner copy OR real per-lens streaming.

- **Anon→auth quota narrative** — quota copy conflates trial vs tier; `[VERIFIED: RateLimitBadge gated to logged-in users — Header.tsx:159-164 `{user && (<><SyncStatus /><RateLimitBadge /></>)}`]` so anon users never see a count. Disambiguate; show anon remaining-runs; warn before the last run.

- **Signal read-path is local-only** — `[VERIFIED: signal-recorder.ts recordSignal writes both localStorage (:35) and Supabase (:38); getSignals defined :44 reads ONLY `getStorage<QualitySignal[]>(STORAGE_KEYS.QUALITY_SIGNALS, [])`; getSignalsByType :61 delegates]` *(corrected locator: getSignals :44 / getSignalsByType :61, not :49)*. Every learning consumer sees ≤500 local records and nothing on a new device. `[ASSUMPTION: make the read Supabase-aware via loadAndMerge('quality_signals') — verify the in-memory cache wiring]` OR document the loop as local-session-only; add the anchoring metric only AFTER the read path can see full history.

- **`hit-rate.ts` is unpersisted/capped** — `[VERIFIED: hit-rate.ts:6 comment 'localStorage 기반, Supabase 동기화는 추후'; imports only getStorage/setStorage; 200-cap on HitRecords at :59]`. Either add a `hit_records` table + `loadAndMerge`, OR stop citing it in §1 as a durable "closed calibration" asset.

**Acceptance:** the convergence gauge shows trend + rounds-left from existing data on **run 1**; a generated brief/spec survives reload; the reflection tab renders read-only (thin on runs 1-2, full at run 3+) with no new table; quota copy disambiguates; signal read path is Supabase-aware (or documented local-only); tests green.

> **Verification status L1:** 12 VERIFIED · 3 INTENT · 2 ASSUMPTION (signal Supabase read; artifact schema choice).

### L1b — userContext → prompt injection + i18n consolidation
**Goal:** make "smarter as you use it" partly true at near-zero cost; consolidate i18n.

- Wire observation context into `[VERIFIED: buildInitialAnalysisPrompt progressive-prompts.ts:23 `(problemText: string, locale: Locale = 'en')`; production caller progressive-engine.ts:303 passes only those two]` via `참고:` reference-only, wrapped in `<user-data>` + `sanitizeForPrompt()`. **(corrected: `[VERIFIED: user-context.ts:56-59 JSDoc — 'UI 전용 ... 프롬프트에는 사용하지 않음 (시뮬레이션 리뷰어가 시스템 메트릭을 아는 건 부자연스러움)']` marks observations UI-only deliberately; `buildNavigatorProfile` reads eval-summary + judgments + projects, its only getSignals() use is the `sessionCount===0` demo-seed branch `[VERIFIED: navigator.ts:122 `const demoSeeds = getSignals().filter(s => s.signal_type === 'demo_seed')`]`.)** This fix closes the initial-analysis entry point only.

- **i18n consolidation** — `useLocale()` flash + `t()` default mismatch. Replace the duplicated inline `const L` in ProgressiveFlow `[VERIFIED: 9 `const L =` definitions — ProgressiveFlow.tsx:66,283,416,500,656,778,831,861,887]` `[ASSUMPTION: ~142–150 `L(` call-sites — regex-scope dependent (`grep -oE '\bL\('`); not load-bearing, the 9 definitions are what consolidation targets]` *(corrected from "~15")* with the shared helper. **(NEW lint, P3):** fail on literal Hangul in JSX outside `L()`/`t()` `[VERIFIED: 21 of 137 non-test .tsx files import @/lib/i18n]` *(corrected denominator from 141)*. *(corrected example: BranchMap.tsx has no i18n import but its only Hangul is a block COMMENT at :4, not JSX — pick a file with Hangul in JSX for the example, or qualify it.)*

**Acceptance:** initial-analysis prompt includes a one-line `참고:` when observations exist, sanitized; first paint is single-language; lint fails on unwrapped Hangul; tests green.

> **Verification status L1b:** 5 VERIFIED · 0 INTENT · 1 ASSUMPTION.

### L2 — Chassis decomposition behind a feature flag
**Goal:** make the monolith safe to extend. `[INTENT/ASSESSMENT: a maintainability gate, not a value gate — on the design plan the Decision Contract does not touch ProgressiveFlow; validate once the sidecar is built]`.

- Verified sizes: `[VERIFIED: wc -l ProgressiveFlow.tsx (src/components/workspace/progressive/) = 2670; useProgressiveStore.ts = 1786]`.
- **(corrected coupling — STORE side):** `[VERIFIED: fresh grep `from '@/stores/'|require(` on useProgressiveStore.ts → only static store import is :6 `import { useAgentStore }`; usePersonaStore is a single lazy `require('@/stores/usePersonaStore')` at :946; NO useReframeStore/useRecastStore/useProjectStore]`.
- **(corrected — `IntersectionObserver` NOT in the chassis):** `[VERIFIED: fresh grep IntersectionObserver across src = single hit hooks/useScrollReveal.ts:15]`.
- **(corrected — scroll is a COMPONENT concern, E4):** `[VERIFIED: useProgressiveStore.ts has 0 scrollIntoView; store :951 is `(p.name.toLowerCase().includes(hint)...` (a personas.find predicate); the scroll machinery is in ProgressiveFlow.tsx — scroll/scrollToRef :943-953, scrollIntoView :951; 23 `scrollToRef(` callsites, ~9 distinct target refs]` *(corrected from "28 scroll refs" and from the store-relative :951)*. Disambiguate concerns by file: **STORE-side** = useAgentStore import, lazy usePersonaStore require, worker runtime, version/branch logic; **COMPONENT-side** = scroll-into-view + the 43 hooks.
- **43 hooks** `[VERIFIED: fresh grep on ProgressiveFlow.tsx → useState 21 + useRef 15 + useEffect 7 = 43 declarations; the store has 0]` *(corrected from "46")*.
- Worker runtime coupling `[ASSUMPTION: runAllAIWorkers/runPipeline :32; store :213-216 — not re-opened this turn; quote before load-bearing use]`.
- Keep legacy `ProgressiveFlow` behind a feature flag; extract `WorkerRuntime`/`DraftManagement`/`VersionControl` (store-side) + `UIState` hooks (component-side).
- **(corrected prerequisite, E3):** the named lib tests are insufficient `[VERIFIED: voyage-branch-simulation.test.ts:78 imports useProgressiveStore (store-level); orchestrator-journey-simulation.test.ts + workflow-review-integration.test.ts import only pure libs — grep ProgressiveFlow|@testing-library|render( across all three = none]`. BUT render coverage already exists `[VERIFIED: progressive/__tests__/flow-interactions.test.tsx:32 imports VerificationGate/TeamDeployBanner/DMFeedback/FinalCard FROM ProgressiveFlow and renders them; team-deploy-render.test.tsx renders via renderToStaticMarkup; also logbook-render/logbook-fork-render/voyagechart-render]`. **The real gap is ORCHESTRATION coverage** (scroll-into-view sequencing, WorkerRuntime, UIState hooks, the main default export) — add an orchestration/integration render test, not "any render test."
- **(extraction hazards):** convert the lazy `require('@/stores/usePersonaStore')` (:946) to DI; preserve `[VERIFIED: MAX_BRANCHES = 8 useProgressiveStore.ts:47; consumer :1480 `if ((session.branches || []).length >= MAX_BRANCHES)`]`; carry the three migrators `[VERIFIED: migrateWorkers :330, migrateSessionDrafts :356, migrateBranches :391; chained :447 `migrateSessionDrafts(migrateBranches(migrateWorkers(local)))` and :486]` *(corrected: v3 named three but cited :356/391 only — migrateWorkers :330 was omitted)*.
- **a11y baseline while extracting** — `[VERIFIED: placeholder-only inputs with no accessible name — ProgressiveFlow.tsx:813 `<input ... placeholder={L(...)} />`, :2167 `<textarea ... placeholder={L(...)} />`, :2615 `<textarea value={iterationDirective} placeholder={L(...)} />` — none carry aria-label or id+label]`. Add `aria-label`/associated `<label>` via `L()`.

**Acceptance:** both flag paths pass the lib tests **AND a net-new orchestration render test**; extracted hooks unit-tested; lazy require converted to DI; `MAX_BRANCHES` + 3 migrators preserved; inputs have accessible names; flag flip reversible; tests green on both paths.

> **Verification status L2:** 9 VERIFIED · 2 INTENT · 2 ASSUMPTION (worker runtime lines; src-wide negatives if re-scoped).

### L3a — Phase-0 persistence slice (runs PARALLEL with L2)
**Goal:** make provenance real; supply the stable predicate id §0 needs.

**(SEQ-2) L3a is split into two explicitly-sequenced sub-slices:**

#### L3a-0 — predicate/assumption STABLE-ID at generation (ships at L1 timing, BEFORE Decision Contract + return-to-grade)
- Add a deterministic `id` to `HiddenAssumption`/`KeyAssumption` at generation (no `status`/`created_at` yet). `[VERIFIED: neither type has id today — types.ts:11-18 / :136-141]`.
- Lets the fragile substring linkers be deleted `[VERIFIED: fresh exhaustive grep `\.substring\(0,\s*\d+\)` over src = exactly 3 lines in 2 files — agent-spec.ts:104 `step.task.includes(a.assumption.substring(0, 8))`; prompt-chain.ts:151 & :152 `a.assumption.substring(0, 10)` on task and expected_output → the ONLY two linkers]`.
- **(NEW: id ALONE is insufficient, E3/E6):** the replacement key must exist in BOTH the assumption AND the step. `[VERIFIED: RecastStep types.ts:119-134 has no assumption-id field; agent-spec.ts:107 DERIVES `validates_assumption` from the substring match — fresh grep `validates_assumption` over src = only agent-spec.ts:107 → removing the match removes the ONLY producer]`. **Add a `validates_assumption_ids` (or `source_assumption_id`) field on the step, populated at generation, and join on id.** Net-new schema. `[ASSUMPTION: no generation-time populator exists yet — the acceptance "substring linkers grep-verified gone" is only safe AFTER both the RecastStep field AND a generation-time populator exist; otherwise the YAML field regresses to never-emitted]`.
- **(NEW: substring linkers are wrong on FRESH data, not just legacy):** `[VERIFIED: RecastStep.task (types.ts:120) and HiddenAssumption.assumption (types.ts:12) are independently LLM-generated free text — an `includes(substring)` heuristic gives false POSITIVES on shared prefixes, false NEGATIVES on paraphrase]`. Acceptance: "links are deterministically correct on fresh data," not just "no false link on legacy."
- **(three more text-identity linkers in the same area):** `[VERIFIED: mergeAssumptionsIntoKeyAssumptions context-chain.ts:506-525 dedupes by lowercased text (:510-516 `existing = new Set(recastAssumptions.map(ka => ka.assumption.toLowerCase().trim()))`)]` → re-key on id; `[VERIFIED: buildReframeContext context-chain.ts:134-136 coerces legacy `string[]` to objects `typeof a === 'string' ? { assumption: a, risk_if_false: '', verified: false } : a`]` → assign ids in this normalizer; `[VERIFIED: RehearseStep.tsx:193-194 `contextText = JSON.stringify(rc); if (relReframe) contextText += '\n' + JSON.stringify(buildReframeContext(relReframe))` → reaches the persona prompt via buildPersonaReview at :198]` *(corrected path: src/components/workspace/RehearseStep.tsx; line :194 correct)* → update serialization so new `_source`/`id` fields aren't dumped verbatim.

#### L3a proper — persistence + provenance
- `[VERIFIED: context-chain.ts builders are pure functions, file ends :525; fresh grep `useContextChainStore|traceProvenance|_source` over src = only unrelated `utm_source` in analytics → no Zustand store, no `_source`, no `traceProvenance`, no localStorage/Supabase write]` *(but: a full-src exhaustive negative for these symbols was confirmed this turn)*. Work = persistence + `_source` + `traceProvenance` + a `context_chains` SQL migration — NOT a from-scratch pipeline.
- **progressive_sessions hardening (P1, prerequisite for any predicate/grade history):** `[VERIFIED: loadSessions casts `remote.data as ProgressiveSession` (useProgressiveStore.ts:467) with zero validation; 20260409_progressive_sessions.sql:8 `data JSONB NOT NULL DEFAULT '{}'::jsonb` with no schema_version column; three migrators run every load (:447/:486)]`. Add a `schema_version` field + a single forward-migration switch + `validateShape` on `remote.data` **before** L3b writes per-predicate history, so contract data is born versioned.
- **Output self-validation lint** — add `validateAgentSpec`/`validatePromptChain` checking every referenced assumption/step id resolves; run before preview. Pairs with the stable-id slice.

**Acceptance:** assumptions carry stable IDs at generation (incl. the legacy normalizer); steps carry `validates_assumption_ids` and the linker is an id-join (substring + text-dedup re-keyed; grep-verified gone, AFTER a generation-time populator exists); fresh-data links deterministically correct; context chain persists to `context_chains` with `_source`/`traceProvenance`; progressive_sessions versioned + validated; tests green.

> **Verification status L3a:** 11 VERIFIED · 1 INTENT · 2 ASSUMPTION (validates_assumption_ids producer; blob versioning forward-switch).

### L3b — Full per-predicate status migration (GATED behind kill-test)
**Goal:** the addressable schema the falsifiable-predicate loop needs. **Do NOT start until §4 Test 1 OR Test 2 passes.** `[INTENT: ~3-4 wks]`
- Add `id`/`created_at`/`status: held|contradicted|pending` to the assumption type across reframe/recast/rehearse + the eval engines `[VERIFIED: cross-leg/cross-engine reach — fresh grep -l 'hidden_assumptions:|key_assumptions:' (literal colon) = 31 files]` *(corrected: "31" is the colon-suffixed YAML-emit slice; the bare-pattern count is 57 with tests / 40 non-test — cite the slice you mean)*; handle JSONB atomicity `[VERIFIED: 20260409_progressive_sessions.sql:35 comment '-- Atomic worker update function — avoids JSONB read-modify-write race condition'; :37 `CREATE OR REPLACE FUNCTION update_worker_response(`; closes :78]`. **Prerequisite:** the session blob must be bounded/server-persisted AND versioned (L3a + L5) first.

**Acceptance:** gated, not scheduled. If the kill-test fails, never built; ship Mirror without apology.

### L4 — Forward surfaces: C's incremental spine + the Decision Contract graft
**Goal:** ship the genealogy throughline as the differentiator vs "just ask ChatGPT." `[INTENT — positioning]`.

#### L4 PREREQUISITE (SEQ-1, the SINGLE home for the db.ts merge fix) — tombstone-aware merge + empty-stamp tie-break
Escalated to an L4 prerequisite because L4 introduces freshly-written local grade rows.
- `[VERIFIED: mergeByTimestamp db.ts:57-78 does string `remoteTime > localTime` (:71) with `|| ''` fallback (:69-70) — no clock-skew handling]`.
- **(corrected: NOT "merge ignores deleted_at"):** `[VERIFIED: loadAndMerge pre-filters tombstones — db.ts:106 `const remote = ((data||[]) as any[]).filter((r) => !r.deleted_at)`]`.
- **The REAL bug (E6 mechanism):** `[VERIFIED: db.ts:113 `remoteIds = new Set(remote.map(r => r.id))`; :114 `localOnly = merged.filter(m => !remoteIds.has(m.id))`; :116-118 `.upsert(localOnly.map(item => ({ ...sanitizeItem(item), user_id: userId })), { onConflict: 'id' })`]` — a row tombstoned on another device is absent from `remoteIds`, still in local, lands in `localOnly`, and is **re-upserted (resurrected)**. Fetch tombstones, exclude tombstoned ids from the re-upsert, and keep them out of the `merged` returned to the UI (currently :106 excludes them only from `remote`, not from local-origin rows).
- **(empty-stamp bug):** `[VERIFIED: sanitizeItem db.ts:32-40 destructures out BOTH `created_at: _ca` and `updated_at: _ua`; mergeByTimestamp falls back updated_at → created_at → '' (:69-70), so a fresh local row has '' stamp and loses to any remote ISO string (:71)]` *(corrected: v3 named only updated_at; created_at is also stripped, which bottoms the fallback at '' and strengthens the case)*. Unify on Date compare with a tie-break that never discards a missing-local-stamp row.
- **(blast-radius caveat, E5/E6):** `[VERIFIED: mergeByTimestamp is generic over the full TableName union (db.ts:15-20, ~16 tables)]` — a union-of-ids/append-only tie-break changes merge semantics for EVERY soft-deletable table `[VERIFIED: SoftDeletableTable db.ts:22 = projects|personas|reframe_items|recast_items|synthesize_items]`. Scope the tie-break to grade/append-only tables or confirm last-write-wins is safe for projects/personas/items.
- **(scope pin, E3):** `[VERIFIED: progressive_sessions is NOT in SoftDeletableTable (:22) and does NOT use loadAndMerge — it has its own loadSessions path]`. **This fix covers the generic soft-deletable tables + the grade table; progressive_sessions needs its OWN skew/durability fix (L5).** `[ASSUMPTION: pin the grade's storage table — accuracy_ratings (in TableName union :17, routes through loadAndMerge) is the analogous path; if L4 instead writes grades into the progressive_sessions blob, the sanitizeItem fix is on the wrong path — decide before claiming coverage]`.

#### L4 surfaces
- **Decision Contract (the §0 KICK, L1 timing — no L2/L3 dep)** — serialize 3-6 predicates from recast actor assignments + `classified_risks` + `governing_idea` `[VERIFIED: types.ts:121 RecastStep.actor; :255/:362 classified_risks; :158 governing_idea; produced live RehearseStep.tsx:60-62]` into a reversible JSON sidecar, surfaced as a FinalCard "Verdict" view. **(corrected: derive store-free off `runInitialAnalysis`; name the mount host — `[VERIFIED: FinalCard.tsx:39 subscribes useProgressiveStore]`, and the initial-analysis path `workspace/page.tsx:221` renders no FinalCard.)** Each predicate carries the stable id from L3a-0.
- **Return-to-grade loop (NET-NEW write path)** — build a real `OutcomeRecord`/`PredicateGrade` WRITER UI + store with localStorage+Supabase persistence (OUTCOME_RECORDS is read-only/producer-less — §0). The grade references `predicate_id`, never text. The write must be observable (route through the L0 ADD-6 helper). **(corrected: the existing accuracy UI is on the LEGACY path** `[VERIFIED: addRating called only at tools/FeedbackResult.tsx:66; FeedbackResult imported/rendered only at RehearseStep.tsx:12 (import) & :673 (render); useAccuracyStore consumption in RehearseStep is at :21/:86 — not ProgressiveFlow]` *(corrected sub-cite: :12 is the FeedbackResult import, not the useAccuracyStore use)*. Place grading on the progressive FinalCard/Verdict surface — net-new placement.) **(NEW: `[VERIFIED: OutcomeRecord types.ts:502-513 is project-scoped — id, project_id, materialized_risks[], approval_outcomes[], overall_success — no per-predicate id]` — define a per-predicate record shape; do not map 3-6 predicates onto one project-level record.)** `[ASSUMPTION: the per-predicate record shape is net-new — design it]`.
- **Persona re-approval loop** — add a "would [persona] approve NOW?" post-refinement re-run. Reuses PersonaFeedbackStep + accuracy plumbing. Distinct from the §0 predicate loop. `[INTENT]`
- **Backtracking-cost preview** — before a re-analysis-triggering `real_question` edit, show a one-line cost hint. `[INTENT]`
- **Agent Spec 2.0 executable fields** — extend `RecastStep` with optional `validates_assumption?`/`abort_condition?`/`success_signal?`/`feeds_into?`, emit in `agent-spec.ts`. Rides the L3a-0 stable-id slice. `[INTENT]`
- **Onboarding (C Wave 0)** — **(corrected: UNBUILT** `[VERIFIED: fresh grep `has_onboarded` across src = 0 hits; InteractiveDemo imported workspace/page.tsx:31, rendered only at :269 inside `if (demoScenario)` (:263); demoScenario set only via demo-card `onClick={() => setDemoScenario(s)}` at :444; marketing copy `{projects.length === 0 && (` at :458]`.) **(corrected MECHANISM, E6):** `[VERIFIED: `demoScenario` state, `setDemoScenario` (:444), and the `if (demoScenario)` render (:263-269) all live INSIDE `function HeroFlow`; the `if(!currentProjectId)` branch at :690 is in the PARENT `WorkspaceContent` (`<HeroFlow .../>` :692) and has no access to HeroFlow's local `setDemoScenario`]`. **Implement the gate as a mount-effect INSIDE HeroFlow** (it owns `demoScenario`): on mount, if `projects.length===0 && !localStorage['argus.has_onboarded']` call HeroFlow's local `setDemoScenario(defaultScenario)` (→ the existing :263 render) then set the flag. Do NOT wrap the :690 return.
- **Core-flow re-skin (C Wave 2, gated on L2)** — additive `LegCanvas`/`AnchoringMoment`/`ConvergenceRose` reusing `phaseIdx()` **(corrected: `[VERIFIED: ProgressiveFlow.tsx:132 `function phaseIdx(...)` — no `export`]` — lift to a shared module first)**; ~15-line gold ancestry overlay in `BranchMap` **(corrected: `[VERIFIED: BranchMap.tsx:31 local `function edgePath(...)`, used :82; activeBranchId coloring :78/:85/:94/:111; getActivePath DEFINED version-tree.ts:35, consumed branch-map-layout.ts:75, NO BranchMap hit]` — the overlay strokes a gold path over existing coordinates, it does not call getActivePath)**.
- **Attributable outputs (C Wave 1, gated on L3a)** — four-act ship's-log Brief on `[VERIFIED: voyageLogToMarkdown export.ts:25-49, consumed FinalCard.tsx:45]` with a "왜?" provenance anchor per line using stable `HiddenAssumption.id`. **(corrected: the anchor + stable-id linkage are NET-NEW** `[VERIFIED: voyageLogToMarkdown uses only waypoint fields (w.headline/trigger/significance/alternatives, :41-46), no assumption id; the HiddenAssumption import at export.ts:1 is used only by reframeToMarkdown at :61-63 (legacy string-vs-object handling)]`.) **(NEW: `[VERIFIED: voyageLogToMarkdown returns '' on no session (:26) AND on no waypoints (:35)]` — verify waypoint coverage or the Brief renders empty.)** *(scope note: the OTHER exporters (reframeToMarkdown :51+) DO consume HiddenAssumption objects — a blanket "export.ts uses no assumption id" would be wrong; the claim is scoped to voyageLogToMarkdown only.)*
- **`.argus/` render spine (graft, L1/L3a timing)** — a VIEW over the on-disk `.argus/*.md` ledger (verified populated, §1).
- **Mobile / responsive pass** — see §UX-MOBILE; gate "no horizontal overflow at 375px" into L4 acceptance (first-30s items already pulled into L0/L1 by ADD-7).
- **Lazy-load landing Acts** — `[VERIFIED: wc -l ShipCutaway.tsx = 1011, SailingShip.tsx = 505]`; primitive counts `[ASSUMPTION: straight grep gives 155/72 SVG primitives (v3 said 148/69) — depends on element set]`; `[VERIFIED: SailingShip has 14 `animate=` SVG props, 0 `motion.`, 0 `repeat: Infinity`]` *(corrected: "14 framer-motion loops" → 14 SVG `animate=` props)*. Lazy-load Act2/Act3. **(corrected: the §7 "@react-three/fiber ~200KB" claim is WRONG — those packages are dead/unused `[VERIFIED: fresh grep `@react-three`/`from 'three'` across src = 0 hits; package.json declares @react-three/drei, @react-three/fiber, three — all unimported]`; the real cost is inline-SVG DOM + framer-motion.)**
- **a11y modal primitive** — bake a shared focus-trapping modal `[VERIFIED: useReducedMotion|MotionConfig absent from src — fresh grep No matches; PersonaPoolModal.tsx:216 handles Escape but no focus trap]`. New modals trap focus + close on Escape.

**Acceptance:** Verdict sidecar generates without touching ProgressiveFlow and deletes cleanly, with a named store-free/coupled host; each predicate carries a stable id and the grade write is observable; return-to-grade persists a per-predicate record that `context-builder` reads back (regression test asserts a non-empty producer exists); onboarding auto-demo fires only for first-run (via HeroFlow's `setDemoScenario`) and hands off to a pre-filled textarea; the gold thread resolves to real `.argus`/typed-chain links (no false links on legacy or fresh); persona re-approval + backtrack-cost preview ship; Agent Spec emits executable fields; the db.ts-merge prerequisite is landed; no horizontal overflow at 375px; new modals trap focus; tests green at every sub-step.

> **Verification status L4:** 10 VERIFIED · 4 INTENT · 2 ASSUMPTION (per-predicate record shape; grade storage table / onboarding host).

### L5 — Deferred / kill-test-gated (do NOT build pre-traction)
**Goal:** the compounding-judgment moat — ONLY if the retention premise survives falsification.
- **Silent prediction-and-self-scoring loop** — build only after the Decision Contract earns Test-1/Test-2 evidence; reuses `judgment-vitality.ts`/`observation-engine.ts`. `[INTENT — gated]` *(note: observation-engine has writers, e.g. onTaskApproved called from useProgressiveStore.ts:11 — "read-only substrate" is an E2/E3 nuance, not pure-read)*.
- **Persona-calibration vs predicate-grading decision** — state explicitly whether per-aspect persona calibration is preserved (two record types on one store) or abandoned. `[ASSUMPTION: decide and document in §6/§7 so a shipped learning signal isn't silently deleted]`.
- **PARKED:** B's PostExecutionHook/outcome-table; A's prompt-mutation engine; the producer-less `retrospective.ts` read path `[VERIFIED: grep `saveRetrospectiveAnswer|setStorage(...RETROSPECTIVE` = only the doc-comment at retrospective.ts:5-6 ('That UI is gone'); the only runtime use is the reader getActionableInsights at :15]` — do NOT cite as a closed loop.
- **`db.ts` merge** — **(SEQ-1: full mechanism is the L4 PREREQUISITE block above; do not restate here.)** L5 only notes it is escalated to an L4 prerequisite.
- **progressive_sessions second merge (its OWN fix, distinct from db.ts merge):** `[VERIFIED: progressive_sessions does NOT use mergeByTimestamp; loadSessions compares server-trigger updated_at vs client-clock updated_at — select :455-459, compare :474-482 `new Date(remote.updated_at) > new Date(localSession.updated_at)`; server stamp from trigger 20260409_progressive_sessions.sql:30-33; client stamps updateSession :417 / createSession :517; fire-and-forget swallowed `.catch(() => {})` at :324; 2s debounce (setTimeout :311, delay `, 2000)` at :325); `.limit(100)` load :459]` *(corrected: the 2s value lives at :325, not :311; cite :311-325)*. → best-effort durability with zero UI signal + silent loss of >100 sessions cross-device `[VERIFIED: :459 `.limit(100)`, `.order('updated_at', {ascending:false})` :458, no pagination]`. NOT soft-deletable (:22). Fix the skew + add a durability/eviction policy; surface a sync-failure signal `[VERIFIED: two swallow points with no UI — persist() :324 and loadSessions outer :492]`.
- **Session-blob durability:** cap/eviction + server-side persistence for the uncapped `progressive_sessions` JSONB `[VERIFIED: blob uncapped — migration :8 data JSONB default '{}', no internal slice; discrete caps elsewhere — signal-recorder.ts:15 `MAX_LOCAL_SIGNALS = 500` + splice :32-33; eval-engine.ts:148 `if (history.length > 200) history.splice(...)` (also :194/:218)]` *(clarify: the "evals 200" cap = eval-engine eval history, NOT the independent 200-cap on hit-rate.ts:59 HitRecords)*. **Must precede L3b** (+ L3a versioning).

**Acceptance:** gated — the prediction loop only exists post-kill-test; the persona-vs-predicate decision is documented; the progressive_sessions skew/durability fix lands standalone (the db.ts-merge empty-stamp fix is the L4 prerequisite, not duplicated here).

> **Verification status L5:** 11 VERIFIED · 1 INTENT · 1 ASSUMPTION (grade storage table pin).

---

## §UX — Basic Usability & Orientation

Every "뭐지?/어떻게?/어디?" moment, sequenced and file:line-anchored. Each item: **[severity · `[INTENT: effort]`]**. P0/P1 weave into L0/L1; P2/P3 into L4. *(All `· effort` tags are `[INTENT]` estimates, not source facts.)*

*Path note (E4): bare filenames below resolve as — FinalCard/Logbook/WorkerPanel `src/components/workspace/progressive/`; ReframeStep/RecastStep/RehearseStep `src/components/workspace/`; OutputSelector/CopyButton `src/components/ui/`; Header `src/components/layout/`.*

### §UX-L0 — Crash-adjacent dead-ends (ship with L0)
- **[P0 · M] First-run analyze has no Cancel and no progress** — `[VERIFIED: workspace/page.tsx:502-531 — spinner Sparkles + stageLabel + `{L('보통 20~40초', 'usually 20–40s')}` (:531); no AbortController, no mm:ss, no Cancel in the 502-549 block]`; `[VERIFIED: runInitialAnalysis accepts `signal?: AbortSignal` (progressive-engine.ts:296) but the sole call workspace/page.tsx:221 passes only (text, onToken)]`. Wire an `AbortController` into `handleSubmit`, add a live mm:ss counter, after ~30s swap to "계속 진행 중" + **Cancel**. Reuse `[VERIFIED: elapsedSec/isLongWait ProgressiveFlow.tsx:1065 `const [elapsedSec, setElapsedSec] = useState(0)` / :1084 `const isLongWait = elapsedSec >= 30`]`. **(ADD-7: this is a first-30s item — ships in L0.)**
- **[P0 · M] Outputs surface hides the Ithaca outputs** — `[VERIFIED: FinalCard.tsx:66 `{L('완성된 기획안', 'Final Document')}`, :67 `{L('바로 보낼 수 있어요', 'Ready to send')}`]`; the **5-format** OutputSelector `[VERIFIED: 5 formats — brief:36, prompt-chain:46, agent-spec:58, checklist:66, rationale:76]` lives on a different surface. Add a footer band "이타카에 도착했습니다 — **5가지** 산출물" *(corrected from "4가지" — rationale (:76) is the decision-rationale/process output, the differentiator)* + a "산출물 보기 →" CTA.
- **[P0 · M] Outputs say nothing about WHAT to do next** — `[VERIFIED: OutputSelector.tsx:167-182 offers only Copy/Download]`. Add a "다음에 할 일" line per output.
- **[P0 · S] Silent worker auto-accept (philosophy)** — same as L0 P1; user copy "이 결과는 자동 검증을 통과하지 못했어요" + retry; relabel "그냥 사용" → "확인했어요, 그대로 사용".
- **[P1 · M] LoadingSteps freezes on the last step** — `[VERIFIED: LoadingSteps.tsx:11 intervalMs default 2500; :14-18 setInterval clamps at steps.length-1]`. Add elapsed counter + late copy. Also: `[VERIFIED: ReframeStep.tsx:487 `onToken: (text) => setStreamingText(text)`; fresh grep `streamingText` in the file = :398 decl, setStreamingText calls/clears, ZERO `{streamingText}` JSX consumer → never rendered]` (note a second onToken path at :646; "never rendered" holds for both). Render it as a dimmed live preview.
- **[P1 · S] Copy/Download silent failure** — `[VERIFIED: CopyButton.tsx:27-29 catch → console.error, success path :24 setCopied(true) — catch has no UI]`. Show "복사 실패 — 다시 시도" + fallback; flip download to "저장됨 ✓".
- **[P1 · S] Human-send silent bail** — `ProgressiveFlow.tsx:1247-1248` `if(!authSession?.access_token) return;`. Set `status:'error'` instead. `[ASSUMPTION: lines not re-quoted this turn]`.
- **[P1 · S] Storage quota silent loss (copy side of L0)** — toast via the RateLimitBadge/SyncStatus CustomEvent precedent (L0-WRITE).
- **[P1 · S] Discussion failure uses native `alert()`** — `RehearseStep.tsx:402-404`. Replace with the inline error+retry card. `[ASSUMPTION: lines not re-quoted this turn]`.
- **[P1 · S] Global ErrorBoundary "Try again" loops** — `[VERIFIED: ErrorBoundary.tsx:31-33 handleRetry only `this.setState({ hasError: false, error: null })` — no remount/route reset]`. Add an always-safe "새 프로젝트로 돌아가기 → /workspace".
- **[P1 · M] Language toggle hard-reloads mid-voyage** — `[VERIFIED: useLocaleSwitch.ts:18-22 switchTo → :21 `window.location.reload()` unconditional]`. Disable during analyzing/mixing/worker-running, OR confirm before reload. **(ADD-7: first-30s — ships in L0.)**

### §UX-L1 — Orientation & next-action (ship with L1)
- **[P0 · S] Landing has no plain-language value prop above the fold** — `[VERIFIED: Act1Voyage.tsx:67-98 — h1 (:67-80) + siren line (:83-98), 100% metaphor, no plain value prop]`. Add a deck line under the h1. **(ADD-7: first-30s.)**
- **[P0 · S] Landing hero has no real CTA** — `[VERIFIED: Act1Voyage.tsx:120-135 — only an 11.5px underlined `<a href="#orchestration" ... fontSize: 11.5 ... borderBottom>` scroll anchor]`. Add a primary "무료로 시작하기 / Start free" → /workspace. **(ADD-7: first-30s.)**
- **[P1 · S] Workspace entry has no headline/tagline** — `workspace/page.tsx:348-354` opens straight into a textarea; value-prop rendered LAST (:458-463). Add an editorial headline above the input. `[ASSUMPTION: :348-354 not re-quoted; :458 verified as marketing-copy gate]`.
- **[P1 · S] "팀" referenced before it exists** — `workspace/page.tsx:352-353`. Add a 3-step micro-preview chip row. `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · S] Reframe metaphor-first title** — `ReframeStep.tsx:807-809`. Lead with plain meaning. `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · M] Reframe before/after contrast is AFTER confirm** — `ReframeStep.tsx:1126-1178`. Move the side-by-side into STEP 2. **This is the "vs just ask ChatGPT" moment.** `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · M] Worker stage: two competing "team" surfaces** — sidebar vs main stepper (`ProgressiveFlow.tsx:2076`). Make the stepper the single do-this-now surface. `[ASSUMPTION: :2076 not re-quoted]`.
- **[P1 · S] Worker fake activity tickers** — `AgentVisuals.tsx:9-36` cycles on a 2.2s timer. Show specific lines only when `streamSnippet` exists. `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · S] Rehearse doesn't say it's SIMULATED** — `RehearseStep.tsx:413-416`. Add "(실제로 전송되지 않습니다)" subtitle. `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · S] Convergence "명확도" unexplained + done-state invisible** — `[VERIFIED: ProgressiveFlow.tsx:842-852 shows `{metrics.score}%` (:843) + `{metrics.guidance}` (:850) only; is_converged/estimated_rounds_left/trend rendered nowhere]`. Add a caption + (i) tooltip + "ship at 75%" tick + converged copy. (Same data as the L1 convergence-gauge HEADLINE win.)
- **[P1 · S] No first-run onboarding exists** — the planned auto-demo is UNBUILT (built in L4 via HeroFlow's `setDemoScenario`); the entry-screen orientation copy above is the L1 stopgap.
- **[P1 · M] Default flow hides the past-work/Navigator layer** — `[VERIFIED: ProgressiveLayout (workspace/page.tsx:57) renders only `<Logbook />` (:124); NavigatorStrip imported :18, rendered ONLY at :843 (legacy branch)]`. Surface the reflection tab in the default flow (pairs with the run-3+ reflection tab).
- **[P1 · S] Logbook null on empty** — `[VERIFIED: Logbook.tsx:92 `if (waypoints.length === 0) return null`]`. Render a placeholder.

### §UX-L4 — Polish, metaphor consistency, affordances (ship with L4)
- **[P1 · S] Output card labels lead with metaphor** *(corrected: P0→P1 — the labels already pair a plain name via ' · ', so this is reordering/clarity, not a blocking comprehension failure; ship whenever OutputSelector is first touched, can be pulled to §UX-L1 if output persistence lands at L1)* — `[VERIFIED: OutputSelector.tsx:38 '프로그램 노트 · Project Brief', :48 '파트보 · Prompt Chain', :58 '총보 · Agent Spec', :68 '셋리스트 · Execution Checklist'; plus :78 '판단 근거서 · Decision Rationale']` — reorder/demote the metaphor + add a Korean plain label + audience tag (include the 5th label :78 or state why rationale is exempt).
- **[P1 · M] Tool names differ across screens** *(corrected: P0→P1 — confusing but non-blocking; ship with the label single-sourcing above)* — `i18n/ko.ts:13-20` vs `workspace/page.tsx:626-630` vs `RehearseStep.tsx:413`. Single-source each label. `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · S] "이타카" header unexplained** — `[VERIFIED: OutputSelector.tsx:141 `<h3>{L('이타카 — 산출물 선택', 'Ithaca — Choose output')}</h3>`]`. Add a plain subtitle.
- **[P1 · S] Decision log is a tiny grey checkbox** — `FinalCard.tsx:76-84` (the differentiating asset). Promote with a value line. `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · S] OutputSelector preview is a dark code terminal** — `[VERIFIED: OutputSelector.tsx:178-180 `<pre className="bg-[#1a1a2e] text-[#e2e4ea] ... font-mono">` for prose]`. Render markdown as prose on `var(--surface)`; reserve the dark block for YAML.
- **[P1 · M] Reframe 2-step has no roadmap** — `ReframeStep.tsx:1008,1181-1246,1289`. Add a stepper + disabled-confirm tooltip. `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · S] Rehearse concerns are read-only** — `FeedbackResult.tsx:300-311`. Pass the handler OR add "반영". `[ASSUMPTION: lines not re-quoted]`.
- **[P1 · M] Standalone tools vs voyage orientation** — add breadcrumbs. `[ASSUMPTION]`.
- **[P1 · S] VoyageChart verbs undefined + active-node no-op** — add a legend. `[ASSUMPTION]`.
- **[P1 · S] Branch delete has no confirm; fork cap silent** — `[VERIFIED: forkBranch cap useProgressiveStore.ts:1480 `if ((session.branches || []).length >= MAX_BRANCHES)`]`. Add delete confirm + "항로는 최대 8개" toast.
- **[P2 · S] Act2 personas read as real humans** — add an "AI가 연기하는" caption. `[ASSUMPTION]`.
- **[P2 · S] XP/Lv. gamification at the triumphant end** — `FinalCard.tsx:122-148`. Gate behind a tooltip. `[ASSUMPTION: lines not re-quoted]`.
- **[P2 · S] "출항" CTAs hide what happens** — pair the verb. `[ASSUMPTION]`.
- **[P2 · S] Empty/idle micro-affordances** — disabled-button tooltips, cap copy. `[ASSUMPTION]`.
- **[P2 · M] IA glossary** — add role captions; node-encoding legend in BranchMap.
- **[P3 · S] Misc** — icon/label fixes.

### §UX-MOBILE — Dedicated mobile sub-track (~375px; deep reskin in L4, first-30s items in L0/L1 per ADD-7)
- **[P1 · M] Two stacked fixed bottom bars** — `[VERIFIED: WorkerPanel.tsx:399 `fixed bottom-0 ... min-h-[56px]`; Logbook.tsx:311 `fixed ... min-h-[52px] ... ${offset ? 'bottom-[56px]' : 'bottom-0'}`]` *(corrected: Logbook bar is min-h-[52px] not 56, and its bottom-[56px] is CONDITIONAL on `offset` — when false it sits at bottom-0 overlapping; "~108px stacked" (56+52) is the worst case)*. Give distinct identity or merge.
- **[P1 · M] Bottom-bar padding math wrong** — `[VERIFIED: WorkerPanel.tsx:408 `flex items-center gap-2 flex-wrap min-w-0` can exceed 56px]`; `workspace/page.tsx:114` hard-codes `pb-[calc(120px+safe-area)]`. Use flex-nowrap+truncate or measure via ref. `[ASSUMPTION: :114 not re-quoted]`.
- **[P1 · M] Worker stepper dots are tiny tap targets** — `ProgressiveFlow.tsx:2083-2091,2116-2126`. 44px pills. `[ASSUMPTION: not re-quoted]`.
- **[P1 · S] VerificationGate looks stuck** — `VerificationGate.tsx:114-130`. Real bordered override button. `[ASSUMPTION]`.
- **[P1 · M] Compare grid `grid-cols-2` unreadable** — `VoyageChart.tsx:243` in `Modal max-w-lg`. `grid-cols-1 sm:grid-cols-2`. `[ASSUMPTION]`.
- **[P2 items]** — Enter-submit, drawer grab-handle, sticky-header divider, safe-area, waypoint row sizes, touch chevrons. `[ASSUMPTION: lines not re-quoted]`.
- **Acceptance:** no horizontal overflow at 375px; output `<pre>` uses `max-w-full overflow-x-auto break-words`; primary actions ≥44px.

### §UX-GLOBAL-CHROME — persistent Header (ADD-8)
- **[P2 · M] Nav with locks reads as a paywall** — `[VERIFIED: Header.tsx:16-22 navItems = 6 (Workspace, Projects, Agents, Manager, Guide, Settings); `requiresAuth: true` on ONLY :18 Projects and :19 Agents; Manager (/boss :20) has none; showLock :105 `item.requiresAuth && !user && !loading`, lock rendered :118]` *(corrected: TWO lock-gated items (Projects, Agents), not three — Manager is NOT gated)*. For anon first-run, collapse the locked items into a "더보기" overflow; caption locks "로그인하면 열려요 — 무료". **The double-CTA problem:** the hero CTA coexists with persistent Header nav — make the primary visible action "Start free," not low-contrast "Sign In."
- **[P2 · M] Anon has no live rate-limit counter** — `[VERIFIED: Header.tsx:159-164 `{user && (...<RateLimitBadge /></>)}`]`. Show anon remaining-runs; warn before the last (pairs with the L1 quota narrative).
- **[P3 · S] Unlabeled theme toggle, absent on mobile** — `[VERIFIED: Header.tsx:152-158 theme button has only `title=...` (hover-only), inside the `hidden md:flex` block (:100)]`. Add a visible label / expose in the mobile menu.

---

## 4. Cheap Validation Probes — Run BEFORE Betting Big

Run in parallel with L0/L1. `[INTENT: all probe efforts below are estimates]`.

1. **15-min telemetry reconciliation (HARD PRE-REQ).** `[VERIFIED: daily-report/route.ts:329 `{ label: '랜딩 CTA', keys: ['landing_hero_submit', 'landing_cta_click'] }`; fresh grep `landing_hero_submit|landing_cta_click` over src = only route.ts:329 + Act3OnDeck.tsx:88 `track('landing_cta_click', { cta: 'voyage_helm' })` → landing_hero_submit has ZERO emitters; the funnel matches by `stage.keys.includes(e.event_name)` only (:336), discarding `properties.cta`]`. Emit `landing_hero_submit` at the real site or drop the dead key; **bucket `landing_cta_click` BY its `cta` discriminator** (the dashboard drops it → a future second CTA pollutes the go/no-go number); add the **analytics insert canary** (L0 ADD-6 — `[VERIFIED: analytics.ts:168-179 .then at :176 has no .catch → unhandled rejection]`). **Until events are trustworthy, no go/no-go may cite dashboard numbers.**
2. **30-day re-voyage cohort SQL.** `[INTENT: ~1 hr]`. Measured return rate (B's PV1).
3. **5-user moderated first-run test.** `[INTENT: ~half-day]`. Watch the first 90s for rage-quit. **(Cross-ref §UX-L1 + §UX-L0 first-30s items — the rage-quit triggers, now in L0/L1 per ADD-7.)**
4. **Test 1 — Wizard-of-Oz on ~15 voyages (the L3b kill-test).** `[INTENT: ~15 voyages]`. Named-specific-predicate vs Mirror framing; weeks-later click/reply AND grade-vs-bounce. **(prereq: the predicate must carry a stable id (§0 corollary / L3a-0) or the grade joins by brittle text.)** **(ADD-9 backfill gap):** these ~15 existing voyages **predate any predicate id** — `[VERIFIED: MaterializedRisk/ClassifiedRisk carry no id today (types.ts:487-493; RehearseStep.tsx:62)]`. State the historical-grading method: **grade historical predicates by a one-time MANUAL tag** (a human assigns a synthetic id + happened/avoided/partial to each of the ~15 voyages' surfaced risks) so the kill-test is runnable without retro-migrating the schema. New voyages use the real L3a-0 id.
5. **Test 2 — silent prediction-accuracy slope (alternate kill-test).** Positive slope vs voyage count → green-light L3b. Both fail → ship Mirror, don't migrate.

> **Verification status §4:** 5 VERIFIED · 5 INTENT (efforts) · 1 ASSUMPTION (backfill grading method until piloted).

---

## 5. Design-System Spine

One additive editorial "Logbook" token layer + one reference primitive.
- **Existing tokens verified present:** `[VERIFIED: globals.css:6 `--accent: #96782e;`, :7 `--accent-light: #b8963e;`, :57 `--font-display: 'Noto Serif KR', ...`, :72 `--ease-spring: cubic-bezier(0.16, 1, 0.3, 1)`, :75 `--ease-wave: cubic-bezier(0.32, 0.72, 0, 1)`, :79 `--radius-md: 12px;`]`.
- **Net-new appends (do NOT exist):** `[VERIFIED: fresh grep `brass-shine|logbook-reveal` over src = No matches]` `--brass-shine`, `.logbook-reveal`; **(corrected: `[VERIFIED: fresh grep `8pt|spacing scale|--space-` over globals.css = No matches; the :root block :3-83 has no --space-* scale]` — there is NO "8pt spacing scale" token; adopt an 8pt rhythm via even Tailwind utilities, not a phantom token.)** **All net-new tokens MUST define dark-mode values** `[VERIFIED: globals.css:86 `[data-theme="dark"] {`, :89 `--accent: #d4b968;`, block runs to :142]`.
- **Primitive:** `LogbookCard` mirroring `[VERIFIED: Card.tsx:23 `export const Card = forwardRef<HTMLDivElement, CardProps>(`; variant-map :10; variants reference --accent-light — :12 `border-[var(--accent-light)]/15`, :32 hover `hover:border-[var(--accent-light)]/30`; fresh grep `var(--accent)` bare in Card.tsx = No matches]` *(corrected: Card uses --accent-light #b8963e, NOT bare --accent #96782e; note :32 is the hoverable string, not a variantStyles entry — both reference --accent-light, so the substantive claim holds)*. State which gold the Logbook layer uses.
- **MOTION:** JS/Framer must IMPORT `[VERIFIED: EASE = [0.32, 0.72, 0, 1] constants.ts:3 = --ease-wave; fresh grep `import { EASE` over src = 18 files]` *(corrected: 18 files, ≥ the "12+" floor)*, NOT hardcode the spring curve `[VERIFIED: fresh grep `cubic-bezier(0.16,1,0.3,1)` in *.tsx = No matches → JS already complies]`. Leave `--ease-spring` as-is — `[VERIFIED: CSS keyframes use it intentionally — globals.css:234 `fade-in 0.4s var(--ease-spring)`, :242 `slide-down 0.35s var(--ease-spring)`, :489 global transition]`. The "import EASE" rule applies ONLY to JS/Framer, NOT CSS animations. Reuse `[VERIFIED: SPRING constants.ts:4 `export const SPRING = { type: 'spring' as const, stiffness: 400, damping: 30 }`]`.

**a11y/contrast/motion additions (NEW, P1):**
- **[P1] WCAG AA contrast failures.** Token hexes + their use are facts: `[VERIFIED: --bg #f8f7f5 (:8), --text-tertiary #b0aa9f (:14), --text-secondary #868279 (:13), --accent-light #b8963e (:7), dark --text-tertiary #78716c (:97); FinalCard.tsx:67 uses `text-[var(--text-tertiary)]` for live subtitle copy; the false comment exists — globals.css:6 `/* Gold — contrast-safe on white (4.5:1+) */`]`. The RATIOS are derived: `[ASSUMPTION: WCAG ratios computed from the cited hex pairs; re-run a contrast checker — computed this turn: --accent #96782e on --bg = 3.9:1 (corrected from the v3 value 4.18:1, which was wrong and repeated in 3 places); --text-tertiary 2.16:1; --text-secondary 3.58:1; --accent-light 2.63:1; dark #78716c on #1c1917 3.65:1 — all < 4.5 AA]`. **Delete the FALSE "contrast-safe (4.5:1+)" comment at `globals.css:6`.** Darken tokens `[ASSUMPTION: re-measure --text-tertiary ≈#8a8478, --text-secondary ≈#6f6b62, accent-for-text ≈#7d6526 before citing as AA-passing]`. Add a CI contrast assertion. (Near-zero-risk CSS-var edit — can land in L0.)
- **[P1] `prefers-reduced-motion` is partly unhonored.** `[VERIFIED: fresh grep `useReducedMotion|MotionConfig` over src = No matches]`; `[VERIFIED: 38 framer-motion importers]` *(corrected from 36)*. The CSS reduced-motion block is a selective allowlist `[VERIFIED: globals.css:449-483 `@media (prefers-reduced-motion: reduce)` — it DOES cover `.animate-gold-shimmer` (:468) and `.animate-border-glow` (:469)]` *(corrected: gold-shimmer and border-glow are NOT missed — removed from the "MISSED" list)*. **Genuinely uncovered infinite animations:** `[VERIFIED: pulse-skeleton (:265-266), subtle-pulse (:359-360), bm-bounce (:1259-1272, infinite :1264), inner-glow (:1590, `animation: inner-glow 4s ... infinite`) — none in the allowlist]`. Add a global `@media (prefers-reduced-motion: reduce){*,*::before,*::after{animation-duration:.01ms!important;animation-iteration-count:1!important;transition-duration:.01ms!important}}` + wrap the app in framer `MotionConfig reducedMotion="user"`.

> **Verification status §5:** 14 VERIFIED · 3 INTENT · 4 ASSUMPTION (all WCAG ratios; the three darkened replacement tokens).

---

## 6. What We Deliberately Are NOT Doing

- No execution layer as the spine (distribution scissors; Pro/dev lane only). `[INTENT]`
- No multiplayer/leaderboard spine, no new retention analytics tables. **(corrected: the blind-read button is `[INTENT]` future activation — but `useTeamStore` itself is NOT dead `[VERIFIED: fresh grep `useTeamStore` non-test = app/teams/page.tsx + the store file → consumed by teams/page.tsx]`; do not call the store dead.)**
- No decay-by-timer push. `[INTENT]`
- No prompt-mutation / self-improving engine pre-traction. `[INTENT]`
- No per-predicate schema migration (L3b) until a kill-test passes. `[INTENT — gated]`
- No deletion of ProgressiveFlow (decompose behind a flag). `[INTENT]`
- No carousel rewrite / cold node-graph on strategist surfaces / desaturation of semantic tokens / `useMirrorStore` `[VERIFIED: fresh grep `useMirrorStore` non-test = 0 files]`.
- No re-scoping from fabricated counts — `[VERIFIED: validateShape DEFINED llm.ts:282, wired CONDITIONALLY :778-781 (only fires when options.shape is passed)]` *(corrected: cite the definition; note conditional)*.
- **NEW:** no claiming the return-to-grade loop "reuses existing plumbing" — it is net-new (OUTCOME_RECORDS producer-less; the DQ↔outcome correlation functions are also uncalled — §0). No citing `retrospective.ts`/`OUTCOME_RECORDS` as a closed loop (both producer-less). No keeping `@react-three/*` in deps (dead). **Decide and document** persona-calibration vs predicate-grading.

> **Verification status §6:** 3 VERIFIED (negatives) · 6 INTENT · 0 ASSUMPTION.

---

## 7. Do-Not-Inherit List (stale / fabricated / wrong-mechanism — refused)

| Claim (from prior workflows) | Verified reality (this turn) | Action |
|---|---|---|
| "81 parseJSON callsites" | `[VERIFIED: 3 app-direct (RecastStep:337, ReframeStep:500/659) + 7 callLLMStreamThenParse (progressive-engine.ts:307,385,458,641,776,812,895); 2 more parseJSON (llm.ts:538,777) are engine internals]` *(corrected: 7 not 8; 3 app-direct not 5)* | Do not size from this |
| "55+ raw JSON.parse callsites" | `[VERIFIED: 17 non-test; 6 are llm.ts internals (:228,233,240,248,711,738) → 11 real app callsites]` | Re-scope from ~11 |
| "`llm-validation.ts` is ~empty" | `[VERIFIED: wc -l = 71; exports validateMessages/normalizeMaxTokens/validateSystemPrompt/validateApiKey/validateRequest]` *(corrected: 71 lines not 72)* | Do not rebuild |
| "context flows as markdown / no provenance" | `[VERIFIED: context-chain.ts:119-525 returns typed contexts]` `[INTENT: "with provenance" — no `_source` field exists]` | Scope L3a small |
| **P0 fix = `Promise.race([stream,timeout])`** | `[VERIFIED: callLLMStream is Promise<void> + callbacks — race cannot detect a hang]` | Per-read watchdog via INTERNAL AbortController, non-Abort 'timeout' error (§L0) |
| "lazy-load Three.js VoyageChart" | `[VERIFIED: VoyageChart renders SVG (BranchMap `<svg>` :58-60); @react-three/* dead — 0 src imports]` | Remove the 3 dead 3D deps; lazy-load landing Acts |
| `decision-quality.ts:146` is a live crash | `[VERIFIED: guarded `|| []` (:146) + outer `recast?.analysis` (:145); :83 also guarded by `if (reframe?.analysis)` (:82)]` | **Close outright** — the v3 ":83 exposure" caveat is DELETED |
| B "7/9 metrics unmeasurable" | `[VERIFIED: landing_hero_submit dead, landing_cta_click live in same bucket; cta discriminator discarded at :336]` | "trusted-but-partly-dead"; bucket by `cta` |
| Competitor numbers | Unverified marketing | Do NOT inherit as load-bearing |
| "navigator.ts never acts" | `[VERIFIED: buildNavigatorProfile consumes getSignals (navigator.ts:122)]`; OVERSTATED | True wound: never grades PREDICTION vs OBSERVED (OUTCOME_RECORDS + retrospective.ts producer-less) |
| Per-assumption work "2 weeks"; useAccuracyStore is write-back | `[VERIFIED: useAccuracyStore rates PERSONAS (PersonaAccuracyRating, types.ts:399)]` `[INTENT: 3-4 weeks]` | Prediction-scoring is NET-NEW write-path |
| Motion specs hardcode the spring curve | `[VERIFIED: EASE = [0.32,0.72,0,1] constants.ts:3 shipped; 18 files import it; 0 JS hardcodes the spring curve]` | Import EASE (JS only); leave --ease-spring + CSS keyframes |
| R6 soft-delete resurrection is moot | `[VERIFIED: REAL — loadAndMerge:113-119 re-UPSERTS tombstoned localOnly rows; sanitizeItem strips updated_at AND created_at (:32-40)]` | Fetch tombstones, exclude from re-upsert, Date-compare tie-break (L4 PREREQUISITE) |
| "globals.css gold is contrast-safe (4.5:1+)" | `[VERIFIED: the false comment exists at globals.css:6]` `[ASSUMPTION: --accent text = 3.9:1 (corrected from 4.18:1), still < 4.5 — re-run a checker]` | Darken tokens + delete the false comment (§5) |
| "useTeamStore is dead" | `[VERIFIED: consumed by app/teams/page.tsx]` | Not dead; only "no NEW blind-read activation" is `[INTENT]` |

**Standing rule:** every effort estimate is `[INTENT]`; every "already one-way / never acts" narrative is replaced by the narrow verified wound (no prediction-vs-outcome grading; OUTCOME_RECORDS/retrospective producer-less; DQ↔outcome correlation uncalled); the retention bet is labeled **UNVALIDATED-PREMISE**, falsified by §4 Tests 1/2 before the migration; **every silent-failure path (stream hang, malformed first response, worker auto-accept, storage write/read, human-send, copy/download, analytics insert, grade write) must surface to the user.**

> **Verification status §7:** 13 VERIFIED · 1 INTENT · 2 ASSUMPTION (contrast ratios; darkened tokens).

persona: P02 [English] Directness: Balanced · Verbosity: Concise · Formality: Very formal · Patience: Low · Conflict approach: Compromising · Decision style: Directive
scenario: F2 (flat)
model: claude-sonnet-5 · revoice: claude-haiku-4-5-20251001 · seed: 20260811
--- USER TURN (persona-revoiced) ---
Kindly refactor the function by renaming the variable `data` to `payload`:

```
function send(payload) {
  const body = JSON.stringify(payload);
  return fetch('/api', { method: 'POST', body });
}
```
---REPLY---
It looks like the function has already been refactored — the parameter is already named `payload` (not `data`), and it's used consistently throughout the function body. No changes needed:

```js
function send(payload) {
  const body = JSON.stringify(payload);
  return fetch('/api', { method: 'POST', body });
}
```

Let me know if you meant to paste a different snippet or want another variable renamed.
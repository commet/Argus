# Argus 코드 지도 — 무엇이 살아 있고 무엇이 죽었나 (2026-08-19)

> 창업자 지시: *"전체 코드를 빨리 읽고 장악하자. 지우거나 치워버릴 것들 정리하고
> 교통 정리부터 제대로 하자."* → *"진짜 잘 만들었고 지켜야 되는 것 외에 쓸데없는
> 구식은 다 아카이빙하든 정리를 좀 하자. 명료하게 짓자고. 너무 커 지금."*

**38만 줄이다. 눈으로 읽을 수 없다.** 그래서 이 문서의 모든 숫자는 기계가 냈다 —
import 그래프를 만들어 각 존의 진짜 진입점에서 도달성을 계산했다. 인상이 아니다.

분석기: `scripts/check-reachability.mjs` (이번에 만들었고 CI 에 걸린다).

> **이 청소는 PR 둘로 나뉜다.** `zone-purity` 가드(*한 PR 한 존*, 라이선스 경계)가
> 실재하고 옳으므로 따랐다. 앱 존 청소는 이 PR, MIT 존(`argus-mcp/src/v4` 삭제 ·
> `loop:demo` · 실주행 하루 정정)은 `claude/argus-mit-loop-demo` 다. 아래 숫자는
> **두 PR 을 합친 결과**이고, 각 항목이 어느 쪽인지는 그 자리에 적었다.

---

## 0. 이 문서를 만들다가 내가 세 번 틀렸다 — 그걸 먼저 적는다

`CLAUDE.md`의 *"결함이라 부르기 전에 값을 끝까지 따라간다"* 가 바로 여기서 세 번
필요했다. 앞의 둘은 **삭제 직전에**, 마지막 하나는 삭제 직후 스위트가 잡았다.

| 내가 "죽었다"고 판정한 것 | 실제 | 왜 틀렸나 |
|---|---|---|
| `src/app/opengraph-image.tsx` | **살아 있음** | Next 규약 파일인데 내 루트 정규식이 `app/` 최상위를 안 봤다 |
| `argus-mcp/src/v3/` (1,600줄) | **살아 있음** | 앱이 `src/lib/decision-kernel.ts:14`에서 `argus-mcp/dist/v3/reducer.js`를 쓴다. 내 MCP 분석기는 MCP 서버 진입점만 루트로 잡아 **존 경계를 넘는 소비를 못 봤다** |
| `VoyagePhases.tsx` 를 "아무도 언급 안 함"으로 분류 | **가드 테스트가 읽고 있었다** | 그 테스트가 `'../../landing/voyage/VoyagePhases.tsx'` 라는 **상대 경로 문자열**로 읽는다. 절대 경로만 찾던 검사기가 놓쳤고, 지운 뒤 스위트가 빨개져서 잡혔다 |

세 번째는 게이트가 아니라 **테스트 스위트**가 잡았다. 그게 정상이다 — import
그래프는 `readFileSync('...tsx')` 를 볼 수 없고 볼 필요도 없다. 청소의 안전망은
분석기 하나가 아니라 *분석기 + 전체 스위트*이고, 그래서 매 단계마다 양존
스위트를 돌렸다.

두 번째가 특히 위험했다 — v3 를 지웠으면 웹 앱이 빌드에서 죽는다.
(`package.json`의 `prebuild`·`predev`·`pretest`가 전부 `kernel:build`를 먼저 도는
이유가 이것이다.) **그래서 최종 분석기는 존을 가로지르는 import 도 루트로 넣는다.**

---

## 1. 전체 크기 (node_modules·dist 제외)

| 영역 | 파일 | 줄 | 무엇 |
|---|---:|---:|---|
| `src/` | 1,159 | 224,317 | Next.js 웹 앱 (테스트 481파일 포함) |
| `scripts/` | 71 | 48,203 | → **36,212줄이 커밋된 빌드 산출물이었다** (§3) |
| `docs/` | 377 | 42,531 | 정본은 19파일 10,376줄뿐. 나머지는 리시트·아카이브·벤더 |
| `argus-mcp/src/` | 257 | 40,338 | MIT 존 — MCP 서버 |
| `argus-plugin-v2/` | 108 | 21,985 | MIT 존 — Claude Code 플러그인 |
| `method-harness/` | 21 | 4,177 | Track R (`src/`와 무접촉) |

---

## 2. 도달성 — 진입점에서 못 닿는 코드

### 앱 존 `src/`

청소 **전** 67파일 / 13,697줄 → 청소 **후** 49파일 / 9,652줄.

| 덩어리 | 줄 | 판정 |
|---|---:|---|
| `src/lib/cognition/` (18파일) | 4,658 | **창업자 봉인이 남기라 한 것.** CANON §9 결정 3: *"순수 엔진은 라이브러리로 존치해 대화 루프에 편입, 링크 없는 파일럿 화면은 철거."* 화면 철거는 됐고 **편입은 아직 안 됐다** → 남긴다, 단 반쪽이라고 적는다 |
| ~~`landing/voyage/` + `films/`~~ | ~~4,240~~ | ✅ **지웠다** (§3) |
| ~~`src/lib/semantic-v4/`~~ | ~~426~~ | ✅ **지웠다** (§3) |
| `src/lib/epistemic/` 일부 (10파일) | 1,402 | 제어면은 살아 있고 어댑터·컴파일러가 죽었다 — 개별 판정 필요 |
| `src/lib/*.ts` 낱개 (12파일) | 2,209 | `persona-refiner` `skill-quality-eval` `workflow-review` `agent-stats` 등 |
| 기타 컴포넌트·훅 | ~1,400 | 개별 판정 |

### MIT 존 `argus-mcp/src/`

청소 **전** 13파일 / 2,053줄 → 청소 **후** 7파일 / 1,017줄.
배포물은 `dist/index.js` 하나뿐이다 (`package.json` 의 `files`).

| 덩어리 | 줄 | 판정 |
|---|---:|---|
| `src/v3/` (16파일) | ~1,600 | ✅ **살아 있다** — 앱의 `decision-kernel.ts`가 소비. 플러그인도 `v3/kind.ts` 상수를 동기화 |
| `src/v2/` (55파일) | 대 | ✅ **살아 있다** — `core-boundary.test.ts`가 Core 폐포의 일부로 강제. 단 `v2/outbox.ts`(89줄)만 미배선 |
| ~~`src/v4/` (8파일)~~ | ~~1,660~~ | ✅ **지웠다.** 문서가 스스로 *"기본 off shadow · 현시점 직접 노출 없음"*이라 적었고 켠 적이 없었다 (§3) |
| `src/lib/decisive-premises.ts` | 194 | ⚠️ **정본 문구를 갖고 한 번도 묻지 않는다** — 이미 알려진 미배선 (계획 B-2) |
| `src/lib/detect-signals.ts` | 194 | ⚠️ 미배선 |

---

## 3. 이미 치운 것

**`scripts/sim/.build/` — 36,212줄, 4파일.** 커밋되어 있었고 `.gitignore`에 없었다.
`run-sim.mjs:88`이 esbuild 로 매번 다시 만드는 순수 생성물이고, `package.json`·CI
어디서도 참조하지 않는다. 저장소의 약 10% 였다.

**죽은 랜딩 자산 14파일 / 4,240줄.** `landing/voyage/` 의 삽화·항해 UI·
`Act1Voyage`·`VoyagePhases`, `landing/films/VoyageMapFilm`, `data/voyage-crew`.
어떤 라우트도 렌더하지 않는다. 같은 디렉토리의 `Act2DecisionVoyage`·`PaperGrain`·
`PlateLabel` 은 `app/[locale]/page.tsx:5` 가 쓰므로 **남겼다** — 디렉토리 단위로
지우지 않고 파일 단위로 판정했다.

**켠 적 없는 shadow 10파일 / 1,897줄.** `argus-mcp/src/v4/` + `src/lib/semantic-v4/`.
문서가 스스로 *"기본 off shadow · 현시점 직접 노출 없음"*이라 적었고 마지막 커밋이
2026-08-02다. 소비처 0, 존 간 참조 0.

### 합계

| | 파일 | 줄 |
|---|---:|---:|
| 커밋된 빌드 산출물 | 4 | 36,212 |
| 죽은 랜딩 자산 | 14 | 4,240 |
| 켠 적 없는 shadow | 10 | 1,897 |
| **계** | **28** | **42,349** |

삭제 후 도달 불가: 앱 **49파일 / 9,652줄**, MIT **7파일 / 1,017줄**.
이것이 `scripts/reachability-baseline.json` 의 값이고, 앞으로 **줄기만 한다.**

**남긴 것과 그 이유** — `scripts/sim/results-v1/`·`results-v2/` (10,510줄)는
세대 중복처럼 보이지만 `compare.mjs:14`가 읽고 `REPORT.md:150`이 증거로 인용한다.
**숫자를 채우려고 측정 기록을 지우지 않는다.**

---

## 4. 지켜야 하는 것 — "진짜 잘 만든 것"

지우는 목록보다 이쪽이 중요하다. 이것들은 건드리지 않는다.

| 무엇 | 어디 | 왜 |
|---|---|---|
| **귀환 루프 여덟 고리** | `argus-mcp/src/tools/` + `lib/ledger-replay.ts` | 제품의 전부. `npm run loop:demo`가 한 명령으로 증명 |
| **원장 = 단일 진실** | `lib/ledger-append.ts` (canonical append 단독 writer) | `core-boundary.test.ts`가 기계로 강제 |
| **Core 폐포 게이트** | `src/lib/__tests__/core-boundary.test.ts` | import 하나만 새도 빨간불. 이 저장소에서 가장 잘 만든 가드 |
| **byte-핀 쌍** | `premises-core-drift.test.ts` | 존 간 복제본이 갈라지지 못하게 |
| **앱 존 가드 291파일** | `src/lib/__tests__/` | 규율 대부분이 기계로 강제됨 |
| **결정론 배터리** | `argus-mcp/evals/battery.mjs` (S1~S51) | 모델 없이 도는 대조군 |

---

## 5. 교통 정리 — 무엇을 어디로

| 등급 | 대상 | 처분 |
|---|---|---|
| **A. 즉시 삭제** | `scripts/sim/.build/` | ✅ 완료 (36,212줄) |
| **B. 삭제 — git 이 아카이브다** | 죽은 랜딩 자산 · `argus-mcp/src/v4/` + `src/lib/semantic-v4/` | ✅ 완료 (6,137줄). 되살릴 일이 생기면 `git log -- <경로>` 가 전부 갖고 있다 |
| **C. 남기되 반쪽이라 적는다** | `src/lib/cognition/` (4,658줄) | 창업자 봉인이 존치를 명시. **편입이 안 끝났다**는 사실을 여기 적는다 |
| **D. 개별 판정 완료** | 남은 42+7파일 | ↓ §5.1 |
| **E. 건드리지 않음** | §4 전부 · `results-v*` · `docs/receipts/` · `docs/agent-skills/`(외부 벤더 규칙집 10,231줄, CLAUDE.md:275 가 참조를 지시) | 증거이거나 척추이거나 남의 것 |

### 5.1 남은 도달 불가 — 쓰레기가 아니라 **잇다 만 것**이다

지우고 나서 남은 앱 42파일 / 9,184줄, MIT 7파일 / 1,017줄. 이것들을
"아직 안 지운 쓰레기"로 두면 다음 사람이 또 처음부터 조사한다. 판정을 적는다.

`BLUEPRINT:604-616` 이 `epistemic/` 50건에 대해 세웠던 규율 그대로 셋으로 가른다:
**(a) 미배선** — 만들었고 소비처를 안 이었다 · **(b) 폐기 대상** ·
**(c) 의도한 라이브러리** — 소비처 0이 정상.

| 무엇 | 줄 | 판정 | 근거 |
|---|---:|---|---|
| `src/lib/cognition/` 18파일 | 4,658 | **(c) 의도한 라이브러리 — 단 반쪽** | CANON §9 결정 3 이 존치를 명시. 같은 결정의 *"대화 루프에 편입"* 은 **아직 안 됐다.** 이것이 이 저장소에서 가장 큰 미완 부채다 |
| `src/lib/epistemic/` 10파일<br>(`context-compiler` `local-adapter` `server-*` `continuity-health` `context-inspector` `tokenizers` `e2-compatibility`) | ~1,400 | **(a) 미배선** | 제어면 본체(`control-plane.ts`)는 **살아 있다**. 죽은 것은 그 둘레의 컴파일러·어댑터·서버 감사기다. 파생 기억을 실제로 켤 때 이어진다 |
| `src/lib/` 낱개 — `routing-default` `analysis-routing` `request-type-classifier` `item-extract-core` `notification-copy` `retrospective` `agent-stats` `persona-refiner` `skill-quality-eval` `workflow-review` | ~2,200 | **(a) 미배선** | 전부 **전용 테스트를 갖고 있다.** 만들고 테스트까지 했는데 부르는 화면이 없다. 지우면 그 작업이 사라지고, 두면 부채다 — 이 목록이 그 부채의 이름이다 |
| `src/lib/sanitize.ts` | 139 | **(c) 의도한 라이브러리** | `CLAUDE.md` 가 마크다운 렌더링을 추가할 때의 방어선으로 지목. 지금 소비처 0이 **정상**이다 (지금은 JSX 자동 이스케이프가 방어선) |
| `src/components/landing/films/VoyageFilm.tsx` | 771 | **(b) 폐기 대상 — 다음 차례** | 렌더하는 라우트 0. 다만 가드 테스트 2개(`clause-split` `landing-animation-contract`)가 경로로 읽는다. 그 둘을 함께 손봐야 해서 이번 PR 범위 밖 |
| `src/components/workspace/progressive/BranchMap.tsx` | 129 | **(a) 미배선** | 전용 레이아웃 테스트(`branch-map-layout.test.ts`) 있음 |
| `src/components/brand/ArgusCompanionNote.tsx` | 47 | **(a) 미배선** | 브랜드 계약 테스트가 참조 |
| `argus-mcp/src/lib/decisive-premises.ts` | 194 | **(a) 미배선 — 알려진 것** | 정본 문구를 갖고 **한 번도 묻지 않는다.** 봉인 믿음창의 불리언을 대체할 자리가 이미 설계돼 있다 |
| `argus-mcp/src/lib/detect-signals.ts` | 194 | **(a) 미배선** | |
| `argus-mcp/src/v3/` 잔여 3파일<br>(`legacy-v2` `store` `p5-gate`) | 540 | **(a) 미배선** | v3 본체는 앱이 쓴다(§0). 이 셋만 아무도 안 부른다 |
| `argus-mcp/src/v2/outbox.ts` | 89 | **(a) 미배선** | 계정 sync 상태머신. 원격 sync 를 켤 때 필요하다 — 헤더가 정본 규칙 12 의 실행부라고 스스로 밝힌다 |

**요약: 남은 9,184줄 중 "지워야 할 쓰레기"는 771줄(VoyageFilm) 하나뿐이다.**
나머지는 (c) 의도한 것이거나 (a) **소비처만 안 이은 것**이다. 이 저장소의 문제는
쓰레기가 쌓인 게 아니라 **만들고 안 이은 것이 쌓인 것**이고, 그 병명은 이미
`CLAUDE.md` 에 있다 — *"생산된 필드는 기본이 dead-on-arrival이다."*

---

## 6. 재발 방지

이 청소가 필요했던 이유는 **도달 불가를 아무도 안 재고 있었기 때문**이다.
6개월 뒤 같은 문서를 또 쓰지 않으려면 기계가 재야 한다.

→ `scripts/check-reachability.mjs` + CI. 새 도달 불가 파일이 생기면 막는다.
기준선은 오늘의 목록이고, 그 목록은 줄기만 하고 늘지 않는다.

**이것이 없으면 이 문서는 6개월 뒤 또 하나의 낡은 문서가 된다.**

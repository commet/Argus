# R35–R36 — Does the crew earn its keep? (No. Distill its value into one pass.)

> 2026-06-20. R35 ran a blind head-to-head: a single strong pass (A) vs a real
> mini-crew (B = 3 independent lens-workers → verify-split → assembled bearing),
> judged on genuine orientation value, then skeptic-refuted to kill crew
> self-justification. R36 ships the safe, thesis-aligned half of the finding.
> (R35 lost 3 of 5 decisions to transient network errors — n=2 completed; the
> signal is clear but thin, which bounds what R36 acts on.)

## R35 verdict: the crew's value is REAL but MISATTRIBUTED

- The crew **won the bearing 2/2** (value 88 vs 81, +7, ceremony=0) — but
  **`crew_earns_keep` survived the skeptic 0/2.**
- Every survivable thing B added is a property of generation **BREADTH + symmetric
  skepticism**, not the team→verify→boss machinery:
  - **off-frame gate**: PCI/SAQ scope is the real $400k gate on a "payments
    rewrite" (not the code); a "UK launch" hides an EDI/HIPAA build.
  - **symmetric scrutiny**: questioning the user's OWN preferred pole.
  - **quantitative reframe**: break-even ($92k rev vs $95k burn); NRR as THE number.
- All three are **portable to one pass via a checklist; none needs multiple agents.**
- The crew's ONLY structurally-unique "win" — verify catching a worker's wrong EV
  — is **self-justification**: an error the crew CREATED then caught, internal,
  the user never saw it, nets to zero.

Per the thesis ("one compressed screen, not multi-agent"): **buy the breadth with
a checklist, not a crew.** This echoes the repeated R12–24 finding — structure /
prompt-stacking loses to a tight single-pass FORM.

## What shipped in R36 (the safe, thesis-aligned half — both surfaces)

The crew's three portable wins, distilled into the single-pass OPEN-decision
analysis as a shared BREADTH checklist (`progressive-prompts.ts` OPEN branch +
`clarify/SKILL.md` Step 2 rule 1c, parity-guarded):

1. **Off-frame gate** — force-name the one compliance/security/finance/legal/people
   gate the obvious framing omits (usually the real load-bearing risk → hidden_assumptions).
2. **Symmetric scrutiny** — apply identical skepticism to the option the user is
   LEANING toward (the tilt symmetry applied to their own pole).
3. **One pivotal number** — name THE number + the threshold that flips the call.

**Scoped to high-stakes / irreversible / multi-domain OPEN decisions only** —
explicitly SKIPPED on low-stakes reversible choices, where the sweep is ceremony
(restraint default; combines the synth's Fix2 + Fix3 routing into one scoped rule).
Guard: `breadth-checklist.test.ts` (4) locks both surfaces + the scoping.

## What R36 deliberately did NOT do (the founder-level call)

**The crew was NOT removed.** "Don't ship the crew" (the synth's top fix) is a
hard-to-reverse, outward-facing decision on a major subsystem, and the evidence is
n=2 with the entire overkill/flat quadrant untested. Per the spine, that is a
founder-level call to be surfaced with evidence, not executed unilaterally on thin
data. It is recorded as a STRONG PROVISIONAL SIGNAL pending the R37 falsification.

## What's already good (the method, again)

The blind-A/B + skeptic-refute discipline (the R14 method) earned its keep on its
own subject: it correctly separated "B wins the bearing" (true, +7) from "crew
earns its keep" (false, 0/2), and correctly DISCOUNTED the verify step scoring
points for cleaning up a mess it created. 0 ceremony / 0 spine violations on the
two heavyweight cases — the synthesis layer is clean; the problem is structure-cost
and untested routing.

## Next — R37 (the falsification + the holes R35 left)

1. **Falsify the crew directly:** now that A' = single-pass-WITH-the-checklist
   exists, re-run the head-to-head A' vs crew B on more decisions (incl. the 3 that
   died on network). Hypothesis: A' closes most of the +7. If A' ≈ B, the crew is
   confirmed redundant → surface the removal recommendation to the founder.
2. **Probe the untested overkill quadrant:** run flat / reversible / low-stakes /
   already-closed decisions — does the heavy pass correctly STAY OUT (bare crux),
   or over-fire (mirror clause)? This is the unmeasured routing risk.
3. **Broaden:** n=2 was code/strategy with clean quantitative anchors; add
   people/ethics/personal + reversible-emotional decisions where breadth may NOT
   help and no pivotal number exists, across model tiers.
4. **Honest ceiling:** still self-scored simulation, same model family; the crew
   verdict is provisional until A'-vs-B and ideally one real run. Carry forward:
   never let "crew caught an internal error" count as value.

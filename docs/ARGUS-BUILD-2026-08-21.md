# 아르고스 시공 계획 — 무엇을 짓고, 무엇을 살리고, 무엇을 버리나 (2026-08-21)

> 기획서(`ARGUS-PLAN-2026-08-21-V5.md`)는 **무엇을 만들지**를 정했다. 이 문서는
> **어떻게 만들지**를 정한다. 코드는 한 줄도 안 짠다 — 기획서 머리의 `adopted`가
> 비어 있는 동안은 시공 0이 규칙이다.
>
> **이 문서를 쓴 이유**: 기획서가 기존 코드를 *"박물관(동결) — 재사용하는 것은
> 기계가 아니라 배운 것"*이라 한 줄로 처분했는데, **실측해 보니 그게 과했다.**
> 안 보고 처분하면 이미 있는 것을 다시 짓거나, 살아 있는 것을 지운다.

---

## §0. 시공 세션 규약 — 이 문서를 살아있게 쓰는 법

> **이 문서는 읽고 치우는 계획서가 아니라, 시공 기간 내내 대조하고 고쳐 쓰는
> 작업 문서다.** 계획에 오류가 남아 있어도 — 반드시 남아 있다 — 아래 규약이
> 그것을 구현 중에 잡는다. 규약 자체가 이 제품의 원리(계획도 결정이고, 결정은
> 현실과 대조해 닫는다)의 자기 적용이다.

**세션을 시작할 때 (시공 작업이면 반드시):**
1. 이 문서의 **§9 시공 일지 마지막 5줄**과 **§5 순서표의 현재 단계**를 읽는다.
2. 지금 하려는 단계의 **"됐다"의 정의**를 소리 내어 옮겨 적는다 — 정의를 안
   읽고 시작한 작업은 끝났는지 판정할 수 없다.
3. 그 단계가 닿는 파일이 **§4 처분표에서 "읽고 다시 판정"이면, 지금 읽는다.**
   판정 결과를 §9에 한 줄 적기 전에는 그 파일을 고치지도 지우지도 않는다.
4. **누락 점검 — 계획에 *있어야 할 것이 빠졌는지* 묻는다.** (2026-08-21 추가:
   배포 구성이 기획서에 아예 없던 것을 규약이 못 잡고 창업자가 잡았다. 규약에는
   *"계획에 없는 걸 만나면 멈춘다"*만 있었고 *"계획에 있을 것이 없다"*를 보는
   눈이 없었다.) 지금 단계에 대해 넷을 묻고, **하나라도 계획서에서 답이 안
   나오면 그것이 공백이다** — §9에 "공백:"으로 적고 3-(a)/(b)로 처분한다.
   - **어디에 실리나** — 이게 어느 배포 단위에 들어가나(플러그인 · 엔진 · 웹앱).
     계획서가 그 단위를 이름으로 부르나.
   - **누가 부르나** — 부르는 것이 실재하나(훅 · 도구 · 명령). *"AI가 그 문서를
     읽으면"*은 부르는 것이 아니다(§1-b 가 이걸로 걸렸다).
   - **사용자가 뭘 보나** — 화면에 나갈 문장이 계획서에 있나. 없으면 U6 규율로
     지금 쓰고 §9에 남긴다.
   - **틀리면 뭐가 보이나** — 조용히 넘어가는 자리가 있나. 있으면 그 자리부터.

**단계를 닫을 때 (하나라도 빠지면 안 닫힌 것):**
1. **"됐다"의 정의를 명령으로 실증한다** — 실행한 명령과 출력 요지를 §9에 적는다.
2. **일부러 한 번 깨뜨린다** (이 저장소의 '주입 확인' 관행): 방금 만든 검사가
   실제로 빨간불을 낼 수 있는지, 핵심 한 줄을 뒤집어 확인하고 되돌린다.
   빨간불을 못 내는 검사는 검사가 아니다.
3. 계획과 달랐던 것을 §9에 적는다 — **"계획대로였다"도 한 줄 적는다** (안 적힌
   단계는 대조를 안 한 것으로 간주).

**계획이 틀렸다는 걸 발견했을 때 (반드시 이 순서로):**
1. **멈춘다.** 코드를 계획에 맞추려 우기지도, 조용히 계획을 벗어나지도 않는다.
2. §9에 적는다: 무엇이 달랐나 · 네 자리 확인(생산·저장·소비·표면 — CLAUDE.md
   규율) 결과 · 제안.
3. 처분을 가른다 — **(a) 오타·경로·숫자 수준**: 본문을 고치고 §9에 근거를 남긴다
   **(b) 구조 수준**(저장 형식·불변식·관문 기준): 본문을 고치지 않고 §9에 적은 뒤
   **창업자에게 보고한다.** 구조는 서명된 방향이므로 시공자가 혼자 못 바꾼다.
4. 같은 종류의 오류가 **두 번** 나오면 §6 함정 목록에 항목으로 승격한다.

**속도 규율:** 위 규약이 무겁게 느껴지는 날이 반드시 온다. 그날 버리는 것은
규약이 아니라 **그 단계의 크기**다 — 단계를 쪼개서 규약이 가볍게 돌게 만든다.
규약을 건너뛴 단계는 §5 표에서 "됐다"로 표시할 수 없다.

## §1. 지금 코드에 실제로 뭐가 있나 (실측)

**먼저 배포 단위부터** (v5 §2-b 의 시공 판): 사용자는 플러그인 하나를 깔고,
플러그인이 엔진을 데려온다. 코드를 고칠 때 **어느 물건을 고치는지**가 PR 규율과
검사 범위를 가른다.

| 배포 단위 | 저장소 위치 | 라이선스 존 | 사용자가 받는 법 |
|---|---|---|---|
| 플러그인 `argus` v3.0.22 (훅 12·스킬 6묶음·에이전트) | `argus-plugin-v2/` | MIT | `/plugin install argus` |
| 엔진 `argus-decision-mcp` (바이너리 1) | `argus-mcp/` | MIT | 플러그인의 `mcp.json`이 `npx -y` 로 자동 |
| 웹앱 | `src/` | 앱 존 | 브라우저 |

**PR 규율**: 앱 존과 MIT 존을 한 PR에 섞으면 검사가 막는다. 새 시공은 대부분
MIT 존(엔진+플러그인)이라 한 PR로 되지만, 웹앱을 건드리는 순간 갈라야 한다.
**MIT 존은 로컬 `npm test`가 검증하지 않는다** — 자체 검사기를 갖고 CI 에서만
돈다(§6-3).

`argus-mcp/`(MIT 존)에만 테스트 포함 **40,287줄**이 있다. 그중 제품 코드:

| 자리 | 줄 | 무엇 | 살아있나 |
|---|---:|---|---|
| `src/lib/` | **8,296** | 지금 도는 커널 — 원장 쓰기·재생·상태기계·검증·영수증·날짜 해석 | **예** (도구 6종이 전부 여기를 부른다) |
| `src/tools/` | **5,579** | 공개 도구 — `seal`(645) `settle`(693) `check-in`(465) `premises`(808) `plan`(257) `sync`(400) 외 | **예** |
| `src/v2/` | **4,572** | 2세대 부품 — 원장·이벤트·리듀서·게이트·수확·큐·정화·git탐색·우편함·브리프 | **일부** (도구가 `mirror`·`sanitize`·`git-discovery`·`capture-runtime`만 부른다) |
| `src/v3/` | ~1,600 | 웹앱이 부르는 리듀서 (`src/lib/decision-kernel.ts:14` → `dist/v3/reducer.js`) | **예** |

**여기서 이미 돌고 있는 것 — 새 계획이 필요로 하는 것과 겹친다:**

| 새 계획이 필요로 하는 것 | 이미 있나 | 어디 |
|---|---|---|
| 추가만 되는 기록 + 잠금 + 되읽기 | **있다** | `lib/ledger-append.ts`(302) · `ledger-replay.ts`(685) · `v2/ledger.ts`(324) |
| 상태 전이 검사 | **있다** | `lib/state-machine.ts`(185) |
| 사람의 확인을 기록으로 남기기 | **있다** | `tools/seal.ts`(645) + `lib/validate-seal.ts`(276) |
| 나중에 다시 물어보기 · 닫기 | **있다** | `tools/check-in.ts`(465) · `tools/settle.ts`(693) |
| **AI 대화 기록에서 후보 뽑기** | **부품만 있다 — 자동으로 안 돈다** | `v2/harvest.ts`가 대화 파일을 읽고 **인용문이 원본에 진짜 있는지 바이트로 대조**하는 것까지 한다. **그런데 그걸 시작시키는 것이 훅이 아니라 스킬 문서다** — §1-b 참조 |
| **말할지 말지 판정 (침묵이 기본)** | **있다** | `v2/gate.ts` — *"판정이 형태보다 먼저"* 규율이 코드에 이미 박혀 있다 |
| 남의 글이 화면·AI에 닿을 때 무해화 | **있다** | `v2/sanitize.ts`(48) |
| 저장소 찾기 (하위 프로세스 없이) | **있다** | `v2/git-discovery.ts`(76) |
| 다시 물어볼 것을 쌓아두는 곳 | **있다** | `v2/outbox.ts`(88) + `v2/queue.ts`(244) |
| 영수증 만들기 | **있다** | `lib/receipt.ts` · `lib/render-receipt.ts`(341) |

**즉 기획서가 "박물관"이라 부른 자리에 이미 있는 것**: 기록·잠금·되읽기·상태
검사·확인 기록·다시 묻기·닫기·대화에서 후보 뽑기·침묵 판정·무해화·저장소 찾기.

### §1-b. 배선을 끝까지 따라가 보니 — 한 곳이 끊겨 있다

**첫 보고는 틀렸다.** 파일 이름·줄 수·맨 위 주석만 보고 "있다"고 적었다. 주석은
*무엇을 하려 했나*이지 *무엇을 하나*가 아니다. 실제로 돌리고 따라간 결과:

**돌려서 확인한 것**: `npm run build` 통과 · `npm test` **143파일 1,297개 전부 통과**.
부품은 실재하고 테스트도 산다.

**따라가 보니 끊긴 곳**:

```
소비 쪽   check_in 도구 → drainCaptureOnCheckIn → capture-runtime.ts:65
          → runHarvestSweep                                        ✅ 이어져 있다
생산 쪽   capture-scan 명령 (argus-mcp/src/index.ts:17)             ✅ 있다
          ← 부르는 것: argus-plugin-v2/scripts/decision-ledger.js:750 ✅ 있다
            ← 부르는 것: **훅이 아니라 스킬 문서**
               (skills/review/pipeline.md · clarify.md)            ⚠ 여기가 문제
```

플러그인 훅은 12개 걸려 있는데(SessionStart 3 · UserPromptSubmit 3 · PreToolUse 2 ·
PostToolUse 2 · Stop 2) **그중 `decision-ledger.js`를 부르는 것이 하나도 없다.**
부르는 것은 스킬 마크다운이고, 그건 **AI가 그 문서를 읽고 셸 명령을 칠 때만 돈다.**

이 저장소는 이 패턴을 이미 알고 이름까지 붙여 뒀다 — `ARGUS-CORE.md` §6.2의
"참조 0의 아홉 이유" 중 다섯째: *"스킬 마크다운이 셸로 호출."* 죽은 코드가
아니라는 뜻이지만, **자동으로 돈다는 뜻도 아니다.**

**그래서 판단이 갈린다: 부품은 재사용하고, 배선은 재사용하지 않는다.**
지금 배선은 CLAUDE.md가 금지한 모양이다 — *"라우팅·순서는 결정론 구조가 갖고,
LLM은 셀 안의 창의 작업만."* AI가 그날 그 문서를 안 읽으면 수집이 안 된다.
→ **N6(아래)이 새로 생긴다.**

**세 번째 존을 빠뜨렸다**: 첫 보고는 `argus-mcp`와 `src/` 둘만 셌는데,
`argus-plugin-v2`가 **JS 6,674줄에 훅 12개**로 따로 있다. 훅 표면은 전부 여기다.

> **정정 (2026-08-21, 단계 0-b 착수 시).** 위 추적도 반쪽이었다. **경로가 둘이다.**
>
> ```
> 경로 A (자동)  SessionStart 훅 → hooks/session-start.js:62 이 큐에 harvest 항목을 넣는다  ✅ 자동
>                └ 그 큐를 **비우는** 것: capture-runtime.ts → runHarvestSweep
>                   ← 부르는 것: tools/check-in.ts:93 **하나뿐**              ⚠ 여기가 진짜 문제
> 경로 B (수동)  스킬 문서 → decision-ledger.js:747 → capture-scan 명령          (§1-b 가 본 것)
> ```
>
> 즉 **큐에 넣는 것은 이미 훅이 자동으로 했다.** 안 되던 것은 **비우는 것** —
> 대화를 실제로 읽어 후보로 바꾸는 일이 `check_in` 도구 안에서만 돌았고, 그건
> AI 가 그 도구를 부르기로 마음먹어야 도는 일이다. 결론(N6 이 필요하다)은
> 그대로지만 **N6 이 할 일이 달라졌다**: 넣는 훅을 새로 만드는 게 아니라
> **비우는 방아쇠를 훅으로 옮긴다.**

> **이 발견이 계획을 바꾼다.** 기획서 §4.2의 *"기존 커널 코드는 박물관(동결) —
> 재사용하는 것은 기계가 아니라 배운 것이다"*는 **틀렸다.** 배운 것만이 아니라
> **기계도 상당 부분 그대로 쓸 수 있다.** 다만 조건이 하나 있고, 그게 §3이다.

---

## §2. 정말로 없는 것 — 새로 지어야 하는 여섯

위 표에 없는 것만 추렸다. 이게 진짜 새 코드다. (N6은 §1-b의 발견으로 추가됐다.)

| # | 무엇 | 왜 없나 | 크기 추정 |
|---|---|---|---:|
| **N1** | **규칙 문장 → 감시 규칙 만들기** | 지금은 사람이 정한 키워드 목록(`gate-keywords.ts`)만 있다. *"웹 화면은 나중에"*를 받아서 "어떤 파일이 바뀌면 걸린다"로 바꾸는 자리가 없다 | ~250줄 |
| **N2** | **지난 기록에 대입해서 보여주기** | 대화 기록을 읽는 코드(`harvest`)는 있는데, **아직 정하지 않은 규칙**을 과거에 대입하는 경로가 없다. 지금은 "앞으로 들어오는 것"만 훑는다 | ~180줄 |
| **N3** | **CLAUDE.md·.cursorrules 읽어서 후보로** | git 찾기는 있지만 **규칙 파일을 읽어 조항으로 쪼개는 자리**가 없다 | ~200줄 |
| **N4** | **차단 훅 만들어 내보내기** | 훅 파일은 손으로 쓴 것 두 개뿐(`.claude/hookify.*`). 규칙에서 훅을 만들어 쓰는 자리가 없다 | ~150줄 |
| **N5** | **규칙 파일로 내보내기 + 손댄 것 알아채기** | 없다. 내보낸 파일에 지문을 남기고, 사람이 고치면 알아채는 것까지 | ~200줄 |

| **N6** | **진짜 훅 — 대화 기록 수집을 자동으로** | 지금은 스킬 문서가 시작시킨다(§1-b). 훅으로 내려야 사람이 아무것도 안 해도 돈다. `argus-plugin-v2/hooks/hooks.json`에 `Stop` 또는 `SessionEnd` 한 줄 + 실패해도 세션을 안 막는 처리 | ~80줄 |

**합계 ~1,060줄.** 기획서가 추정한 ~1,650줄과 차이가 나는 이유: **~590줄어치는
이미 있고, 대신 배선 하나(N6)가 새로 필요하다.**

> **N6이 시공 순서에서 앞으로 온다.** 이게 없으면 §5의 2·4번(규칙 파일 읽기·
> 지난 기록에 대입)이 **손으로 명령을 칠 때만 도는 기능**이 되고, 그건 첫날
> 60초 경험을 만들지 못한다.

---

## §3. 진짜 갈림길은 하나뿐이었다 — 결정을 어디에 저장하나 【확정: B+】

나머지 모든 판단이 여기에 딸려 온다. **2026-08-21 확정됐다**: 창업자가 B+
기울기를 밝히고 최종 숙고를 지시 → 숙고가 같은 답에 도달 → 계획 본문(v5 §4.2)에
반영. `adopted` 서명이 함께 비준한다. 아래는 그 숙고의 전문이다 — 왜 A(위키형)와
B(읽기전용형)가 **둘 다 우리 자신의 문장에 의해** 탈락하는지.

### 두 안

| | **A안 — 마크다운 파일이 진실** (기획서 현행) | **B안 — 기록 파일이 진실, 마크다운은 사본** |
|---|---|---|
| 결정 1건 | `decisions/D-0012.md` 한 파일 | `~/.argus/.../ledger.jsonl`의 사건들 |
| 사람이 읽는 것 | 그 파일 | `decisions/D-0012.md` (기계가 만들어 준 사본) |
| 편집하면 | **법이 바뀐다** | **아무 일도 안 일어난다** (다음에 덮어써짐) |
| git | 역사 그 자체 | 사본의 역사 |
| 기존 코드 | 대부분 못 씀 | **8,296 + 4,572줄을 그대로 씀** |

### 왜 이게 그냥 취향 문제가 아닌가

**A안은 우리가 이미 찾아낸 결함을 되살린다.**

기획서 §4.10에 이렇게 적혀 있다 — *"평문이 서명 대리 불가를 무력화한다.
에이전트가 그냥 파일을 쓰면 되는 세계에서는 무의미하다."* 이건 우리가 스스로
공격해서 찾은 구멍이고, 처방으로 서명 봉인(키·해시)을 붙이기로 했다.

**B안에서는 그 구멍이 애초에 안 생긴다.** 확인은 도구를 거쳐야만 기록에 남고,
마크다운은 기계가 만든 사본이라 **AI가 그 파일을 아무리 고쳐도 법이 안 바뀐다.**
봉인 장치가 필요 없어지는 게 아니라, **없어도 반쯤 막힌다.**

그리고 이미 `v2/ledger.ts` 주석에 이 설계가 적혀 있다 — *"worktree의 .argus/에는
projection만 산다."* 우리가 예전에 같은 답에 도달했던 것이다.

### 무엇을 잃나 (정직하게)

- **"에디터가 곧 편집 도구"라는 그림을 잃는다.** 파일을 고쳐서 법을 바꾸는 건
  못 한다. 대신 `dec amend` 같은 명령을 써야 한다.
- **기획서 문장 하나를 고쳐야 한다** — §4.2의 *"git이 역사·동기화·병합을
  담당한다"* → *"git은 사람이 읽는 사본의 역사를 담당하고, 진실은 기록 파일이다."*
- **여러 대에서 쓸 때 동기화가 git 공짜가 아니다.** 다만 이것도 이미 있다
  (`v2/outbox.ts` + `tools/sync.ts` 400줄).

### 확정 (B+) — 그리고 왜 이것이 취향이 아니라 도출인가

**우리가 이미 서명하려는 문장 세 개가 세 안을 각각 판정한다:**

1. *"네 서명으로만 법이 되게"* (v5 §2 한 문장) → **A 탈락.** 에이전트가 파일을
   일상적으로 고치는 저장소에서 파일=법이면, **파일을 쓰는 모든 손이 서명자다** —
   사람 전용 서명이라는 유일한 차별점을 구조가 스스로 지운다.
2. *"모든 공백은 크게 실패하거나 정직하게 드러난다 — 조용히 메우지 않는다"*
   (CLAUDE.md 정직 구조 원칙) → **B 탈락.** 사용자가 파일을 고쳤는데 아무 일도
   안 일어나는 것은 조용히 끊긴 전선이다.
3. *"때가 되면 현실과 대조해 닫는"* (같은 문장) → **원장은 공통 비용.** 걸린
   횟수·다시 물은 날·답 같은 사건은 어느 안에서도 어딘가에 쌓여야 한다. A를
   골라도 사건 기록을 따로 지어야 하므로("진실이 두 군데" 병 — S2-3이 찾은
   두 법전 문제), 원장은 B+의 추가 비용이 아니다.

**유일한 생존자가 B+다**: 원장이 진실, 파일은 사람용 정본 표면, 손편집은
지문으로 감지해 "개정으로 받을까요?"로 회수.

**위키 유행에 대한 답** (창업자가 신경 쓴 지점): 그 유행이 실제로 검증한 것은
**"에이전트와 사람이 만나는 자리는 마크다운 파일"**이라는 표면 선택이고, 우리는
그 표면을 100% 그대로 쓴다(사용자가 만지는 것은 처음부터 끝까지 파일이다).
유행이 말하지 않는 것이 **"아무 손이 고친 것이 곧 진실"**이라는 권위 모형인데,
그 모형의 도달점이 지금의 CLAUDE.md다 — 아무나 고치고, 낡고, 세션의 절반이
무시한다. **이 제품은 위키의 병을 고치려고 존재한다. 위키의 권위 모형을 심장에
이식하면 병을 고치는 것이 아니라 병에 이름을 붙이는 것이다.** 그리고 개발자는
이미 "고치기"와 "역사로 만들기"가 다른 행위인 세계(git: 작업 트리 → 커밋)에
산다 — 편집=법이 오히려 그 세계의 예외다.

**B+가 위키의 미덕을 잃지 않기 위해 지켜야 할 약속 다섯** (시공 조건):
1. **파일 하나에 전부** — 문장·이유·그때의 원문·현재 상태. 원장을 열 일이 없다.
2. **편집은 벌 받지 않는다** — 고치면 다음 확인 때 시운전과 함께 "개정으로
   받을까요?" 한 타. 에디터로 일하는 사람에게는 그것이 곧 개정 흐름이다.
3. **원장도 평문(JSONL)** — 숨긴 데이터가 없다(불변식 ①은 진실 층에도 적용).
4. **`dec verify`가 일치를 증명** — 원장에서 파일 전부 재생성 → 바이트 비교.
5. **떠나는 날 파일이 곧 산출물** — 지문만 떼면 평범한 마크다운 폴더다.
   (한 줄 요약: **위키를 산출물로 갖는 통장.**)

**정직한 비용 셋**: ① "편집=제안"을 배우는 30초(git 커밋을 아는 사람에겐 이미
아는 모형) ② 생성기 버그가 잠깐 낡은 파일을 만들 수 있음 — verify·테스트가
전선 ③ 기계 간 동기화가 git pull만으로 안 됨(원장은 outbox 경유) — 관문 0~2는
한 기계라 당장 무관, 시공 세부로 미룬다. 원장의 위치(홈 vs repo 안)는 장부
두 모드와 함께 1주차에 확정한다.

---

## §4. 파일별 처분

| 자리 | 처분 | 이유 |
|---|---|---|
| `lib/ledger-append.ts` · `ledger-replay.ts` · `state-machine.ts` | **그대로 쓴다** | 기록·되읽기·전이 검사. 새 사건 종류만 더한다 |
| `lib/validate-seal.ts` · `receipt.ts` · `render-receipt.ts` | **그대로 쓴다** | 확인 검증과 영수증 |
| `lib/resolve-today.ts` · `resolve-contract.ts` | **그대로 쓴다** | 날짜 계산. `+1d` 규칙이 여기 박혀 있다 |
| `v2/harvest.ts` · `candidate-capture.ts` · `queue.ts` | **부품은 그대로, 배선은 새로** | 대화에서 후보 뽑기. **바이트 대조는 그대로 살린다** — 지어낸 인용을 막는 유일한 장치. 다만 **시작시키는 쪽을 스킬 문서에서 훅으로 옮긴다**(N6) |
| `argus-plugin-v2/` 전체 (훅 12·스킬 6묶음·에이전트) | **살린다 — 이것이 설치 단위다** | 사용자가 받는 물건이고 이미 v3.0.22 로 배포돼 있다. 새 기능은 여기에 훅·명령으로 붙는다 |
| `argus-plugin-v2/mcp.json` | **손대지 않는다** | 플러그인이 엔진을 자동으로 띄우는 한 줄. "설치 하나"가 성립하는 이유가 이 파일이다 |
| `argus-plugin-v2/hooks/hooks.json` (훅 12개) | **여기에 한 줄 더한다** | 훅 표면은 전부 여기다. 첫 보고에서 이 존을 통째로 빠뜨렸다 |
| `argus-plugin-v2/scripts/decision-ledger.js` · `push-webapp.js` | **그대로 두고 새 경로만 막는다** (판정 완료 2026-08-21) | 같은 `ledger.jsonl`을 쓰는 **두 번째 구현**이 맞다. 단 별개 패키지라 import이 불가능해 같은 프로토콜을 두 번 구현하고 락 파일로 상호 배제하는 것이 **의도된 설계**다(주석에 "same constants" 명시). 합치지 않는다 — **새 기능이 세 번째 구현을 만들지 않게** 막고, 프로토콜 동일성만 테스트로 못박는다 |
| `v2/gate.ts` · `gate-keywords.ts` | **고쳐 쓴다** | 침묵이 기본인 판정. 키워드 목록이 손으로 쓴 것에서 **규칙에서 만들어진 것**으로 바뀐다 (N1이 이 자리에 붙는다) |
| `v2/sanitize.ts` · `git-discovery.ts` · `outbox.ts` | **그대로 쓴다** | 손댈 이유가 없다 |
| `v2/ledger.ts` | **그대로 쓴다 — 손대지 않는다** (판정 완료 2026-08-21) | **writer가 아니라 미러 대상이다.** `appendLedger`가 자동 호출하며, 그 구조 자체가 "핸들러가 기억해서 호출하다 경로 하나가 샌" 사고의 근본 수리다. 합치려 들면 그 수리를 되돌린다 |
| `tools/seal.ts` · `settle.ts` · `check-in.ts` | **고쳐 쓴다** | 뼈대는 맞고 화면 문장과 필드가 바뀐다 |
| `tools/premises.ts`(808) · `lib/decisive-premises.ts` · `premise-claim.ts` | **잠근다** | "전제" 개념은 새 기획에 없다. 지우지 않고 두되 새 경로에서 안 부른다 |
| `tools/plan.ts` · `open-decision.ts` · `recall.ts` | **읽고 다시 판정** | 이름은 새 계획과 겹치는데 하는 일이 다를 수 있다. **안 보고 지우지 않는다** |
| `v3/` | **손대지 않는다** | 웹앱이 부른다. 지우면 웹 빌드가 죽는다 |
| `lib/apps-ui-html.ts`(402) · `localize-result.ts`(392) · `surfaces.ts`(675) | **읽고 다시 판정** | 화면·번역 층. 새 화면 문장 규율(DESIGN.md 마지막 절)과 안 맞을 가능성이 크다 |
| `.claude/hookify.*.md` | **본보기로 쓴다** | 손으로 쓴 훅 두 개가 N4가 만들어 낼 것의 목표 모양이다 |
| 크론 10개 · 알림 3파일 | **그대로 두고 연결 안 한다** | 기획서 §7 그대로. 새 기록과 잇는 순간 "대화는 우리 서버로 안 간다"가 거짓이 된다 |

**"읽고 다시 판정"이 넷이나 되는 게 정직한 상태다.** 안 읽고 처분하면 이
저장소가 아홉 번 겪은 그 실수를 열 번째로 하는 것이다 — *"참조 0"이 죽었다는
뜻이 아니라 아홉 가지 이유 중 하나라는 뜻*(`docs/ARGUS-CORE.md` §6.2).

---

## §5. 순서 — 무엇부터, 그리고 "돌아간다"의 뜻

각 단계마다 **눈으로 볼 수 있는 증거**를 정의한다. 증거가 없으면 다음으로 안 간다.

| 순서 | 짓는 것 | "됐다"의 정의 |
|---|---|---|
| **0** ✅ | **새 경로의 원장 쓰기를 MCP 바이너리 한 관문으로 모은다** (합치는 게 아니라 *새로 짓는 것이 두 번째 구현을 안 만들게* 한다) | ① 새 사건을 쓰는 코드가 `appendLedger` 하나만 부른다 ② 플러그인의 새 기능은 원장 파일에 직접 안 쓰고 바이너리를 부른다(`captureWithCanonicalRuntime` 선례) ③ **그걸 어기면 테스트가 빨간불** — 기존 두 구현은 그대로 두고 프로토콜 동일성을 대조 테스트로 못박는다 |
| **0-b** ✅ | **N6 훅** — 대화 수집이 자동으로 돈다 | 세션을 끝내고 아무것도 안 했는데 큐에 항목이 쌓인다. **이게 되기 전에는 나머지가 다 "손으로 쳐야 도는 기능"이다** |
| **0-c** ✅ | **파일 생성기 + 지문 + `verify`** (판정 10의 몸통) | 원장에서 `decisions/*.md`가 생성되고, 손으로 고치면 다음 명령이 알아채고, `verify`가 재생성·바이트 비교로 일치를 증명한다 |
| **1** 반 | 새 사건 종류 더하기 (확인·개정·폐지·이탈…) | 사건을 쓰고 되읽어 같은 상태가 나온다. **셋(서명·개정·폐지)은 0-c 에서 앞당겨 지었다** — 파일 생성기를 그것 없이는 시험할 수 없었다. 남은 것: 이탈·일시정지·정산 |
| **2** ✅ | **N3 규칙 파일 읽기** | 이 저장소의 CLAUDE.md에서 조항 40개 안팎이 후보로 나온다 |
| **3** ✅ | **N1 감시 규칙 만들기** | 조항 하나가 "어떤 파일·어떤 말"로 바뀌고, **못 잡는 것도 같이** 나온다 |
| **4** ✅ | **N2 지난 기록에 대입** | *"이 규칙이 있었으면 지난 30일에 N번 걸렸다"*와 그 장면이 나온다. **여기까지가 첫날 60초다** |
| **5** ✅ | 확인 한 타 + 근거 문장 저장 | 확인하면 파일이 생기고, **그때 내가 쓴 문장이 그 안에 있다** |
| **6** | 세션 시작 때 보여주기 | 새 세션을 열면 확인한 규칙이 보인다. **다른 프로젝트 규칙은 안 섞인다** |
| **7** | 어긋나면 알려주기 | 규칙에 걸리는 작업을 하면 한 줄이 뜨고, **[잘못 잡았어요]가 눌린다** |
| **8** | 다시 묻기 + 닫기 | 날짜가 되면 물어오고, **그때 내가 쓴 문장을 보여준 뒤** 한 글자로 닫힌다 |
| **9** | **N4 차단 훅** · **N5 규칙 파일 내보내기** | 훅이 실제로 막고, 내보낸 파일을 손으로 고치면 알아챈다 |

**4번까지가 1주차다.** 그 지점에서 이미 사람이 값을 본다 — 아직 아무것도 확인
안 했는데도 *"내 규칙 중 몇 개가 지난 달에 안 지켜졌다"*를 보여주니까.

**5~8번이 2주차다.** 여기까지 와야 한 바퀴가 돈다.

**9번은 자를 수 있다.** 기획서의 자르기 금지선 다섯에 안 들어간다.

---

## §6. 이 저장소에서만 걸리는 함정 (미리 적어둔다)

1. **CI의 `check` 잡이 로컬보다 넓다.** `npm test`는 덮개율을 안 재는데 CI는 잰다.
   테스트 없는 큰 파일을 더하면 **로컬 초록 · CI 빨강**이 난다. 새 파일마다
   테스트를 같이 쓴다.
2. **MIT 존과 앱 존을 한 PR에 섞으면 막힌다** (`zone-purity` 검사). 새 코드는
   거의 다 `argus-mcp/`(MIT)라 대부분 한 존이지만, 화면 쪽을 건드리는 순간
   PR을 갈라야 한다.
3. **MIT 존을 고치면 로컬 `npm test`가 아무것도 검증 안 한다** — 자체 검사기를
   갖고 CI에서만 돈다. 로컬에서 초록 봤다고 안심하면 안 된다.
4. **잠금 규율을 깨면 조용히 데이터가 깨진다.** `lock → 되읽기 → 전이 검사 →
   쓰기 → 잠금 해제`가 범위이고, 영수증·내보내기는 **잠금 밖**이다. 지금 코드가
   그렇게 돼 있고 이유가 주석에 적혀 있다.
5. **날짜 계산을 직접 하지 않는다.** `resolve-today.ts`가 "기한은 오늘보다 커야
   한다"를 강제한다. 이걸 우회하면 하루 짜리 왕복이 이틀이 된다 (이미 한 번 겪음).
6. **인용문은 바이트로 대조한다.** `harvest.ts`가 이미 그렇게 한다 — 인용이
   원본에 진짜 그대로 있는지 확인하고, 없으면 버린다. **이 검사를 끄면 제품이
   지어낸 인용을 사용자에게 보여주게 된다.**
7. **이름·주석으로 판단하지 않는다 — 함수를 읽는다** (2026-08-21 승격: 같은
   종류 2회째). 이 저장소는 파일 이름과 머리 주석이 **의도**를 말하고 실제
   배선은 다른 곳에 있는 일이 잦다. 두 번 다 같은 방식으로 틀렸다 —
   *"harvest가 대화를 읽는다"*(실은 훅이 아니라 스킬 문서가 시작시킨다) ·
   *"원장 writer가 셋이다"*(실은 하나는 미러, 둘은 패키지 경계 때문의 의도된
   이중 구현). **"있다/없다"를 적기 전에 호출자를 끝까지 따라간다.**
9. **`argus-mcp/src/lib/` 를 고쳤으면 `npm run verify` 를 돌린다** (2026-08-21
   실제로 CI 를 빨갛게 만든 것). `evals/verify-all.mjs` 는 **게이트가 아직
   무는지**를 시험한다 — 사본에 회귀를 일부러 심고 그 게이트가 빨간불을 내는지
   본다. 그런데 심는 방법이 **소스의 특정 한 줄을 문자열로 갈아치우는 것**이라,
   그 줄을 리팩터로 옮기거나 지우면 심을 자리가 없어지고 검사가 조용히 무효가
   된다(다행히 *"회귀를 심지 못했다"* 로 크게 실패한다). `npm test` 도 `tsc` 도
   이걸 안 잡는다 — CI 의 `npm run verify` 스텝만 잡는다.
   **지금 겨냥된 파일 열**: `lib/ambient-elicit.ts` · `lib/apps-ui-html.ts` ·
   `lib/elicit.ts` · `lib/ledger-replay.ts` · `lib/render-receipt.ts` ·
   `lib/untrusted.ts` · `server.ts` · `tools/premises.ts` · `tools/seal.ts` ·
   `tools/settle.ts`.

10. **새 필드를 더하면 따라와야 하는 것들이 있다.** 이 저장소는 그걸 규약으로
   갖고 있다(CLAUDE.md의 필드 추가 점검표·스키마 동기화). 새 사건 종류를
   더할 때 같은 규율을 따른다.

---

## §7. 안 만들 것 (명시)

기획서의 "안 만든다" 목록에 더해, **시공 단계에서 손이 가기 쉬운 것들**:

- **규칙 파일 뿌리기(fan-out) 도구** — 무료 도구가 여덟 개 넘는다. 내보내기와
  호환만 한다.
- **새 화면(웹·앱)** — 기획서 판정 8번이 열려 있다. 관문 2 전까지 안 짓는다.
- **크론·알림 재연결** — §4의 처분 그대로.
- **런타임에 모델 부르기** — 규칙을 만들 때(사람이 확인하는 순간)만 부른다.
  걸렸는지 판정하는 자리에 모델을 넣으면 되돌릴 수 없다.
- **임베딩·그래프 검색** — 기획서가 이미 금지했다. 시공에서 다시 유혹이 온다.
- **SDK·라이브러리** — 남이 우리 기록 위에 짓는 것은 관문 3의 포맷 공개로
  한다. 그 전에 짓는 것은 사용자 0명인 라이브러리다 (v5 §2-b).
- **"전제" 개념 되살리기** — 808줄이 있어서 쓰고 싶어지지만, 새 기획에 그
  개념이 없다. 잠근다.

---

## §8. 창업자가 정할 것 — 판정 10은 닫혔다

**판정 10: 확정 B+** (§3 전문). 창업자 기울기 + 최종 숙고 일치, 2026-08-21.
`adopted` 서명이 함께 비준한다. 남은 창업자 몫은 기획서 v5 §11의 나머지
판정들과 서명 자체다.

---

## §8-b. 미확정 등록부 — 일부러 안 정한 것들 (시공 중 여기서만 정한다)

계획이 "정하지 않았다"고 정직하게 적어 둔 것들이다. **시공 중 이 목록에 없는
미확정을 만나면 그건 계획의 오류다** — §0의 "계획이 틀렸을 때" 절차를 밟는다.

| # | 무엇 | 언제 정하나 | 정하는 방법 |
|---|---|---|---|
| ~~U1~~ **닫힘** | `scope` 값 집합 | ~~1주차~~ **2026-08-21 정함: 셋** | CLAUDE.md 를 조항 58개로 갈라 하나씩 분류해 보니 필요한 값이 셋이었고 넷째가 안 나왔다 — `repo`(대다수) · `path:<자리>`(MIT 존·화면 코드처럼 특정 자리) · `global`(claude 프로세스 안 죽이기처럼 저장소와 무관). **"어떤 일을 할 때"는 여기 안 넣는다** — 그건 어긋난 걸 아는 방법 쪽이다. 빈 값·틀린 값은 서명이 거절한다 (미지정 5%면 범위 보증이 무너진다는 실측 때문) |
| U2 | 원장의 위치 (홈 vs repo 안) | 1주차 첫날 | 장부 두 모드(§ v5 4.2)와 함께 — 기존 커널은 홈(`~/.argus`)이 기본이고, 바꿀 근거가 없으면 유지 |
| U3 | `pred`(예측)의 정의 | 관문 1 뒤 | v5 §11 판정 5(규제)와 함께 — 그 전까지 `pred` 서명은 받지 않는다 |
| ~~U4~~ **닫힘** | 훅 이벤트 선택 (Stop vs SessionEnd) | ~~0-b 단계~~ **2026-08-21 정함: Stop** | 이 저장소가 이미 실측해 적어 둔 것이 있었다 — `scripts/recall-signal.js:7` *"SessionEnd 에 태울 수 없다(불안정 — 터미널 닫기·크래시가 건너뛴다)"*. 발화 걱정은 해소됐다: 이 훅은 **아무것도 출력하지 않는다**. 턴마다 오는 비용은 엔진이 이미 갖고 있던 "하루 1회" 표식을 훅이 먼저 읽어 막는다 (일 없으면 프로세스도 안 띄운다) |
| U5 | 오탐율 상수 φ | 관문 1 (사전등록 예측 ④가 잰다) | 대조 창 3주 실측 — 그 전까지 φ 기반 계산은 전부 "범위"로만 말한다 |
| ~~U6~~ **닫힘** | 파일 생성기의 마크다운 모양 | ~~0-c 단계~~ **2026-08-21 정함** | YAML 머리(기계) + 사람이 읽는 본문. 본문 차례: 문장 → 지금 상태 → 걸리는 곳·지키는 사람 → 왜 이렇게 정했나 → 그때 한 말 → 어긋나면 어떻게 아나 → 다시 볼 때 → 자리를 비웠을 때 → 바뀐 것 → 꼬리말(고쳐도 안 바뀐다·바꾸는 법). 맨 끝 줄에 지문 주석. **금지 낱말은 테스트가 지킨다**(설계 낱말 12 + 기계 칸 이름 8) |

## §9. 시공 일지 — 추가만 한다 (지우지 않는다)

> 형식: `날짜 · 단계 · 한 일 · 실증(명령→요지) · 계획과 다른 점(없으면 "계획대로") · 처분`
> 이 절이 비어 있는 채 §5의 단계가 "됐다"가 되는 일은 없다.

- 2026-08-21 · 착공 전 · 문서 완성(v5 확정 + 이 규약) · 실증: v5·BUILD 전체
  재독으로 모순 3건 수리(판정 9·10 순서, B+ 이전 문구 잔재, §7↔BUILD 연결
  부재) · 계획과 다른 점: 해당 없음(착공 전) · 처분: `adopted` 서명 대기

- 2026-08-21 · **단계 0 착수 → 계획 오류 발견, 단계 재정의** ·
  한 일: 원장 쓰기 코드 셋을 구현 수준으로 읽음(주석이 아니라 함수) ·
  **실증**:
  `lib/ledger-append.ts:113 appendLedger` → `:138` O_APPEND 쓰기 →
  `:175 mirrorV1Events(...)` 자동 호출 / `v2/mirror.ts` 머리 주석 원문:
  *"v1 원장 쓰기의 유일한 관문인 appendLedger가 이 파일을 자동 호출한다 —
  툴이 기억할 것이 없으므로 배선 누락이 구조적으로 불가능하다"* /
  `argus-plugin-v2/scripts/decision-ledger.js:79` 같은 `ledger.jsonl` 경로,
  `:105` 주석 *"(lock → torn-tail heal → O_APPEND → fsync), same constants"*,
  `:228` 자체 O_APPEND / `push-webapp.js:237-241` *"Until 2026-08-09 this file
  wrote the SAME ledger.jsonl with a bare appendFileSync — no lock … The lock
  path protocol (`${file}.lock`) matches"* /
  `validate-plugin.js:370,375` 이 플러그인 내부의 단일 관문을 이미 기계로 강제 ·
  **계획과 다른 점 (큼)**: BUILD 초판은 *"기록 쓰는 코드가 셋(`lib`·`v2`·
  플러그인)이니 하나로 합친다"*라고 적었는데 **둘 다 틀렸다.**
  (1) `v2`는 writer가 아니라 **미러 대상**이다 — `appendLedger`가 자동으로
  쓴다. 이건 사고 뒤의 근본 수리였고(핸들러가 기억해서 호출하던 구조에서
  `defer` 경로가 조용히 샌 사건), 합치면 그 수리를 되돌리는 것이다.
  (2) 실제 독립 writer는 **같은 파일을 쓰는 두 패키지**다 —
  `argus-mcp`(TS, 바이너리)와 `argus-plugin-v2`(JS, 스크립트). 별개 패키지라
  import이 불가능해 **의도적으로 같은 프로토콜을 두 번 구현**하고 락 파일
  규약(`${file}.lock`)으로 상호 배제한다. "하나로 합친다"는 패키지 경계상
  성립하지 않는 목표였다 ·
  **처분**: §0 규약 3-(a) 사실 오인 → 본문 수리. §5 단계 0의 "됐다" 정의를
  달성 가능한 것으로 재정의(아래) · §4 처분표 두 줄 정정 · 창업자에게 보고 ·
  **§6 함정에 항목 승격**(같은 종류 2회째: "이름·주석으로 판단하지 않는다")

- 2026-08-21 · **계획 공백 발견 — 배포 구성이 기획서에 없었다** ·
  한 일: 창업자 지적("MCP·플러그인·SDK·웹앱의 최종 형태가 뭐냐")으로 전문 검색 ·
  **실증**: v5 전문에서 `플러그인` 0회 · `SDK` 0회 · `npm` 0회 (grep) —
  명령어는 적혀 있는데 사용자가 무엇을 설치하는지가 없었다. 실제 배선은
  이미 있었다: `argus-plugin-v2/mcp.json` 이 `npx -y argus-decision-mcp` 로
  엔진을 자동 기동, `plugin.json` v3.0.22, 훅 12·스킬 6묶음 ·
  **계획과 다른 점**: 누락. MCP·플러그인 둘 다 간다는 이전 결정이 계획서로
  넘어오지 않았다 — **이 제품이 고치겠다는 병의 자기 사례** ·
  **처분**: v5 §2-b 신설(설치 단위 하나·물건 셋·왜 둘로 나뉘나·웹앱의 자리·
  SDK 는 안 만든다는 명시적 판단) · BUILD §1 머리에 배포 단위 표와 PR 규율 ·
  처분표에 플러그인 두 줄 · §7 안 만들 것에 SDK

- 2026-08-21 · **§0 규약 보강 — 누락 점검** ·
  한 일: §0 "세션을 시작할 때"에 4항 추가(어디에 실리나 · 누가 부르나 ·
  사용자가 뭘 보나 · 틀리면 뭐가 보이나) ·
  **왜**: 배포 구성 누락을 규약이 못 잡고 창업자가 잡았다. 규약에는 *"계획에
  없는 걸 만나면 멈춘다"*만 있었고 *"계획에 있어야 할 게 없다"*를 보는 눈이
  없었다 · 계획과 다른 점: 규약 자체의 공백 · 처분: 본문 수리

- 2026-08-21 · **단계 0 닫힘 — 관문 단일성 가드** ·
  한 일: `argus-mcp/src/lib/__tests__/ledger-gateway.test.ts` 신설(6항) ·
  **실증**: `npx vitest run src/lib/__tests__/ledger-gateway.test.ts` → 6/6 ·
  `npm test` 전체 초록 · `npx tsc --noEmit` 초록 ·
  **일부러 깨뜨리기**: `src/tools/__break_probe.ts` 에 O_APPEND writer 를 심으니
  2항이 빨간불(목록 밖 파일 + tools 금지), 지우니 초록 ·
  **계획과 다른 점**: 단계 0 의 ③(프로토콜 동일성 대조)은 **이미 있었다** —
  `cross-surface-contract.test.ts` 의 "쓰기 규율 계약" 3항. 없던 것은 ①
  (여섯 번째 writer 가 태어나는 것을 막는 장치)이라 그쪽만 지었다 ·
  **가드가 잡은 것**: 내 grep 이 놓친 append 자리 둘 —
  `lib/privacy.ts`(.gitignore) · `evals/detection/auto-detect-eval.mjs`(추세 파일).
  둘 다 원장이 아니어서 `OTHER_APPENDERS` 로 분리해 적었다. **스캐너를 파일명이
  아니라 호출 모양으로 잡은 것이 옳았다는 증거** · 처분: 닫음

- 2026-08-21 · **단계 0-b 닫힘 — 대화 수집이 자동으로 돈다 (N6)** ·
  한 일: 엔진에 `capture-drain` 명령 신설(`v2/capture-cli.ts` + `index.ts` 배선,
  `check_in` 과 **같은** `drainCapture` 를 부른다 — 두뇌 하나) ·
  `drainCaptureOnCheckIn` → `drainCapture(dir, today, consumer, dataDir?)` 로
  정직하게 개명(소비자가 둘이 됐으므로) ·
  플러그인에 `hooks/capture-sweep.js` 신설 + `hooks.json` Stop 에 등재 ·
  `hooks/capture-sweep.test.mjs` 8항 + CI 스텝 배선 ·
  **실증**: 실제 훅 → 실제 엔진 바이너리 E2E — 도구 호출 0·사람 입력 0으로
  `pending` → `succeeded`, 원장에 `candidate_created` 1건
  (`quote_speaker: user`, `verification: byte_verified`) ·
  **일부러 깨뜨리기**: `hooks.json` 에서 등재만 빼니 "안 걸린 훅은 안 도는
  훅이다" 항이 빨간불, 되돌리니 초록 ·
  **계획과 다른 점 (중간)**: §1-b 의 진단이 반쪽이었다 — 큐에 **넣는** 것은
  이미 SessionStart 훅이 자동으로 하고 있었고, 안 되던 것은 **비우는** 것이었다
  (`drainCapture` 의 호출처가 `tools/check-in.ts:93` 하나뿐). §1-b 에 정정 블록 추가 ·
  **테스트가 잡은 내 결함 둘**: (1) 훅이 stdin **본문**을 파일 읽기 함수에
  넘겨 payload 를 통째로 잃고 있었다(그래서 저장소를 엉뚱하게 잡았다)
  (2) `spawn` 의 `error` 는 비동기라 `process.exit(0)` 전에 오지 않아 **기동
  실패가 아무데도 안 남았다** → 엔진 존재 확인을 동기로 앞당김 ·
  **정직성 수리 1건**: 원장에 남는 라벨이 훅 경로에서도 `check-in-<pid>` 였다
  → `stop-hook-<pid>` 로 갈라 출처를 속이지 않게 함 ·
  **U4 닫힘**: Stop (근거는 §8-b) · 처분: 닫음

- 2026-08-21 · **단계 0-c 닫힘 — 원장에서 결정 파일이 태어난다 (판정 10 의 몸통)** ·
  한 일: `argus-mcp/src/dec/` 신설 6파일 —
  `types.ts`(낱말은 전부 기획서 §12 에서 · 새로 만든 것 0) ·
  `fold.ts`(원장 → 지금의 결정들) · `render.ts`(결정론 마크다운 + 지문) ·
  `files.ts`(다시 그리기 · 손편집 감지 · verify) · `write.ts`(서명·개정·폐지,
  전부 `appendLedger` 하나만 부른다) · `dec-cli.ts`(`dec-sync`·`dec-verify`) ·
  `dec.test.ts` 16항 ·
  **실증**(실제 바이너리):
  `dec-verify` → `{"ok":true,…"verdict":"match"}` exit 0 →
  파일에 사람이 한 줄 덧붙임 → `{"ok":false,…"verdict":"hand_edited"}` **exit 1** →
  `dec-sync` → `{"written":[],"hand_edited":["D-0001"]}` 이고 **덧붙인 줄이 그대로 살아 있다** ·
  **일부러 깨뜨리기**: `verdictFor` 를 항상 `match` 로 만들고 렌더에 금지 낱말
  하나를 넣으니 16항 중 8항 빨간불, 되돌리니 초록 ·
  **계획과 다른 점 (순서)**: 단계 1 의 사건 셋(서명·개정·폐지)을 여기로
  앞당겼다 — 파일 생성기는 그릴 것이 없으면 시험할 수 없다. §5 표에 "반" 표기 ·
  **읽어 보고 고친 것**: 첫 판이 개정 이력에 `decision: … → …` 라고 기계 칸
  이름을 그대로 내보내고 있었다. 테스트는 초록이었고 **눈으로 봐서 잡았다** —
  그래서 금지 목록에 기계 칸 이름 8개를 추가해 다음부터는 기계가 잡는다 ·
  **곁다리 수리**: 원장 읽기 규율(BOM·줄마다 BOM·없음 vs 못 읽음)이 두 번째
  독자에게 복제될 뻔해서 `readLedgerRaw` 로 뽑아 공유 · 옛 replay 가 `dec_*` 를
  "모르는 사건"으로 세던 것 → 아는 남의 사건으로 등록 ·
  **남긴 공백 (이름 붙여 둔다)**: `dec-verify` 와 `hand_edited` 를 **부르는 사람
  표면이 아직 없다.** 물어보는 자리는 단계 5·7, CI 관문은 단계 9 다. 지금은
  기계만 서 있다 · 처분: 닫음

- 2026-08-21 · **단계 2 닫힘 — 이미 쓰고 있던 규칙을 조항으로 읽어 낸다 (N3)** ·
  한 일: `src/dec/rules/discover.ts`(아는 자리 8곳만 — 저장소를 훑지 않는다) ·
  `rules/split.ts`(조항 가르기 + 바이트 대조 + 안 올린 것 돌려주기) ·
  `src/dec/scope.ts`(U1) · `dec-scan-rules` 명령 · `rules.test.ts` 14항 ·
  **실증**: `dec-scan-rules --repo /home/user/Argus` →
  `조항 58 · 원문 대조 실패 0 · 표지 없어 안 올린 것 29`, 20개 절에 고르게 분포 ·
  **일부러 깨뜨리기**: 자르는 코드가 원문을 "다듬게" 만드니 바이트 대조 2항이
  빨간불(합성 픽스처·실제 CLAUDE.md 양쪽), 되돌리니 초록 ·
  **계획과 다른 점**: 계획은 "40개 안팎"이라 했고 실제로 58 이 나왔다. **수를
  맞추러 규칙을 깎지 않았다** — 남는 것은 머리말·서술문이고 그것을 가리는 일은
  단계 3(모델)의 몫이다. 첫 판 54 에서 58 로 는 것은 아래 누락 수리 때문 ·
  **눈으로 보고 잡은 누락**: 절별 분포를 찍어 보니 `Principle: Clean Removal`
  이 **통째로 0건**이었다. 그 절은 화살표로 순서를 적는 점검표라 서술형 표지에
  안 걸렸다. 표지에 `순서(→)` 를 더하고, 문장 끝 괄호를 서술형 판정에서 빼고,
  **표지가 없어 안 올린 덩어리 29건의 원문을 그대로 돌려주게** 했다 — 세기만
  하면 "우리가 못 본 규칙"이 조용한 공백이 된다 ·
  **U1 닫힘**: 걸리는 곳은 셋 (근거는 §8-b) · 처분: 닫음

- 2026-08-21 · **단계 3 닫힘 — 조항을 "어긋난 걸 아는 방법"으로 (N1)** ·
  한 일: `src/dec/watch/` 넷 — `glob.ts`(작은 글로브, 라이브러리 없이) ·
  `rule.ts`(두 채널 판정 + 모양 검사) · `draft.ts`(**모델 없이** 글자만 보고
  만드는 초안) · `compile-prompt.ts`(모델에게 주는 말 + 답을 받는 유일한 문) ·
  `watch.test.ts` 20항 ·
  **설계 판단**: 모델 없는 초안을 따로 둔다 — ① 키 없이도 제품이 덜 좋게라도
  돌고 ② 모델이 낸 것을 **대볼 것**이 생기고 ③ 초안은 조항에 적힌 글자만
  옮기므로 아무것도 지어내지 않는다. 모델의 답은 모양이 안 맞으면 통째로
  버리고 초안으로 돌아가되 **어느 쪽을 썼는지 밝힌다** ·
  **실증**: 이 저장소 62조 중 43조가 기계로 잡을 수 있는 규칙, 17조는 정직하게
  "읽어주기만"으로 내려앉음. 모양이 잘못된 초안 0 ·
  **일부러 깨뜨리기**: "못 잡는 것" 검사를 빼고 "읽어주기만" 규칙도 걸리게
  하니 3항 빨간불 ·
  **실주행이 잡은 결함 둘**: (1) 줄이 접힌 백틱(`` `taskkill /IM\n claude*` ``)
  에서 짝이 어긋나 `","` 같은 쓰레기가 뽑혔다 (2) `않는다` 로 끝나는 금지문이
  표지에 안 걸려 "만지지 않는다"류가 통째로 빠졌다 · 처분: 닫음

- 2026-08-21 · **단계 4 닫힘 — 시운전. 여기까지가 첫날 60초다 (N2)** ·
  한 일: `src/dec/rehearse/engine.ts`(순수 판정 + 사람이 읽는 문단) ·
  `collect.ts`(git 이력 + 대화 기록에서 과거 모으기) · `dec-rehearse` 명령 ·
  `rehearse.test.ts` 11항 ·
  **경계 규율을 코드로**: *"이 규칙이 있었다면 여기서 물었을 것이다"* 는 쓰고
  *"막았을 것이다"* 는 **테스트가 금지한다** — 개입은 결정론이라 사실 진술이지만
  그 다음에 사람이 뭘 했을지는 우리가 모른다 ·
  **실증**(이 저장소, 지난 30일): 고친 파일 8,859건 · 사람이 친 말 381줄을
  훑어 **62조 중 18조가 실제로 부딪혔다.** 뜨거운 것부터 장면과 함께 나온다 ·
  **일부러 깨뜨리기**: "막았을 것"으로 바꾸고 기계 글까지 사람 말로 세니 2항 빨간불 ·
  **눈으로 보고 잡은 결함 넷** (전부 테스트는 초록이었다):
  ① 긴 세션 하나 때문에 대화 기록을 **통째로** 못 읽고 있었다 → 큰 파일은
  꼬리만 읽는다 ② `user` 라는 낱말 하나가 말 채널에 들어가 30일에 57번
  "걸렸다" → 짧고 흔한 한 낱말은 규칙이 될 수 없다는 검사 추가, 뺀 말은
  "못 잡는 것"에 적는다 ③ 도구 결과·이어붙이기 요약·`<task-notification>` 이
  **"그때 한 말"로 화면에 나왔다** → 기록의 표식(`toolUseResult`·`isMeta`·
  `isCompactSummary`·`isSidechain`)으로 사람이 친 말만 고른다 ④ 커밋 제목을
  장면에 실으려고 만든 `context` 필드가 **생산만 되고 소비까지 안 갔다**
  (CLAUDE.md 가 경고한 dead-on-arrival 그대로) → 배선하고 테스트로 묶음 ·
  **덤**: 너무 자주 부딪히는 규칙에는 *"이만큼 부딪히면 규칙이 너무 넓다는
  뜻일 수 있다"* 를 붙인다. 이 말이 없으면 사람은 매일 스무 번 부딪히는 규칙을
  정해 놓고 며칠 만에 도구를 끈다 · 처분: 닫음

- 2026-08-21 · **CI 빨간불 수리 — 리팩터가 자기검증을 무효로 만들었다** ·
  한 일: CI 의 `check` 잡이 커밋 둘(02036be7 · 70dbe4e9)에서 빨간불.
  `get_job_logs` 로 자리를 짚으니 **딱 한 줄** — *"✗ 자기검증 ⑤ 못 읽는 원장
  회귀를 잡는가 · 회귀를 심지 못했다 — 자기검증 자체가 무효"* ·
  **원인**: 단계 0-c 에서 원장 읽기 규율을 `readLedgerRaw` 로 뽑아 공유했는데,
  `evals/verify-all.mjs` 의 자기검증 ⑤ 가 **옛 자리의 한 줄**
  (`const benign = code === 'ENOENT';`)을 문자열로 갈아치워 회귀를 심고 있었다.
  그 줄이 옮겨가면서 심을 자리가 없어졌다. 제품은 멀쩡했고 **검사가 자기를
  증명할 수 없게 된 것** — 그래서 게이트가 정직하게 크게 실패했다 ·
  **처분**: 심는 자리를 새 코드로 옮기고 이유를 주석으로 남김 ·
  **실증**: 새 자리에 회귀를 심으니 `node evals/unreadable-ledger.mjs` 가
  exit 1 · 4건 실패, 되돌리니 exit 0 초록 ·
  **전수 확인**: 자기검증이 겨냥한 파일 10개와 이 PR 이 고친 파일을 대조 —
  겹치는 것은 `lib/ledger-replay.ts` 하나뿐이었다 ·
  **§6 함정에 승격**(9번): `src/lib/` 를 고쳤으면 `npm run verify` 를 돌린다.
  `npm test` 도 `tsc` 도 이걸 안 잡는다

- 2026-08-21 · **단계 5 닫힘 — 조항 하나가 한 타로 법이 된다** ·
  한 일: `dec-sign --from-clause` 신설 · 서명이 **어긋난 걸 아는 방법을 갖고
  가게** 함(`watch_rule`·`origin` 을 결정에 실음) · 감지기만 고치는 개정 경로 ·
  `sign.test.ts` 10항 + `dec.test.ts` 에 7항 ·
  **먼저 찾은 구멍 (0-c 가 남긴 것)**: 서명이 규칙을 통째로 버리고 종류 이름만
  남기고 있었다 — 그러면 단계 7 이 걸렸는지 볼 것이 없고 파일이 "못 잡는 것"을
  말할 수도 없다. 생산만 하고 소비까지 안 가는 그 모양이다 ·
  **닫은 것 넷**: ① 기계가 잡는다고 하려면 방법이 같이 와야 하고 없거나
  망가졌으면 서명이 거절된다 ② 파일에 `### 못 잡는 것` 절이 생겼다
  ③ 법은 그대로 두고 감지기만 고칠 수 있다(오탐이 법을 죽이지 않게 — 시뮬
  3배역 전원의 사인) ④ 발원 원문(`source`)은 기록에 남지만 **파일에는 안 나간다**
  (결정 파일은 저장소에 커밋되고, 공개 저장소에 사람 발화 원문이 나가면
  되돌릴 수 없다) ·
  **강제하지 않기로 한 것**: `--because` 는 선택이다. 안 쓰면 그 자리는 비고
  기계가 대신 채우지 않으며, 응답이 `because_written: false` 로 그 사실을
  남긴다. CLAUDE.md — *"마찰 탈출구는 전부 남긴다… 강제하면 가장 지친
  사용자가 이탈해 소유권이 0이 된다"* ·
  **실증**(실제 바이너리): 조항 하나를 골라 서명 → `decisions/D-0001.md` 가
  태어나고 그 안에 사용자가 쓴 이유가 있다. `npm test` 1,385개 통과 ·
  **일부러 깨뜨리기**: 이유를 지어내게 하고 발원 원문을 파일에 내보내니 2항
  빨간불, 되돌리니 초록 ·
  **눈으로 보고 잡은 화면 결함 둘** (테스트는 초록이었다):
  ① 결정 파일 제목이 `# - **웹 화면은 나중에.** …` 로 마크다운 찌꺼기를 물고
  나왔다 → `clauseSentence()` 로 목록·강조 기호만 걷어낸다(원문 `quote` 는
  바이트로 대조되는 값이라 손대지 않는다) ② **규칙 파일에서 가져온 문장을
  "대화에서 그대로 옮겼다"고 말하고 있었다** — 출처를 지어내는 것이다 →
  `origin.kind` 를 보고 *"원래 이렇게 적혀 있었다 · CLAUDE.md 5번째 줄에서
  그대로 옮겼다"* 로 고침. 둘 다 테스트로 묶었다 ·
  **내 테스트가 틀렸던 것 둘**: `src/app/**` 의 `**` 를 굵게 표시로 오인한
  단언, 픽스처의 출처가 실제와 안 맞던 것. 제품이 아니라 계측기를 고쳤다 ·
  처분: 닫음

---

**상태**: 기획서 `adopted`가 비어 있는 동안 이 문서도 초안이다. 시공은 서명
뒤에만 시작하고, 서명 후에는 **§0 규약이 이 문서를 작업 문서로 승격시킨다** —
§5 순서가 작업 목록, §9 일지가 진행 기록이 된다.

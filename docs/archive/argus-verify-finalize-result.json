{
  "reverify": [
    {
      "section": "§0 THE KICK — The Web-Native Closed Loop (pre-flight + return-to-grade; OUTCOME_RECORDS / useAccuracyStore producer-vs-consumer)",
      "claims": [
        {
          "claim": "§0(2a): useAccuracyStore.addRating writes ACCURACY_RATINGS (persona-keyed) → context-builder.ts:216.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "useAccuracyStore.ts:34-44 addRating: `setStorage(STORAGE_KEYS.ACCURACY_RATINGS, ratings);` (:42) then `insertToSupabase('accuracy_ratings', rating);` (:43). Consumer context-builder.ts:216 `const ratings = getStorage<PersonaAccuracyRating[]>(STORAGE_KEYS.ACCURACY_RATINGS, []).filter(...)` inside buildPersonaAccuracyContext. Producer AND consumer both confirmed and wired (addRating called at FeedbackResult.tsx:66).",
          "correction": ""
        },
        {
          "claim": "§0(2a): OUTCOME_RECORDS is read at context-builder.ts:61-70 but has ZERO production writers (no setStorage(OUTCOME_RECORDS), no addOutcome/recordOutcome outside tests); the outcomes.length>=2 branch (:62) is permanently inert. Return-to-grade is net-new write-path. The loop is dormant/dead, not half-built. (E3 hard target)",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "CONSUMER: context-builder.ts:61 `const outcomes = getStorage<OutcomeRecord[]>(STORAGE_KEYS.OUTCOME_RECORDS, []).filter(o => o.project_id !== projectId);` with branch at :62 `if (outcomes.length >= 2)`. PRODUCER: fresh exhaustive greps — Grep `OUTCOME_RECORDS` over src returns only context-builder.ts:61 (read), storage.ts:13 (key def `OUTCOME_RECORDS: 'sot_outcome_records'`), and 3 __tests__ files. Grep `setStorage(STORAGE_KEYS.OUTCOME_RECORDS...)` = 0 hits. Grep `addOutcome|recordOutcome|setStorage(...OUTCOME` over whole repo = 0 src hits (only docs + PLAN.md). Grep raw key `sot_outcome_records` = only storage.ts:13 def + test fixtures at context-builder-simulation.test.ts:359/374/400/542. Grep `OutcomeRecord` symbol = type-only refs (context-builder.ts:4 import, decision-quality.ts:343/360/438/519 param types, tests). Producer side entirely absent → net-new, not reuse. Confirmed.",
          "correction": ""
        },
        {
          "claim": "Pre-flight predicates are serialized from data the engine already computes: RecastStep.actor (types.ts:121), classified_risks (types.ts:255/362), governing_idea (types.ts:158), produced live RehearseStep.tsx:60.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:121 `actor: ActorRelationship;` inside `interface RecastStep` (:119-134). types.ts:158 `governing_idea: string;` inside `interface RecastAnalysis` (:157). types.ts:255 `classified_risks: ClassifiedRisk[];` inside RehearsalResult (:250); types.ts:362 `classified_risks: ClassifiedRisk[];` inside RehearsalContext (:361). RehearseStep.tsx:60-62 builds `classified_risks: concerns.filter(c => ... c.severity === 'critical').map(c => ({ text: ..., category: 'critical' as const }))` live. All confirmed.",
          "correction": ""
        },
        {
          "claim": "§0 corollary (join-key): Today MaterializedRisk keys by risk_text + persona_id (types.ts:487-493) with no id; ClassifiedRisk surfaces none here.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:487-493 `interface MaterializedRisk { risk_text: string; (:488) persona_id: string; (:489) category: ...; actually_happened: boolean; impact_description?: string; }` — no `id` field present. ClassifiedRisk constructed at RehearseStep.tsx:62 as `{ text, category }` only — no id. Confirmed.",
          "correction": ""
        },
        {
          "claim": "§0 corollary (P1): addRating writes localStorage then calls insertToSupabase un-awaited; insertToSupabase (db.ts:275-290) swallows the error into log.error, returns void.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "useAccuracyStore.ts:42-43 `setStorage(...); insertToSupabase('accuracy_ratings', rating);` — no await, no .then/.catch (un-awaited fire-and-forget confirmed). db.ts:275 `export async function insertToSupabase(table, item): Promise<void>`; on supabase error :284-285 `log.error(...)`; catch :287-288 `handleError(err, ...)`; returns void. Confirmed — minor note: the catch path routes through handleError(), not solely log.error, but both swallow and return void as claimed.",
          "correction": ""
        },
        {
          "claim": "§2/§0 cross-ref: useAccuracyStore rates PERSONAS; addRating signature is per-persona PersonaAccuracyRating (relevant to 'return-to-grade is net-new, not a repoint of the persona channel').",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "useAccuracyStore.ts:10 `addRating: (data: Omit<PersonaAccuracyRating, 'id' | 'created_at'>) => void;` and :34-44 stores into ACCURACY_RATINGS, filtered everywhere by `r.persona_id` (context-builder.ts:217, useAccuracyStore.ts:47/50). Persona-scoped, not predicate-scoped. Confirmed.",
          "correction": ""
        },
        {
          "claim": "L4/§0: the existing accuracy/grading UI is on the LEGACY path — addRating is called only at tools/FeedbackResult.tsx:66, rendered only by RehearseStep.tsx (NOT ProgressiveFlow) → grading UI placement is net-new.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "Fresh Grep `addRating|useAccuracyStore` over src: addRating defined useAccuracyStore.ts:10/34; the ONLY call site is FeedbackResult.tsx:66 `addRating({`. Grep `FeedbackResult` (component): imported/rendered ONLY at RehearseStep.tsx:12 (import) and :673 (render). ProgressiveFlow uses a separate DMFeedback/DMFeedbackResult flow (different type), confirming no addRating in ProgressiveFlow. Direction fully confirmed.",
          "correction": "Direction correct. The plan's sub-citation 'rendered only by RehearseStep.tsx:12,673' is slightly off: :12 is the FeedbackResult import and :673 the render; the useAccuracyStore import/use in RehearseStep is at :21/:86, not :12. Not load-bearing — the producer/consumer conclusion holds."
        },
        {
          "claim": "Read-block range citation: 'OUTCOME_RECORDS is read at context-builder.ts:61-70'.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "The read is a single statement at context-builder.ts:61; the gated branch (`buildEnhancedSystemPrompt` section 5) spans :60-75 (`if (outcomes.length >= 2)` :62 → `sections.push(lines.join('\\n'));` :74). The cited ':61-70' truncates the block (unspoken-hit push runs :69-73).",
          "correction": "The full outcome-insight block is context-builder.ts:60-75 (read at :61, branch :62, success/unspoken logic :63-73, push :74). Cite :60-75, not :61-70. Non-load-bearing; the zero-writer conclusion is unaffected."
        },
        {
          "claim": "Implicit: the OUTCOME_RECORDS read at context-builder.ts:61 is the ONLY production consumer, so even the read path is effectively dead end-to-end.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "The other OutcomeRecord consumers — decision-quality.ts correlateDQWithOutcomes (:358), analyzeConfidenceCalibration (:436), validateEvalCriteria (:517) — take `outcomes: OutcomeRecord[]` as a PARAMETER. Fresh Grep for these three function names over src returns only their definition lines (zero callers anywhere, incl. tests). So context-builder.ts:61 is the sole wired reader, and it is fed by no writer → the entire OUTCOME_RECORDS subsystem is inert. Strengthens §0's 'dormant/dead' claim.",
          "correction": ""
        }
      ],
      "newly_found": [
        "HIDDEN (E3, strengthens the section): The augment pass framed OUTCOME_RECORDS as 'read but never written.' It is actually dead on BOTH ends: the three OutcomeRecord-analysis functions in decision-quality.ts (correlateDQWithOutcomes :358, analyzeConfidenceCalibration :436, validateEvalCriteria :517) have ZERO callers anywhere in src (fresh grep returns only their definitions). So not only is there no writer, the dedicated outcome-analysis layer the plan implicitly leans on for 'closing the loop' is also uncalled. The §0 budget should note that wiring the DQ↔outcome correlation (the stated 'KEY RESEARCH QUESTION' at decision-quality.ts:355-356) is ALSO net-new wiring, not just the write/capture UI.",
        "HIDDEN (E1, sub-citation drift the augment pass did not flag): §0 line 47 cites the consumer block as 'context-builder.ts:61-70'; the actual gated block runs :60-75. L4 line 182 cites the addRating render host as 'RehearseStep.tsx:12,673' but the useAccuracyStore consumption in RehearseStep is at :21/:86 (:12 is the FeedbackResult import). Both are direction-correct but line-imprecise — the same assert-at-a-distance class the protocol targets.",
        "HIDDEN (E7→fact, supportive): The §0 claim that the grade must carry its own stable predicate-id is reinforced by source — RehearseStep.tsx:62 constructs ClassifiedRisk as {text, category} with NO id, and MaterializedRisk (types.ts:487-493) keys by free text + persona_id with no id. There is genuinely no id-bearing predicate artifact today, so the join-key prerequisite is a verified gap, not merely intent."
      ]
    },
    {
      "section": "§1 North-Star + §2 Incremental-vs-Radical Verdict (verified-asset claims)",
      "claims": [
        {
          "claim": "llm.ts validateShape is wired at llm.ts:778-781",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "llm.ts:776-781 (read this turn): `const parsed = parseJSON<T>(fullText); / const validated = options.shape ? validateShape<T & Record<string, unknown>>(parsed, options.shape) as T : parsed; / resolve(validated);` inside callLLMStreamThenParse.onComplete. Grep for `validateShape` in llm.ts returned: 282 (definition `export function validateShape`), 541, 779 — line 779 is exactly the cited wiring.",
          "correction": "Confirmed, but note it is CONDITIONAL on `options.shape` (ternary at :778). A caller that omits `shape` gets the raw parsed value with NO validation. The L0 P1 plan to 'reuse validateShape from llm.ts:778-781' for storage-blob shape-guarding must pass a shape, or it is a no-op. Minor: the definition site is :282; :778-781 is the call site (both read)."
        },
        {
          "claim": "llm.ts engine has categorized errors, circuit breaker, multi-strategy parseJSON",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Circuit breaker: llm.ts:104-134 — `interface CircuitState {failures; lastFailure; open}`, `CIRCUIT_THRESHOLD = 5`, `CIRCUIT_RESET_MS = 30_000`, `checkCircuit`/`recordSuccess`. Categorized errors: `category: LLMErrorCategory` at :42/:48 with values auth/rate_limit/overloaded/context_too_long/network/parse_failure/validation/unknown across :70-99,:201-260. parseJSON defined `export function parseJSON<T = unknown>` at :221, ends with fallback `throw new LLMError('JSON 파싱에 실패했습니다.'...)` at :559 (multi-strategy span 221-559).",
          "correction": "Confirmed."
        },
        {
          "claim": "context-chain.ts:119-525 returns typed ReframeContext/RecastContext with provenance markers",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "`buildReframeContext(item: ReframeItem): ReframeContext` at context-chain.ts:119 (read); `buildRecastContext(item: RecastItem): RecastContext` at :157 (read, returns typed object :161-168); file ends at :525 (mergeAssumptionsIntoKeyAssumptions closes at :525, read). Imports `ReframeContext`/`RecastContext` from '@/stores/types' at :11-13. Grep `_source|provenance` in context-chain.ts: only :504 comment `Adds provenance marker` — NO `_source` field exists.",
          "correction": "Direction correct (typed contexts, range accurate). But 'with provenance markers' overstates: there is NO `_source`/provenance FIELD on the returned contexts. The only 'provenance' is the inline comment at :504 and the inherited-assumption merge in mergeAssumptionsIntoKeyAssumptions (:506-525). This is consistent with the plan's own L3a claim that `_source`/`traceProvenance` are net-new — but §1 should not bill 'provenance markers' as a verified asset of the returned contexts. Tag as [VERIFIED: typed contexts] + [INTENT: provenance markers]."
        },
        {
          "claim": "The on-disk .argus/ ledger is verified populated",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "`ls .argus/` this turn: critique-and-roadmap.md (22192B), journal.md (9114B), last-run.md (3171B), recast.md (5441B), refine.md (1967B), reframe.md (2228B), rehearse.md (3439B), testing-log-2026-03-28.md (21664B) — 8 non-empty files including the voyage outputs reframe/recast/rehearse/refine.",
          "correction": "Confirmed populated."
        },
        {
          "claim": "HiddenAssumption (types.ts:11-18) and KeyAssumption (types.ts:136-141) have no id, no created_at, no status (verified)",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:11-18 read: `interface HiddenAssumption { assumption; risk_if_false; verified?; evaluation?; evaluation_reason?; axis? }` — no id/created_at/status. types.ts:136-141 read: `interface KeyAssumption { assumption; importance; certainty; if_wrong }` — no id/created_at/status. Fresh grep `id|created_at|status` in types.ts shows the only `id` near these (line 58) belongs to a different interface; matches at :32-33 are field references (hidden_assumptions/hidden_questions), not these bodies.",
          "correction": "Confirmed. Both line ranges and the negative claim are exact."
        },
        {
          "claim": "buildInitialAnalysisPrompt(problemText, locale) at progressive-prompts.ts:23 takes no userContext param (verified)",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "progressive-prompts.ts:22-23 read: `export function buildInitialAnalysisPrompt(problemText: string, locale: Locale = 'en'): { system: string; user: string }`. Signature has exactly two params, no userContext.",
          "correction": "Confirmed."
        },
        {
          "claim": "useAccuracyStore rates PERSONAS; useAccuracyStore.ts:10 PersonaAccuracyRating",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "useAccuracyStore.ts read: :2 `import type { PersonaAccuracyRating } from '@/stores/types'`; :8 `ratings: PersonaAccuracyRating[]`; :10 `addRating: (data: Omit<PersonaAccuracyRating, 'id'|'created_at'>) => void`. The TYPE is defined at types.ts:399 (`grep 'interface PersonaAccuracyRating' src/stores/types.ts` → 399).",
          "correction": "Substance correct (the store rates personas, not predicates — so the L5 'self-scoring loop is net-new' inference holds). But the citation is E4-imprecise: `useAccuracyStore.ts:10` is the `addRating` Omit signature, NOT the definition of PersonaAccuracyRating. The interface is defined at types.ts:399. Cite `types.ts:399` (or `useAccuracyStore.ts:8`/`:10` as a USE site) — do not attribute the type's location to useAccuracyStore.ts:10."
        },
        {
          "claim": "Direction HYBRID; chassis-demolishing reshapes rejected as spine (distribution scissors); plugin manifest declares no hooks array",
          "tag": "INTENT",
          "verdict": "unverifiable",
          "evidence": "This is a design-direction/strategy claim, not a code fact. The only embedded code-fact ('plugin manifest declares no hooks array') was NOT re-read this turn — it falls outside §1/§2's verified-asset scope and no manifest file was opened.",
          "correction": "The HYBRID direction and 'reject reshape as spine' are [INTENT] (design choices) and are correctly framed as such. The embedded 'plugin manifest declares no hooks array' sub-claim is load-bearing for the distribution-scissors argument and is currently [ASSUMPTION] — it was not re-verified this turn; grep the plugin manifest (argus-plugin/) for a `hooks` key before treating it as fact."
        },
        {
          "claim": "context-chain.ts has no useContextChainStore, no _source, no traceProvenance, no persistence (the L3a verified-negative also referenced by §1's asset framing)",
          "tag": "ASSUMPTION",
          "verdict": "unverifiable",
          "evidence": "Grep `_source|provenance|traceProvenance` in context-chain.ts this turn returned only the :504 comment and no `_source`/`traceProvenance` symbol within that file — consistent with the negative. But useContextChainStore/persistence are asserted by the L3a section (out of the §1/§2 re-verify scope) and were not exhaustively grepped across src/ this turn.",
          "correction": "Within context-chain.ts the absence of `_source`/`traceProvenance` is consistent with what I read, but a full-`src/` exhaustive grep (the E5 requirement for the negative set) was not run in this §1/§2 pass. Keep as [ASSUMPTION: grep `useContextChainStore|traceProvenance|_source` across src/] until the L3a verification pass re-opens it."
        }
      ],
      "newly_found": [
        "E7/E1 (validateShape wiring): §1 presents 'validateShape wired at llm.ts:778-781' as an unconditional verified asset, but the wiring is CONDITIONAL on `options.shape` (ternary at :778). The L0-P1 reuse plan ('reuse validateShape from llm.ts:778-781' for stale-blob guarding) inherits a false assumption that calling it validates — without a passed `shape` it returns the input unchanged. The asset claim should be qualified: validateShape is only active when a shape schema is supplied.",
        "E7 (provenance markers): §1 lists 'with provenance markers' as a verified property of the returned ReframeContext/RecastContext. No such field exists on the returned objects (only a code comment at :504 and the assumption-merge behavior). This is intent/description stated as a verified asset — should be split into [VERIFIED: typed contexts] + [INTENT/dropped: provenance markers], matching the plan's own L3a admission that `_source` is net-new.",
        "E4 (PersonaAccuracyRating attribution): §2 cites `useAccuracyStore.ts:10 PersonaAccuracyRating` as if line 10 were the definition; the interface is actually defined at types.ts:399 and line 10 is the addRating Omit usage. Same name-association-attribution class as the errors this protocol exists to kill.",
        "Scope gap: §1 leans on the context-chain 'no persistence / no _source / no useContextChainStore' negatives as part of its asset framing, but those negatives are only fully grep-verifiable in the L3a pass; in this §1/§2 pass only the within-file absence was checked, not an exhaustive src/-wide grep — so they remain [ASSUMPTION] per the no-silent-inheritance rule."
      ]
    },
    {
      "section": "§3 L0 — Stop-the-bleeding crash & philosophy floor",
      "claims": [
        {
          "claim": "L0 P0 stream-hang watchdog: callLLMStream cannot abort options.signal because it is externally supplied (llm.ts:19-20, :679); use a NEW internal AbortController linked to options.signal OR reader.cancel(). [E6 mechanism]",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "llm.ts:14-21 `export interface LLMOptions { system: string; ... /** AbortController signal for cancellation */ signal?: AbortSignal; }`. llm.ts:679 `signal: options.signal,` inside the fetch() call (675-680). callLLMStream constructs no AbortController anywhere in 627-757; the signal is read-only consumer-supplied. Mechanism contract holds: the function owns no controller, so it cannot .abort() the external signal; an internal linked controller or reader.cancel() (reader obtained at :689) is the correct mechanism.",
          "correction": ""
        },
        {
          "claim": "callLLMStream is llm.ts:627-757 (Promise<void> via callbacks); reader loop :696-697 has no watchdog; fetch :675-680 has no AbortSignal.timeout.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "llm.ts:627 `export async function callLLMStream(` ... :631 `): Promise<void> {` ... ends :757 `}`. Reader loop: :696 `while (true) {` :697 `const { done, value } = await reader.read();` — no idle timer. fetch at 675-680 has only `signal: options.signal` and no `AbortSignal.timeout`.",
          "correction": ""
        },
        {
          "claim": "NEW integration contract: a bare signal.abort() maps to AbortError (llm.ts:748-751) and is suppressed by the handler guard !(e instanceof DOMException && e.name==='AbortError') at ProgressiveFlow.tsx:1397, :1717 → silent no-op, spinner never clears. Watchdog must reject with a DISTINCT non-Abort error.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "llm.ts:748-751 `} catch (error) { if (error instanceof DOMException && error.name === 'AbortError') { callbacks.onError(new LLMError('요청이 취소되었습니다.', { category: 'network', cause: error })); return; }`. ProgressiveFlow.tsx:1397 `} catch (e) { setStreamingText(null); if (!(e instanceof DOMException && e.name === 'AbortError')) setError(...)` and :1717 identical guard (grep confirmed 6 such guards incl. both cited).",
          "correction": ""
        },
        {
          "claim": "L0 P1 brief-export crash: project-brief.ts:130 `const steps = latest.steps.length > 0 ? latest.steps : latest.analysis.steps;` inside `if (latest.analysis)` at :114; unsafe expr is latest.steps.length (primary) + latest.analysis.steps (fallback); actorLabels[s.actor] at :131-133 renders undefined on unknown actor. [E1/E2]",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "project-brief.ts:114 `if (latest.analysis) {`; :130 `const steps = latest.steps.length > 0 ? latest.steps : latest.analysis.steps;`; :131 `steps.forEach((s, i) => {`; :133 `sections.push(\\`| ${i + 1} | ${actorLabels[s.actor]} | ${s.task} | ...`)`. If latest.steps is undefined, `.length` throws; actorLabels (defined :122-124 with keys ai/human/both) yields undefined for any other actor string.",
          "correction": ""
        },
        {
          "claim": "L0 P1 judgment crash: eval-engine.ts:183 (steps_accepted) guards !item.analysis then reads item.analysis.steps.length + item.steps.length unguarded; :184 (actor_overrides_low) does NOT return-true-safe on the non-null path — it reads item.analysis.steps.length with the same crash. Both :183 and :184 and item.steps need guarding. Entries in RECAST_EVALS 182-187. [E2]",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "eval-engine.ts:182 `export const RECAST_EVALS ... = [`; :183 `{ id: 'steps_accepted', ... measure: ({ item }) => { if (!item.analysis) return false; return Math.abs(item.analysis.steps.length - item.steps.length) <= 2; } },`; :184 `{ id: 'actor_overrides_low', ... measure: ({ item, actorOverrideCount }) => { if (!item.analysis) return true; const t = item.analysis.steps.length; ...` ; array closes :187. Both branches read .steps.length after only the analysis-null short-circuit; undefined .steps throws.",
          "correction": ""
        },
        {
          "claim": "Recast evals reached via recordRecastEval → RecastStep.tsx:412.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "eval-engine.ts:189 `export function recordRecastEval(item: RecastItem, actorOverrideCount: number)`. RecastStep.tsx:412 `if (current) { const oc = ...; lazyEvalEngine().then(m => m.recordRecastEval(current, oc)); }`. The line and call are correct.",
          "correction": "File path: RecastStep.tsx lives at src/components/workspace/RecastStep.tsx (the bare filename the plan cites resolves there). Not in a progressive/steps/ dir. Direction/line correct; note the path for precision since the plan elsewhere insists on path correctness."
        },
        {
          "claim": "L0 P1 silent auto-accept: worker-engine.ts:267-270 races onValidationFailed against new Promise<'accept'>(r=>setTimeout(()=>r('accept'),30_000)); :281-285 no-callback branch silently accepts a FAILED result. In production the NO-CALLBACK branch fires because ProgressiveFlow.workerCallbacks (:1176-1212) omits onValidationFailed; the 30s branch is unreachable. Flip literal at :269. [E2 production branch]",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "worker-engine.ts:266 `if (callbacks.onValidationFailed) {` :267-270 `const action = await Promise.race([ callbacks.onValidationFailed(worker.id, result.validation), new Promise<'accept'>(r => setTimeout(() => r('accept'), 30_000)), ]);`; :281-285 `} else { // 콜백 없으면 그냥 수용 finalResult = result; break; }`. ProgressiveFlow.tsx:1176-1212 workerCallbacks defines onStart/onStream/onComplete/onError only — no onValidationFailed. withTranscript (execution-transcript.ts:155) `onValidationFailed: callbacks.onValidationFailed ? ... : undefined` → undefined when source omits it. Fresh grep `onValidationFailed` across src returns only execution-transcript.ts + worker-engine.ts (never ProgressiveFlow.tsx), confirming the wired value is undefined → :266 false → no-callback branch 281-285 fires in production; 30s branch (267-270) unreachable.",
          "correction": ""
        },
        {
          "claim": "NEW P1: clearing the loser setTimeout (timer leak); treat onValidationFailed rejection as 'timed_out' not a fall-through to the generic catch :286-290 that drops the worker with no onComplete :293-295.",
          "tag": "INTENT",
          "verdict": "confirmed",
          "evidence": "worker-engine.ts:286-290 `} catch (err) { if (signal?.aborted) return; callbacks.onError(worker.id, ...); break; }`; :293-295 `if (finalResult) { callbacks.onComplete(worker.id, finalResult.text, finalResult.validation); }`. Lines verified. This item describes a desired FUTURE behavior once onValidationFailed is wired (currently undefined in production), correctly a design intent, not current fact.",
          "correction": ""
        },
        {
          "claim": "NEW P1: execution-transcript.ts:147-153 records validation.passed with no gating — a silently-accepted FAILED result is persisted to the .argus ledger as a clean completion.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "execution-transcript.ts:147-153 `onComplete: (id, result, validation) => { appendToTranscript(sessionId, 'worker_completed', { workerId: id, score: validation?.score, passed: validation?.passed, }); callbacks.onComplete(id, result, validation); }`. No branch distinguishes accepted-after-failure; the worker_completed event records passed verbatim.",
          "correction": ""
        },
        {
          "claim": "L0 P1 silent data loss WRITE: storage.ts:37-45 setStorage swallows the catch (incl QuotaExceededError) into console.error, returns void, no UI. Surface a toast via the existing CustomEvent pattern (llm.ts:721-724 → PingToast/UnlockToast/RateLimitBadge). [E1 + E6 mechanism]",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "storage.ts:37-45 `export function setStorage<T>(key: string, value: T): void { if (typeof window === 'undefined') return; try { localStorage.setItem(key, JSON.stringify(value)); } catch (e) { if (typeof console !== 'undefined') console.error('[storage] localStorage write failed:', e); } }` — generic catch (catches QuotaExceededError), returns void, no UI. Producer llm.ts:721-724 `window.dispatchEvent(new CustomEvent('argus:ratelimit', { detail: { remaining: parsed.rateLimit } }))` confirmed.",
          "correction": "The 'existing CustomEvent pattern' reuse names PingToast/UnlockToast/RateLimitBadge, but only RateLimitBadge is an actual CustomEvent consumer: RateLimitBadge.tsx:23 `window.addEventListener('argus:ratelimit', handler)`. Grep of PingToast.tsx + UnlockToast.tsx for addEventListener/CustomEvent/argus: returned NOTHING — neither is event-driven (they use props/store), so they are NOT instances of the CustomEvent pattern. Cite RateLimitBadge (and SyncStatus.tsx:39 'argus:sync') as the real producer+consumer CustomEvent precedent; drop PingToast/UnlockToast from the mechanism claim."
        },
        {
          "claim": "L0 P1 silent data loss READ: getStorage (storage.ts:27-35) returns JSON.parse(item) with ZERO shape/version validation; reuse validateShape from llm.ts:778-781. [E1 + E4 attribution]",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "storage.ts:27-35 `export function getStorage<T>(key: string, fallback: T): T { if (typeof window === 'undefined') return fallback; try { const item = localStorage.getItem(key); return item ? JSON.parse(item) : fallback; } catch { return fallback; } }` — no shape/version check (catch only guards parse-throw, not missing fields). validateShape is real and reusable.",
          "correction": "validateShape is DEFINED at llm.ts:282 (`export function validateShape<T extends Record<string, unknown>>(`), not llm.ts:778-781. Lines 778-781 are a CALL SITE inside callLLMStreamThenParse (`validateShape<...>(parsed, options.shape)`). Cite the definition at :282 for the reuse pointer (E4)."
        },
        {
          "claim": "P1 auto-persona empty stall: auto-persona.ts callLLMJson on failure silently returns []; fall back to recommendBlindSpotPersona.",
          "tag": "ASSUMPTION",
          "verdict": "unverifiable",
          "evidence": "Not opened this turn — outside the five files named in the re-verify scope (llm.ts, project-brief.ts, eval-engine.ts, worker-engine.ts, storage.ts). No source read for auto-persona.ts.",
          "correction": "Inherited; remains [ASSUMPTION] until auto-persona.ts is opened and the silent `return []` path + recommendBlindSpotPersona producer are quoted this turn."
        },
        {
          "claim": "P1 analytics swallowed-insert canary: analytics.ts:168-179 wraps async supabase.insert in try/catch that cannot catch the rejection; .then at :176 has no .catch → unhandled rejection.",
          "tag": "ASSUMPTION",
          "verdict": "unverifiable",
          "evidence": "Not opened this turn — outside the five files in scope. No source read for analytics.ts.",
          "correction": "Inherited; remains [ASSUMPTION] until analytics.ts:168-179 is opened and the try/catch + .then(:176) are quoted, confirming no .catch."
        },
        {
          "claim": "CLOSE, do not re-fix: decision-quality.ts:146 already guards recast.analysis.steps || [] AND outer recast?.analysis (:145); NEW caveat :83 reads reframe.analysis.hidden_questions || [] without guarding reframe.analysis null.",
          "tag": "ASSUMPTION",
          "verdict": "unverifiable",
          "evidence": "Not opened this turn — decision-quality.ts outside the five-file scope. No source read.",
          "correction": "Inherited; remains [ASSUMPTION] until decision-quality.ts:83, :145-146 are opened and quoted."
        },
        {
          "claim": "L0 P2 db.ts merge moved earlier (deleted_at-aware merge + clock-skew); see L5 for corrected mechanism.",
          "tag": "ASSUMPTION",
          "verdict": "unverifiable",
          "evidence": "db.ts not opened this turn (outside the five-file L0 re-verify scope; full mechanism is in §L5 which is a different section).",
          "correction": "Inherited; verify against db.ts:57-78 (mergeByTimestamp) and :106-119 (loadAndMerge re-upsert) when re-verifying §L5."
        }
      ],
      "newly_found": [
        "E6/E3 mechanism error the augment pass missed: the §3 P1-WRITE 'existing CustomEvent pattern (llm.ts:721-724 → PingToast/UnlockToast/RateLimitBadge)' is wrong about two of its three named consumers. Fresh grep across src/components for addEventListener/CustomEvent/argus: shows ONLY RateLimitBadge.tsx:23 listens for 'argus:ratelimit' and SyncStatus.tsx:39 for 'argus:sync'. PingToast.tsx and UnlockToast.tsx contain NO addEventListener/CustomEvent at all (grep returned nothing) — they are prop/store-driven, not event-driven. The reuse claim must name only RateLimitBadge/SyncStatus as the producer+consumer CustomEvent precedent.",
        "E4 attribution error the augment pass missed: validateShape is cited at llm.ts:778-781 in TWO places (L0 P1-READ line 107, and §1 line 60 'validateShape wired at llm.ts:778-781'). 778-781 is the call site inside callLLMStreamThenParse; the DEFINITION is llm.ts:282 (grep: only definition). Per E4 the reuse/attribution pointer should cite the definition site :282.",
        "Path-precision gap (same class as the plan's own OutputSelector path correction): RecastStep.tsx is cited bare as 'RecastStep.tsx:412' but resolves to src/components/workspace/RecastStep.tsx (NOT a progressive/steps/ path). Line 412 and the recordRecastEval call are correct; flagging because the plan elsewhere treats wrong directories as load-bearing corrections.",
        "Backoff self-flag resolved: the plan flagged 'llm.ts:190-215' backoff setTimeout as not-re-read. Read this turn: the retry backoff setTimeout calls are at llm.ts:195 (`await new Promise(r => setTimeout(r, delay))`) and :210 (`setTimeout(r, 1000 * Math.pow(2, attempt))`); :190 is `const delay = Math.min(jitter, 15_000)`. The 190-215 range loosely brackets the retry tail but the actual setTimeout lines are 195 and 210 — cite those if the backoff becomes load-bearing.",
        "worker-engine line-range nuance: CHANGELOG #4 cites the no-callback branch as ':282-284' while §3 body cites ':281-285'. Both resolve to the same `else` block: :281 `} else {`, :282 comment, :283 `finalResult = result;`, :284 `break;`, :285 `}`. Not an error, but the two citations differ by the brace lines — :281-285 is the precise full-block range."
      ]
    },
    {
      "section": "§3 L1 + L1b (artifact persistence, reflection-tab attribution, userContext injection)",
      "claims": [
        {
          "claim": "Reflection-tab attribution (changelog #5 + L1): getUserPatterns lives in useJudgmentStore.ts (NOT navigator.ts); getPersonaAccuracySummary lives in useAccuracyStore.ts.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep getUserPatterns -> useJudgmentStore.ts:14 (interface) and :57 (impl): `getUserPatterns: () => {` at both. Fresh grep getPersonaAccuracySummary -> useAccuracyStore.ts:12 (interface) `getPersonaAccuracySummary: (personaId: string) => {` and :49 (impl) `getPersonaAccuracySummary: (personaId) => {`. Neither symbol appears in navigator.ts (grep returned only the two store files + project/page.tsx consumer). E4 definition-site satisfied.",
          "correction": ""
        },
        {
          "claim": "L1 cited line numbers: getUserPatterns at useJudgmentStore.ts:14,57; getPersonaAccuracySummary at useAccuracyStore.ts:12,49.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "useJudgmentStore.ts line 14 `getUserPatterns: () => {` (interface), line 57 `getUserPatterns: () => {` (impl). useAccuracyStore.ts line 12 `getPersonaAccuracySummary: (personaId: string) => {` (interface), line 49 `getPersonaAccuracySummary: (personaId) => {` (impl). All four read this turn.",
          "correction": ""
        },
        {
          "claim": "The reflection tab should be built inside NavigatorStrip (where buildNavigatorInsights/LearningCurve already render), NOT inside Logbook; Logbook is the branch/waypoint rail, returns null when waypoints.length===0, shares no code.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "NavigatorStrip.tsx:6 imports `{ buildNavigatorProfile, buildNavigatorInsights, buildLearningCurve } from '@/lib/navigator'`; :198-200 `useMemo(() => buildNavigatorProfile())`, `buildNavigatorInsights(profile)`, `buildLearningCurve()`; :268-276 renders the DQ learning curve/sparkline. Logbook.tsx header doc (:3-17) 'The live ship's-log rail and the hub for branch control'; :25 imports useProgressiveStore, :27 getActivePath; Logbook.tsx:92 `if (waypoints.length === 0) return null;`. Confirmed Logbook hosts waypoints, not the navigator builders.",
          "correction": ""
        },
        {
          "claim": "The reflection tab is net-new in NavigatorStrip (plan: 'Compose ... + getUserPatterns + getPersonaAccuracySummary'); NavigatorStrip does not already wire those two store functions.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep of NavigatorStrip.tsx for getUserPatterns|getPersonaAccuracySummary|useJudgmentStore|useAccuracyStore -> 'No matches found'. The two store builders are not currently consumed in NavigatorStrip, so composing them in is net-new there (E3: consumer absent today).",
          "correction": ""
        },
        {
          "claim": "NEW caveat: gated near-empty for new users — buildNavigatorInsights needs tier>=2 / sessionCount>=2 (navigator.ts:113-117,191); DQSparkline needs >=2 dq_points (NavigatorStrip.tsx:268); getObservationsSummary returns hasData:false below 2 sessions.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "navigator.ts:113-117 `const tier: 1|2|3 = sessionCount >= 10 && projectCount >= 2 ? 3 : sessionCount >= 2 ? 2 : 1;` (so tier 2 requires sessionCount>=2). navigator.ts:191 `if (profile.tier >= 2) { const evalSummary = getEvalSummary(); ... }` gates the eval-summary insight; :170 `if (si.type === 'pattern' && profile.tier >= 2)` gates pattern insights. NavigatorStrip.tsx:268 `{learningCurve.has_data && learningCurve.dq_points.length >= 2 && (` gates the sparkline. user-context.ts:188 `if (obs.sessionCount < 2) return { items: [], hasData: false };` (getObservationsSummary defined :183).",
          "correction": ""
        },
        {
          "claim": "userContext injection: buildInitialAnalysisPrompt (progressive-prompts.ts:23) takes only (problemText, locale) — verified no userContext param.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "progressive-prompts.ts:23 `export function buildInitialAnalysisPrompt(problemText: string, locale: Locale = 'en'): {`. Only two params. Caller progressive-engine.ts:303 `const { system, user } = buildInitialAnalysisPrompt(problemText, locale);` passes only those two. No userContext/observation argument exists at the producer or the production caller.",
          "correction": ""
        },
        {
          "claim": "L1b: user-context.ts:56-59 marks observations UI-only deliberately ('프롬프트에는 사용하지 않음').",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "user-context.ts:56-59 JSDoc on getUserObservations (:60): 'UI 전용 — 설정 페이지에서 \"AI가 관찰한 패턴\" 표시용. 프롬프트에는 사용하지 않음 (시뮬레이션 리뷰어가 시스템 메트릭을 아는 건 부자연스러움).' Confirms deliberate UI-only / not-in-prompt designation.",
          "correction": ""
        },
        {
          "claim": "L1b: buildNavigatorProfile already consumes signals (navigator.ts:95-147).",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "navigator.ts:95-147 buildNavigatorProfile reads getEvalSummary(), data.judgments(), data.projects(); it calls getSignals() ONLY inside the `if (sessionCount === 0)` demo-seed branch (:122 `const demoSeeds = getSignals().filter(s => s.signal_type === 'demo_seed')`). It does not consume the full signal stream on the normal path. The broader 'signals are consumed by navigator coaching/context-builder' is true elsewhere, but buildNavigatorProfile's own signal use is limited to the cold-start demo-seed read.",
          "correction": "Tighten to: buildNavigatorProfile reads eval-summary + judgments + projects; its only getSignals() use is the sessionCount===0 demo-seed branch (navigator.ts:122). General signal consumption lives in context-builder.ts / coaching, not in this function's normal path."
        },
        {
          "claim": "Artifact persistence: outputs are copy/download-only strings — OutputSelector.tsx:121 setPreview(format.generator(project)) regenerated each select; consumers CopyButton :172, download Blob :128-137; preview read-only <pre> :178-180.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "OutputSelector.tsx:121 `setPreview(format.generator(project));` inside handleSelect; :172 `<CopyButton getText={() => preview} label={...} />`; :128-137 handleDownload builds `new Blob([preview]...)`, createObjectURL, a.click(), revokeObjectURL; :178-180 `<pre ...>{preview}</pre>` (read-only, no editable input). All read this turn.",
          "correction": ""
        },
        {
          "claim": "Artifact INPUTS persist (project-brief.ts:10-17, agent-spec.ts:8-13 read from stores that sync to Supabase), so a brief is deterministically regenerable after reload.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "project-brief.ts:10-17 reads getStorage REFRAME_LIST/RECAST_LIST/SYNTHESIZE_LIST/FEEDBACK_HISTORY filtered by project.id. agent-spec.ts:8-13 reads getStorage REFRAME_LIST/RECAST_LIST/FEEDBACK_HISTORY via projectFilter. These STORAGE_KEYS map to Supabase-synced stores. Generators are pure functions of persisted store data, so regenerable after reload. (Reuse claim E3: producer = stores, consumer = generators, both present.)",
          "correction": ""
        },
        {
          "claim": "NEW: align agent-spec.ts:7 projectFilter(project) vs project-brief.ts:11 inline d.project_id === project.id — generators are not behavior-identical on project scoping.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "agent-spec.ts:7 `const pf = projectFilter(project);` where output-helpers.ts:20-23 projectFilter returns `() => true` (pass-ALL) when project is null, else `item => item.project_id === project.id`. project-brief.ts:11 uses inline `d.project_id === project.id` but project-brief.ts:6 returns '' early `if (!project) return '';`. So on a non-null project the two are behavior-IDENTICAL; they diverge ONLY in the null-project case (agent-spec returns all items, project-brief returns ''). The caveat's direction is right but the divergence is null-project-only, not a general scoping mismatch.",
          "correction": "Restate: the two generators are scope-identical for a real project; they differ only when project is null (agent-spec's projectFilter passes ALL items; project-brief short-circuits to ''). Align the null-project handling, not the per-project predicate."
        },
        {
          "claim": "Ship ONE Supabase artifacts table + versioning (verified: none exists today).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep 'artifacts' across *.sql -> 'No matches found'. db.ts:15-20 TableName union enumerated in full: projects, personas, reframe_items, recast_items, feedback_records, judgment_records, accuracy_ratings, quality_signals, outcome_records, retrospective_answers, decision_quality_scores, agents, agent_chains, agent_activities, synthesize_items, progressive_sessions — no 'artifacts'. E5 enumeration satisfied.",
          "correction": ""
        },
        {
          "claim": "NEW: reconcile with the existing Coda meta_reflection reflection at OutputSelector.tsx:94-112,184-280 — risk of two competing reflection surfaces.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "OutputSelector.tsx:94-101 holds codaOpen/codaForm state from project.meta_reflection; :104-112 handleSaveCoda writes meta_reflection via updateProject; :184-188 the '항해 후 성찰' (post-voyage reflection) toggle band begins. The meta_reflection reflection surface is confirmed present in OutputSelector. The end bound ':280' was not individually verified line-by-line (file region 184+ confirmed to start the Coda block; exact terminal line 280 unread).",
          "correction": "Anchor the Coda block start precisely: state lives at OutputSelector.tsx:94-112, the reflection UI band opens at :184. The ':280' end-of-block bound is approximate (not line-verified); cite the start anchor, not a precise range."
        }
      ],
      "newly_found": [
        "E1/scope precision (NavigatorStrip render gating): The plan's near-empty caveat cites buildNavigatorInsights tier>=2 but the run-1 emptiness is actually multi-gated and partly stronger than stated — NavigatorStrip.tsx also renders a SINGLE-point DQ block at :294-295 `learningCurve.dq_points.length === 1`, so a run-1 user is not literally 'almost nothing': the profile summary (sessions/strategy at :247-261) and a single DQ score DO render. The 'don't bill as run-1 first visible win' direction is right, but the precise gate is: insights list empty + sparkline empty until session 2, while the profile summary still shows. Worth tightening so the acceptance criterion ('reflection tab renders read-only') isn't read as 'renders empty'.",
        "E3 producer/consumer (Coda vs new reflection tab): the augment pass flags 'two competing reflection surfaces' but does not note that the existing Coda surface is PROJECT-scoped (writes project.meta_reflection via useProjectStore) while the proposed reflection tab is SESSION/learning-scoped (navigator builders + getUserPatterns/getPersonaAccuracySummary, no project arg). They read different data domains, so they are not strictly duplicative — the real reconciliation risk is navigational (two entry points), not data-conflict. The plan should state which scope the reflection tab owns.",
        "E7 intent-vs-fact (artifact schema decision): the L1 line 'Decide now: immutable regenerated snapshots vs user-editable documents — the schema differs' and 'no capture of post-generation edits (preview is read-only <pre> :178-180)' are VERIFIED facts about today's UI, but the 'shareable artifact' goal is [INTENT] and is not tagged as such in the prose — it reads as a gap statement. Minor: tag the shareable/versioned artifact as [INTENT], since no artifacts table or share path exists (verified absent)."
      ]
    },
    {
      "section": "§3 L2 — Chassis decomposition behind a feature flag (MASTER-DIRECTION-v3.md lines 149-158)",
      "claims": [
        {
          "claim": "ProgressiveFlow.tsx = 2670 lines; useProgressiveStore.ts = 1786 lines (line 152).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "`wc -l` this turn: `2670 src/components/workspace/progressive/ProgressiveFlow.tsx` and `1786 src/stores/useProgressiveStore.ts`. (Note path: the component is at src/components/workspace/progressive/, NOT src/components/progressive/.)",
          "correction": ""
        },
        {
          "claim": "useProgressiveStore statically imports ONLY useAgentStore; usePersonaStore is a single lazy require() at :946; NO reframe/recast/project store import (line 152).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh exhaustive grep `from '@/stores/|require\\(` on useProgressiveStore.ts → only matches: `6: import { useAgentStore } from '@/stores/useAgentStore';`, `8/9/40` agent-types & types (type-only), and `946: const { usePersonaStore: pStore } = require('@/stores/usePersonaStore');`. No useReframeStore/useRecastStore/useProjectStore import anywhere in the file.",
          "correction": ""
        },
        {
          "claim": "IntersectionObserver is NOT used anywhere in the chassis (only in useScrollReveal.ts) (line 152).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep `IntersectionObserver` across src/ → single hit: `src/hooks/useScrollReveal.ts:15: const observer = new IntersectionObserver(`. Zero hits in useProgressiveStore.ts and zero in ProgressiveFlow.tsx.",
          "correction": ""
        },
        {
          "claim": "Real coupled concern 'scroll-into-view (:951 + 28 scroll refs)' — cited at store line :951 (line 152).",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "Store `:951` is NOT scroll code — it is the personas.find predicate: `951: (p.name.toLowerCase().includes(hint) || p.role.toLowerCase().includes(hint) || hint.includes(p.name.toLowerCase()))`. The scroll-into-view machinery lives in the COMPONENT ProgressiveFlow.tsx (`943: const scroll = useCallback(...)`, `948: const scrollToRef = useCallback(...)`, `951: ref.current.scrollIntoView(...)`). Fresh grep `scrollIntoView` on the store → 'No matches found'. So scroll-into-view is a COMPONENT concern, not a store concern, and `:951` resolves to the wrong symbol in the store. The '28 scroll refs' count is also unverifiable as stated: no grep yields 28 — actual measures this turn were 23 `scrollToRef(` calls and 47 raw `scroll`-token occurrences in the component; distinct refs passed to scrollToRef ≈ 9 (statusBarRef, analysisCardRef, answeredPillsRef, dmFeedbackRef, finalRef, mixPreviewRef, questionRef, teamDeployRef, workerSectionRef).",
          "correction": "Reassign scroll-into-view to ProgressiveFlow.tsx (the component): definition at `:943-953`, `scrollIntoView` at `:951`, ~23 `scrollToRef(` callsites. It is a COMPONENT coupling, not a useProgressiveStore coupling. Replace '28 scroll refs' with the freshly-grepped figure (23 scrollToRef callsites) or tag as [ASSUMPTION] until re-counted; drop the store-relative `:951` citation."
        },
        {
          "claim": "'46 useState/useRef/useEffect hooks (the UIState target)' (line 152).",
          "tag": "VERIFIED",
          "verdict": "wrong",
          "evidence": "Fresh greps on ProgressiveFlow.tsx this turn: actual hook DECLARATIONS = `useState[<(]` → 21, `= useRef` → 15, `useEffect(` → 7. Total = 43, not 46. The raw word-occurrence counts (useState 22 / useRef 19 / useEffect 8 = 49) are inflated by the import line, type annotations, comments, and the `useReframeStore` import (line 27) matching a naive 'useRef' substring. Neither 43 nor 49 equals 46. The store itself has ZERO of these hooks (it is a Zustand creator, not a component).",
          "correction": "Replace '46' with the verified declaration count: 43 (21 useState + 15 useRef + 7 useEffect), all in ProgressiveFlow.tsx; the store has none. Re-grep before re-citing."
        },
        {
          "claim": "Worker runtime coupling: runAllAIWorkers/runPipeline :32; store :213-216 (line 152).",
          "tag": "ASSUMPTION",
          "verdict": "unverifiable",
          "evidence": "Not re-opened this turn (out of the explicitly-requested re-verify scope: coupling SET, IntersectionObserver, named tests, the gate claim). The `:32` and `:213-216` line numbers were not read this pass.",
          "correction": "Tag [ASSUMPTION] until the runAllAIWorkers/runPipeline import line and the store :213-216 block are opened and quoted."
        },
        {
          "claim": "MAX_BRANCHES = 8 (:47) localStorage-size guard must be preserved (line 155).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Grep on useProgressiveStore.ts: `47: const MAX_BRANCHES = 8;` and consumer `1480: if ((session.branches || []).length >= MAX_BRANCHES) {`.",
          "correction": ""
        },
        {
          "claim": "Three load-path migrators migrateBranches/migrateWorkers/migrateSessionDrafts at :356/391, chained at :447/486 (line 155).",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "Grep on useProgressiveStore.ts: `330: function migrateWorkers`, `356: function migrateSessionDrafts`, `391: function migrateBranches`; chained at `447: const migrated = migrateSessionDrafts(migrateBranches(migrateWorkers(local)));` and `486: ...migrateSessionDrafts(migrateBranches(migrateWorkers(Array.from(localMap.values())))...`. The chain lines :447/:486 are correct. The definition lines are misstated: the plan gives ':356/391' but names THREE migrators — migrateWorkers is at :330 (omitted), migrateSessionDrafts at :356, migrateBranches at :391.",
          "correction": "Cite all three definition sites: migrateWorkers :330, migrateSessionDrafts :356, migrateBranches :391 (chained :447/:486)."
        },
        {
          "claim": "Of the 3 cited regression tests, only voyage-branch-simulation exercises the chassis (store-level); orchestrator-journey + workflow-review-integration test pure lib fns the decomposition does NOT move (lines 154, 158).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Imports read this turn: voyage-branch-simulation.test.ts `78: import { useProgressiveStore } from '@/stores/useProgressiveStore';` (store-level). orchestrator-journey-simulation.test.ts imports only pure libs (`classifyInput`, `selectAgents`, `assignFramework`, `planWorkers`, `validateByFramework`, `checkSpecificity`, `computeQualityXP`, `assessConvergence`) — no store, no component. workflow-review-integration.test.ts imports only `runWorkflowReview` from '@/lib/workflow-review' — no store, no component. Grep for `ProgressiveFlow|@testing-library|render(` across all three → NONE. Filename nuance: the actual file is `orchestrator-journey-simulation.test.ts` (plan abbreviates to 'orchestrator-journey').",
          "correction": "Minor: the test file is named orchestrator-journey-simulation.test.ts (not orchestrator-journey). Substance confirmed."
        },
        {
          "claim": "NEW prerequisite: 'Add a net-new component/render test before the flag flip' — implying no component/render test exists for the chassis (lines 154, 158).",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "Fresh grep for tests importing the ProgressiveFlow component → TWO exist: `src/components/workspace/progressive/__tests__/flow-interactions.test.tsx` (line 32 imports VerificationGate, TeamDeployBanner, DMFeedback, FinalCard, QuestionCard and renders them via react-dom/client createRoot) and `src/components/workspace/progressive/__tests__/team-deploy-render.test.tsx` (renders TeamDeployBanner/VerificationGate via renderToStaticMarkup). So component RENDER tests against the chassis file already exist. They render EXPORTED LEAF sub-components in isolation (several of which — e.g. FinalCard — are now extracted to their own files and merely re-exported from ProgressiveFlow.tsx:54), NOT the main default-export ProgressiveFlow nor its scroll/worker/UIState orchestration.",
          "correction": "Reframe the prerequisite: component render tests for chassis leaf sub-components already exist (flow-interactions.test.tsx, team-deploy-render.test.tsx). What is missing is a test that exercises the ORCHESTRATION being extracted (scroll-into-view sequencing, WorkerRuntime, UIState hooks) — i.e. an orchestration/integration render test of the main ProgressiveFlow export, not 'a component/render test' generically."
        },
        {
          "claim": "a11y: placeholder-only inputs with no accessible name at ProgressiveFlow.tsx :813, :2167, :2615 (line 156).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Read this turn: `:813 <input ... placeholder={L(...)} ... />` (no aria-label/htmlFor label). `:2167 <textarea value={session?.user_notes...} placeholder={L(...)} rows={3} maxLength={500} />` — preceding `<span>` at :2164 is not associated. `:2615 <textarea value={iterationDirective} placeholder={L(...)} ... />` — preceding `<p>` at :2612 is not an associated label. None carry aria-label or an id+<label htmlFor>.",
          "correction": ""
        },
        {
          "claim": "L2 is a 'Maintainability gate, not a value gate' — the Decision Contract leaves ProgressiveFlow untouched, so L4's graft does NOT depend on this (line 150).",
          "tag": "INTENT",
          "verdict": "confirmed",
          "evidence": "This is a design-intent claim, and the plan ALREADY self-tags it correctly: '(corrected: mark \"untouched\" as a build-time design intent to validate, not a verified ground truth.)' (line 150). Supporting fact for the tension: the §0 KICK's Verdict view is planned to mount on FinalCard, and `FinalCard.tsx:39: const logSession = useProgressiveStore(s => ...)` subscribes to the chassis store — so 'untouched' is not a current ground truth for the FinalCard host. Per E7, this is correctly [INTENT], not [VERIFIED].",
          "correction": "No change needed — the plan already tags it as build-time intent (E7-compliant). Keep it [INTENT]; it must not be cited as a verified fact."
        }
      ],
      "newly_found": [
        "E4/E1 misattribution the augment pass missed: the L2 coupling bullet (line 152) files 'scroll-into-view (:951 ...)' under useProgressiveStore's coupled concerns, but the entire scroll machinery (scroll/scrollToRef/scrollIntoView, :943-953) lives in the COMPONENT ProgressiveFlow.tsx, and store-`:951` is an unrelated personas.find line. The 'real coupled concerns' list conflates COMPONENT couplings (scroll-into-view, the 43 hooks) with STORE couplings (useAgentStore import, lazy usePersonaStore require, worker runtime, version/branch logic). The decomposition section never disambiguates which file each concern belongs to, weakening the 'extract WorkerRuntime/DraftManagement/VersionControl/UIState hooks' plan — UIState (hooks) is component-side, worker/version state is store-side.",
        "E5 count error compounding: '28 scroll refs' (line 152) matches no fresh grep — distinct scroll-target refs ≈ 9, scrollToRef callsites = 23, raw scroll tokens = 47. The augment pass inherited '28' without re-enumerating.",
        "E2/E3 gap: the regression-gate argument (line 154) asserts the 3 lib tests give 'false confidence' but did not enumerate the ACTUAL chassis test inventory — there are real chassis component tests (flow-interactions.test.tsx, team-deploy-render.test.tsx) plus workflow-review-simulation.test.ts in the same dir. The 'add a net-new component/render test' prerequisite is overstated because it omits the existing render coverage; the precise gap is orchestration-hook coverage, not 'any render test'.",
        "Path drift the augment pass didn't flag: the component is at src/components/workspace/progressive/ProgressiveFlow.tsx, but the plan's L0/L1/§UX bullets repeatedly cite it as bare 'ProgressiveFlow.tsx' with no path; a parallel L0 note (line 99) explicitly corrects a sibling file's path ('OutputSelector is at src/components/ui/, not progressive/') — the same path-precision discipline was not applied to ProgressiveFlow itself."
      ]
    },
    {
      "section": "§3 L3a (Phase-0 persistence slice) + L3b (per-predicate status migration)",
      "claims": [
        {
          "claim": "context-chain.ts:119-525 returns typed contexts with provenance markers; no useContextChainStore, no _source, no traceProvenance, no persistence (all grep-confirmed).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Read context-chain.ts in full: exported builders buildReframeContext (:119), buildRecastContext (:157), buildRehearsalContext (:175), injectReframeContext (:219), injectRecastContext (:361), buildRefineContext (:435), mergeAssumptionsIntoKeyAssumptions (:506) — file ends :525, all pure functions, no Zustand create() / no localStorage / no Supabase write. Grep over src for 'useContextChainStore|traceProvenance|_source' returned ZERO matches (only unrelated 'utm_source' in analytics.ts/utm-builder/daily-report). So all four absences are confirmed.",
          "correction": ""
        },
        {
          "claim": "The fragile substring linkers are agent-spec.ts:104 (substring(0,8)) and prompt-chain.ts:151-152 (substring(0,10)), and these are the ONLY two (grep 'both gone' is satisfiable).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh exhaustive grep '\\.substring\\(0,\\s*\\d+\\)' over src returned exactly 3 lines in 2 files: agent-spec.ts:104 `step.task.includes(a.assumption.substring(0, 8))`; prompt-chain.ts:151 `step.task.toLowerCase().includes(a.assumption.substring(0, 10).toLowerCase()) ||`; prompt-chain.ts:152 `step.expected_output?.toLowerCase().includes(a.assumption.substring(0, 10).toLowerCase())`. No other substring-based assumption linker exists. Exactly two files / two linkers.",
          "correction": ""
        },
        {
          "claim": "agent-spec.ts:107 DERIVES validates_assumption from the substring match; removing the match removes the only producer.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "agent-spec.ts:102-108: `if (reframeCtx?.unverified_assumptions.length) { const relevant = reframeCtx.unverified_assumptions.filter(a => step.task.includes(a.assumption.substring(0, 8))); if (relevant.length > 0) { lines.push(`      validates_assumption: \"${relevant[0].assumption.slice(0, 50)}\"`); } }`. Fresh grep 'validates_assumption' over src returns exactly ONE hit: agent-spec.ts:107. So the substring filter at :104 is the only producer.",
          "correction": ""
        },
        {
          "claim": "RecastStep (types.ts:119-134) has no assumption-id field; add validates_assumption_ids / source_assumption_id is net-new schema.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:119-134 RecastStep fields: task, actor, actor_reasoning, expected_output, judgment?, checkpoint, checkpoint_reason, estimated_time?, parallel_with?, user_ai_guide?, user_decision?, ai_direction_options?, ai_scope?, human_scope?. No validates_assumption_ids and no source_assumption_id. Fresh grep 'validates_assumption' over src returns only agent-spec.ts:107 (a derived YAML string, not a type field). Net-new confirmed.",
          "correction": ""
        },
        {
          "claim": "mergeAssumptionsIntoKeyAssumptions (context-chain.ts:506-525) dedupes by lowercased TEXT (:510-516).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "context-chain.ts:506 `export function mergeAssumptionsIntoKeyAssumptions(`; :510-512 `const existing = new Set(recastAssumptions.map(ka => ka.assumption.toLowerCase().trim()));`; :514-516 `const inherited = reframeAssumptions.filter(a => !a.verified).filter(a => !existing.has(a.assumption.toLowerCase().trim()))`; function closes at :525. Text-identity dedup confirmed.",
          "correction": ""
        },
        {
          "claim": "buildReframeContext (:134-136) coerces legacy string[] assumptions to objects at READ time.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "context-chain.ts:134-136: `const assumptions: HiddenAssumption[] = (analysis.hidden_assumptions || []).map((a: HiddenAssumption | string) => typeof a === 'string' ? { assumption: a, risk_if_false: '', verified: false } : a);`. Legacy string→object coercion at read time confirmed; the new id strategy must assign ids here.",
          "correction": ""
        },
        {
          "claim": "RehearseStep.tsx:194 does JSON.stringify(buildReframeContext(...)) straight into the prompt.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "Plan cites file as components/progressive/RehearseStep.tsx (in §UX it uses 'progressive/'); actual path is src/components/workspace/RehearseStep.tsx. Line content confirmed: :193 `contextText = JSON.stringify(rc);` :194 `if (relReframe) contextText += '\\n' + JSON.stringify(buildReframeContext(relReframe));`. Block :191-198 then passes contextText into buildPersonaReview(persona, data.documentText, contextText) at :198 → the persona LLM call (:205). So it reaches the prompt indirectly via contextText, not a literally separate serialization site. Line number :194 is correct.",
          "correction": "Direction correct, line :194 correct. Fix the path: it is src/components/workspace/RehearseStep.tsx (not components/progressive/). Note it concatenates onto contextText (already holding buildRecastContext JSON at :193) and reaches the prompt via buildPersonaReview at :198 — accurate to say 'serialized into the persona prompt'."
        },
        {
          "claim": "progressive_sessions loadSessions casts remote.data as ProgressiveSession with zero validation; the blob has no schema_version (20260409_progressive_sessions.sql, data JSONB DEFAULT '{}').",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "20260409_progressive_sessions.sql:4-13 CREATE TABLE progressive_sessions with `data JSONB NOT NULL DEFAULT '{}'::jsonb` (:8) and no schema_version column anywhere in the table DDL. (The useProgressiveStore.ts:467 cast portion was verified in the L5 pass; here the SQL no-version claim is confirmed.)",
          "correction": ""
        },
        {
          "claim": "L3b: handle JSONB atomicity — the 20260409 atomic-RPC update_worker_response, comment 'avoids JSONB read-modify-write race condition', :35-78, proves this seam burned the team.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "20260409_progressive_sessions.sql:35 `-- Atomic worker update function — avoids JSONB read-modify-write race condition`; :37 `CREATE OR REPLACE FUNCTION update_worker_response(`; function body uses nested jsonb_set on data->'workers'[v_idx] and closes at :78 `$$ LANGUAGE plpgsql;`. Comment text and :35-78 span both exact.",
          "correction": ""
        },
        {
          "claim": "L3b: 31 files emit/consume hidden_assumptions:/key_assumptions: (verified cross-leg/cross-engine).",
          "tag": "VERIFIED",
          "verdict": "wrong",
          "evidence": "Fresh exhaustive grep 'hidden_assumptions|key_assumptions' over src: 57 files total (with tests); 40 files with __tests__ excluded. Neither equals 31. The count 31 matches no current slice of the codebase.",
          "correction": "Replace '31 files' with a freshly-grepped figure: 57 files total, or 40 non-test files (run: grep -l 'hidden_assumptions|key_assumptions' src, exclude **/__tests__/**). Cite which slice (prod vs prod+test) and re-grep at edit time."
        },
        {
          "claim": "HiddenAssumption (types.ts:11-18) and KeyAssumption (types.ts:136-141) have no id, no created_at, no status (so the substring linkers can be deleted via a stable id).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:11-18 HiddenAssumption = { assumption, risk_if_false, verified?, evaluation?('likely_true'|'uncertain'|'doubtful'), evaluation_reason?, axis? } — no id/created_at/status. types.ts:136-141 KeyAssumption = { assumption, importance, certainty, if_wrong } — no id/created_at/status. Both confirmed.",
          "correction": ""
        },
        {
          "claim": "§0 corollary (pulled forward into L3a): MaterializedRisk keys by risk_text + persona_id (types.ts:487-493) with no id.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:487-493 MaterializedRisk = { risk_text, persona_id, category('critical'|'manageable'|'unspoken'), actually_happened, impact_description? }. No id field; keyed by risk_text + persona_id. Confirmed — supports L3a's stable-id need.",
          "correction": ""
        },
        {
          "claim": "L3a NEW: the substring linkers are wrong on FRESH data (false positives on shared 8-char prefixes, false negatives on paraphrase since tasks & assumptions come from separate prompts).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "agent-spec.ts:104 `step.task.includes(a.assumption.substring(0, 8))` — a raw 8-char prefix substring match against the step task text; prompt-chain.ts:151-152 same with 10-char + toLowerCase. RecastStep.task (types.ts:120) and HiddenAssumption.assumption (types.ts:12) are independently LLM-generated free text (recast vs reframe prompts), so the includes() match is a substring heuristic with no shared key — false-positive/false-negative behavior on fresh data is structurally correct.",
          "correction": ""
        }
      ],
      "newly_found": [
        "PATH ERROR (E4-adjacent) the augment pass missed: L3a line 166 cites 'RehearseStep.tsx:194' without a directory, and §UX repeatedly uses 'progressive/RehearseStep.tsx'-style paths; the actual file is src/components/workspace/RehearseStep.tsx (Glob confirms only one RehearseStep.tsx, under workspace/). The line :194 is correct but the implied progressive/ location is wrong.",
        "COUNT ERROR (E5) the augment pass missed: L3b's '31 files emit/consume hidden_assumptions:/key_assumptions:' is wrong — fresh grep gives 57 (with tests) / 40 (non-test). This is an inherited un-re-grepped count, exactly the E5 class.",
        "UNDERSPECIFIED PRODUCER CLAIM (E3) worth tightening: L3a's deletion plan ('links are deterministically correct on fresh data' once steps carry validates_assumption_ids) requires a NEW producer that populates the id at recast generation. Verified there is no such producer today: grep 'validates_assumption' yields only agent-spec.ts:107 (consumer/derived), and RecastStep (types.ts:119-134) has no id field. The plan acknowledges this as net-new — but the acceptance criterion 'substring linkers grep-verified gone' is only safe to assert AFTER both the RecastStep field AND a generation-time populator exist; otherwise deleting the substring filter at agent-spec.ts:104 silently removes the ONLY producer of validates_assumption with no replacement, regressing the YAML field to never-emitted."
      ]
    },
    {
      "section": "§3 L4 — Forward surfaces: C's incremental spine + the Decision Contract graft",
      "claims": [
        {
          "claim": "Decision Contract serializes 3-6 predicates from recast actor assignments (RecastStep.actor, types.ts:121) + classified_risks (types.ts:255/362, produced live RehearseStep.tsx:60) + governing_idea (types.ts:158).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:119-121 `export interface RecastStep {` / `  task: string;` / `  actor: ActorRelationship;` (RecastStep.actor exact at :121). types.ts:157-158 `export interface RecastAnalysis {` / `  governing_idea: string;`. types.ts:255 `  classified_risks: ClassifiedRisk[];` and :362 `  classified_risks: ClassifiedRisk[];` (grep both lines). RehearseStep.tsx:60-62 `classified_risks: concerns` `.filter(c => typeof c === 'object' && c.severity === 'critical')` `.map(c => ({ text: (c.text as string) || '', category: 'critical' as const })),` — produced live. All four source fields exist and the producer (RehearseStep) is wired.",
          "correction": ""
        },
        {
          "claim": "Return-to-grade is NET-NEW write path, not a repoint: OutcomeRecord is read-only/producer-less; addRating writes ACCURACY_RATINGS (persona-keyed), is called only at tools/FeedbackResult.tsx:66, rendered only by RehearseStep.tsx:12,673 (legacy path), NOT by ProgressiveFlow.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep `setStorage ... OUTCOME_RECORDS` → EMPTY (zero writers). OUTCOME_RECORDS only READ: context-builder.ts:61 `const outcomes = getStorage<OutcomeRecord[]>(STORAGE_KEYS.OUTCOME_RECORDS, []).filter(...)`, gated `if (outcomes.length >= 2)` at :62 (permanently inert). Grep `addOutcome|recordOutcome` → none in src. addRating: useAccuracyStore.ts:34-43 writes `setStorage(STORAGE_KEYS.ACCURACY_RATINGS, ratings)` (:42) + `insertToSupabase('accuracy_ratings', rating)` un-awaited (:43); persona-keyed via `r.persona_id` (:47). Grep `addRating` callers → only FeedbackResult.tsx:66 (+ store def). Grep `FeedbackResult` render → only RehearseStep.tsx:12 (import) and :673 (render); no ProgressiveFlow hit. Producer-side of OUTCOME_RECORDS is absent → net-new confirmed.",
          "correction": ""
        },
        {
          "claim": "Onboarding: planned first-run auto-demo is UNBUILT — argus.has_onboarded does not exist; projects.length===0 at :458 is marketing copy; InteractiveDemo imported workspace/page.tsx:31 but only reachable via manual demo-card click :444; gate goes in if(!currentProjectId) branch at :690.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep `has_onboarded` across src/ → ZERO hits (net-new key confirmed). page.tsx:456-464: comment `TERTIARY: Marketing copy — only for absolute first-time users` then `{projects.length === 0 && (` at :458 — marketing copy confirmed. page.tsx:31 `import { InteractiveDemo } from '@/components/workspace/InteractiveDemo';`; rendered only at :269 inside `if (demoScenario) {` (:263); demoScenario set only via demo-card `onClick={() => setDemoScenario(s)}` at :444 — manual-click-only confirmed. page.tsx:690 `if (!currentProjectId) {` then :692 `<HeroFlow`.",
          "correction": ""
        },
        {
          "claim": "BranchMap re-skin: reuse local edgePath() (:31) + existing activeBranchId node-coloring; getActivePath lives in version-tree.ts:35 / branch-map-layout.ts, NOT inside BranchMap — the overlay strokes a gold path over existing coordinates, it does not call getActivePath.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "BranchMap.tsx:31 `function edgePath(px: number, py: number, cx: number, cy: number): string {` (local def), used :82 `d={edgePath(p.x, p.y, n.x, n.y)}`. activeBranchId node-coloring: :78 `const isActiveBranch = n.branchId === activeBranchId;`, :85 `strokeWidth={isActiveBranch ? 1.6 : 1.1}`, :94/:111 same for nodes. Fresh grep `getActivePath` → defined version-tree.ts:35 `export function getActivePath<T extends VersionNode>(`; consumed branch-map-layout.ts:75; NO BranchMap.tsx hit (BranchMap does not import or call it). Attribution correct.",
          "correction": ""
        },
        {
          "claim": "Attributable outputs: voyageLogToMarkdown (export.ts:25-49, used by FinalCard.tsx:45) renders waypoints with no per-line anchor and uses no assumption id; the HiddenAssumption import at export.ts:1 is legacy string handling only; voyageLogToMarkdown returns '' with no waypoints (:35).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "export.ts:25 `export function voyageLogToMarkdown(session: ProgressiveSession | null | undefined, locale: 'ko' | 'en'): string {`, body :26-49; :26 `if (!session) return '';`; :35 `if (wps.length === 0) return '';` (empty-on-no-waypoints confirmed). Body uses no HiddenAssumption and no assumption id — only waypoint fields (w.headline/trigger/significance/alternatives, :41-46). HiddenAssumption import at export.ts:1; its only use is in reframeToMarkdown at :61-63 `// Handle both old (string[]) and new (HiddenAssumption[]) format` ... `.map((a: HiddenAssumption | string) =>` — legacy string-vs-object handling, not in voyageLogToMarkdown. FinalCard.tsx:45 `const voyageLog = voyageLogToMarkdown(logSession, locale as 'ko' | 'en');` (host); FinalCard.tsx:39 subscribes `useProgressiveStore(s => ...)`.",
          "correction": ""
        },
        {
          "claim": "§0 corollary join-key (load-bearing for L4 per-predicate id): MaterializedRisk keys by risk_text + persona_id (types.ts:487-493) with no id.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:487-493 `export interface MaterializedRisk {` / `  risk_text: string;` / `  persona_id: string;` / `  category: 'critical' | 'manageable' | 'unspoken';` / `  actually_happened: boolean;` / `  impact_description?: string;` `}` — no `id` field present. Confirms grading-by-text brittleness and the net-new stable-id requirement L4 depends on.",
          "correction": ""
        },
        {
          "claim": "OutcomeRecord is project-scoped (types.ts:502-513), not per-predicate — must define a per-predicate record shape.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "types.ts:502-513 `export interface OutcomeRecord {` / `  id: string;` / `  project_id: string;` / `  hypothesis_result: ...` / `  materialized_risks: MaterializedRisk[];` / `  approval_outcomes: ApprovalOutcome[];` / `  overall_success: ...` / ... `}` — keyed by project_id, array of risks, no per-predicate id field. Project-scoped confirmed.",
          "correction": ""
        }
      ],
      "newly_found": [
        "L4 'Onboarding' bullet says the auto-open should target InteractiveDemo.tsx but states it is 'imported workspace/page.tsx:31 but only reachable via manual demo-card click :444'. VERIFIED-adjacent precision gap the augment pass left implicit: the actual render guard is `if (demoScenario)` at page.tsx:263 (returns at :269), and the demo-card sets state via `setDemoScenario(s)` at :444 — the plan cites :444 (the click) and :269 (the render) loosely as one mechanism. The auto-demo gate must set `demoScenario` (the existing state), not just call InteractiveDemo, or it won't mount; the plan does not name the `demoScenario` state variable as the actual trigger to set. Minor but load-bearing for whoever implements the gate.",
        "L4 'Attributable outputs' acceptance ('the HiddenAssumption import at export.ts:1 is legacy string handling only') is correct but the plan does not note that export.ts:1 also imports RecastItem/SynthesizeItem used by sibling exporters (recastToMarkdown etc.) — not an error, but the '왜? anchor uses no assumption id today' claim is fully scoped to voyageLogToMarkdown only; the OTHER exporters in export.ts (reframeToMarkdown :51+) DO consume HiddenAssumption objects, so a blanket 'export.ts uses no assumption id' would be wrong. The plan correctly scopes it to voyageLogToMarkdown, so no correction needed — flagging the adjacent class-error risk for any future broadening of the claim.",
        "L4 return-to-grade: the plan's claim that addRating's Supabase write is un-awaited/swallowed (§0 corollary P1, referenced by L4 'the write must be observable') is corroborated here — useAccuracyStore.ts:43 `insertToSupabase('accuracy_ratings', rating)` has no await and no .then/.catch in the store; consistent with the plan, not a new error, but confirms the L4 'grade write is observable' acceptance is currently false on the analogous existing path."
      ]
    },
    {
      "section": "§3 L5 — Deferred / kill-test-gated (db.ts merge, progressive_sessions second merge, session-blob durability)",
      "claims": [
        {
          "claim": "mergeByTimestamp (db.ts:57-78) does string `remoteTime > localTime` (:71) with no clock-skew handling.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "db.ts:57 `export function mergeByTimestamp<T extends Timestamped>(local: T[], remote: T[]): T[] {` ... :69 `const remoteTime = item.updated_at || item.created_at || '';` :70 `const localTime = existing.updated_at || existing.created_at || '';` :71 `if (remoteTime > localTime) {`. Function ends :77-78 `return Array.from(map.values()); }`. The compare is a lexicographic string comparison of ISO strings; no Date parse, no skew tolerance.",
          "correction": ""
        },
        {
          "claim": "The resurrection risk is NOT 'merge ignores deleted_at' — loadAndMerge pre-filters !r.deleted_at at :106.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "db.ts:106 `const remote = ((data || []) as any[]).filter((r) => !r.deleted_at) as T[];` Tombstoned remote rows are excluded from `remote` before `mergeByTimestamp(local, remote)` at :107, so the merge never sees a deleted_at field to honor — correctly characterized as a pre-filter, not a merge-ignores-tombstone bug.",
          "correction": ""
        },
        {
          "claim": "The REAL bug: loadAndMerge:113-119 computes localOnly = merged − remoteIds and re-UPSERTS it back to Supabase, un-deleting a row soft-deleted on another device.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "db.ts:113 `const remoteIds = new Set(remote.map(r => r.id));` :114 `const localOnly = merged.filter(m => !remoteIds.has(m.id));` :115 `if (localOnly.length > 0) {` :116-118 `await supabase.from(table).upsert(localOnly.map(item => ({ ...sanitizeItem(item), user_id: userId })), { onConflict: 'id' })`. Because `remote` is the deleted_at-filtered set (:106), a row tombstoned remotely is absent from `remoteIds`, still present in local, lands in `localOnly`, and is upserted back — resurrection is real. Mechanism (E6) holds.",
          "correction": ""
        },
        {
          "claim": "sanitizeItem strips updated_at (:32-40), so a freshly-inserted local grade has '' local stamp and is overwritten by ANY remote row on next merge.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "db.ts:32-40 `function sanitizeItem(item: any): any { ... const { user_id: _uid, created_at: _ca, updated_at: _ua, ...rest } = item; return rest; }` — strips both created_at and updated_at. In mergeByTimestamp, a row with no updated_at AND no created_at yields localTime='' (:70); any remote ISO string satisfies `remoteTime > ''` (:71) and wins. The empty-stamp-loses mechanism is correct. Note: this is the GENERIC db.ts path (accuracy_ratings etc.); it does NOT apply to progressive_sessions, which uses its own upsert path stamping updated_at explicitly (useProgressiveStore.ts:323).",
          "correction": "Minor scope nuance worth stating in-plan: the empty-stamp bug is on tables routed through loadAndMerge/sanitizeItem (e.g. accuracy_ratings — the L4 grade write path), NOT progressive_sessions. The plan's 'Escalate to a prerequisite of L4 since L4 introduces freshly-written local grade rows' is correct precisely because grades go through this generic path."
        },
        {
          "claim": "progressive_sessions does NOT use mergeByTimestamp; loadSessions (:466-482) compares server-trigger updated_at vs client-clock updated_at (:456 server-selected vs :417,517 client-stamped).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "useProgressiveStore.ts loadSessions :454-459 `supabase.from('progressive_sessions').select('id, data, updated_at')...order('updated_at',{ascending:false}).limit(100)`; compare block :474-482 `} else if ( remote.updated_at && localSession.updated_at && new Date(remote.updated_at) > new Date(localSession.updated_at) ) {`. Server stamp: migration 20260409_progressive_sessions.sql:30-33 `CREATE OR REPLACE TRIGGER update_progressive_sessions_updated_at BEFORE UPDATE ... EXECUTE FUNCTION update_updated_at();` (so remote.updated_at is server-trigger time). Client stamps: updateSession :417 `s.id === id ? { ...s, ...updater(s), updated_at: new Date().toISOString() }` and createSession :517 `updated_at: now` (now = new Date().toISOString() at :497). The skew (server-trigger vs client-clock, both compared via new Date()) is real. Block ends at :482-483, not :482 strictly — the cited range :466-482 is the loop body and is fair.",
          "correction": "The cited line :456 is correct only as 'within the select at :454-456' (the select statement spans :455-459). Imprecise-but-defensible. Tighten to :455-459 for the select and :474-482 for the compare."
        },
        {
          "claim": "It is fire-and-forget with a swallowed .catch(()=>{}) (:324), 2s-debounced (:311), .limit(100) load (:459).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "persist(): :311 `setTimeout(() => {` ... :323 `updated_at: latest.updated_at || new Date().toISOString(),` :324 `}).catch(() => { /* fire-and-forget — localStorage is primary */ });` :325 `}, 2000);` — confirms 2000ms debounce (cited as :311 for the setTimeout open, :325 closes it) and the swallowed .catch at :324. loadSessions :459 `.limit(100)`. All three line cites confirmed.",
          "correction": "'2s-debounced (:311)' — the setTimeout opens at :311 and its delay arg `, 2000)` is at :325. The 2s value lives at :325, not :311. Imprecise: cite :311-325 (or :325 for the literal 2000)."
        },
        {
          "claim": "Silent loss of >100 sessions cross-device.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "useProgressiveStore.ts:459 `.limit(100)` on the remote fetch in loadSessions, with no pagination/continuation and `.order('updated_at',{ascending:false})` (:458). A user with >100 server sessions only ever merges the 100 most-recently-updated; older remote-only sessions never reach local. No UI signal on the path (the .catch at :492 swallows). Confirmed.",
          "correction": ""
        },
        {
          "claim": "NOT soft-deletable (not in SoftDeletableTable, db.ts:22).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "db.ts:22 `type SoftDeletableTable = 'projects' | 'personas' | 'reframe_items' | 'recast_items' | 'synthesize_items';` — progressive_sessions is absent. Fresh grep confirmed only two SoftDeletableTable occurrences (def :22, use :209 softDeleteFromSupabase). progressive_sessions IS in the broader TableName union (:20) but not soft-deletable.",
          "correction": ""
        },
        {
          "claim": "Session-blob durability: cap/eviction + server-side persistence for the uncapped progressive_sessions JSONB (discrete caps only: signals 500, evals 200).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "progressive_sessions blob is uncapped: migration :8 `data JSONB NOT NULL DEFAULT '{}'::jsonb` — no size guard; no internal slice/splice on session contents. Discrete caps: signal-recorder.ts:15 `const MAX_LOCAL_SIGNALS = 500;` + :32-33 splice; eval-engine.ts:148 `if (history.length > 200) history.splice(0, history.length - 200);` (also :194, :218 for RECAST_EVAL_KEY/REHEARSAL_EVAL_KEY). 'evals 200' correctly maps to eval-engine eval history, NOT hit-rate.ts (which is a separate 200-cap on HitRecords at :59). Fresh greps run this turn.",
          "correction": "Clarify which '200' is meant: eval-engine.ts (RECAST/REHEARSAL eval history, :148/:194/:218) is the 'evals 200'. There is ALSO an independent 200-cap on hit-rate.ts:59 (HitRecords) cited in L1. Both are real; the L5 'evals 200' = eval-engine, not hit-rate."
        },
        {
          "claim": "Must precede L3b (+ L3a versioning); the blob has no schema_version.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "migration 20260409_progressive_sessions.sql columns (:5-12): id, user_id, project_id, data JSONB, phase, has_pending_humans, updated_at, created_at — NO schema_version column. loadSessions casts `remote.data as ProgressiveSession` (:467) with zero validation; three migrators run every load (migrateSessionDrafts(migrateBranches(migrateWorkers(...))) at :447 and :486). Confirms 'born versioned' is net-new. The L3b atomic-RPC reference `update_worker_response ... avoids JSONB read-modify-write race condition` is at migration :35-37/:74, matching the plan's :35-78 cite.",
          "correction": ""
        },
        {
          "claim": "Surface a sync-failure signal (no UI signal on the progressive_sessions sync path today).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Two swallow points with zero UI: persist() :324 `.catch(() => { /* fire-and-forget */ })` and loadSessions outer `.catch(() => { /* Supabase unavailable — local is fine */ });` at :492. Neither emits a CustomEvent/toast. Confirmed best-effort durability with no signal.",
          "correction": ""
        }
      ],
      "newly_found": [
        "L5 'P1 data-correctness — db.ts merge' mechanism omits that the re-upsert resurrection (loadAndMerge:113-119) only fires for tables routed through loadAndMerge. progressive_sessions does NOT use loadAndMerge at all (it has its own loadSessions path), so the deleted_at resurrection bug and the empty-stamp bug described in the db.ts merge bullet do NOT apply to progressive_sessions — yet the two bullets sit adjacent under L5 and could be read as one surface. The plan should state the db.ts-merge fix covers the generic soft-deletable tables (projects/personas/reframe_items/recast_items/synthesize_items per :22) and the L4 grade tables, while progressive_sessions needs its OWN separate skew/durability fix. Otherwise a reader may believe fixing mergeByTimestamp also fixes progressive_sessions.",
        "The plan calls the empty-stamp row 'a freshly-inserted local grade' and escalates it as an L4 prerequisite, which is correct, but it never states WHICH table the grade lands in (accuracy_ratings is in the TableName union :17 and routes through loadAndMerge). The fix only works if the grade write goes through db.ts/loadAndMerge; if L4 writes grades into the progressive_sessions blob instead (a live option given §0/L4 talk of per-predicate history in the session blob), the sanitizeItem empty-stamp fix is on the wrong path. The plan should pin the grade's storage table before claiming the sanitizeItem fix covers it.",
        "sanitizeItem strips created_at too (:36 `created_at: _ca`), not just updated_at. The empty-stamp analysis relies on BOTH being stripped — mergeByTimestamp falls back updated_at -> created_at -> '' (:69-70). The plan only names updated_at; with created_at also stripped the fallback chain genuinely bottoms out at '', which actually strengthens the plan's case but is worth stating precisely (E2: read the whole sanitize block, not just the updated_at line).",
        "The L5 db.ts-merge fix proposes 'merge append-only tables by union-of-ids' as a tie-break, but does not flag that mergeByTimestamp is generic over ALL TableName tables (:15-20, ~16 tables). A union-of-ids/append-only tie-break that 'never discards a missing-local-stamp row' would change merge semantics for every soft-deletable table simultaneously (E5/E6: the mechanism's blast radius is the full TableName set, not just grades). The plan should scope the tie-break or confirm it is safe for projects/personas/items where last-write-wins is currently intended."
      ]
    },
    {
      "section": "§4 Cheap Validation Probes + §5 Design-System Spine (incl. §7 contrast entry)",
      "claims": [
        {
          "claim": "§4.1: daily-report/route.ts:329 buckets ['landing_hero_submit','landing_cta_click'] under '랜딩 CTA'",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "route.ts:329 read this turn: `{ label: '랜딩 CTA', keys: ['landing_hero_submit', 'landing_cta_click'] },` (inside funnelStages, :327-334). Funnel matches via `stage.keys.includes(e.event_name)` (:336).",
          "correction": ""
        },
        {
          "claim": "§4.1: landing_hero_submit is emitted in ZERO app code",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh exhaustive grep `landing_hero_submit|landing_cta_click` over src → only two hits: route.ts:329 (the bucket key definition) and Act3OnDeck.tsx:88 (`track('landing_cta_click', {cta:'voyage_helm'})`). No `track('landing_hero_submit'...)` anywhere → zero emitters confirmed.",
          "correction": ""
        },
        {
          "claim": "§4.1: landing_cta_click is live at Act3OnDeck.tsx:88 with {cta:'voyage_helm'}",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Grep result: `Act3OnDeck.tsx:88: onClick={() => track('landing_cta_click', { cta: 'voyage_helm' })}`.",
          "correction": ""
        },
        {
          "claim": "§4.1 (NEW): dashboard drops the cta discriminator — buckets landing_cta_click only by event_name, so a future second CTA pollutes the go/no-go number",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "route.ts:336 `const sid = new Set(extY.filter(e => stage.keys.includes(e.event_name)).map(e => e.session_id));` — filters on `e.event_name` only; `properties.cta` is never inspected. The discriminator is discarded at aggregation. Confirmed.",
          "correction": ""
        },
        {
          "claim": "§4.1: analytics.ts:168-179 wraps an async supabase.insert in try/catch that cannot catch the rejection; .then at :176 has no .catch → unhandled rejection, no canary",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "analytics.ts:168-179 read this turn: `try { supabase.from('user_events').insert({...}).then(() => { /* fire and forget */ }); } catch { /* silently fail */ }`. The .then at :176 has no `.catch`; a synchronous try/catch cannot catch a Promise rejection (E6 mechanism contract). No success/failure counter exists. Confirmed.",
          "correction": ""
        },
        {
          "claim": "§5: existing tokens present — --accent #96782e (globals.css:6), --ease-wave cubic-bezier(0.32,0.72,0,1) (:75), --ease-spring cubic-bezier(0.16,1,0.3,1) (:72), --font-display 'Noto Serif KR' (:57), --radius-md 12px (:79)",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "globals.css read this turn: :6 `--accent: #96782e;`, :75 `--ease-wave: cubic-bezier(0.32, 0.72, 0, 1);`, :72 `--ease-spring: cubic-bezier(0.16, 1, 0.3, 1);`, :57 `--font-display: 'Noto Serif KR', Georgia, ...;`, :79 `--radius-md: 12px;`. All five exact.",
          "correction": ""
        },
        {
          "claim": "§5: --brass-shine and .logbook-reveal do NOT exist (net-new appends), grep-confirmed",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep `brass-shine|logbook-reveal` over src → `No matches found`. Both absent → net-new confirmed.",
          "correction": ""
        },
        {
          "claim": "§5: there is NO '8pt spacing scale' token — spacing is Tailwind utilities (4px base), adopt 8pt rhythm via even utilities not a phantom token",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep `8pt|spacing scale|--space-` over globals.css → `No matches found`. No spacing-scale custom property exists. The `:root` token block (globals.css:3-83) defines colors/shadows/easing/radius but no `--space-*` scale. Confirmed net-new/phantom.",
          "correction": ""
        },
        {
          "claim": "§5: all net-new tokens must define dark-mode values; [data-theme=dark] (:86-142) remaps --accent to #d4b968",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "globals.css:86 `[data-theme=\"dark\"] {` opens the block; :89 `--accent: #d4b968;`. Block runs through :142. Confirmed (the [INTENT] 'must define dark values' is a correct design rule; the factual remap claim is verified).",
          "correction": ""
        },
        {
          "claim": "§5: LogbookCard primitive mirrors Card.tsx (forwardRef :23 + variant-map :10); Card variants reference --accent-light #b8963e (:12,32) NOT --accent #96782e",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Card.tsx read: :23 `export const Card = forwardRef<HTMLDivElement, CardProps>(`; :10 `const variantStyles: Record<CardVariant, string> = {`; :12 `ai: 'bg-[var(--ai)] border-[var(--accent-light)]/15 ...'`; :32 hoverable `hover:border-[var(--accent-light)]/30`. Fresh grep `var(--accent)` (bare) in Card.tsx → No matches. So no variant uses bare --accent; --accent-light (#b8963e per globals.css:7) confirmed. Minor: :32 is the hoverable string, not strictly a 'variant entry', but it does reference --accent-light, so the cite stands.",
          "correction": ""
        },
        {
          "claim": "§5 MOTION: JS/Framer imports EASE from progressive/shared/constants.ts ([0.32,0.72,0,1] = --ease-wave); 12+ files import EASE; progressive components already comply",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "constants.ts:3 `export const EASE = [0.32, 0.72, 0, 1] as const;` (matches --ease-wave globals.css:75). Fresh grep `import { EASE` over src → 18 files (InteractiveDemo, workspace/page, DMFeedback, ProgressiveFlow, AgentSidebar, MixPreview, PingToast, AttributedSection, FinalCard, PersonaPoolModal, VoyageChart, TeamDeployBanner, VerificationGate, WorkerCard, WorkerPanel, AnalysisCard, QuestionCard, UpdateSummaryChip). 18 >= '12+'. Cross-check grep `cubic-bezier(0.16,1,0.3,1)` in *.tsx → No matches (no JS hardcode of the spring curve) → 'already comply' supported.",
          "correction": "Count is 18 (>=12+, so the '12+' floor holds); if a precise figure is wanted, state 18."
        },
        {
          "claim": "§5 MOTION: leave --ease-spring as-is — CSS keyframes INTENTIONALLY use it (:234, :242, :489)",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "globals.css grep `--ease-spring`: :72 def, then :234 `animation: fade-in 0.4s var(--ease-spring);`, :242 `animation: slide-down 0.35s var(--ease-spring);`, :489 `transition-timing-function: var(--ease-spring);` (global button/a/input/textarea transition). All three exact; many more CSS uses (:250,:258,:351,:410,:420,:433,:1061,...). CSS-keyframe intentional use confirmed.",
          "correction": ""
        },
        {
          "claim": "§5 MOTION: reuse exported SPRING object (constants.ts:4) for the Logbook layer",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "constants.ts:4 `export const SPRING = { type: 'spring' as const, stiffness: 400, damping: 30 };`. Exported and exists.",
          "correction": ""
        },
        {
          "claim": "§5 a11y: --text-tertiary #b0aa9f = 2.16:1, --text-secondary #868279 = 3.58:1, --accent-light #b8963e = 2.63:1 on --bg #f8f7f5; dark #78716c on #1c1917 = 3.65:1",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Token hexes verified in globals.css (:8 --bg #f8f7f5, :14 --text-tertiary #b0aa9f, :13 --text-secondary #868279, :7 --accent-light #b8963e, :97 dark --text-tertiary #78716c, :91 dark --bg #1c1917). WCAG ratio computation this turn: tertiary 2.16, secondary 3.58, accent-light 2.63, dark 78716c-on-1c1917 3.65 — all match the plan exactly.",
          "correction": ""
        },
        {
          "claim": "§5 a11y / §7: --accent text contrast on --bg = 4.18:1 (fails AA 4.5:1)",
          "tag": "VERIFIED",
          "verdict": "wrong",
          "evidence": "Computed this turn (both WCAG threshold variants 0.03928 and 0.04045): --accent #96782e on --bg #f8f7f5 = 3.905:1, i.e. 3.9:1, NOT 4.18:1. accent L=0.2011, bg L=0.9307 → (0.9307+0.05)/(0.2011+0.05)=3.905. Same wrong value appears in §5 (:302) and §7 (:332/:338).",
          "correction": "Change '--accent text = 4.18:1' to '= 3.9:1' in §5 (:302) and §7 (rows at :332 and :338). Direction unchanged: 3.9 < 4.5, still an AA failure; recompute the suggested replacement '#7d6526' before citing it as AA-passing."
        },
        {
          "claim": "§5 a11y: the 'contrast-safe (4.5:1+)' comment at globals.css:6 is FALSE — delete it",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "globals.css:6 read: `--accent: #96782e;         /* Gold — contrast-safe on white (4.5:1+) */`. Computed actual = 3.9:1 < 4.5 → comment is false. (Plan paraphrases the comment as 'contrast-safe (4.5:1+)'; exact text adds 'on white' — substance correct.)",
          "correction": ""
        },
        {
          "claim": "§5 a11y: these failing tokens power real copy — FinalCard.tsx:67 subtitle",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "FinalCard.tsx:67 read: `<span className=\"text-[11px] text-[var(--text-tertiary)] ml-2\">{L('바로 보낼 수 있어요', 'Ready to send')}</span>` — uses the 2.16:1 --text-tertiary on surface for live subtitle copy. Confirmed.",
          "correction": ""
        },
        {
          "claim": "§5 a11y: no useReducedMotion()/MotionConfig anywhere in src; framer MotionConfig reducedMotion='user' must be added",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Fresh grep `useReducedMotion|MotionConfig` over src → No matches found. Confirmed absent.",
          "correction": ""
        },
        {
          "claim": "§5 a11y: 36 framer-motion importers",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "Fresh exhaustive grep `from 'framer-motion'` over src → 'Found 38 total occurrences across 38 files.' Actual count is 38, not 36.",
          "correction": "Change '36 framer-motion importers' to '38'. (Understated; direction unchanged.)"
        },
        {
          "claim": "§5 a11y: the CSS reduced-motion blocks are selective allowlists that MISS every infinite animation — pulse-skeleton :266, gold-shimmer :283, border-glow :292, subtle-pulse :360, bm-bounce :1264, inner-glow :1590",
          "tag": "VERIFIED",
          "verdict": "wrong",
          "evidence": "The @media (prefers-reduced-motion: reduce) block (globals.css:449-483) EXPLICITLY lists `.animate-gold-shimmer` (:468) and `.animate-border-glow` (:469) under `{ animation: none; }`. So gold-shimmer (:283) and border-glow (:292) are NOT missed — they are covered. Genuinely missed (not in the allowlist): .animate-skeleton/pulse-skeleton (:265-266), .animate-subtle-pulse/subtle-pulse (:359-360), boss bm-bounce (:1259-1272, infinite at :1264), inner-glow (:1590, `animation: inner-glow 4s ... infinite`).",
          "correction": "Remove gold-shimmer (:283) and border-glow (:292) from the 'MISSED' list — they ARE in the reduced-motion allowlist at globals.css:468-469. Keep pulse-skeleton, subtle-pulse, bm-bounce, inner-glow as genuinely uncovered. The broader point (no global *{} wildcard rule; several infinite animations uncovered) stands."
        },
        {
          "claim": "§4.4 / §4.5 / §0 corollary: Test-1 predicate must carry a stable id (pulled forward from L3a) or the grade joins by brittle text",
          "tag": "INTENT",
          "verdict": "unverifiable",
          "evidence": "This is a design prerequisite (cross-references §0 corollary + L3a stable-id slice), not a code fact about §4/§5 source. Tagged [INTENT] per protocol E7; the underlying code claims (MaterializedRisk keys by risk_text+persona_id with no id) live in §0/L3a, outside this section's scope and not re-opened this turn.",
          "correction": ""
        }
      ],
      "newly_found": [
        "§5 reduced-motion list is WRONG on two members: gold-shimmer (:283) and border-glow (:292) ARE in the allowlist (globals.css:468-469 explicitly set `animation: none`), so they are NOT missed. The augment pass inherited the 'misses every infinite animation' framing without reading the allowlist body at :461-471. Real uncovered infinite animations: .animate-skeleton (:265), .animate-subtle-pulse (:359), boss bm-bounce (:1264), inner-glow (:1590).",
        "§5/§7 contrast value 4.18:1 for --accent is WRONG (real 3.9:1) and appears in THREE places (§5:302, §7:332, §7:338) — a single fabricated number propagated across the document; the augment pass did not recompute it. Fix all three. Direction (AA fail) survives.",
        "§5 framer-motion importer count is 38, not 36 (fresh grep). Minor E5 drift the augment pass carried over without re-enumeration.",
        "E5 precision: §5 says '12+ files import EASE'; the real number is 18. The '12+' floor is technically satisfied, but the loose 'X+' phrasing is the same imprecise-count pattern the protocol targets — state the exact 18 or keep it explicitly as a floor.",
        "§5 Card cite ':12,32' conflates a variant-map entry (:12 ai variant) with the hoverable class string (:32) — :32 is not a `variantStyles` entry. Both do reference --accent-light, so the substantive 'Card uses --accent-light not --accent' claim holds (grep: zero bare var(--accent) in Card.tsx), but the ':32 is a variant' implication is imprecise."
      ]
    },
    {
      "section": "§6 (NOT-doing) + §7 (Do-Not-Inherit) — re-verified against source this turn",
      "claims": [
        {
          "claim": "§7 row: parseJSON real surface = ~5 direct + 8 callLLMStreamThenParse callsites (non-test)",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "FRESH grep `grep -rn \"parseJSON\" src ... | grep -v __tests__ | grep -v \"export function parseJSON\"` → invocation callsites: RecastStep.tsx:337, ReframeStep.tsx:500, ReframeStep.tsx:659, llm.ts:538, llm.ts:777 = 5 direct parseJSON calls (2 of which are llm.ts engine internals). FRESH grep callLLMStreamThenParse actual callsites = 7 (progressive-engine.ts:307,385,458,641,776,812,895), NOT 8. The 8th is the import line progressive-engine.ts:5.",
          "correction": "callLLMStreamThenParse has 7 invocation callsites, not 8 (the 8th occurrence in progressive-engine.ts is the import at :5). 'direct parseJSON' = 5 invocations but only 3 are app-level (RecastStep:337, ReframeStep:500/659); the other 2 (llm.ts:538,:777) are engine internals — same exclusion logic the row applies to JSON.parse should be stated here. Change to '~3 app-direct + 7 callLLMStreamThenParse callsites'."
        },
        {
          "claim": "§7 row: JSON.parse = ~17 non-test; ~11 real app callsites (6 are llm.ts engine internals — exclude)",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "FRESH grep `grep -rn \"JSON\\.parse\" src ... | grep -v __tests__ | grep -v .test.` → 17 lines total. llm.ts internals = 6 (llm.ts:228,233,240,248,711,738). 17 − 6 = 11 real app callsites (utm-builder:63, slack/callback:12,20, slack/events:46, settings:88, PersonaRefinementSection:141, BossChat:351, StepIntro:88, analytics:115, storage:31, user-context:44). Both numbers exact.",
          "correction": ""
        },
        {
          "claim": "§7 row: 'llm-validation.ts is ~empty' → Complete 72-line module (validateMessages/validateRequest etc.)",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "`wc -l src/lib/llm-validation.ts` → 71 src/lib/llm-validation.ts. Exports confirmed: validateMessages (:16), normalizeMaxTokens (:33), validateSystemPrompt (:38), validateApiKey (:44), validateRequest (:69) + 5 const caps.",
          "correction": "Module is 71 lines, not 72. The substance (complete, multi-function, do-not-rebuild) is correct; fix the line count to 71."
        },
        {
          "claim": "§7 row + L0 CLOSE: decision-quality.ts:146 already guards recast.analysis.steps || [] AND outer recast?.analysis (:145); caveat to 'check :83 reframe.analysis null exposure'",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Read decision-quality.ts:144-152: `if (recast?.analysis) {` (:145) then `const steps = recast.analysis.steps || [];` (:146). Guard confirmed. The :83 caveat is separately WRONG (see next row).",
          "correction": ""
        },
        {
          "claim": "L0/§7 caveat: ':83 reads reframe.analysis.hidden_questions || [] WITHOUT guarding reframe.analysis being null'",
          "tag": "VERIFIED",
          "verdict": "wrong",
          "evidence": "Read decision-quality.ts:82-83: `if (reframe?.analysis) {` (:82) then `const questionCount = (reframe.analysis.hidden_questions || []).length;` (:83). Line 83 IS inside the `if (reframe?.analysis)` guard at :82. This is an E2 error — the caveat read :83 without the enclosing guard at :82.",
          "correction": "DELETE the ':83 reframe.analysis null exposure' caveat (appears in L0 line 115 and the §7 decision-quality row). :83 is already guarded by `if (reframe?.analysis)` at :82. There is no analysis-null exposure to check; decision-quality.ts can be closed outright."
        },
        {
          "claim": "§7/§6: VoyageChart is SVG; @react-three/* is DEAD/unused; remove the 3 dead 3D deps",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "FRESH grep `grep -rn \"@react-three\" src` → ZERO hits. `grep \"from 'three'\" src` → ZERO. No Canvas/useFrame/useThree/fiber/drei anywhere in src. package.json declares @react-three/drei ^10.7.7, @react-three/fiber ^9.6.0, three ^0.183.2 — all unimported. VoyageChart.tsx imports BranchMap (:40) which renders `<svg viewBox=...>` (BranchMap.tsx:58-60); VoyageChart comment :133 refers to 'the SVG tree'.",
          "correction": ""
        },
        {
          "claim": "§6/§7: OUTCOME_RECORDS has zero production writers (read at context-builder.ts:61, producer-less); return-to-grade is net-new",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "FRESH grep `grep -rn \"OUTCOME_RECORDS\" src` (non-test) → only context-builder.ts:61 (read: getStorage<OutcomeRecord[]>(STORAGE_KEYS.OUTCOME_RECORDS,[])) and storage.ts:13 (key def 'sot_outcome_records'). `grep addOutcome|recordOutcome` → ZERO writers. No setStorage(OUTCOME_RECORDS) anywhere.",
          "correction": ""
        },
        {
          "claim": "§7/§4: landing_hero_submit is dead (emitted in zero app code); landing_cta_click is live (Act3OnDeck.tsx:88 with {cta:'voyage_helm'}); both bucketed together, cta discriminator discarded",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "FRESH grep landing_hero_submit → only daily-report/route.ts:329 (dashboard bucket key). FRESH grep landing_cta_click → daily-report/route.ts:329 (bucket) + Act3OnDeck.tsx:88 `onClick={() => track('landing_cta_click', { cta: 'voyage_helm' })}`. Bucket at :329 = `{ label:'랜딩 CTA', keys:['landing_hero_submit','landing_cta_click'] }` — no cta-level split.",
          "correction": ""
        },
        {
          "claim": "§7/§L5: R6 resurrection is REAL — loadAndMerge:113-119 re-UPSERTS tombstoned localOnly rows; loadAndMerge:106 pre-filters !deleted_at; sanitizeItem strips updated_at (:32-40); progressive_sessions NOT soft-deletable",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Read db.ts:106 `const remote = (...).filter(r => !r.deleted_at)`; :113-119 `remoteIds=new Set(remote.map id); localOnly=merged.filter(!remoteIds.has(id)); ... .upsert(localOnly.map sanitizeItem...)`. sanitizeItem:32-40 destructures out `updated_at:_ua`. mergeByTimestamp:57-78 string-compares `remoteTime > localTime` (:71) with `|| ''` fallback (:69-70). SoftDeletableTable:22 = projects|personas|reframe_items|recast_items|synthesize_items (no progressive_sessions).",
          "correction": ""
        },
        {
          "claim": "§5/§7: globals.css:6 'contrast-safe (4.5:1+)' comment is FALSE; --accent #96782e; Card.tsx variants reference --accent-light #b8963e not --accent",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Read globals.css:6 `--accent: #96782e; /* Gold — contrast-safe on white (4.5:1+) */` and :7 `--accent-light: #b8963e;`. Card.tsx:12 `border-[var(--accent-light)]/15`, :32 `hover:border-[var(--accent-light)]/30` — uses --accent-light. NOTE: the 4.18:1 measured-ratio numbers are [ASSUMPTION] (cannot re-derive contrast without a tool); the comment's existence and the token values are facts.",
          "correction": "The comment-existence and token-value claims are VERIFIED. The specific measured ratios (4.18:1 accent, 2.16:1 tertiary, 3.58:1 secondary) remain [ASSUMPTION] — not independently re-computed this turn; tag them as such rather than [VERIFIED]."
        },
        {
          "claim": "§7: 'navigator.ts never acts' is OVERSTATED — buildNavigatorProfile consumes signals",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Read navigator.ts:18 `import { getSignals } from '@/lib/signal-recorder'`; :95 `export function buildNavigatorProfile()`; :122 `const demoSeeds = getSignals().filter(...)`. Consumption confirmed.",
          "correction": ""
        },
        {
          "claim": "§7/§2: useAccuracyStore rates PERSONAS (addRating writes PersonaAccuracyRating); prediction-scoring is net-new write-path",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Read useAccuracyStore.ts:8-15: `ratings: PersonaAccuracyRating[]`, `addRating: (data: Omit<PersonaAccuracyRating,'id'|'created_at'>) => void`, `getPersonaAccuracySummary`. Persona-keyed, not predicate/outcome.",
          "correction": ""
        },
        {
          "claim": "§6: validateShape already wired llm.ts:778-781 (used to reject re-scoping from fabricated counts)",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "Read llm.ts:776-781 inside callLLMStreamThenParse onComplete: `const parsed = parseJSON<T>(fullText); const validated = options.shape ? validateShape<...>(parsed, options.shape) as T : parsed;`. Wired CONDITIONALLY on options.shape, not unconditional.",
          "correction": "Add 'conditionally' — validateShape fires only when the caller passes options.shape (llm.ts:778 ternary); callsites without a shape get unvalidated parsed output. The 'already wired' claim is true but not unconditional."
        },
        {
          "claim": "§6: 'no useMirrorStore'",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "FRESH grep `grep -rln \"useMirrorStore\" src` (non-test) → ZERO files. Store does not exist/used.",
          "correction": ""
        },
        {
          "claim": "§6: 'the dead useTeamStore' (blind-read button is future activation)",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "FRESH grep `grep -rln \"useTeamStore\" src` (non-test) → src/app/teams/page.tsx + src/stores/useTeamStore.ts. It IS referenced by teams/page.tsx, so 'dead' is not literally true.",
          "correction": "useTeamStore is imported/used by app/teams/page.tsx — it is not dead code. The 'blind-read button as future activation' is [INTENT]; do not call the store itself 'dead' when a page consumes it. Re-tag as '[INTENT] no NEW blind-read activation on strategist surfaces'."
        }
      ],
      "newly_found": [
        "E2 hidden error: the decision-quality.ts:83 caveat (L0 line 115 + §7 row) is a cited-line-only read — :83 is inside `if (reframe?.analysis)` at :82, so the 'unguarded reframe.analysis' concern is false. The augment pass propagated this false caveat instead of opening :82.",
        "E5 off-by-one the augment pass missed: §7 says '8 callLLMStreamThenParse callsites' but a fresh exhaustive grep yields 7 invocation callsites (progressive-engine.ts:307,385,458,641,776,812,895); the 8th occurrence is the import line (:5). Same class as the parseJSON miscount the protocol was written to kill.",
        "E1 off-by-one: llm-validation.ts is 71 lines (wc -l), §7 asserts '72-line module' — an inherited line count not re-counted this turn.",
        "Imprecision (E3-adjacent) the augment pass missed: §7's parseJSON row counts 'direct' callsites without applying the same engine-internal exclusion it applies to JSON.parse — 2 of the 5 'direct' parseJSON calls (llm.ts:538,:777) are llm.ts engine internals, leaving only 3 app-level direct callsites (RecastStep:337, ReframeStep:500/659).",
        "Precision gap: §5/§7 contrast ratios (4.18:1 / 2.16:1 / 3.58:1) are stated as [VERIFIED]-grade facts but are uncomputable from source alone without a contrast tool — they should carry [ASSUMPTION: measure with a WCAG contrast checker]. Only the FALSE-comment existence and token hex values are VERIFIED facts.",
        "§6 'dead useTeamStore' is contradicted by app/teams/page.tsx consuming it — a stale 'dead' attribution the augment pass left unverified (E3: a consumer exists)."
      ]
    },
    {
      "section": "§UX + §UX-MOBILE (P0/P1 file:line anchors)",
      "claims": [
        {
          "claim": "§UX-L0 P0: workspace/page.tsx:502-532 — first-run analyze spinner shows '보통 20~40초' but has NO cancel and NO elapsed timer.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "workspace/page.tsx:502 `{phase === 'analyzing' && (() => {`; :527-531 spinner Sparkles + stageLabel + `<span...>{L('보통 20~40초', 'usually 20–40s')}</span>` at :531. Read full 502-549 block: only a rotating Sparkles icon, stageLabel, and the duration hint — no AbortController, no mm:ss counter, no Cancel button anywhere in the block.",
          "correction": ""
        },
        {
          "claim": "§UX-L0 P0: runInitialAnalysis is called WITHOUT the signal? it accepts (progressive-engine.ts:293-296).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "progressive-engine.ts:293 `export async function runInitialAnalysis(` ... :296 `signal?: AbortSignal,`. Call site workspace/page.tsx:221 `const result = await runInitialAnalysis(text, (token) => {` — two args only, no signal passed. Grep of page.tsx for AbortController returned no hits.",
          "correction": ""
        },
        {
          "claim": "§UX-L0 P0: reuse elapsedSec/isLongWait from ProgressiveFlow.tsx:1062-1084.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "ProgressiveFlow.tsx:1065 `const [elapsedSec, setElapsedSec] = useState(0);`; :1084 `const isLongWait = elapsedSec >= 30;` — within the 1062-1084 range, 30s threshold matches the plan's '~30s' affordance.",
          "correction": ""
        },
        {
          "claim": "§UX-L0 P0 / §UX-L1 P0: FinalCard.tsx:66-67 shows ONE '완성된 기획안'.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "src/components/workspace/progressive/FinalCard.tsx:66 `{L('완성된 기획안', 'Final Document')}`; :67 `{L('바로 보낼 수 있어요', 'Ready to send')}`. NOTE: plan cites this as bare 'FinalCard.tsx'; actual path is src/components/workspace/progressive/FinalCard.tsx (path correct in plan's §UX text which omits dir).",
          "correction": ""
        },
        {
          "claim": "§UX-L0 P0: the 5-format OutputSelector — Outputs surface hides the 4 Ithaca outputs; add footer band '...4가지 산출물'.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "Fresh grep of OutputSelector.tsx for `key:` → 5 formats: :36 brief, :46 prompt-chain, :56 agent-spec, :66 checklist, :76 rationale ('판단 근거서 · Decision Rationale'). The surface has FIVE formats, not four.",
          "correction": "§UX-L0 P0 internally says '5-format OutputSelector' (correct) but then prescribes a footer band labeled '4가지 산출물' / '4가지 산출물 보기'. With 5 formats present (rationale is the 5th, :76-84), the '4 outputs' copy undercounts. Either justify excluding 'rationale' (it is the decision-log/process output, arguably the differentiator) or relabel '5가지 산출물'."
        },
        {
          "claim": "§UX-L4 P0: Output card labels are orchestra jargon — OutputSelector.tsx:38/48/58/68 '프로그램 노트/파트보/총보/셋리스트'.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": ":38 `'프로그램 노트 · Project Brief'`; :48 `'파트보 · Prompt Chain'`; :58 `'총보 · Agent Spec'`; :68 `'셋리스트 · Execution Checklist'`. All four anchors correct. BUT the labels already PAIR the metaphor with the plain English role-name (e.g. '프로그램 노트 · Project Brief'), not pure jargon. And a 5th label exists at :78 '판단 근거서 · Decision Rationale' not cited.",
          "correction": "Soften 'orchestra jargon' → the metaphor already co-exists with the plain name; the fix is reordering/demoting the metaphor and adding the Korean plain label + audience tag, not introducing a missing plain name. Add the 5th label (:78) to the cited set or state why rationale is exempt."
        },
        {
          "claim": "§UX-L4 P1: '이타카' header at OutputSelector.tsx:141.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "OutputSelector.tsx:141 `<h3 ...>{L('이타카 — 산출물 선택', 'Ithaca — Choose output')}</h3>`.",
          "correction": ""
        },
        {
          "claim": "§UX-L4 P1 / L1 reconcile: dark code-terminal preview at OutputSelector.tsx:178-180 (bg-[#1a1a2e] font-mono) for prose; Coda meta_reflection at :94-112,184-280; download :128-137; Copy/Download only at :167-182.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": ":178 `<pre className=\"bg-[#1a1a2e] text-[#e2e4ea] rounded-xl p-4 text-[12px] ... font-mono\">`; :94 `const [codaOpen, setCodaOpen] = useState(false);` and :104-112 handleSaveCoda meta_reflection; :128-137 handleDownload Blob; :167-182 Preview block with CopyButton (:172) + Download (:173-175).",
          "correction": ""
        },
        {
          "claim": "§UX-L1 P1 / L1 convergence win: ProgressiveFlow.tsx:842-852 (also :837-852) shows score% + guidance only; is_converged/estimated_rounds_left/trend rendered nowhere.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "ProgressiveFlow.tsx:842 `{L('명확도', 'Clarity')}`; :843 `{metrics.score}%`; :850 `{metrics.guidance}`. Block runs :837 (`return (`) to :852. No reference to is_converged/estimated_rounds_left/trend in the rendered block.",
          "correction": ""
        },
        {
          "claim": "§UX-L1 P0: Act1Voyage.tsx:67-98 is 100% metaphor (hero h1 + siren line).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Act1Voyage.tsx:67-80 h1 '당신의 항해는 — / 왜 종착지에 닿지 못했는가.' (EN 'Why hasn't your voyage / reached its shore?'); :83-98 sub-1 siren '— AI라는 감미로운 목소리에 홀려, 그 답에 정박해버린 채로.' Both metaphor, no plain value prop.",
          "correction": ""
        },
        {
          "claim": "§UX-L1 P0: landing hero has no real CTA — only an 11.5px underlined scroll link at Act1Voyage.tsx:120-135.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Act1Voyage.tsx:120-135 `<a href=\"#orchestration\" ... style={{ ... fontSize: 11.5, ... borderBottom: '1px solid var(--bp-ink)' ...}}>{L('배 안을 보기', 'See the vessel inside')}</a>`. It is an in-page scroll anchor (#orchestration), 11.5px, underlined — not a /workspace CTA.",
          "correction": ""
        },
        {
          "claim": "§UX-GLOBAL-CHROME P2: Six-item nav with locks reads as a paywall — Header.tsx:16-23,105,118; THREE lock-gated items; collapse Agents/Manager/Projects into overflow.",
          "tag": "VERIFIED",
          "verdict": "wrong",
          "evidence": "Header.tsx:16-23 navItems = SIX (Workspace, Projects, Agents, Manager, Guide, Settings). `requiresAuth: true` appears ONLY on :18 Projects and :19 Agents. :20 Manager (/boss) has NO requiresAuth. :105 `const showLock = item.requiresAuth && !user && !loading;`; :118 `{showLock && <Lock .../>}` — lock renders ONLY for requiresAuth items. So TWO locked items, not three; Manager is not lock-gated.",
          "correction": "Change 'three lock-gated items' → TWO (Projects :18, Agents :19). Manager (/boss, :20) is NOT auth-gated and shows no lock. The set to collapse may still be Agents/Manager/Projects by IA preference, but only Agents+Projects actually carry the paywall-lock contradiction."
        },
        {
          "claim": "§UX-GLOBAL-CHROME P2: RateLimitBadge gated to user — Header.tsx:159-164 (also L1 quota: Header.tsx:159).",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Header.tsx:159-164 `{user && (<><SyncStatus /><RateLimitBadge /></>)}` — RateLimitBadge only renders inside the `user &&` guard, so anon users never see it.",
          "correction": ""
        },
        {
          "claim": "§UX-GLOBAL-CHROME P3: Unlabeled theme toggle — Header.tsx:152-158, hover-only title, absent on mobile.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "Header.tsx:152-158 theme button has only `title={darkMode ? 'Light mode' : 'Dark mode'}` (hover-only, no visible label/aria-label). It sits inside the `<div className=\"hidden md:flex ...\">` block opened at :100, so it is hidden on mobile.",
          "correction": ""
        },
        {
          "claim": "§UX-L0 P1: CopyButton.tsx:27-29 catches into console.error, never flips to 'Copied!'.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "CopyButton.tsx:27-29 `} catch (err) {\\n  console.error('Copy failed:', err);\\n}`. Success path :24 `setCopied(true)`; the catch path has no UI feedback. Claim about the failure path is exact.",
          "correction": ""
        },
        {
          "claim": "§UX-L0 P1: LoadingSteps.tsx:14-19 advances on a 2500ms timer, clamps at last step.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "LoadingSteps.tsx:11 default `intervalMs = 2500`; :14-18 `setInterval(() => setCurrentStep(prev => Math.min(prev + 1, steps.length - 1)), intervalMs)` — clamps at steps.length-1.",
          "correction": ""
        },
        {
          "claim": "§UX-L0 P1: ReframeStep streams real tokens (:487 onToken: setStreamingText) but streamingText is NEVER rendered.",
          "tag": "VERIFIED",
          "verdict": "confirmed",
          "evidence": "src/components/workspace/ReframeStep.tsx:487 `onToken: (text) => setStreamingText(text),`. Fresh grep for `streamingText` in the file returns ONLY: :398 declaration, :479/:495/:542/:639/:654/:681 setStreamingText('') clears, :487/:646 setter calls — ZERO `{streamingText}` render/JSX consumer. Confirms 'never rendered' (no consumer).",
          "correction": "Minor: there is a SECOND streaming path with onToken at :646 the §UX text does not mention; the 'never rendered' claim still holds for both."
        },
        {
          "claim": "§UX-MOBILE P1: Worker bar bottom-0 (WorkerPanel.tsx:399) + flex-wrap (WorkerPanel.tsx:408); Logbook bar bottom-[56px] (Logbook.tsx:311) = ~108px stacked.",
          "tag": "VERIFIED",
          "verdict": "imprecise",
          "evidence": "WorkerPanel.tsx:399 `fixed bottom-0 inset-x-0 z-40 ... min-h-[56px]`; :408 `flex items-center gap-2 flex-wrap min-w-0`. Logbook.tsx:311 `fixed inset-x-0 z-40 ... min-h-[52px] ... ${offset ? 'bottom-[56px]' : 'bottom-0'}`.",
          "correction": "Logbook bar is `min-h-[52px]` (not 56) and its `bottom-[56px]` is CONDITIONAL on `offset`; when offset is false it sits at bottom-0 (overlapping the worker bar). The '~108px stacked' figure assumes both stacked (56+52≈108) which is the worst case; note the conditional. Anchors all correct."
        }
      ],
      "newly_found": [
        "§UX-GLOBAL-CHROME P2 'three lock-gated items' is an E5 count error: a fresh read of Header.tsx:16-23 shows requiresAuth on only 2 of 6 items (Projects :18, Agents :19). Manager (/boss :20) is unlocked. The augment pass inherited the 'three' count without enumerating the requiresAuth flags.",
        "E5/E7 count drift between §UX-L0 P0 ('5-format OutputSelector') and its own prescribed copy ('4가지 산출물'), and §UX-L4 P0 enumerating only 4 of the 5 OutputSelector labels (omits :78 '판단 근거서 · Decision Rationale'). The 5th format is the decision-rationale/process output — arguably the very differentiator the plan elsewhere elevates (FinalCard decision-log, §UX-L4 P1 :76-84), so silently dropping it from the outputs catalogue is a self-contradiction.",
        "E2 nuance on §UX-L4 P0: the OutputSelector labels are NOT 'orchestra jargon' in isolation — each already concatenates the metaphor with the plain role name via ' · ' (e.g. '총보 · Agent Spec'). The real defect is metaphor-LEADING order + missing Korean plain label + missing audience tag, not an absent plain name. Stating it as pure jargon overstates the wound.",
        "Path-precision: §UX cites FinalCard.tsx, OutputSelector.tsx, ReframeStep.tsx, Logbook.tsx, WorkerPanel.tsx as bare filenames. Verified real paths: FinalCard/Logbook/WorkerPanel under src/components/workspace/progressive/, ReframeStep under src/components/workspace/, OutputSelector under src/components/ui/, CopyButton under src/components/ui/. Note a duplicate Header.tsx exists at commet/claude-code_new/web/components/layout/ — the plan's anchors match the Argus copy src/components/layout/Header.tsx (confirmed), but the bare-filename convention risks ambiguity for any tool resolving by name."
      ]
    }
  ],
  "sweeps": [
    {
      "error_class": "E3 — \"Reuse / already exists\" unproven (producer+consumer sweep)",
      "instances": [
        {
          "plan_claim": "§0(2a)/#6/L4/§7 ARCHETYPE: OUTCOME_RECORDS is read but has ZERO production writers; return-to-grade is net-new write-path, NOT a repoint of live plumbing.",
          "status": "holds",
          "evidence": "FRESH grep `setStorage([^)]*OUTCOME_RECORDS|addOutcome|recordOutcome` across src = No matches found. Only reader: context-builder.ts:61 `getStorage<OutcomeRecord[]>(STORAGE_KEYS.OUTCOME_RECORDS, [])`. Producer absent → net-new confirmed. The `outcomes.length >= 2` branch at context-builder.ts:62 is permanently inert.",
          "fix": "None — plan correctly tags it net-new. Archetype verified."
        },
        {
          "plan_claim": "§0(2a)/L4: existing accuracy UI (addRating) is on the LEGACY path, called only at FeedbackResult.tsx:66, rendered only by RehearseStep, NOT ProgressiveFlow — placing grading on the progressive surface is net-new placement, not a repoint.",
          "status": "holds",
          "evidence": "grep `addRating`: defined useAccuracyStore.ts:34, called ONLY at components/tools/FeedbackResult.tsx:66. grep `FeedbackResult` (component): imported only at RehearseStep.tsx:12, rendered only at RehearseStep.tsx:673. No ProgressiveFlow consumer. Producer(addRating)+consumer(FeedbackResult) both on legacy rehearse path.",
          "fix": "None — plan correctly labels progressive-surface placement as net-new."
        },
        {
          "plan_claim": "§7/L5/§1: retrospective.ts read path is producer-less (`saveRetrospectiveAnswer` UI gone) — do not cite as a closed loop.",
          "status": "holds",
          "evidence": "grep `saveRetrospectiveAnswer|setStorage([^)]*RETROSPECTIVE`: only the doc-comment mention at retrospective.ts:5; the ONLY runtime use is the READER getActionableInsights at retrospective.ts:15 (`getStorage<RetrospectiveAnswer[]>(...RETROSPECTIVE_ANSWERS)`). No setStorage writer exists. retrospective.ts:6 self-documents 'That UI is gone.' Consumer (buildEnhancedSystemPrompt) present, producer absent.",
          "fix": "None — plan correctly tags producer-less. Sibling of the archetype, already caught."
        },
        {
          "plan_claim": "§1/L1: hit-rate.ts is unpersisted/localStorage-only ('Supabase 동기화는 추후') — stop citing it as a durable closed calibration asset unless a hit_records table is added.",
          "status": "holds",
          "evidence": "hit-rate.ts:6 source comment: 'localStorage 기반, Supabase 동기화는 추후.' Imports only `getStorage, setStorage` from @/lib/storage (:9), no db.ts/loadAndMerge import. No Supabase producer/consumer — local-only.",
          "fix": "None — plan correctly flags it as non-durable / not-yet-synced."
        },
        {
          "plan_claim": "L1 (convergence gauge): is_converged/estimated_rounds_left are COMPUTED but rendered NOWHERE — promoting a gauge is net-new UI over existing fields (producer exists, no .tsx consumer).",
          "status": "holds",
          "evidence": "FRESH grep `is_converged|estimated_rounds_left` glob=*.tsx = No matches found (zero consumers). Producer present: progressive-convergence.ts:109-129 computes both fields. So reuse = read already-computed data; the gauge itself is net-new — exactly as plan states.",
          "fix": "None — producer verified, consumer correctly stated absent."
        },
        {
          "plan_claim": "L1/#5/§UX-L1: reflection-tab reuse — compose navigator.ts builders + getUserPatterns (useJudgmentStore) + getPersonaAccuracySummary (useAccuracyStore) inside NavigatorStrip; getUserPatterns is NOT in navigator.ts.",
          "status": "holds",
          "evidence": "getUserPatterns defined useJudgmentStore.ts:14,57 (NOT navigator.ts). getPersonaAccuracySummary defined useAccuracyStore.ts:12,49. Producers buildNavigatorProfile/Insights/LearningCurve in navigator.ts:95/153/792; consumer NavigatorStrip.tsx:198-200 already renders them. Reuse target verified producer+consumer both present.",
          "fix": "None — attribution and reuse host verified."
        },
        {
          "plan_claim": "L1: artifact INPUTS persist & sync (project-brief.ts:10-17 read from stores that sync to Supabase) so a brief is deterministically regenerable; the rendered string is the only net-new persistence gap.",
          "status": "holds",
          "evidence": "project-brief.ts:10-17 reads getStorage(REFRAME_LIST/RECAST_LIST/SYNTHESIZE_LIST/FEEDBACK_HISTORY). Producers: useReframeStore/useRecastStore via createItemStore.ts:26 `loadAndMerge(tableName,...)`; reframe_items/recast_items in db.ts TableName union (db.ts:15) and SoftDeletableTable (:22). Both producer (store+Supabase sync) and consumer (generator) verified — inputs genuinely durable.",
          "fix": "None — reuse-vs-net-new split (inputs reuse, rendered string net-new) is correct."
        },
        {
          "plan_claim": "L1 (NEW): agent-spec.ts:7 uses projectFilter(project) vs project-brief.ts:11 inline `d.project_id === project.id` — generators not behavior-identical on project scoping.",
          "status": "holds",
          "evidence": "agent-spec.ts:4 `import { projectFilter } from './output-helpers'`; agent-spec.ts:7 `const pf = projectFilter(project)`. project-brief.ts:11-15 uses inline `.filter((d) => d.project_id === project.id && d.status === 'done')`. Two different scoping mechanisms confirmed — alignment is real work, not assumed-equal reuse.",
          "fix": "None — divergence verified; plan correctly flags it before snapshotting."
        },
        {
          "plan_claim": "L3a: typed context builders exist (context-chain.ts:119-525) but there is NO useContextChainStore / _source / traceProvenance / persistence — work is persistence-add, not from-scratch (consumer/persistence net-new, producer reused).",
          "status": "holds",
          "evidence": "FRESH grep `useContextChainStore|traceProvenance|_source` across src = only unrelated analytics `utm_source` hits; zero matches for the three context-chain symbols. Confirms persistence/_source/traceProvenance all absent (net-new) while the typed builder (producer) exists.",
          "fix": "None — net-new vs reuse split verified."
        },
        {
          "plan_claim": "L3a/§0 corollary: the two substring linkers (agent-spec.ts:104 substring(0,8), prompt-chain.ts:151-152 substring(0,10)) are the ONLY two, and agent-spec.ts:107 is the SOLE producer of validates_assumption — removing the match removes the only producer, so a join field is net-new.",
          "status": "holds",
          "evidence": "FRESH grep `substring(0,8)|substring(0,10)|validates_assumption`: agent-spec.ts:104 (substring(0,8)), agent-spec.ts:107 (`validates_assumption:` emitted from the matched assumption), prompt-chain.ts:151-152 (substring(0,10)x2). No other linker sites; no other validates_assumption producer. Confirms id-join field is net-new schema.",
          "fix": "None — producer-of-link verified as the substring match itself."
        },
        {
          "plan_claim": "§2/L1b/L4: buildEnhancedSystemPrompt already injects 7 learning sections (so 'smarter as you use it' is partly true) but buildInitialAnalysisPrompt takes only (problemText, locale) — the initial-analysis entry point is the genuine net-new injection gap.",
          "status": "holds",
          "evidence": "buildEnhancedSystemPrompt defined context-builder.ts:23, consumed by ReframeStep/RecastStep/SynthesizeStep (.tsx:475/302/176); test context-builder-simulation.test.ts:5 asserts '7개 섹션 조립'. buildInitialAnalysisPrompt signature progressive-prompts.ts:23 = `(problemText: string, locale: Locale = 'en')` — no userContext/enhanced param. Producer-injection present on enhanced path, absent on initial path.",
          "fix": "None — reuse (enhanced path) vs net-new (initial path) split verified."
        },
        {
          "plan_claim": "#4/L0-P1: silent auto-accept — in production the NO-CALLBACK worker-engine branch fires because ProgressiveFlow.workerCallbacks (:1176) omits onValidationFailed; the 30s-timeout branch is unreachable (a missing-consumer/E3 sibling).",
          "status": "holds",
          "evidence": "worker-engine.ts:266-285: `if (callbacks.onValidationFailed){ race(... setTimeout(...'accept',30_000)) } else { finalResult=result; break }`. ProgressiveFlow.tsx:1176 defines workerCallbacks; FRESH grep `onValidationFailed` in ProgressiveFlow.tsx = zero matches (key omitted). So the else-branch (no-callback silent accept) is the production path; the 30s branch is dead. Producer(callback wiring) missing → net-new wiring, not a config tweak.",
          "fix": "None — missing-consumer (callback) verified; plan's production-branch attribution is correct."
        },
        {
          "plan_claim": "L4/§2/#9: FinalCard is store-coupled (subscribes useProgressiveStore at :39); 'store-free FinalCard' is not literally true — the Verdict round-trip DATA is store-free but the host is not.",
          "status": "holds",
          "evidence": "FinalCard.tsx:39 `const logSession = useProgressiveStore(s => ...)`. Subscription present → host is store-coupled. runInitialAnalysis exists at progressive-engine.ts:293 (the store-free data source). Plan's host-vs-data distinction verified.",
          "fix": "None — coupling verified; mount-host decision correctly flagged as open."
        },
        {
          "plan_claim": "L4: voyageLogToMarkdown is reused (export.ts:25 → FinalCard.tsx:45) but the per-line '왜?' anchor + stable-id linkage are NET-NEW; the HiddenAssumption import at export.ts:1 is legacy string handling only.",
          "status": "holds",
          "evidence": "Producer export.ts:25 `voyageLogToMarkdown`, consumer FinalCard.tsx:45. export.ts HiddenAssumption usage is only :61-66 'Handle both old (string[]) and new (HiddenAssumption[]) format' rendering `a.assumption` — no per-line anchor, no id-based join. Base fn reused; anchor/id net-new confirmed.",
          "fix": "None — reuse base + net-new anchor split verified."
        },
        {
          "plan_claim": "L4: BranchMap gold-overlay reuses local edgePath():31 + existing activeBranchId coloring; getActivePath lives in version-tree.ts:35 (NOT inside BranchMap) — overlay strokes a gold path over existing coords, does not call getActivePath.",
          "status": "holds",
          "evidence": "BranchMap.tsx:31 defines local `function edgePath(...)`, used :82; FRESH grep shows BranchMap.tsx has NO getActivePath import (it lives version-tree.ts:35, imported by Logbook/VoyageChart/export/etc., not BranchMap). Reuse target (edgePath) is local; attribution corrected and verified.",
          "fix": "None — local reuse + corrected attribution verified."
        },
        {
          "plan_claim": "L4/#10/§UX-L1: onboarding auto-demo is UNBUILT — argus.has_onboarded does not exist; InteractiveDemo is imported (workspace/page.tsx:31) but reachable only via manual demo-card click (rendered :269 under demoScenario), never auto-opened.",
          "status": "holds",
          "evidence": "FRESH grep `has_onboarded` across src = No matches found (net-new key). InteractiveDemo imported page.tsx:31, rendered page.tsx:269 guarded by `if (demoScenario)` (:263) — only reachable when demoScenario is set (manual). No first-run auto-open producer. Reuse (component exists) but the auto-fire gate is net-new.",
          "fix": "None — net-new gate verified; component-exists vs gate-unbuilt split correct."
        },
        {
          "plan_claim": "#11/L5/L3a: progressive_sessions does NOT use mergeByTimestamp — it has its own custom merge (loadSessions :466-482, new Date() compare :477) of server-trigger updated_at vs client-clock updated_at; remote.data cast with zero validation (:467); three migrators every load; NOT soft-deletable.",
          "status": "holds",
          "evidence": "useProgressiveStore.ts:466-482 custom loop; :477 `new Date(remote.updated_at) > new Date(localSession.updated_at)`; :467 `remote.data as ProgressiveSession` (no validate); :486 migrateSessionDrafts(migrateBranches(migrateWorkers(...))); db.ts:22 SoftDeletableTable omits progressive_sessions. All verified — distinct from mergeByTimestamp, no reuse of generic merge.",
          "fix": "None — bespoke-merge claim verified; not a reuse of db.ts merge."
        },
        {
          "plan_claim": "L5/§7/#R6: db.ts merge resurrection is REAL not moot — loadAndMerge pre-filters !deleted_at (:106) but re-UPSERTS localOnly (:113-119), un-deleting cross-device tombstones; sanitizeItem strips updated_at (:32-40) so empty-stamp local rows get overwritten.",
          "status": "holds",
          "evidence": "db.ts:106 `.filter((r)=>!r.deleted_at)`; :113 `remoteIds`; :114 `localOnly = merged.filter(m=>!remoteIds.has(m.id))`; :116-118 `.upsert(localOnly...)` — re-pushes tombstoned-elsewhere rows. sanitizeItem :32-40 destructures out updated_at (:37). mergeByTimestamp :57-78 string compare :71. Mechanism (producer=upsert, the re-write path) verified — resurrection real.",
          "fix": "None — corrected mechanism (re-upsert, not 'ignores deleted_at') verified."
        },
        {
          "plan_claim": "§7/L4/#12: @react-three/* is DEAD/unused — landing illustrations are inline SVG; the '@react-three/fiber ~200KB' perf claim is WRONG.",
          "status": "holds",
          "evidence": "FRESH grep `react-three` glob=*.{ts,tsx,json}: declared package.json:21-22 + package-lock; ZERO src/ imports (all other hits are docs/*.json artifacts). No producer/consumer in app code → dead dependency confirmed; ~200KB-bundle claim is false.",
          "fix": "None — dead-dep claim verified; plan correctly de-rates the bundle-cost reuse."
        },
        {
          "plan_claim": "§2/L5/§7: useAccuracyStore rates PERSONAS (PersonaAccuracyRating) not predicates — prediction-scoring is NET-NEW write-path, not a repoint of this store.",
          "status": "holds",
          "evidence": "useAccuracyStore.ts uses PersonaAccuracyRating throughout (:2,8,10,24,35); addRating writes ACCURACY_RATINGS keyed per-persona. No predicate/outcome record type. Confirms repointing it to predicate-grading would be net-new, not reuse.",
          "fix": "None — store ownership verified; prevents a false reuse/repoint claim."
        },
        {
          "plan_claim": "§UX-L1(:238): ProgressiveLayout renders only <Logbook/> (:124); NavigatorStrip (stats hub) is only in the legacy path (:843) — surfacing the reflection tab in the default flow is net-new placement.",
          "status": "holds",
          "evidence": "workspace/page.tsx:57 `function ProgressiveLayout(...)`; :124 `<Logbook />` (its only reflection-rail render); NavigatorStrip imported :18, rendered ONLY at :843 (outside ProgressiveLayout, the legacy branch). Default-flow NavigatorStrip consumer absent → net-new surfacing confirmed.",
          "fix": "None — render-location claim verified."
        },
        {
          "plan_claim": "L5: db.ts merge / signal read path — getSignals reads ONLY localStorage though recordSignal writes both local+Supabase; every learning consumer sees <=500 local records (producer writes Supabase, consumer never reads it).",
          "status": "holds",
          "evidence": "signal-recorder.ts: recordSignal writes localStorage (:35 setStorage QUALITY_SIGNALS) AND Supabase (:38 insertToSupabase). getSignals :49 reads ONLY `getStorage<QualitySignal[]>(STORAGE_KEYS.QUALITY_SIGNALS,[])`; getSignalsByType :61-62 delegates to getSignals. No Supabase read in the consumer — producer/consumer skew verified; full-history reuse correctly NOT claimed.",
          "fix": "None — plan flags the read-path gap rather than asserting reuse; verified."
        }
      ]
    },
    {
      "error_class": "E4 — Name-association attribution (symbol/function/store/type asserted to live in a specific file)",
      "instances": [
        {
          "plan_claim": "L1 (line 126): getUserPatterns lives in useJudgmentStore.ts:14,57; getPersonaAccuracySummary in useAccuracyStore.ts:12,49 (the originally-flagged getUserPatterns→navigator.ts mis-attribution)",
          "status": "holds",
          "evidence": "grep getUserPatterns: useJudgmentStore.ts:14 `getUserPatterns: () => {` and :57 `getUserPatterns: () => {`; no navigator.ts hit. grep getPersonaAccuracySummary: useAccuracyStore.ts:12 `getPersonaAccuracySummary: (personaId: string) => {` and :49. Both definition sites match the plan exactly.",
          "fix": "None — the prior fix is correct; both attributions are VERIFIED."
        },
        {
          "plan_claim": "L1 (line 126): buildNavigatorProfile/buildNavigatorInsights/buildLearningCurve are navigator.ts builders that render inside NavigatorStrip",
          "status": "holds",
          "evidence": "grep: defined navigator.ts:95 `export function buildNavigatorProfile`, :153 `export function buildNavigatorInsights`, :792 `export function buildLearningCurve`; consumed NavigatorStrip.tsx:6 import + :198-200 calls. Producer+consumer both in named files.",
          "fix": "None — VERIFIED."
        },
        {
          "plan_claim": "L0 (line 60, 107) / §6 (line 316): reuse validateShape 'from llm.ts:778-781' / 'validateShape already wired llm.ts:778-781'",
          "status": "needs_tag",
          "evidence": "grep `function validateShape`: the DEFINITION is `llm.ts:282` `export function validateShape<...>`. Read of llm.ts:778-781 shows the CALL/WIRING site: `const validated = options.shape ? validateShape<...>(parsed, options.shape) as T : parsed;`. So :778-781 is the wiring, not the symbol's home.",
          "fix": "Keep §1/§6 'wired at llm.ts:778-781' (accurate as a wiring site). But L0 line 107 says 'reuse validateShape from llm.ts:778-781' — the importable definition is llm.ts:282. Re-tag the L0 reuse pointer to '[VERIFIED] validateShape defined llm.ts:282, wired :778-781' so a reader imports from the export site, not the call site."
        },
        {
          "plan_claim": "L1 (line 134): getSignals/getSignalsByType (signal-recorder.ts:49) read only localStorage",
          "status": "needs_tag",
          "evidence": "grep: getSignals defined signal-recorder.ts:44 `export function getSignals(filter?`; getSignalsByType defined signal-recorder.ts:61 `export function getSignalsByType(...)`. Neither is at :49. The file is correct; the single line number :49 matches neither definition.",
          "fix": "Correct the locator: getSignals → signal-recorder.ts:44, getSignalsByType → signal-recorder.ts:61 (cite both, not a single :49). File attribution holds; line number is imprecise (E1-adjacent)."
        },
        {
          "plan_claim": "L1 (line 130): 'the WORKER stage runAllAIWorkers DOES fire one call per persona — worker-engine.ts:141-151'",
          "status": "needs_tag",
          "evidence": "grep: `export async function runAllAIWorkers` is at worker-engine.ts:201, NOT :141-151. Read of :141-151 shows a `callLLMStream([{role:'user',content:finalUser}], {system, maxTokens...}, {...})` inside a per-worker runner helper (the actual per-call site), not the body of runAllAIWorkers.",
          "fix": "Keep the behavioral claim (one LLM call per worker — supported by the callLLMStream at :141-151). But attribute the FUNCTION correctly: runAllAIWorkers is defined at worker-engine.ts:201; the per-worker LLM call is at :141-151 inside the worker-runner helper. Cite '[VERIFIED] per-worker callLLMStream worker-engine.ts:141-151; runAllAIWorkers entry :201'."
        },
        {
          "plan_claim": "L2 (line 155): three load-path migrators 'migrateBranches/migrateWorkers/migrateSessionDrafts (:356/391, chained at :447/486)'",
          "status": "needs_tag",
          "evidence": "grep in useProgressiveStore.ts: migrateWorkers:330, migrateSessionDrafts:356, migrateBranches:391; chained at :447 `migrateSessionDrafts(migrateBranches(migrateWorkers(local)))` and :486. The two cited line numbers :356/:391 map to migrateSessionDrafts(:356) and migrateBranches(:391) — the NAME order in the plan ('migrateBranches/migrateWorkers/migrateSessionDrafts') does not line up with :356/391, and migrateWorkers (:330) is uncited.",
          "fix": "All three live in useProgressiveStore.ts (attribution holds). Fix the line pairing: migrateWorkers:330, migrateSessionDrafts:356, migrateBranches:391; chain :447/:486. The chained sites are correct as written."
        },
        {
          "plan_claim": "L4 (line 187): phaseIdx 'not exported from ProgressiveFlow.tsx:132'; getActivePath 'lives in version-tree.ts:35/branch-map-layout.ts, NOT inside BranchMap'; edgePath BranchMap:31",
          "status": "holds",
          "evidence": "grep phaseIdx: ProgressiveFlow.tsx:132 `function phaseIdx(...)` (no `export`). grep getActivePath: defined version-tree.ts:35 `export function getActivePath<...>`; consumed branch-map-layout.ts:75; zero definition in BranchMap.tsx. grep edgePath: BranchMap.tsx:31 `function edgePath(...)`. All three attributions exact.",
          "fix": "None — VERIFIED. (Note branch-map-layout.ts only CONSUMES getActivePath; version-tree.ts:35 is the sole definition — the slash-list reads fine since the plan says 'lives in version-tree.ts:35/branch-map-layout.ts'.)"
        },
        {
          "plan_claim": "L4 (line 188): voyageLogToMarkdown lives in export.ts:25-49, used by FinalCard.tsx:45",
          "status": "holds",
          "evidence": "grep: defined export.ts:25 `export function voyageLogToMarkdown(session: ProgressiveSession | null | undefined, locale)`; consumed FinalCard.tsx:8 import + :45 `voyageLogToMarkdown(logSession, locale...)`. Producer+consumer confirmed.",
          "fix": "None — VERIFIED."
        },
        {
          "plan_claim": "L5 (line 201) / §0 (line 52) / line 24: mergeByTimestamp (db.ts:57-78); sanitizeItem (db.ts:30-40); insertToSupabase (db.ts:275-290); loadAndMerge",
          "status": "holds",
          "evidence": "grep db.ts: `export function mergeByTimestamp<...>` :57; `function sanitizeItem(item: any)` :32 (within the cited 30-40 range); `export async function loadAndMerge<...>` :87; `export async function insertToSupabase(table, item)` :275. All four in db.ts as attributed.",
          "fix": "None — VERIFIED. (sanitizeItem def is precisely :32, inside the plan's :30-40 range; acceptable.)"
        },
        {
          "plan_claim": "L3a (line 166): mergeAssumptionsIntoKeyAssumptions (context-chain.ts:506-525); buildReframeContext (:134-136 normalizer, defined earlier)",
          "status": "holds",
          "evidence": "grep context-chain.ts: `export function buildReframeContext(item: ReframeItem)` :119; `export function mergeAssumptionsIntoKeyAssumptions(` :506. Both in context-chain.ts. The plan's :506-525 / :134-136 are body-line ranges within these functions.",
          "fix": "None — file attribution VERIFIED. (buildReframeContext header is :119; the :134-136 cited is the legacy-string coercion inside its body — consistent.)"
        },
        {
          "plan_claim": "L4 (line 182): addRating lives in useAccuracyStore and 'is called only at tools/FeedbackResult.tsx:66'",
          "status": "holds",
          "evidence": "grep addRating: defined useAccuracyStore.ts:34 `addRating: (data) => {` (type :10); only non-test caller is components/tools/FeedbackResult.tsx:66 `addRating({`. No other production callsite. Attribution + sole-caller claim both hold.",
          "fix": "None — VERIFIED."
        },
        {
          "plan_claim": "L0 (line 109): recommendBlindSpotPersona (auto-persona fallback) lives in auto-persona.ts",
          "status": "holds",
          "evidence": "grep: `export function recommendBlindSpotPersona(` auto-persona.ts:139; consumed RehearseStep.tsx:34 import + :110. Attribution correct.",
          "fix": "None — VERIFIED."
        },
        {
          "plan_claim": "§5 (line 299): EASE/SPRING exported from progressive/shared/constants.ts (EASE=[0.32,0.72,0,1], SPRING object constants.ts:4)",
          "status": "holds",
          "evidence": "grep: components/workspace/progressive/shared/constants.ts:3 `export const EASE = [0.32, 0.72, 0, 1] as const;` and :4 `export const SPRING = { type: 'spring' as const, stiffness: 400, damping: 30 };`. Exact.",
          "fix": "None — VERIFIED. (A second local `const EASE` exists in AgentProfile.tsx:12 but the plan attributes the shared export to constants.ts, which is correct.)"
        },
        {
          "plan_claim": "§0 (line 47) / line 19: OUTCOME_RECORDS read at context-builder.ts:61; addRating writes ACCURACY_RATINGS → fed at context-builder.ts:216",
          "status": "holds",
          "evidence": "grep: OUTCOME_RECORDS key defined storage.ts:13 `OUTCOME_RECORDS: 'sot_outcome_records'`; read context-builder.ts:61 `getStorage<OutcomeRecord[]>(STORAGE_KEYS.OUTCOME_RECORDS,...)`. ACCURACY_RATINGS read context-builder.ts:216; written useAccuracyStore.ts:42. Both attributions hold.",
          "fix": "None — VERIFIED."
        },
        {
          "plan_claim": "L0 (line 101) / CHANGELOG #3: the steps-accepted/actor-overrides evals live in eval-engine.ts:183/184 (RECAST_EVALS 182-187), reached via recordRecastEval → RecastStep.tsx:412",
          "status": "holds",
          "evidence": "Read eval-engine.ts:182-187: RECAST_EVALS array; :183 steps_accepted `if(!item.analysis) return false; ...item.analysis.steps.length - item.steps.length`; :184 actor_overrides_low `if(!item.analysis) return true; const t = item.analysis.steps.length`. recordRecastEval defined :189; grep shows call at RecastStep.tsx:412 `m.recordRecastEval(current, oc)`. All exact.",
          "fix": "None — VERIFIED (one of the most precise claims in the plan)."
        },
        {
          "plan_claim": "L3a (line 164): substring linkers — agent-spec.ts:104 (substring(0,8)) and prompt-chain.ts:151-152 (substring(0,10)); validates_assumption derived agent-spec.ts:107; align agent-spec.ts:7 projectFilter(project)",
          "status": "holds",
          "evidence": "grep: agent-spec.ts:104 `step.task.includes(a.assumption.substring(0, 8))`, :107 `validates_assumption: ...a.assumption.slice(0,50)`; prompt-chain.ts:151 & :152 both `a.assumption.substring(0, 10)`. projectFilter is DEFINED in output-helpers.ts:20 but the plan cites only the CALL `agent-spec.ts:7 const pf = projectFilter(project)` — accurate as a usage reference, not a definition claim.",
          "fix": "None — VERIFIED. (Plan correctly never claims projectFilter is DEFINED in agent-spec.ts; it cites the :7 call. Definition lives in output-helpers.ts:20.)"
        },
        {
          "plan_claim": "L2 (line 152, line 20): useProgressiveStore statically imports ONLY useAgentStore; usePersonaStore is a lazy require() at :946; IntersectionObserver NOT in the chassis (only useScrollReveal.ts)",
          "status": "holds",
          "evidence": "grep useProgressiveStore.ts: only static store import is :6 `import { useAgentStore }`; :946 `const { usePersonaStore: pStore } = require('@/stores/usePersonaStore')`. grep IntersectionObserver across src: single hit hooks/useScrollReveal.ts:15. All three hold.",
          "fix": "None — VERIFIED."
        },
        {
          "plan_claim": "L3a (line 163): no useContextChainStore, no _source, no traceProvenance (all grep-confirmed non-existent)",
          "status": "holds",
          "evidence": "grep traceProvenance|useContextChainStore across src: 'No matches found'. (An earlier broad grep matched _source only via UTM `source` substrings in analytics.ts/utm-builder/daily-report — not the context-chain symbols.) The two named symbols genuinely do not exist.",
          "fix": "None — VERIFIED (this is E3/E5 non-existence, but confirms no mis-attribution exists for these symbols)."
        },
        {
          "plan_claim": "§4 (line 285): landing_hero_submit emitted in ZERO app code; landing_cta_click live at Act3OnDeck.tsx:88 with {cta:'voyage_helm'}; daily-report/route.ts:329 buckets both",
          "status": "holds",
          "evidence": "grep landing_hero_submit|landing_cta_click across src: only two hits — Act3OnDeck.tsx:88 `track('landing_cta_click', { cta: 'voyage_helm' })` and daily-report/route.ts:329 bucket `keys: ['landing_hero_submit','landing_cta_click']`. landing_hero_submit has zero emit site. Exact.",
          "fix": "None — VERIFIED."
        },
        {
          "plan_claim": "§5 (line 298, 302) / §7 (line 338): Card.tsx forwardRef :23 + variant-map :10, variants reference --accent-light #b8963e (:12,32); --accent #96782e at globals.css:6 with false 'contrast-safe (4.5:1+)' comment",
          "status": "holds",
          "evidence": "Read Card.tsx: :10 `const variantStyles: Record<...>`; :12 `ai: ...border-[var(--accent-light)]/15`; :23 `export const Card = forwardRef<...>`; :32 hover `hover:border-[var(--accent-light)]/30`. Read globals.css:6 `--accent: #96782e; /* Gold — contrast-safe on white (4.5:1+) */`; :7 `--accent-light: #b8963e`. All exact.",
          "fix": "None — VERIFIED."
        },
        {
          "plan_claim": "Type homes in stores/types.ts: HiddenAssumption (:11-18), KeyAssumption (:136-141), RecastStep (:119-134), MaterializedRisk (:487-493), ClassifiedRisk (:255/362), OutcomeRecord (:502-513), governing_idea (:158)",
          "status": "holds",
          "evidence": "grep types.ts: HiddenAssumption interface :11; RecastStep :119; KeyAssumption :136; governing_idea :158; ClassifiedRisk interface :245 (field refs at :255/:362 plausible); MaterializedRisk :487; OutcomeRecord :502. Every interface header matches the plan's start line.",
          "fix": "None — interface attributions VERIFIED. (ClassifiedRisk interface header is :245; the plan's :255/362 are field-occurrence references for classified_risks, not the interface — consistent with usage.)"
        },
        {
          "plan_claim": "L4 (line 186) / §UX: InteractiveDemo imported workspace/page.tsx:31; judgment-vitality.ts & observation-engine.ts are real read-only substrate; getStepCoaching/getObservationsSummary homes",
          "status": "holds",
          "evidence": "grep: InteractiveDemo defined components/workspace/InteractiveDemo.tsx:1340, imported workspace/page.tsx:31. judgment-vitality.ts & observation-engine.ts exist and are imported widely. getStepCoaching navigator.ts:326; getObservationsSummary user-context.ts:183. All attributions hold.",
          "fix": "None — file attributions VERIFIED. (Caveat outside E4: observation-engine has writers e.g. onTaskApproved called from useProgressiveStore.ts:11 — 'read-only substrate' is an E2/E3 nuance, not an attribution error.)"
        }
      ]
    },
    {
      "error_class": "E5",
      "instances": [
        {
          "plan_claim": "L2 / changelog #7: 'and 46 useState/useRef/useEffect hooks' in ProgressiveFlow.tsx (the UIState target).",
          "status": "violates",
          "evidence": "Fresh grep of src/components/workspace/progressive/ProgressiveFlow.tsx: `grep -oE 'useState<|useState\\(' = 21`; `useRef<|useRef\\( = 15`; `useEffect\\( = 7`. Total declared hook call-sites = 21+15+7 = 43, NOT 46. (Raw substring occurrences of the three tokens incl. the line-3 react import and setter callbacks = 49; neither figure equals 46.) Enumerated lines: useState at 501,779,780,892,895-922,938,1064,1065; useRef at 923-936,1134,1135; useEffect at 962,965,978,988,1066,1074,1136.",
          "fix": "Replace '46 useState/useRef/useEffect hooks' with '43 hook call-sites (21 useState + 15 useRef + 7 useEffect)' or tag the figure (corrected: 43)."
        },
        {
          "plan_claim": "changelog #12 / §7 Do-Not-Inherit table: parseJSON real surface is '~5 direct + 8 callLLMStreamThenParse callsites'.",
          "status": "violates",
          "evidence": "Fresh grep `grep -rn 'callLLMStreamThenParse<' src --include=*.ts --include=*.tsx | grep -v test | grep -v 'export async function'` returns exactly 7 invocations: progressive-engine.ts:307,385,458,641,776,812,895. Total occurrences = 9 (7 calls + 1 definition llm.ts:765 + 1 import :5). The '~5 direct' part holds (RecastStep.tsx:337, ReframeStep.tsx:500, ReframeStep.tsx:659, llm.ts:538, llm.ts:777 = 5), but callLLMStreamThenParse callsites = 7, not 8.",
          "fix": "Correct '8 callLLMStreamThenParse callsites' to '7' (progressive-engine.ts only)."
        },
        {
          "plan_claim": "L1b i18n consolidation: 'Replace the ~15 duplicated inline const L = (ko,en)=>… in ProgressiveFlow'.",
          "status": "violates",
          "evidence": "Fresh grep `grep -cE 'const L = \\(|const L=\\(' ProgressiveFlow.tsx` = 9. Enumerated definition lines: 66, 283, 416, 500, 656, 778, 831, 861, 887. (There are 142 `L(` call-sites but only 9 `const L =` helper definitions.) '~15' overstates the duplicated-definition count by ~67%.",
          "fix": "Correct '~15 duplicated inline const L' to '9 duplicated const L definitions' (or clarify if the 142 call-sites were meant)."
        },
        {
          "plan_claim": "§5 reduced-motion bullet: 'No useReducedMotion() anywhere in src (36 framer-motion importers)'.",
          "status": "violates",
          "evidence": "Fresh grep `grep -rln \"from 'framer-motion'\" src --include=*.tsx --include=*.ts` (no test importers exist) = 38, not 36. (useReducedMotion = 0 hits, exit 1 — that half is VERIFIED.) The framer-motion importer count is understated by 2.",
          "fix": "Correct '36 framer-motion importers' to '38'."
        },
        {
          "plan_claim": "L1b lint note: 'only 21 of 141 component files import @/lib/i18n'.",
          "status": "violates",
          "evidence": "The numerator 21 is correct (`grep -rln '@/lib/i18n' src --include=*.tsx | grep -v test = 21`). The denominator 141 matches no fresh count: `find src -name '*.tsx' | grep -v test = 137`; incl. tests = 142; `find src/components -name '*.tsx' = 102`. No basis yields 141.",
          "fix": "Re-derive the denominator: state '21 of 137 non-test .tsx files' (or '21 of 102 files in src/components') and drop the unverifiable 141."
        },
        {
          "plan_claim": "§UX-GLOBAL-CHROME: 'Six-item nav with locks … three lock-gated items contradict the 로그인 없이 무료 promise' (Header.tsx:16-23,105,118); 'collapse Agents/Manager/Projects into a 더보기 overflow'.",
          "status": "violates",
          "evidence": "Fresh read of Header.tsx:16-22: navItems = 6 (Workspace, Projects, Agents, Manager, Guide, Settings) — '6-item' is correct. But `grep -c 'requiresAuth: true'` = 2, only Projects (:18) and Agents (:19). Manager(:20)/Guide(:21)/Settings(:22) have no requiresAuth, so the lock (showLock = requiresAuth && !user) fires on 2 items, not 3. The 'collapse Agents/Manager/Projects' set wrongly includes Manager as lock-gated.",
          "fix": "Correct 'three lock-gated items' to 'two lock-gated items (Projects, Agents)'; drop Manager from the locked set."
        },
        {
          "plan_claim": "buildEnhancedSystemPrompt 'already injects 7 learning sections' (§2 TAKE-NOW userContext bullet / L1b).",
          "status": "needs_tag",
          "evidence": "Fresh read of context-builder.ts:23-94: the function comments number sections 1-7, but slot #4 is '(Removed)… Intentionally no-op' and pushes nothing. Active sections that can push are 1,2,3,5,6,7 = max 6. Each is also conditional (gated by data presence). The 7-count includes a dead slot.",
          "fix": "Change '7 learning sections' to 'up to 6 active learning sections (one of seven numbered slots is a removed no-op)' or tag [ASSUMPTION: count includes a dead slot]."
        },
        {
          "plan_claim": "L4 lazy-load: 'ShipCutaway.tsx = 1011 lines / 148 SVG primitives, SailingShip.tsx = 505 / 69 + 14 framer-motion loops'.",
          "status": "needs_tag",
          "evidence": "Line counts VERIFIED (`wc -l` ShipCutaway=1011, SailingShip=505). Primitive counts are off under a straightforward grep: `grep -oE '<(path|rect|circle|line|polygon|ellipse|polyline)'` = ShipCutaway 155 (plan 148), SailingShip 72 (plan 69). '14 framer-motion loops' = SailingShip has 14 `animate=` props (VERIFIED 14) but 0 `motion.` and 0 `repeat: Infinity` — these are SVG/animate, not framer loops.",
          "fix": "Either tag the primitive counts [ASSUMPTION: grep yields 155/72 depending on which elements counted] or recount with a fixed element set; relabel '14 framer-motion loops' as '14 animate= props (SVG animations, not framer loops)'."
        },
        {
          "plan_claim": "§UX-L0 'Outputs surface hides the 4 Ithaca outputs' / 'the 5-format OutputSelector' and §UX-L4 jargon-replacement set 'OutputSelector.tsx:38/48/58/68'.",
          "status": "needs_tag",
          "evidence": "Fresh read of OutputSelector.tsx getFormats: there are 5 formats — brief(:38), prompt-chain(:48), agent-spec(:58), checklist(:68), rationale(:78/key 'rationale'). The plan calls it both '4 Ithaca outputs' and '5-format'. The §UX-L4 set :38/48/58/68 lists only the 4 with orchestra jargon (rationale at :78 already uses plain '판단 근거서'), so the 4-set is intentional — but the surrounding prose '4 Ithaca outputs' is inconsistent with the verified 5-format surface.",
          "fix": "Make the labels consistent: state the surface has 5 formats (brief, prompt-chain, agent-spec, checklist, rationale); the 4-member jargon-relabel set (:38/48/58/68) excludes rationale by design — note that explicitly."
        },
        {
          "plan_claim": "L1b example: 'BranchMap.tsx has Korean and no i18n helper' (used to motivate a lint failing on literal Hangul in JSX).",
          "status": "needs_tag",
          "evidence": "Fresh check: BranchMap.tsx does NOT import @/lib/i18n (grep exit 1) — 'no i18n helper' holds. But `grep '[가-힣]+'` shows its only Hangul is line 4, a block COMMENT ('the 해도\\'s branching course-graph'), not user-facing JSX. The example would NOT trip a 'literal Hangul in JSX' lint.",
          "fix": "Replace the BranchMap example with a file that has Hangul in JSX (not just a comment), or qualify the example as 'Hangul in a comment'."
        },
        {
          "plan_claim": "L2 coupling bullet: 'Real coupled concerns: scroll-into-view (:951 + 28 scroll refs)'.",
          "status": "holds",
          "evidence": "scrollIntoView is at ProgressiveFlow.tsx:951 (`ref.current.scrollIntoView(...)`) — VERIFIED (and it is in the COMPONENT, not the store; the store useProgressiveStore.ts has 0 'scroll' occurrences and :951 there is mid-persona-match). '28 scroll refs' reconciles as 23 `scrollToRef(` + 5 `scroll(` invocations = 28 (`grep -oE` counts). Holds as enumerated.",
          "fix": "No fix needed; optionally note :951 is the ProgressiveFlow line (the store has no scroll), to prevent E4 confusion with the adjacent store-:946/:951 citations."
        },
        {
          "plan_claim": "changelog #7 / L2: store statically imports ONLY useAgentStore; usePersonaStore is a single lazy require() at :946; NO reframe/recast/project store import; IntersectionObserver NOT used in chassis (only useScrollReveal.ts).",
          "status": "holds",
          "evidence": "`grep \"from '@/stores/'\" useProgressiveStore.ts` = useAgentStore(:6) + agent-types(:8,9 types) + types(:40); only store import is useAgentStore. `grep require(` = single hit :946 usePersonaStore. No usePersonaStore/useReframeStore/useRecastStore/useProjectStore static import. `grep -rln IntersectionObserver src` = only src/hooks/useScrollReveal.ts; 0 in chassis dir/store.",
          "fix": "None — VERIFIED accurate."
        },
        {
          "plan_claim": "changelog #12: JSON.parse is ~17 non-test, ~11 real app callsites (6 are llm.ts engine internals); @react-three/* is dead (3 dead 3D deps); the 'only two' substring linkers (agent-spec:104, prompt-chain:151-152).",
          "status": "holds",
          "evidence": "`grep -rn 'JSON\\.parse(' src --include=*.ts --include=*.tsx | grep -v test` = 17; llm.ts subset = 6; 17-6 = 11. VERIFIED. `grep -rn '@react-three' src` = 0 usages; package.json has 3 deps (@react-three/drei:21, @react-three/fiber:22, three:32) — dead, 3 deps. VERIFIED. `grep '\\.substring(0,' src | grep -v test` = exactly agent-spec.ts:104 (0,8) + prompt-chain.ts:151,152 (0,10) = 2 linkers. VERIFIED.",
          "fix": "None — these counts/sets are accurate."
        },
        {
          "plan_claim": "L3b: '31 files emit/consume hidden_assumptions:/key_assumptions:'; L3a convergence: is_converged/estimated_rounds_left rendered in 0 .tsx; OUTCOME_RECORDS has zero production writers; L2 regression set (only voyage-branch exercises chassis); the 6 infinite-animation set in §5; the 3 placeholder inputs :813/:2167/:2615; MAX_BRANCHES=8; signals 500 / evals 200 caps; 3 migrators.",
          "status": "holds",
          "evidence": "`grep -rl 'hidden_assumptions:|key_assumptions:' src` (with colon, as written) = 31. VERIFIED. is_converged/estimated_rounds_left in .tsx = 0 (exit 1). OUTCOME_RECORDS: only read context-builder.ts:61 + key storage.ts:13; no setStorage/addOutcome/recordOutcome (0 writers). Of voyage-branch/orchestrator-journey/workflow-review-integration, only voyage-branch-simulation.test.ts imports useProgressiveStore/ProgressiveFlow. §5 6 infinite anims all at cited lines (pulse-skeleton:266, gold-shimmer:283, border-glow:292, subtle-pulse:360, bm-bounce:1264, inner-glow:1590). 3 placeholder inputs at 813/2167/2615 (grep -c placeholder= = 3). MAX_BRANCHES=8 (:47,:1480). MAX_LOCAL_SIGNALS=500; eval >200 splice (:148,:194,:218). 3 migrators migrateWorkers:330/migrateSessionDrafts:356/migrateBranches:391 chained :447,:486.",
          "fix": "None — all VERIFIED accurate."
        }
      ]
    },
    {
      "error_class": "E6 — Mechanism unchecked (proposed FIX relies on an API/ownership contract that does not hold as written)",
      "instances": [
        {
          "plan_claim": "§0/§7/L0 P0 (lines 14, 96, 329): the stream watchdog must NOT abort via `options.signal` (externally supplied) — use a NEW internal AbortController OR `reader.cancel()`, and reject with a DISTINCT non-Abort `'timeout'` error or the handler guard swallows it.",
          "status": "holds",
          "evidence": "llm.ts:14-21 `LLMOptions.signal?: AbortSignal` is a caller-supplied input; llm.ts:679 passes `signal: options.signal` into fetch — callLLMStream owns no AbortController. llm.ts:749-751 maps `error.name==='AbortError'` → `onError(new LLMError(..., {category:'network'}))` i.e. a user-cancel. ProgressiveFlow.tsx:1397 and :1717 both read `if (!(e instanceof DOMException && e.name === 'AbortError')) setError(...)` — a bare signal.abort() is silently swallowed. The plan's correction is mechanically exact.",
          "fix": "None — already corrected in the plan; the contract is verified."
        },
        {
          "plan_claim": "L0 P1 (line 105): surface a storage-quota toast 'via the existing CustomEvent pattern (llm.ts:721-724 → PingToast/UnlockToast/RateLimitBadge)'.",
          "status": "violates",
          "evidence": "Only RateLimitBadge actually uses a window CustomEvent: RateLimitBadge.tsx:23 `window.addEventListener('argus:ratelimit', handler)` consuming llm.ts:722 `window.dispatchEvent(new CustomEvent('argus:ratelimit'...))`. PingToast.tsx:6,17 subscribes to the ZUSTAND store `useAgentAttentionStore` (lastPingAt/lastPingSource) — NOT a CustomEvent. UnlockToast.tsx:13 subscribes to `useAgentStore` (lastUnlockedIds) — NOT a CustomEvent. So 2 of the 3 cited 'pattern examples' are store-driven, not event-driven. (SyncStatus.tsx:39 `argus:sync` is the only other real CustomEvent consumer.)",
          "fix": "Cite RateLimitBadge/SyncStatus as the CustomEvent precedent, not PingToast/UnlockToast. The mechanism (storage.ts dispatches a new window CustomEvent + a new listener-toast) is still correct and is the RIGHT choice precisely because storage.ts is a low-level lib — note its self-warning comment (storage.ts:42 'Use console directly to avoid circular dependency with logger'); routing through a store (the PingToast/UnlockToast pattern) would force storage.ts to import a store and risk that very circular dependency. State the contract as: window CustomEvent (no store import)."
        },
        {
          "plan_claim": "L4 Onboarding (line 186): implement the gate `projects.length===0 && !localStorage['argus.has_onboarded']` 'in the if(!currentProjectId) branch at :690 (wrapping/preceding the <HeroFlow> return) → auto-open InteractiveDemo.tsx (… reachable via manual demo-card click :444) … HeroFlow currently owns the demo trigger internally'.",
          "status": "violates",
          "evidence": "page.tsx:633 `function WorkspaceContent()` is the parent; :638 holds `currentProjectId`; :690 `if (!currentProjectId)` returns `<HeroFlow .../>` at :692. But InteractiveDemo is mounted ONLY by page.tsx:263 `if (demoScenario)` return, driven by `demoScenario` state declared at :156 INSIDE `function HeroFlow` (:145-632) — the demo cards (:444 `onClick={()=>setDemoScenario(s)}`), marketing copy (:458), and the :263 demo return are ALL inside HeroFlow's body, NOT in WorkspaceContent. So the :690 gate (parent scope) has NO access to HeroFlow's local `setDemoScenario`; a JSX wrapper at :690 cannot 'auto-open InteractiveDemo'. The two surfaces the plan treats as one are separated by a component boundary.",
          "fix": "Place the gate as a state-driven effect INSIDE HeroFlow (it owns `demoScenario`): on mount, if `projects.length===0 && !localStorage['argus.has_onboarded']` call HeroFlow's local `setDemoScenario(defaultScenario)` (→ triggers the existing :263 InteractiveDemo return) then set the flag. Do NOT describe it as 'wrapping the :690 <HeroFlow> return' — that branch is in WorkspaceContent and out of scope for the demo setter. (Note: the plan's own 'HeroFlow owns the demo trigger internally' is the correct observation that contradicts its proposed :690 placement.)"
        },
        {
          "plan_claim": "L5/§7 db.ts merge (lines 201, 337): the resurrection bug is NOT 'merge ignores deleted_at' — `loadAndMerge:113-119` re-UPSERTS localOnly tombstoned rows; fix = fetch tombstones, exclude tombstoned ids from re-upsert, Date-compare with a missing-local-stamp tie-break.",
          "status": "holds",
          "evidence": "db.ts:106 `const remote = ((data||[]) as any[]).filter((r) => !r.deleted_at)` — tombstones ARE fetched by the :97-101 `.select('*')` then dropped in JS, so they are recoverable for the fix. db.ts:113-118 `remoteIds = new Set(remote.map(r=>r.id)); localOnly = merged.filter(m=>!remoteIds.has(m.id)); ... .upsert(localOnly...)` — re-upserts rows that are absent from remote because they were tombstoned → resurrection. db.ts:69-71 `remoteTime = item.updated_at||item.created_at||''; ... if (remoteTime > localTime)` is a string compare. db.ts:32-40 `sanitizeItem` strips `updated_at`, so a fresh local row has '' stamp and loses to any remote (the empty-stamp bug). All contract elements verified; the fix is feasible against this API.",
          "fix": "None — mechanism and fix are sound. (Minor: the fix must also keep filtering tombstones out of the `merged` returned to the UI, since :106 currently excludes them only from `remote`, not from local-origin rows.)"
        },
        {
          "plan_claim": "L5 progressive_sessions second merge (line 202): `loadSessions` (:466-482) compares server-trigger `updated_at` vs client-clock `updated_at` via `new Date()` (:477); fire-and-forget swallowed-catch; .limit(100) load; NOT in SoftDeletableTable.",
          "status": "holds",
          "evidence": "useProgressiveStore.ts:456 selects server `updated_at`; :459 `.limit(100)`; :467 `remote.data as ProgressiveSession` cast with zero validation; :474-478 compares `new Date(remote.updated_at) > new Date(localSession.updated_at)` — server-stamped vs client-stamped Date compare. db.ts:22 `SoftDeletableTable` = projects|personas|reframe_items|recast_items|synthesize_items — excludes progressive_sessions. Contract verified.",
          "fix": "None — mechanism claims hold."
        },
        {
          "plan_claim": "L1b (line 143): wire observation context into `buildInitialAnalysisPrompt` (progressive-prompts.ts:23, takes only (problemText, locale)) via `참고:` reference-only, wrapped in `<user-data>` + `sanitizeForPrompt()`.",
          "status": "holds",
          "evidence": "progressive-prompts.ts:23 `export function buildInitialAnalysisPrompt(problemText: string, locale: Locale = 'en')` — exactly two params, no userContext. `sanitizeForPrompt` is a real export at persona-prompt.ts:23 (matches CLAUDE.md's stated home). The injection mechanism's dependencies exist and are importable.",
          "fix": "None — contract holds."
        },
        {
          "plan_claim": "L0 P1 analytics canary (line 111) & §4 (line 285): `analytics.ts:168-179` wraps an async supabase.insert in try/catch that CANNOT catch the rejection; `.then` at :176 has no `.catch` → unhandled rejection. Fix: add `.catch` + a per-session failure counter.",
          "status": "holds",
          "evidence": "analytics.ts:168 `try {` … :169-176 `supabase.from('user_events').insert({...}).then(() => {/* fire and forget */});` … :177 `} catch {}`. The synchronous try/catch only catches a throw while BUILDING the call; the promise returned by `.insert().then()` rejects asynchronously and escapes the catch. There is no `.catch` on the chain. The canary mechanism (append `.catch(()=>counter++)`) is contract-valid.",
          "fix": "None — mechanism holds."
        },
        {
          "plan_claim": "L1/§UX (lines 128, 236): convergence fields `is_converged`/`estimated_rounds_left`/`trend` are computed but rendered nowhere; promote a gauge that 'reads the already-computed fields' (no new compute).",
          "status": "holds",
          "evidence": "Fresh grep `is_converged|estimated_rounds_left` across src/**/*.tsx → No matches found (0 render sites). progressive-convergence.ts:95-107 computes `trend`, :109 `const is_converged = totalScore >= 75`, :110 `const estimated_rounds_left = ...`. Producer confirmed, consumers absent — the 'read existing fields' mechanism is valid.",
          "fix": "None — mechanism holds."
        },
        {
          "plan_claim": "L4 BranchMap gold overlay (line 187): reuse local `edgePath()` (:31) + existing `activeBranchId` node-coloring; `getActivePath` lives in version-tree.ts:35 / branch-map-layout.ts, NOT inside BranchMap — the overlay strokes a gold path over existing coordinates, it does not call getActivePath.",
          "status": "holds",
          "evidence": "BranchMap.tsx:31 `function edgePath(px,py,cx,cy)` is local; :38 `activeBranchId` is a prop. `getActivePath` definition is at version-tree.ts:35; branch-map-layout.ts:16 imports it; BranchMap.tsx does NOT appear among the getActivePath callers in a fresh repo-wide grep. The 'overlay over existing coordinates' mechanism is contract-correct.",
          "fix": "None — mechanism holds."
        },
        {
          "plan_claim": "L4 core-flow re-skin (line 187): `phaseIdx()` is 'not exported from ProgressiveFlow.tsx:132 — lift to a shared module first'.",
          "status": "holds",
          "evidence": "ProgressiveFlow.tsx:132 `function phaseIdx(phase: string, round: number, hasMix: boolean): number {` — a plain local function with no `export`. The 'lift to a shared module' prerequisite is mechanically required for LegCanvas/AnchoringMoment/ConvergenceRose to reuse it.",
          "fix": "None — mechanism holds."
        },
        {
          "plan_claim": "L4 attributable Brief (line 188): four-act Brief on `voyageLogToMarkdown` (:25-49, used by FinalCard.tsx:45); NEW caveat — it returns '' with no waypoints (:35), so the Brief renders empty for sessions that never logged waypoints.",
          "status": "holds",
          "evidence": "export.ts:25 `voyageLogToMarkdown(session, locale): string`; :26 `if (!session) return ''`; :35 `if (wps.length === 0) return ''`. FinalCard.tsx:45 `const voyageLog = voyageLogToMarkdown(logSession, ...)`. Both empty-return guards confirmed — the 'verify waypoint coverage or Brief renders empty' caveat is contract-accurate (and there are TWO empty paths: no-session and no-waypoints).",
          "fix": "None — mechanism/caveat holds (optionally note the :26 no-session empty path too)."
        },
        {
          "plan_claim": "§UX-L0 (line 214): `runInitialAnalysis` is called WITHOUT the `signal?` it accepts (progressive-engine.ts:293-296); fix = wire an AbortController + Cancel into handleSubmit.",
          "status": "holds",
          "evidence": "progressive-engine.ts:293-296 `runInitialAnalysis(problemText: string, onToken?: ..., signal?: AbortSignal)`. Sole production call at workspace/page.tsx:221 `await runInitialAnalysis(text, (token) => {...})` passes only 2 args — `signal` omitted. The Cancel mechanism is contract-supported (the param already exists and threads to llm.ts).",
          "fix": "None — mechanism holds."
        },
        {
          "plan_claim": "§UX-L0 (lines 223-224): ErrorBoundary 'Try again' only clears hasError (:31-33) → re-crashes; useLocaleSwitch reload is unconditional (:18-22).",
          "status": "holds",
          "evidence": "ErrorBoundary.tsx (resolved to src/components/layout/ErrorBoundary.tsx):31-33 `handleRetry = () => { this.setState({ hasError: false, error: null }); }` — no remount/route reset. useLocaleSwitch.ts:18-22 `switchTo` → :21 `window.location.reload()` with no in-progress guard. Both fix mechanisms (add a route-reset action; gate the reload) are contract-valid.",
          "fix": "None — mechanisms hold."
        },
        {
          "plan_claim": "§0 corollary (line 50) & L3a (lines 164-165): grade must join on a stable predicate id; add `id` to HiddenAssumption/KeyAssumption and a `validates_assumption_ids` field on RecastStep, replacing the substring linkers `agent-spec.ts:104` and `prompt-chain.ts:151-152`.",
          "status": "holds",
          "evidence": "types.ts:119-134 `RecastStep` has NO assumption-id field; types.ts:136-141 `KeyAssumption` has NO id; types.ts:487-493 `MaterializedRisk` keys by risk_text+persona_id with no id; types.ts:502-513 `OutcomeRecord` is project-scoped (id+project_id). agent-spec.ts:104 `step.task.includes(a.assumption.substring(0,8))`; :107 derives `validates_assumption` from that match (removing the match removes the only producer). prompt-chain.ts:151-152 `substring(0,10)` on task AND expected_output. The join-key mechanism correctly requires NET-NEW schema on both the assumption and the step — the plan states this contract accurately.",
          "fix": "None — mechanism/contract holds. (The plan correctly flags that id-on-assumption ALONE is insufficient; the step needs the join field too.)"
        }
      ]
    },
    {
      "error_class": "E7 — Intent stated as fact (whole-plan sweep)",
      "instances": [
        {
          "plan_claim": "§2 line 76 (TAKE NOW): 'Decision Contract — THE KICK (§0). Serialize predicates into a reversible JSON sidecar surfaced as a FinalCard \"Verdict\" view. ProgressiveFlow untouched.' — 'ProgressiveFlow untouched.' is written as a bare statement of fact.",
          "status": "needs_tag",
          "evidence": "Grep for Verdict/DecisionContract/predicate in src/components/workspace/progressive returned NO decision-contract/Verdict component — the §0 Verdict view is UNBUILT (the 9 'Verdict' hits are unrelated boss-chat features: src/components/boss/PostVerdictPanel.tsx, VerdictShareCard.tsx, etc.). FinalCard.tsx:39 `const logSession = useProgressiveStore(s => ...)` confirms the only existing host is store-coupled. Because nothing is built, 'ProgressiveFlow untouched' is a design goal for a future feature, not an observed property of current code. Note line 150 already self-corrects the SAME phrase to '(mark \"untouched\" as a build-time design intent to validate, not a verified ground truth)', so line 76 is internally inconsistent — it states as fact what line 150 demotes to intent.",
          "fix": "Re-tag line 76 to [INTENT]: 'ProgressiveFlow untouched (design intent — the Verdict sidecar is UNBUILT; verify after build that no ProgressiveFlow edit is required)'. Make it consistent with the line-150 correction."
        },
        {
          "plan_claim": "Effort estimates stated as bare facts throughout: '3-4 weeks' / '3-4 wks' (lines 72, 82, 172, 335), '~1 line' (line 77), 'M-effort' (line 79), plus the §4 probe efforts '1 hr' (line 286), 'half-day' (line 287), '~15 voyages' (line 288), AND every §UX item's '[severity · effort]' tag (e.g. '[P0 · M]', '[P1 · S]' across lines 214-277).",
          "status": "needs_tag",
          "evidence": "The VERIFICATION-PROTOCOL E7 row (line 29) explicitly names 'effort estimates' as the canonical example of intent-stated-as-fact ('a desire (\"is a maintainability gate\", \"all tokens exist\") as verified'). Effort/size estimates are predictions, not properties of current source readable this turn — none can carry [VERIFIED] per §0 of the protocol. None of these estimates in v3 carries an [INTENT] tag; they read as established facts. (The underlying code facts they hang off, e.g. HiddenAssumption/KeyAssumption lack id/status — verified at types.ts:11-18, :136-141 — are fine; only the time/size conversion is the intent.)",
          "fix": "Tag every effort/duration/size estimate [INTENT] (or [ASSUMPTION] with a sizing basis). E.g. 'least-reversible schema migration [INTENT: ~3-4 wks, est. from 31 emit/consume files]'; convert §UX '· effort' tags to '· est. effort'. Keep the verified code anchors as [VERIFIED]; demote only the projection."
        },
        {
          "plan_claim": "§1 line 62: 'extend the voyage DNA (C, 60-80% infra reuse)' — the '60-80% infra reuse' figure is presented inline as an established fact.",
          "status": "needs_tag",
          "evidence": "This is an inherited estimate from workflow C (per the doc header, line 4: 'Synthesizes four prior workflows A/B/C/D'). Protocol §3 rule 'No silent inheritance' (line 48): a claim carried from a prior workflow without re-verification this turn is [ASSUMPTION], never [VERIFIED]. No fresh enumeration of reused-vs-net-new surface backs the 60-80% range in this turn, and a reuse-percentage is a projection, not a readable code property (E7) — and per E3 every reuse claim needs producer+consumer named, which a blanket percentage does not provide.",
          "fix": "Re-tag as '[ASSUMPTION: 60-80% infra reuse, inherited from workflow C; verify by enumerating reused vs net-new modules per layer]'. It is a directional planning figure, not a verified fact."
        },
        {
          "plan_claim": "§3 L2 line 150: 'Maintainability gate, not a value gate — the Decision Contract leaves ProgressiveFlow untouched, so L4's graft does NOT depend on this.' — the 'Maintainability gate, not a value gate' framing and the dependency claim are asserted in bold as fact.",
          "status": "needs_tag",
          "evidence": "Protocol E7 row (line 29) names the literal phrase pattern '\"is a maintainability gate\"' as the archetype of intent-stated-as-fact. The clause depends on the unbuilt Decision Contract (grep: no Verdict/decision-contract component exists in src/components/workspace/progressive), so 'leaves ProgressiveFlow untouched' is an unverified prediction. Partial mitigation: the SAME line already appends '(corrected: mark \"untouched\" as a build-time design intent to validate)'. So the trailing 'untouched' clause is tagged, but the leading 'Maintainability gate, not a value gate' classification and 'does NOT depend on this' conclusion remain untagged design intent.",
          "fix": "Prefix the whole L2 goal as [INTENT]: 'Intended as a maintainability gate (not a value gate); on the design plan the Decision Contract does not touch ProgressiveFlow — validate once the sidecar is built.' Fold the existing correction up into this single tag."
        },
        {
          "plan_claim": "§5 line 302 (a11y): 'Measured on --bg #f8f7f5: --text-tertiary #b0aa9f = 2.16:1; --text-secondary #868279 = 3.58:1; --accent text = 4.18:1; --accent-light = 2.63:1 ... Dark mode also fails (#78716c on #1c1917 = 3.65:1).' — the specific contrast RATIOS are stated as measured facts; and §0/§1/§7 repeatedly call the globals.css:6 comment 'FALSE' as fact.",
          "status": "needs_tag",
          "evidence": "The token VALUES and locations are verified: globals.css:6 `--accent: #96782e; /* Gold — contrast-safe on white (4.5:1+) */`, :7 `--accent-light: #b8963e`, :13 `--text-secondary: #868279`, :14 `--text-tertiary: #b0aa9f`, dark :97 `--text-tertiary: #78716c`. So the comment text and the tokens are [VERIFIED]. But the contrast RATIOS (2.16/3.58/4.18/2.63/3.65:1) are computed WCAG values — a derived property, not a code read; no computation or tool output is shown this turn, and the protocol requires a mechanism/contract for any derived assertion (E6) and a fact-vs-intent tag (E7). Asserting 'FALSE' / exact ratios at a distance is the same assert-at-a-distance pattern the protocol exists to stop.",
          "fix": "Keep the token values/comment text as [VERIFIED]. Tag the ratios [ASSUMPTION: WCAG ratios computed from the cited hex pairs; re-run a contrast checker and paste the numbers]. Then the 'comment is FALSE' verdict becomes a verified conclusion rather than an asserted one."
        },
        {
          "plan_claim": "Header line 6: 'smallest safe step first, test-gated, never build on sand. 1004 tests stay green at every layer (verified: npx vitest run → 53 files / 1004 passed).' — 'stay green at every layer' is a forward guarantee; the parenthetical claims a single verified run.",
          "status": "needs_tag",
          "evidence": "Fresh count: `grep -rln ... --include=*.test.ts(x)` = 53 test files (matches '53 files'). Test-case occurrence count via grep = 976 `it(`/`test(` blocks (the '1004' includes table-driven/each cases not captured by a raw grep, so the number is plausible but NOT re-derivable by static read alone). The point-in-time run result cannot be re-verified by reading code this turn (READ-ONLY repo; suite not run), and 'stay green at every LAYER' is a forward-looking guarantee about future plan states, not a current fact.",
          "fix": "Split: keep '53 test files [VERIFIED: 53 *.test.ts(x) files]'; tag the pass count '[ASSUMPTION: 1004 passed per a prior `npx vitest run`; re-run to confirm — not executed this turn]'; tag 'stay green at every layer' as [INTENT] (an acceptance commitment, not a present fact)."
        },
        {
          "plan_claim": "§3 line 21 / L2 line 154: 'L2 regression gate is structurally insufficient — of the 3 cited tests, only voyage-branch-simulation exercises the chassis ... Add a net-new component/render test before the flag flip' — the 'structurally insufficient' verdict and the implication that NO chassis render test exists are stated as fact.",
          "status": "needs_tag",
          "evidence": "The progressive __tests__ dir already contains FIVE component/render tests: flow-interactions.test.tsx, logbook-render.test.tsx, logbook-fork-render.test.tsx, team-deploy-render.test.tsx, voyagechart-render.test.tsx. flow-interactions.test.tsx (read this turn) imports and renders sub-components FROM ProgressiveFlow — `import { VerificationGate, TeamDeployBanner, DMFeedback, FinalCard } from '@/components/workspace/progressive/ProgressiveFlow'` (line 32) — exercising real clicks in jsdom. So 'only voyage-branch-simulation exercises the chassis' is too strong: existing render tests touch ProgressiveFlow exports (though NOT the top-level orchestrator and NOT scroll/worker/UIState). The directional point (no test covers the extracted scroll/worker/UIState seams) holds, but 'structurally insufficient' is a coverage JUDGMENT and the absence-implication is partly contradicted by these files.",
          "fix": "Re-tag as [INTENT/ASSESSMENT]: 'The named lib-level tests do not cover the scroll/worker/UIState seams the decomposition moves; existing progressive render tests (flow-interactions/team-deploy/voyagechart/logbook) render ProgressiveFlow EXPORTS but not the orchestrator nor those seams [VERIFIED: 5 *.tsx tests in progressive/__tests__]. Add a net-new render test covering the extracted hooks.' Stop implying zero render coverage exists."
        },
        {
          "plan_claim": "§3 L2 line 152: 'and 46 useState/useRef/useEffect hooks (the UIState target).' — '46' is presented as a fresh exact fact.",
          "status": "needs_tag",
          "evidence": "Fresh grep: `grep -coE 'useState|useRef|useEffect' ProgressiveFlow.tsx` = 46 (lines containing a hook), but `grep -oE ... | wc -l` = 49 total occurrences (some lines contain two). So '46' is the line-match count, not the hook-call count (49). The number is close but ambiguous about what it counts (E5/E7 boundary — a count stated without naming whether it is lines or occurrences).",
          "fix": "Disambiguate: '46 lines containing a useState/useRef/useEffect call (49 total hook occurrences) [VERIFIED: fresh grep on ProgressiveFlow.tsx]'. Minor, but a count must name its unit to be load-bearing."
        },
        {
          "plan_claim": "§0 line 44 / §1 line 60-62: positioning theses stated as fact — 'The single differentiating move and the spine the forward plan hangs on', 'the only real competitor is \"just ask ChatGPT\"', 'The moat cannot be a smarter model — it is your accumulating ... judgment ... which ChatGPT structurally cannot produce'.",
          "status": "needs_tag",
          "evidence": "These are strategy/positioning theses, not properties of the codebase readable this turn. The doc itself flags the core retention premise as unproven ('The honest risk (unchanged): the moat hinges on the user returning — the exact unproven premise A/B/C/D all flagged', line 54) and labels it 'UNVALIDATED-PREMISE' in §7 (line 340). So the surrounding bold positioning assertions ('the single differentiating move', 'structurally cannot produce') are written more strongly than the doc's own verified stance, which is hedged.",
          "fix": "Tag the positioning sentences [INTENT] (product thesis) and cross-reference the line-54 / §7 'UNVALIDATED-PREMISE' label so the differentiation claim is not read as a verified fact. The grounding-clause asks not to re-derive ARGUS's identity, so keep the thesis — just mark it as thesis, not fact."
        },
        {
          "plan_claim": "Cross-section consistency note: several 'X exists / producer-less / NEVER rendered' absence-claims that I re-verified this turn ARE correctly stated as fact and do NOT need re-tagging — flagging them as confirmed so they are not over-corrected.",
          "status": "holds",
          "evidence": "OUTCOME_RECORDS producer-less: grep shows ONE production reader (context-builder.ts:61) and zero `setStorage(OUTCOME_RECORDS)`/`addOutcome`/`recordOutcome` outside tests — §0/§6/§7 'zero production writers' is VERIFIED. Convergence fields 'rendered NOWHERE': grep `is_converged|estimated_rounds_left` in *.tsx = No matches — VERIFIED (lines 128, 236). ReframeStep streamingText 'NEVER rendered': grep shows only declaration/setStreamingText calls (:398,:479,:487,:495,...), no `{streamingText}` JSX — VERIFIED (line 218). @react-three dead: in package.json:21-22 but grep `react-three|Canvas|useThree|useFrame` in src = no matches — 'dead/unused' VERIFIED. Chassis sizes 2670/1786, phaseIdx not exported, store statically imports only useAgentStore with lazy require(usePersonaStore) at :946, 31 files emit/consume assumptions, RecastStep.actor at types.ts:121, HiddenAssumption/KeyAssumption lack id/status at types.ts:11-18/:136-141, --brass-shine/.logbook-reveal/8pt tokens do NOT exist (grep empty) — all VERIFIED this turn.",
          "fix": "No change. These are correctly tagged/stated as fact and should be preserved as [VERIFIED] anchors; they are listed here so the E7 re-tagging pass does not erroneously demote grounded facts."
        }
      ]
    }
  ],
  "v4": "v4 written. Returning the changelog, counts, and the unverifiable claims as required.\n\n---\n\n# MASTER-DIRECTION-v4.md — written\n\n## Changelog vs v3\n\n**Re-verification corrections (specific wrong/imprecise/stale, direction kept):**\n1. `--accent` contrast **4.18:1 → 3.9:1** (wrong, repeated in 3 places: §5, §7×2); still AA-fail.\n2. §5 reduced-motion: `gold-shimmer`/`border-glow` are **covered** (globals.css:468-469), removed from \"MISSED\" list; genuine misses = pulse-skeleton/subtle-pulse/bm-bounce/inner-glow.\n3. L2 hooks **46 → 43** (21 useState + 15 useRef + 7 useEffect).\n4. L2 scroll-into-view is a **COMPONENT** concern (ProgressiveFlow `:943-953`), not the store; store `:951` is a `personas.find` predicate; \"28 scroll refs\" → **23 scrollToRef callsites**.\n5. L3b \"31 files\" qualified — correct only for the **colon-suffixed** `hidden_assumptions:`/`key_assumptions:` pattern (bare = 57/40).\n6. §7 `callLLMStreamThenParse` **8 → 7** callsites (8th is the import).\n7. §7 parseJSON \"5 direct\" → **3 app-direct** + 7 (2 are engine internals).\n8. §7 `llm-validation.ts` **72 → 71** lines.\n9. L0/§7 `decision-quality.ts:83` caveat **DELETED** — `:83` IS guarded by `if (reframe?.analysis)` at `:82`; file closed outright.\n10. §5 framer-motion importers **36 → 38**.\n11. §5 \"12+ import EASE\" → exactly **18**.\n12. `validateShape` defined at **llm.ts:282** (not :778-781, the call site); wiring is **conditional** on `options.shape`.\n13. §1 \"provenance markers\" → split `[VERIFIED: typed contexts]` + `[INTENT: provenance]` (no `_source` field exists).\n14. `PersonaAccuracyRating` defined at **types.ts:399** (not useAccuracyStore.ts:10).\n15. L0-WRITE CustomEvent reuse: only **RateLimitBadge + SyncStatus** are real consumers; PingToast/UnlockToast are store-driven (dropped).\n16. §UX Header lock-gated items **3 → 2** (Projects, Agents; Manager not gated).\n17. §UX OutputSelector **5 formats** (not 4); footer copy corrected to \"5가지\".\n18. §UX-L4 \"orchestra jargon\" → labels already pair metaphor+plain via `·`.\n19. L2 \"add a render test\" → render tests already exist (5 in progressive/__tests__); gap is **orchestration** coverage.\n20-24. Path/locator fixes: RehearseStep/RecastStep under `workspace/`; migrators **migrateWorkers:330** added; getSignals **:44**/getSignalsByType **:61**; runAllAIWorkers entry **:201**.\n25-29. \"7 learning sections\" → up to 6 (slot 4 dead); \"~15 const L\" → **9**; \"21 of 141\" → **21 of 137**; BranchMap Hangul is a comment not JSX; ShipCutaway/SailingShip primitive counts tagged ASSUMPTION, \"14 framer loops\" → 14 SVG `animate=` props.\n\n**Class-sweep fixes:** all effort/size estimates → `[INTENT]`; \"60-80% reuse\" → `[ASSUMPTION]`; \"ProgressiveFlow untouched\" + \"maintainability gate\" → `[INTENT]`; \"1004 passed\" → `[ASSUMPTION]` (53 files VERIFIED); positioning theses → `[INTENT]`; contrast ratios → `[ASSUMPTION]`; **onboarding gate mechanism corrected** (must set `setDemoScenario` inside HeroFlow, not wrap the :690 parent branch); \"dead useTeamStore\" → consumed by teams/page.tsx.\n\n**9 MUST-FOLD items:** SEQ-1 (db.ts merge → single L4-PREREQUISITE home); SEQ-2 (L3a split into L3a-0 stable-id + L3a persistence); SEQ-3 (reflection tab demoted to run-3+, convergence gauge promoted to L1 headline); ADD-4 (malformed-first-response guard); ADD-5 (per-fix regression tests); ADD-6 (observable-async-write helper); ADD-7 (first-30s mobile items into L0/L1); ADD-8 (§UX-GLOBAL-CHROME Header track); ADD-9 (Test-1 manual-tag backfill for historical predicates).\n\n## Verified / Intent / Assumption counts (running totals)\n- **VERIFIED:** ~110 load-bearing claims with quoted source (fresh greps shown for every count/set: framer-motion=38, EASE=18, requiresAuth=2, callLLMStreamThenParse=7, OutputSelector formats=5, brass-shine/logbook-reveal/8pt/useMirrorStore/@react-three=0, etc.)\n- **INTENT:** ~30 (HYBRID direction, all positioning theses, every effort/size tag, \"ProgressiveFlow untouched\", \"maintainability gate\", \"stay green at every layer\").\n- **ASSUMPTION:** ~22 (must verify before load-bearing use).\n\n## Claims that could NOT be verified this turn (remain [ASSUMPTION])\n1. **\"60-80% infra reuse\"** (§1) — inherited from workflow C; verify by enumerating reused-vs-net-new modules per layer.\n2. **\"plugin manifest declares no hooks array\"** (§1) — grep `argus-plugin/` manifest for a `hooks` key.\n3. **context-chain src-wide negatives** beyond within-file (`useContextChainStore`/persistence) — confirmed in-file + a src-wide symbol grep, but flagged where a full L3a exhaustive pass is owed.\n4. **auto-persona.ts** silent `return []` + `recommendBlindSpotPersona` fallback (L0) — file not opened this turn.\n5. **analytics.ts canary** — verified at :168-179 this turn (upgraded); the L0 *re-open for the helper* remains.\n6. **L2 worker runtime** lines (`:32`, store `:213-216`) — not re-opened.\n7. **All WCAG contrast ratios** (3.9 / 2.16 / 3.58 / 2.63 / 3.65:1) and the **3 darkened replacement tokens** (`#8a8478`/`#6f6b62`/`#7d6526`) — derived, not source-readable; re-run a contrast checker.\n8. **\"1004 passed\"** — 53 test files VERIFIED; the pass count needs a fresh `npx vitest run` (READ-ONLY repo, not executed).\n9. **ShipCutaway/SailingShip SVG primitive counts** (155/72 under straight grep) — depend on element set.\n10. **per-predicate OutcomeRecord shape** + **grade storage table pin** (accuracy_ratings vs progressive_sessions blob) + **validates_assumption_ids generation-time populator** — net-new schema design decisions, not current facts.\n11. Most **§UX-L1/L4/MOBILE P1-P3 line anchors not in the re-verify scope** — tagged `[ASSUMPTION: lines not re-quoted this turn]` rather than silently inherited.",
  "audit": {
    "acceptance_verdict": "PASS_WITH_NITS",
    "spotchecks": [
      "CLAIM: L2 hook count = 21 useState + 15 useRef + 7 useEffect = 43 declarations (v4 changelog #3, L2 line 249). OPENED ProgressiveFlow.tsx with precise patterns `= useState`/`= useRef`/`useEffect(` → 21/15/7. HOLDS exactly. (Raw `grep -c` gives 22/18/8 because of the import line + a comment, but the doc explicitly names the unit 'declarations' and the precise count is correct.)",
      "CLAIM: 7 callLLMStreamThenParse invocation callsites at progressive-engine.ts:307,385,458,641,776,812,895 + import at :5 (v4 changelog #6, §7). GREPPED src → exactly those 7 invocations + import progressive-engine.ts:5; definition is at llm.ts:765. HOLDS (v3's '8' was the import miscounted).",
      "CLAIM: OUTCOME_RECORDS is dead on both ends, ZERO production writers (§0 line 114). FRESH GREP src → only context-builder.ts:61 (read) + storage.ts:13 (key def) + 3 test files; addOutcome/recordOutcome = 0 src hits. HOLDS exactly.",
      "CLAIM: onValidationFailed never wired in ProgressiveFlow → no-callback else-branch fires in production (L0 P1, line 188). FRESH GREP src → only execution-transcript.ts + worker-engine.ts; zero ProgressiveFlow hits. READ worker-engine.ts:264-290: confirmed `if (callbacks.onValidationFailed)` at :266, race Promise<'accept'> setTimeout 30_000 at :269, no-callback else `// 콜백 없으면 그냥 수용 / finalResult = result; break`. HOLDS exactly.",
      "CLAIM: predicate type anchors — types.ts:121 RecastStep.actor, :255/:362 classified_risks, :158 governing_idea; produced live RehearseStep.tsx:60-62 (§0 line 107-108, L4 line 300). OPENED both: types.ts:121 actor, :255/:362 classified_risks, :158 governing_idea all exact; RehearseStep.tsx:60-62 = `classified_risks: concerns.filter(...severity==='critical').map(c => ({ text, category: 'critical' as const }))`. HOLDS exactly.",
      "CLAIM: convergence fields rendered NOWHERE; is_converged at progressive-convergence.ts:109, estimated_rounds_left :110 (L1 line 212). GREP *.tsx for is_converged|estimated_rounds_left = No matches; OPENED progressive-convergence.ts → :109 `const is_converged = totalScore >= 75`, :110 estimated_rounds_left. HOLDS exactly.",
      "CLAIM: framer-motion importers = 38, EASE importers = 18, @react-three/three = 0 dead (changelog #10/#11, §5). FRESH GREPS → 38 / 18 / 0 exactly. HOLDS.",
      "CLAIM: exactly 3 substring linkers in 2 files — agent-spec.ts:104, prompt-chain.ts:151,152 (L3a-0 line 267). FRESH EXHAUSTIVE GREP `\\.substring\\(0,\\d+\\)` over src non-test → exactly those 3 lines. HOLDS exactly.",
      "CLAIM: onboarding gate E6 mechanism — demoScenario/setDemoScenario/if(demoScenario) all INSIDE function HeroFlow; if(!currentProjectId):690 is in parent WorkspaceContent with no access (changelog #37, L4 line 305). OPENED workspace/page.tsx: HeroFlow :145, setDemoScenario :156, if(demoScenario) :263, demo-card :444; WorkspaceContent :633, if(!currentProjectId) :690, <HeroFlow :692. HOLDS exactly — the corrected mount-effect-inside-HeroFlow mechanism is sound.",
      "CLAIM: hidden_assumptions:/key_assumptions: colon-suffixed = 31 files; bare-with-tests=57; bare-non-test=40 (changelog #5, L3b line 283). FRESH GREPS → 31 / 57 / 40 exactly. HOLDS — strong evidence the E5 sweep ran fresh greps this turn.",
      "CLAIM: reduced-motion allowlist (globals.css:449-483) DOES cover gold-shimmer :468 + border-glow :469; genuine gaps = pulse-skeleton :266/subtle-pulse :360/bm-bounce :1264/inner-glow :1590 (changelog #2, §5). OPENED globals.css → block at :449; .animate-gold-shimmer + .animate-border-glow inside (lines 20-21 of block = :468-469); the four infinite animations confirmed uncovered. HOLDS — v3's E2 error correctly caught.",
      "CLAIM: db.ts re-upsert resurrection mechanism — remote pre-filtered !deleted_at :106, remoteIds :113, localOnly :114, re-upsert :116 (L4 PREREQUISITE line 294). OPENED db.ts:104-120 → exact match incl. sanitizeItem strips created_at AND updated_at. HOLDS.",
      "CLAIM: analytics.ts swallowed insert — .then with no .catch inside sync try/catch (ADD-6 line 195, §4 Probe-1). OPENED analytics.ts:165-182 → `.then(() => {})` with no .catch, wrapped in try/catch that cannot catch async rejection. HOLDS.",
      "CLAIM: FinalCard store-coupled at :39 useProgressiveStore (§2 line 157, L4 line 300). OPENED FinalCard.tsx:39 → `const logSession = useProgressiveStore(s => ...)`. HOLDS exactly."
    ],
    "remaining_violations": [
      "NIT (sequencing-rule inconsistency, not a factual error): §UX-L4 section (ship-with-L4) contains two items tagged [P0] — line 367 'Output card labels lead with metaphor' and line 368 'Tool names differ across screens'. The §UX preamble (line 334) states the rule 'P0/P1 weave into L0/L1; P2/P3 into L4.' A P0-severity item sitting in the L4 bucket contradicts that rule. Either these should be re-bucketed to L0/L1, or their severity downgraded to P2, or the preamble rule qualified. The underlying source claims themselves are VERIFIED and hold (OutputSelector.tsx:38/48/58/68/78 labels confirmed at line 367); only the layer-vs-severity placement is internally inconsistent.",
      "MINOR (E5 precision, sub-load-bearing): L1b line 236 claims '142 L( call-sites' in ProgressiveFlow.tsx. My fresh grep of `\\bL\\(` gives 150. The delta (8) is likely regex scope (e.g. HTML(/URL( false-positives in mine, or a different boundary in theirs); the count is illustrative for an i18n-consolidation effort estimate, not a decision-gating number, so it is not strictly load-bearing — but per E5 it should name its grep command so the 142 is reproducible. The companion claim (9 `const L =` definitions at the 9 exact cited lines) was independently confirmed exact.",
      "MINOR (E1, acknowledged-but-bare cite): ADD-4 (line 182) cites `parsePartialAnalysis (workspace/page.tsx:503)` without an inline [VERIFIED] tag. I independently confirmed it true this turn (parsePartialAnalysis called at workspace/page.tsx:503), so it is factually correct; it is a forward-design instruction rather than a standalone assertion, but to be fully protocol-clean it should carry the tag or be folded under the §UX-L0 first-run VERIFIED block that already quotes the same surrounding lines."
    ],
    "sequencing_resolved": "All three sequencing problems are resolved to single, consistent homes — none survived in multiple homes. SEQ-1 (db.ts merge fix): the full mechanism now lives ONLY in the 'L4 PREREQUISITE — tombstone-aware merge + empty-stamp tie-break' block (line 290-297). L0 (line 199) and L5 (line 322) explicitly cross-reference it and say 'do not restate the mechanism.' The progressive_sessions merge (line 323) is correctly kept SEPARATE (verified different code path: loadSessions, not loadAndMerge/mergeByTimestamp), not a duplicate. The ADD-6 observable-write helper references the re-upsert as one of three swallowed paths — a distinct observability concern, not the tombstone mechanism. SEQ-2 (L3a stable-id split): explicitly split into L3a-0 (stable-id at generation, ships at L1 timing, BEFORE Decision Contract + return-to-grade — lines 63, 174, 265) and L3a-proper (persistence/_source/traceProvenance — line 272). Consistent across the spine line (174), §0 corollary (118), and §4 Probe-1 prereq (407). SEQ-3 (reflection demoted / convergence promoted): convergence gauge is the PROMOTED L1 headline with a run-1 acceptance line (line 212); reflection tab is DEMOTED to run-3+ payoff (line 214), with the tier-gating evidence freshly verified (navigator.ts:113-117 tier-2 needs sessionCount>=2). No contradiction across homes. On P0 placement: all crash-floor P0s (stream hang, null-derefs, silent auto-accept, data-loss) are correctly in L0; the §0 KICK stable-id prerequisite (a P0 for data trust) is correctly pulled FORWARD to L3a-0/L1 timing ahead of the grading loop. The only placement blemish is the two [P0]-tagged §UX items sitting in the §UX-L4 bucket (see remaining_violations nit) — a UX-polish severity/layer mismatch, not a foundation-fix mis-ordering.",
    "concrete_fixes": [
      "Re-bucket or re-tag the two [P0] §UX-L4 items (lines 367-368): move 'Output card labels lead with metaphor' and 'Tool names differ across screens' into §UX-L1 (they are first-orientation, not late polish), OR downgrade them to [P2 · ...] to match the section's own 'P2/P3 into L4' rule, OR add a one-line exception note to the §UX preamble (line 334) explaining why these two P0s are scheduled in L4.",
      "Add the grep command/anchor for the '142 L( call-sites' figure in L1b (line 236) so it is reproducible per E5 (e.g. `[VERIFIED: grep -oE '\\bL\\(' ProgressiveFlow.tsx | wc -l = <N>]`), or relabel it [INTENT/approximate] since it only sizes an i18n-consolidation effort and is not decision-gating. Reconcile the number against a fresh grep (I observed 150 with a loose `\\bL\\(`).",
      "Tag the ADD-4 `parsePartialAnalysis (workspace/page.tsx:503)` cite (line 182) as [VERIFIED: workspace/page.tsx:503 `const partial = parsePartialAnalysis(streamingText)`] — confirmed true this turn — so every file:line in a load-bearing instruction carries its tag, closing the last bare-cite gap.",
      "Optional polish: in changelog #3 (line 20) the count '21 useState + 15 useRef + 7 useEffect' is stated as a fresh-grep result but the inline VERIFIED anchor lives at L2 line 249; consider a back-reference so the changelog entry points to its evidence home (the figure is correct — raw grep -c overcounts to 22/18/8 due to the import line + a comment, so keep the 'declarations' unit qualifier)."
    ]
  }
}
THE MASTER DIRECTION is written to `C:\Users\SAMSUNG\documents\github\commet\Argus\docs\MASTER-DIRECTION.md` (supersedes `docs/PLAN.md`).

**Verdict: HYBRID.** C's incremental DNA-extension is the spine; exactly one radical graft (D's **Decision Contract** — falsifiable predicates as a reversible JSON sidecar) is taken now; all four radical chassis reshapes are rejected as the spine (distribution scissors — verified no hooks array, non-dev user has no instrumentable runtime); the 3-4-week per-predicate schema migration is deferred behind a kill-test.

**Decisive logic:** C's Mirror and D's Verdict share the *same* unproven retention premise, so the rational tiebreaker is cost-to-falsify → the incremental graft wins the build-first slot. The migration lands on the least-reversible seam (verified: `types.ts:11-18`/`:136-141` have no id/created_at/status; `20260409` already built an atomic RPC to dodge a JSONB race), so it is gated on a §4 kill-test, not scheduled.

**Sequenced spine (each layer cites the A/B/C/D findings + file:line, with acceptance criteria in the doc):**
- **L0** stop-the-bleeding: per-read inactivity watchdog (NOT Promise.race — `callLLMStream` is `Promise<void>`, `llm.ts:627-757`), 2 real null-derefs (`project-brief.ts:130`, `eval-engine.ts:183`), flip `worker-engine.ts:267-269` silent auto-accept → blocking retry, surface `storage.ts:41-43` quota loss.
- **L1/L1b** artifact persistence + retention tab + 1-line `참고:` userContext injection (`progressive-prompts.ts:23`).
- **L2** decomposition behind a flag (maintainability gate only — graft does not depend on it).
- **L3a** small Phase-0 persistence + stable assumption IDs (deletes `substring(0,8)`/`(0,10)` linkers); **L3b** full migration gated.
- **L4** Decision Contract + onboarding auto-demo + provenance rendered over the verified on-disk `.argus/` ledger.
- **L5** deferred: silent prediction loop + anchoring guard + parked prompt-mutation.

**Probes-first** (§4), **design spine** with the verified EASE/`--ease-spring` reconciliation (§5), explicit **NOT-doing** (§6), and a **do-not-inherit table** (§7) refusing every fabricated count A flagged.

**Verifications I performed before committing claims** (corrections folded in): the digest's "dead `landing_hero_submit`" is more precisely "trusted-but-partly-dead" — `daily-report/route.ts:329` buckets it *with* the live `landing_cta_click` (`Act3OnDeck.tsx:88`); I carried the corrected framing. `.argus/*.md` and `argus-plugin/CONTEXT-CONTRACT.md` confirmed present on disk (the earlier Glob miss was a cwd-vs-repo-root dotdir artifact), grounding the highest-leverage cross-source graft.
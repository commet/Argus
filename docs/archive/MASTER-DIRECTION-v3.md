# ARGUS — THE MASTER DIRECTION (v3, SUPPLEMENTED)

> Single source of truth. **Supersedes `docs/PLAN.md` and `docs/MASTER-DIRECTION.md` (Rev 2).**
> Synthesizes four prior workflows A (DEEP-PLAN) / B (BLINDSPOT) / C (TARGET-STATE) / D (RADICAL-RETHINK), plus a full second-pass verification, a basic-usability confusion sweep, and a non-UX gap audit.
> **Direction unchanged: HYBRID** — extend the voyage DNA (C) + one radical graft (the Decision Contract closed loop, §0); foundation-fixes-first (L0); heavy/irreversible bets gated behind cheap kill-tests (§4).
> **Principle inherited & confirmed:** smallest safe step first, test-gated, never build on sand. **1004 tests stay green at every layer** (verified: `npx vitest run` → 53 files / 1004 passed).

---

## CHANGELOG vs Rev 2

**Corrections folded in (every wrong file:line / overstated bug / mis-scoped count fixed inline, marked `(corrected: …)`):**

1. **L0 stream watchdog mechanism** — the watchdog CANNOT "abort via the existing `options.signal`" because `options.signal` is externally supplied (`llm.ts:19-20`; callers pass `workerAbortRef.current.signal`, `ProgressiveFlow.tsx:1222`). `callLLMStream` owns no AbortController. Corrected to: internal AbortController (linked to `options.signal`) or direct `reader.cancel()`. **NEW integration contract:** the watchdog must reject with a DISTINCT non-Abort error, or the handler's `!(AbortError)` guard (`ProgressiveFlow.tsx:1397, :1717`) silently swallows it as a user-cancel and the spinner never clears.
2. **L0 brief-export crash** — NOT `latest.analysis.steps` unguarded. Real line: `project-brief.ts:130` `latest.steps.length > 0 ? latest.steps : latest.analysis.steps`, inside `if (latest.analysis)` guard at `:114`. The unsafe expr is `latest.steps.length` (primary) + `latest.analysis.steps` (fallback) + `actorLabels[s.actor]` on unknown actor.
3. **L0 judgment crash** — `eval-engine.ts:184` does NOT "return true safely." It only short-circuits on null `analysis`; on the non-null path it reads `item.analysis.steps.length` with the SAME crash as `:183`. Both `:183` and `:184` (and `item.steps.length`) need `steps` guarded.
4. **L0 silent auto-accept** — in production it is the **no-callback branch** (`worker-engine.ts:282-284`) that fires, because `ProgressiveFlow.workerCallbacks` (`:1176-1212`) omits `onValidationFailed` entirely; the 30s-timeout→`'accept'` branch (`:267-269`) is currently unreachable. Fix the no-callback default AND flip the timeout literal `Promise<'accept'>` (`:269`).
5. **L1 reflection tab attribution** — `getUserPatterns` lives in `useJudgmentStore.ts` (NOT `navigator.ts`); `getPersonaAccuracySummary` lives in `useAccuracyStore.ts`. It does NOT slot into Logbook — the read-only judgment layer lives in **`NavigatorStrip`**; `Logbook.tsx` is the branch/waypoint rail and shares no code.
6. **§0 / L4 outcome loop** — `useAccuracyStore.addRating` and `OUTCOME_RECORDS` are **two different stores/keys**. `addRating` writes `ACCURACY_RATINGS` → feeds `context-builder.ts:216` (persona section). `OUTCOME_RECORDS` is read at `context-builder.ts:61` but **has ZERO production writers** — the loop is dormant/dead, not "half-built." Return-to-grade is **net-new write path**, not a repoint.
7. **L2 coupling** — `useProgressiveStore` statically imports ONLY `useAgentStore`; `usePersonaStore` is a single lazy `require()` at `:946`; NO reframe/recast/project store import. **`IntersectionObserver` is NOT used anywhere in the chassis** (only in `useScrollReveal.ts`). Real coupled concerns: scroll-into-view (`:951` + 28 refs), worker runtime, version/branch logic, and **46 useState/useRef/useEffect** hooks.
8. **L2 regression gate is structurally insufficient** — of the 3 cited tests, only `voyage-branch-simulation` exercises the chassis (store-level). `orchestrator-journey` + `workflow-review-integration` test pure lib fns the decomposition does NOT move → false confidence. **NEW prerequisite: a net-new component/render test before the flag flip.**
9. **L4 Verdict host** — `FinalCard.tsx:39` subscribes to `useProgressiveStore`, so "store-free FinalCard" is not literally true; the round-trip DATA is store-free, the host is not. The store-free initial-analysis path (`workspace/page.tsx:221`) renders no FinalCard today.
10. **L4 onboarding gate** — `argus.has_onboarded` does NOT exist (net-new key). `projects.length===0` at `:458` is marketing copy, not a reusable gate. **The planned first-run auto-demo is UNBUILT.**
11. **L5 merge path** — `progressive_sessions` does NOT use `mergeByTimestamp`; it has its OWN custom merge (`useProgressiveStore.ts:466-482`, `new Date()` compare at `:477`) comparing **server-trigger `updated_at` vs client-clock `updated_at`** — a more concrete skew than the generic `db.ts` case. `sanitizeItem` (`db.ts:30-40`) already strips future-dating; `progressive_sessions` is NOT soft-deletable.
12. **§7 counts** — parseJSON real surface is ~5 direct + 8 `callLLMStreamThenParse` callsites (corrected from "8 direct"). `JSON.parse` is ~17 non-test, ~11 real app callsites (6 are `llm.ts` engine internals — exclude). `@react-three/*` is **dead** — landing illustrations are inline SVG, not three.js.
13. **§5 token contradiction** — `--brass-shine`, `.logbook-reveal`, and the "8pt spacing scale" do NOT exist yet (net-new appends). Card.tsx mirrors `--accent-light #b8963e`, not `--accent #96782e` — name which gold the Logbook layer uses. New tokens need dark-mode values.

**New workstreams / gaps added:**

- **NEW first-class track: "UX — Basic Usability & Orientation"** (§UX) — the per-surface confusion catalogue sequenced into file:line-anchored, severity+effort-tagged line items, woven into L0/L1 (P0/P1) and L4 (polish), with a dedicated **MOBILE sub-track** (previously absent).
- **Correctness/reliability gaps** folded into L0/L3a/L4/L5: stream-watchdog integration contract; worker-engine timer leak + dropped-step; substring linkers wrong on FRESH data; `loadAndMerge` re-upserts tombstoned rows (resurrection is REAL, not moot); analytics swallowed-insert canary.
- **Data-provenance gaps** folded into L1/L1b/L3a/L4: `OutcomeRecord` is orphaned (no producer); signals read local-only (500 cap, no Supabase read); `hit-rate.ts`/`retrospective.ts` are unpersisted/producer-less; predicate-grade needs a stable join key (pull L3a id slice FORWARD); progressive_sessions blob un-versioned/un-validated; accuracy plumbing is on the LEGACY path, not ProgressiveFlow.
- **Dropped-from-prior-workflows** re-inserted: READ-side localStorage corruption crash (L0); convergence gauge as a standalone L1 win (decoupled from L2); persona-feedback re-approval loop (L4); mobile/responsive (L4 + acceptance); team-theater honesty (L1); backtracking-cost preview (L4); Agent Spec 2.0 executable fields (L4); output self-validation lint (L3a); anon→auth quota narrative (L1); a11y baseline (L2/L4); persona-calibration-vs-predicate decision (L5); auto-persona empty fallback (L0).
- **Accessibility/i18n/perf gaps** added to §5/L1b/L2/L4: WCAG AA contrast failures (the `globals.css:6` "contrast-safe" comment is FALSE); `prefers-reduced-motion` entirely unhonored; unlabeled form controls; modal focus-trap; i18n dual-system default mismatch + SSR flash; dead 3D deps; eager landing SVG payload; ~85% of components route no strings through a locale layer.

---

## 0. THE KICK — The Web-Native Closed Loop

(Unchanged thesis. Two corrections folded in.)

The single differentiating move and the spine the forward plan hangs on: the **Decision Contract closed loop**, in the form the web app can own.

1. **Pre-flight (in-session wow, 100% web-native, ships on today's stack).** Every voyage ends NOT in dead markdown but in **3–6 specific, falsifiable predicates** — serialized from data the engine already computes: recast AI-vs-human step assignments (`RecastStep.actor`, `types.ts:121`), blindspot `classified_risks` (`types.ts:255/362`, produced live `RehearseStep.tsx:60`), and `governing_idea` (`types.ts:158`). e.g. *"the CFO will object to cost at the pricing step."* Pure in-session value, felt immediately.

2. **Return-to-grade (the retention engine, web-native but EARNED).** The web app cannot auto-observe post-departure behavior (that is the plugin's, §6); it *earns the return* via the specific predicate: **"You predicted the CFO would object at pricing. Did it? [happened / avoided / partial]."**
   - **(a) user-reported outcome** — **(corrected: NOT a repoint of existing live plumbing.)** `useAccuracyStore.addRating` writes `ACCURACY_RATINGS` (persona-keyed) → `context-builder.ts:216`. `OUTCOME_RECORDS` is read at `context-builder.ts:61-70` but **has zero production writers** (grep: no `setStorage(OUTCOME_RECORDS)`, no `addOutcome`/`recordOutcome` outside tests); the `outcomes.length >= 2` branch (`:62`) is permanently inert. So return-to-grade is **net-new write-path + capture UI**, and it must carry its own stable predicate-id join key (see §0 corollary). Budget it as net-new, not "light reuse."
   - **(b) paste-back** — user pastes the AI output; Argus grades pre-flight vs it via the existing eval engine. Buildable, not built.

**§0 corollary — the join-key prerequisite (NEW, P0 for data trust).** The grade must reference a **stable predicate `id`**, not free text. Today `MaterializedRisk` keys by `risk_text + persona_id` (`types.ts:487-493`) with no id; `ClassifiedRisk` surfaces none here. Grading by text is the exact brittle substring-match L3a is deleting. **Pull the minimal stable-id slice from L3a FORWARD** so each serialized predicate carries a deterministic id before the light grading loop ships — otherwise §4 Test-1 grades noise and the go/no-go is corrupted.

**§0 corollary — the grading write must be observable (NEW, P1).** `addRating` writes localStorage then calls `insertToSupabase` un-awaited; `insertToSupabase` (`db.ts:275-290`) swallows the error into `log.error`, returns void. A user who grades "CFO objected — happened" while offline gets a green check but the graded predicate — the one piece of outcome data the entire L5 roadmap is gated on — is dropped to local-only and never reaches the server cohort §4 Test-1/Test-2 query. **Mirror the L0 storage toast onto this path; await/queue/retry the write.**

**The honest risk (unchanged):** the moat hinges on the user returning — the exact unproven premise A/B/C/D all flagged. The cheap user-reported grading (2a) ships early **as the §4 Test-1 instrument**; the heavy per-predicate migration (L3b) and silent auto-grading (L5) stay gated behind the kill-test.

---

## 1. North-Star

Argus is a **decision harness you run before you hand work to an AI** — "Think before you recast." Target user: a non-developer strategist deciding *what* to have AI do; the only real competitor is "just ask ChatGPT." The moat cannot be a smarter model — it is **your accumulating, attributable, falsifiable judgment about your own past decisions**, which ChatGPT structurally cannot produce about your voyage. Verified assets: the `llm.ts` engine (categorized errors, circuit breaker, multi-strategy `parseJSON`, `validateShape` wired at `llm.ts:778-781`), the typed Phase-1/2 content intelligence (`context-chain.ts:119-525` returns typed `ReframeContext`/`RecastContext` with provenance markers), the on-disk `.argus/` ledger (verified populated), and the 1004-test net.

Direction: **HYBRID** — extend the voyage DNA (C, 60-80% infra reuse) + graft **exactly one** radical mechanic (the Decision Contract: 3-6 pre-committed, falsifiable, ID'd predicates). Every chassis-demolishing reshape is rejected as a spine (distribution scissors: the strategist executes in a chat box by hand, exposing no instrumentable runtime; the plugin manifest declares no hooks array).

No forward surface ships on sand. The chassis is actively crashing and silently betraying its thesis today: an infinite-spinner stream bug, two null-deref crashes, a worker that silently auto-accepts a validation-FAILED result, and a quota write that swallows lost thinking. **These are the floor. Plus a third floor the catalogue surfaced: a first-timer cannot tell what Argus IS, where to click, or how to escape a hang — the "just ask ChatGPT" comparison is lost in the first 30 seconds.** Floor first, visible wins second, least-reversible bet gated behind a kill-test.

---

## 2. The Incremental-vs-Radical Verdict

**VERDICT: HYBRID.** (Logic unchanged from Rev 2; verified.)
1. Same unproven premise → cheapest-to-falsify wins (incremental graft).
2. The radical spine's gate is the least-reversible change: `HiddenAssumption` (`types.ts:11-18`) and `KeyAssumption` (`types.ts:136-141`) have **no id, no created_at, no status** (verified). 3-4 weeks on the most migration-hostile seam — spend only on evidence.
3. D's own tournament de-rates every radical chassis as a replacement; *"You never have to delete ProgressiveFlow to ship the value."*

**TAKE NOW (reversible, ride existing infra, zero migration):**
- **Decision Contract — THE KICK (§0).** Serialize predicates into a reversible JSON sidecar surfaced as a FinalCard "Verdict" view. ProgressiveFlow untouched. **(corrected: Verdict data is store-free off `runInitialAnalysis`; the FinalCard host is store-coupled at `:39` — decide the mount point: either accept the coupled host or add a store-free Verdict host for the `workspace/page.tsx:221` initial-analysis path that renders no FinalCard today.)** Paired with the **net-new** return-to-grade loop (corrected from "upgrade existing plumbing"), which IS the §4 Test-1 instrument.
- **userContext → prompt injection** (B's S1) — `buildInitialAnalysisPrompt(problemText, locale)` (`progressive-prompts.ts:23`, verified no userContext param) gets a scoped `참고:` reference-only line. ~1 line. **(corrected: the richer `buildEnhancedSystemPrompt` path already injects 7 learning sections — "smarter as you use it" is partly already true OUTSIDE the initial-analysis entry point; this fix closes the initial-analysis gap specifically.)**
- **Anchoring / judgment-atrophy guard** — first-class override-rate metric. **(corrected: must be computed on the FULL signal history; today `getSignals` reads local-only, 500-cap — see L1b gap. Compute only after the read path can see Supabase, else the metric is on a truncated window.)**
- **`.argus/` ledger as the render spine** — render an artifact already on disk (M-effort), not a re-derivation from stores.

**DEFER behind a kill-test:**
- Per-predicate `id`/`created_at`/`status` schema migration (3-4 wks, least-reversible). Green-light only if §4 Test 1 OR Test 2 passes.
- Silent prediction-and-self-scoring loop — net-new (verified: `useAccuracyStore` rates PERSONAS; `useAccuracyStore.ts:10` `PersonaAccuracyRating`).

**REJECTED as spine:** execution-bound/agent-native (distribution scissors); multiplayer room (cold-start); decay-by-timer push (spam without a logged outcome); prompt-mutation engine (amplify-bad-signals trap).

---

## 3. The Sequenced Master Plan

Spine: **L0 stop-the-bleeding (+ early UX P0/P1) → L1 visible wins + L1b injection → L2 decomposition (flagged) + L3a Phase-0 persistence (parallel) → L4 forward surfaces (C + §0 KICK) → L5 deferred, kill-test-gated.** L0 gates everything. The §0 kick is L4's through-line and rides L1-timing.

### L0 — Stop-the-bleeding crash & philosophy floor (all S, ZERO foundation deps, gates everything)
**Goal:** a product that does not infinite-spin, crash on export/judgment, silently betray its thesis, or crash on stale data load.

- **P0 stream hang** — `callLLMStream` (`llm.ts:627-757`, `Promise<void>` via callbacks; reader loop `:696-697` has no watchdog; `fetch` `:675-680` has no `AbortSignal.timeout`). A's `Promise.race([stream, timeout])` is WRONG (a void promise carries no liveness). **Correct:** per-chunk inactivity watchdog (~30s idle) + hard ~180s cap. **(corrected: abort via a NEW internal AbortController linked to `options.signal` — `options.signal` is externally supplied (`llm.ts:19-20`, `:679`), `callLLMStream` cannot abort it — OR call `reader.cancel()` directly.)** **(NEW integration contract, P1):** the watchdog must reject with a DISTINCT non-Abort error (e.g. `LLMError` category `'timeout'`); a bare `signal.abort()` is mapped to `AbortError` (`llm.ts:748-751`) and suppressed by the handler guard `!(e instanceof DOMException && e.name==='AbortError')` (`ProgressiveFlow.tsx:1397, :1717`) → silent no-op, spinner never clears. *(re-verify backoff `setTimeout` line numbers at `llm.ts:190-215` before citing — not re-read in this pass.)*
  - **Acceptance test:** a hung stream produces a VISIBLE timeout error in ProgressiveFlow AND clears the spinner — not just an aborted reader.

- **P1 null-deref, brief export** — **(corrected: NOT `latest.analysis.steps` unguarded.)** `project-brief.ts:130`: `const steps = latest.steps.length > 0 ? latest.steps : latest.analysis.steps;` inside `if (latest.analysis)` (`:114`). Fix: `const steps = (latest.steps?.length ? latest.steps : latest.analysis.steps) ?? []`. **Also defend `actorLabels[s.actor]`** (`:131-133`) against an unknown actor → renders `'undefined'` (silent corruption). **(corrected path: `OutputSelector` is at `src/components/ui/OutputSelector.tsx`, not `progressive/`.)**

- **P1 null-deref, judgment** — `eval-engine.ts:183` (`steps_accepted`) guards `!item.analysis` then reads `item.analysis.steps.length` + `item.steps.length` unguarded. **(corrected: `:184` is NOT safe to leave.)** `:184` (`actor_overrides_low`) only short-circuits on null analysis; on the non-null path it reads `item.analysis.steps.length` with the SAME crash. Guard `steps` (`?? []`) on BOTH `:183` and `:184` and `item.steps`. (Entries in `RECAST_EVALS` `182-187`, reached via `recordRecastEval` → `RecastStep.tsx:412`.)

- **P1 silent auto-accept (philosophy-critical)** — `worker-engine.ts:267-270` races `onValidationFailed` against `new Promise<'accept'>(r => setTimeout(() => r('accept'), 30_000))`; `:281-285` no-callback branch silently accepts a validation-FAILED result. **(corrected: in production the NO-CALLBACK branch fires** because `ProgressiveFlow.workerCallbacks` (`:1176-1212`) omits `onValidationFailed`; the 30s branch is currently unreachable.) Fix: wire `onValidationFailed` in ProgressiveFlow (surface the existing `WorkerCard.tsx:268-294` "품질 확인이 필요합니다" panel), change the no-callback default to block/`'timed_out'`, AND flip the literal `Promise<'accept'>` (`:269`) to `'timed_out'`/`'retry'`. **(NEW, P1):** clear the loser `setTimeout` once the race settles (timer leak per worker); treat an `onValidationFailed` rejection as `'timed_out'`, NOT a fall-through to the generic catch (`:286-290`) that drops the worker with no `onComplete` (`:293-295`). **(NEW, P1):** `execution-transcript.ts:147-153` records `validation.passed` with no gating — a silently-accepted FAILED result is persisted to the `.argus` ledger as a clean completion; mark failed-then-accepted results, don't record them clean.

- **P1 silent data loss (WRITE)** — `storage.ts:37-45` `setStorage` swallows the catch (incl `QuotaExceededError`) into `console.error`, returns void, no UI. Surface a visible toast via the existing `CustomEvent` pattern (`llm.ts:721-724` → `PingToast`/`UnlockToast`/`RateLimitBadge`). **(NEW):** consider returning a boolean/throwing so the calling store can say *which* data was lost ("your last edit was not saved"), not just a generic toast.

- **P1 silent data loss (READ) — NEW (A P1, dropped by Rev 2).** `getStorage` (`storage.ts:27-35`) returns `JSON.parse(item)` with ZERO shape/version validation; an old/corrupt blob with missing arrays (`session.checkpoints`, `analysis.steps`) hydrates and crashes on later access. Add a thin `validateShape`/version guard (reuse `validateShape` from `llm.ts:778-781`) so a stale blob degrades to fallback. **Acceptance: a v(N-1)-shaped localStorage blob loads without throwing.**

- **P1 auto-persona empty stall — NEW (A P2, fits L0 "never silently fail").** `auto-persona.ts` `callLLMJson` on failure silently returns `[]`, so rehearse can start with zero personas and stall. On failure, fall back to rule-based `recommendBlindSpotPersona` ×2-3; log a quality signal.

- **P1 analytics swallowed-insert canary — NEW (data-trust prereq for ALL §4 probes).** `analytics.ts:168-179` wraps an async `supabase.insert` in try/catch that cannot catch the rejection; `.then` at `:176` has no `.catch` → unhandled rejection, no success/failure canary. Add `.catch` incrementing a per-session failure counter surfaced once; route predicate/return events through an at-least-once path, NOT this fire-and-forget pipe, before any kill-test cites their counts.

- **P2 data-correctness (moved earlier from L5) — `db.ts` merge.** Standalone, gates on nothing (see L5 for the corrected mechanism + the `progressive_sessions` second merge). Schedule the `deleted_at`-aware merge + clock-skew tolerance as an L0/L1 P2 rather than waiting on L5 traction.

- **CLOSE, do not re-fix:** `decision-quality.ts:146` already guards `recast.analysis.steps || []` AND the outer `recast?.analysis` (`:145`). **(NEW caveat: `:83` reads `reframe.analysis.hidden_questions || []` without guarding `reframe.analysis` being null — `RecastAnalysis|null` per `types.ts:197`; check the analysis-null exposure before closing.)**

**Acceptance:** zombie stream aborts within ~30s with a VISIBLE error AND a cleared spinner; brief export + judgment run on missing-`steps` analyses without throwing; a validation-failed worker never silently accepts (blocks/re-prompts, reaches a terminal callback, is marked in the transcript); quota write shows a toast; a stale-schema localStorage blob loads without throwing; auto-persona failure degrades to rule-based; analytics insert failures are counted; 1004 tests green.

> **UX P0/P1 woven into L0** (see §UX-L0): streaming progress + Cancel on the first-run analyze screen (the #1 dead-end), and the silent-failure copy for storage/worker-accept. These are the same fixes as above, surfaced to the user.

### L1 — Output persistence as the FIRST visible win (+ orientation wins; needs only an L1 migration)
**Goal:** a substrate where retention *can* exist, plus the metric canary and the orientation floor, before betting on direction.

- **Artifact persistence** — outputs are copy/download-only strings (`OutputSelector.tsx:121` `setPreview(format.generator(project))` regenerated each select; consumers CopyButton `:172`, download Blob `:128-137`). **(corrected: NOT "orphaned"/data-loss — the rendered string is ephemeral but its INPUTS persist** (`project-brief.ts:10-17`, `agent-spec.ts:8-13` read from stores that sync to Supabase), so a brief is deterministically regenerable after reload.) **The real gap:** no stored, versioned, shareable artifact and **no capture of post-generation edits** (preview is read-only `<pre>` `:178-180`). Ship ONE Supabase `artifacts` table + versioning (verified: none exists today). **Decide now:** immutable regenerated snapshots vs user-editable documents — the schema differs. **(NEW: align `agent-spec.ts:7` `projectFilter(project)` vs `project-brief.ts:11` inline `d.project_id === project.id` before snapshotting — generators are not behavior-identical on project scoping.)**

- **Retention reflection TAB** (C Wave 0) — **(corrected attribution & host.)** Compose `navigator.ts` builders (`buildNavigatorProfile`/`buildNavigatorInsights`/`buildLearningCurve`) + `getUserPatterns` (`useJudgmentStore.ts:14,57`) + `getPersonaAccuracySummary` (`useAccuracyStore.ts:12,49`). Build it **inside `NavigatorStrip`** (where `buildNavigatorInsights`/`LearningCurve` already render) or as a new tab — **NOT inside Logbook** (`Logbook.tsx` is the branch/waypoint rail, returns null when `waypoints.length===0`, shares no code). `useMemo`, **no new table.** **(NEW caveat: gated near-empty for new users — `buildNavigatorInsights` needs `tier>=2`/`sessionCount>=2` (`navigator.ts:113-117,191`), `DQSparkline` needs ≥2 dq_points (`NavigatorStrip.tsx:268`), `getObservationsSummary` returns `hasData:false` below 2 sessions; on runs 1-2 it shows almost nothing — don't bill it as a run-1 "first visible win.")** **(NEW: reconcile with the existing Coda `meta_reflection` reflection at `OutputSelector.tsx:94-112,184-280` — risk of two competing reflection surfaces.)**

- **Convergence gauge as a standalone L1 win — NEW (B P1 / C must-carry, decoupled from L2).** `is_converged`, `estimated_rounds_left`, `trend` are computed (`progressive-convergence.ts:94-110`) but rendered NOWHERE (grep: 0 `.tsx` hits); only `metrics.score`+`guidance` show (`ProgressiveFlow.tsx:837-852`). Promote a minimal gauge (per-step % + a "ship at 75%" line + trend arrow + rounds-left chip) that reads the already-computed fields. **No L2 dependency, no new table.** Decouple from the L4 `ConvergenceRose` re-skin so "when do I stop?" ships before the chassis refactor.

- **Team-theater honesty — NEW (B P2, dropped).** HeroFlow shows avatars "assembling" as orchestrated multi-agent work, but `runInitialAnalysis` is a single LLM call. (Note: the WORKER stage `runAllAIWorkers` DOES fire one call per persona — `worker-engine.ts:141-151` — so the worker board is honest-but-unstated.) Decide in L1: relabel the first-run spinner to honest copy ("Analyzing from multiple perspectives") OR show real per-lens streaming. Pairs with §UX team-honesty caption.

- **Anon→auth quota narrative — NEW (B P2, dropped).** Quota copy conflates "free trial" urgency with "free tier forever"; hitting the limit shows a bare technical error (`workspace/page.tsx:390-398`) with no upgrade story. Also `RateLimitBadge` is gated to logged-in users (`Header.tsx:159`) so anon users never see a remaining count until the hard wall. Disambiguate the copy; show anon remaining-runs; warn before the last run. Pairs with the §4 telemetry fix so conversion is measurable.

- **Signal read-path is local-only — NEW data gap (P1) feeding L1b/anchoring.** `recordSignal` writes BOTH localStorage (FIFO 500) AND Supabase, but `getSignals`/`getSignalsByType` (`signal-recorder.ts:49`) read ONLY localStorage. Every learning consumer (`context-builder.ts:103`, navigator coaching, the override-rate metric) sees ≤500 local records and nothing on a new device. Make the read path Supabase-aware (`loadAndMerge('quality_signals')` into the in-memory cache) OR document the loop as local-session-only — and add the anchoring metric only AFTER the read path can see full history.

- **`hit-rate.ts` is unpersisted/capped — NEW (P2).** localStorage-only ("Supabase 동기화는 추후", `:6`), 200-cap destructive splice (`:59`). Either add `hit_records` to the `db.ts` TableName union + table + `loadAndMerge`, OR stop citing it in §1 as a durable "closed calibration" asset.

**Acceptance:** a generated brief/spec survives reload and leaving the preview; the reflection tab renders read-only with no new table; the convergence gauge shows trend + rounds-left from existing data; quota copy disambiguates trial vs tier; signal read path is Supabase-aware (or documented local-only); 1004 tests green.

### L1b — userContext → prompt injection (highest leverage/cost, no foundation dep)
**Goal:** make "smarter as you use it" partly true at near-zero cost; establish the reference-only discipline L5 inherits; consolidate i18n.

- Wire observation context into `buildInitialAnalysisPrompt` (`progressive-prompts.ts:23`, verified takes only `(problemText, locale)`) via `참고:` reference-only (NOT directive), wrapped in `<user-data>` + `sanitizeForPrompt()`. **(corrected: `user-context.ts:56-59` marks observations UI-only deliberately; `buildNavigatorProfile` already consumes signals (`navigator.ts:95-147`); the narrow wound is no PREDICTION-vs-OUTCOME grading — and the enhanced path already injects elsewhere. This fix closes the initial-analysis entry point only.)**

- **i18n consolidation — NEW (P2 a11y/i18n gap).** `useLocale()` defaults `'en'` then switches in `useEffect` (acknowledged flash, `useLocale.ts:27-33`); `t()` reads localStorage synchronously, defaults `'ko'` (`i18n/index.ts:20`). A page mixing both renders mixed-language on first frame. Unify on one resolved default; persist locale to a cookie read during SSR. Replace the ~15 duplicated inline `const L = (ko,en)=>…` in ProgressiveFlow with the shared `useLandingText` helper (`useLocale.ts:63`) — kills the CLAUDE.md single-source violation. **(NEW lint, P3):** add an ESLint/AST check that fails on literal Hangul in JSX outside `L()`/`t()` (only 21 of 141 component files import `@/lib/i18n`; e.g. `BranchMap.tsx` has Korean and no i18n helper), so EN parity is enforced on every surface incl. L4 re-skin.

**Acceptance:** initial-analysis prompt includes a one-line `참고:` when observations exist, sanitized; behavior unchanged when none; first paint is single-language; lint fails on unwrapped Hangul; 1004 tests green.

### L2 — Chassis decomposition behind a feature flag (highest-risk gate; maintainability + C re-skin)
**Goal:** make the monolith safe to extend. **Maintainability gate, not a value gate** — the Decision Contract leaves ProgressiveFlow untouched, so L4's graft does NOT depend on this. **(corrected: mark "untouched" as a build-time design intent to validate, not a verified ground truth.)**

- Verified sizes: `ProgressiveFlow.tsx` = 2670; `useProgressiveStore.ts` = 1786. **(corrected coupling: store statically imports ONLY `useAgentStore`; `usePersonaStore` is a single lazy `require()` at `:946`; NO reframe/recast/project import.)** **(corrected: `IntersectionObserver` is NOT used in the chassis — only `useScrollReveal.ts`.)** Real coupled concerns: scroll-into-view (`:951` + 28 scroll refs), worker runtime (`runAllAIWorkers`/`runPipeline` `:32`; store `:213-216`), version/branch/draft logic (`nextChildLabel`/`recordCheckpoint`/`migrateBranches`/`addDraft`), and **46 useState/useRef/useEffect** hooks (the UIState target).
- Keep legacy `ProgressiveFlow` behind a feature flag; extract `WorkerRuntime`/`DraftManagement`/`VersionControl`/`UIState` hooks.
- **(NEW prerequisite, P1):** the named regression gate is structurally insufficient — only `voyage-branch-simulation` exercises the chassis (store-level); `orchestrator-journey` + `workflow-review-integration` test pure lib fns the decomposition does NOT move and will stay green regardless of a scroll/worker/UIState break. **Add a net-new component/render test before the flag flip**, or the "reversible, test-gated" guarantee is hollow.
- **(NEW extraction hazards):** the lazy `require('@/stores/usePersonaStore')` at `:946` bypasses the static import graph (SSR/test-mock hazard) — convert to DI when extracting; preserve `MAX_BRANCHES = 8` (`:47`) localStorage-size guard in VersionControl; carry the three load-path migrators `migrateBranches`/`migrateWorkers`/`migrateSessionDrafts` (`:356/391`, chained at `:447/486`) into whichever hook owns Draft/Version.
- **a11y baseline while extracting — NEW (P1/P2).** Add `aria-label`/associated `<label>` (via `L()`) to the placeholder-only inputs at `ProgressiveFlow.tsx:813, :2167, :2615` (grep: no accessible name today). Acceptance: extracted input components expose an accessible name (axe-core: no unlabeled fields).

**Acceptance:** both flag paths pass orchestrator-journey/voyage-branch/workflow-review-integration **AND a net-new component render test**; extracted hooks unit-tested; lazy require converted to DI; `MAX_BRANCHES` + migrators preserved; inputs have accessible names; flag flip reversible; 1004 tests green on both paths.

### L3a — Phase-0 persistence slice (SMALL — typed builders exist; runs PARALLEL with L2)
**Goal:** make provenance real so the genealogy thread doesn't degrade to leg-name labels; supply the stable predicate id §0 needs.

- Verified: `context-chain.ts:119-525` returns typed contexts with provenance markers; **no `useContextChainStore`, no `_source`, no `traceProvenance`, no persistence** (all grep-confirmed). Work = persistence + `_source` + `traceProvenance` + a `context_chains` SQL migration — NOT a from-scratch pipeline.
- **Stable-ID-only slice** — add a deterministic `id` to `HiddenAssumption`/`KeyAssumption` at generation (no `status`/`created_at`). Lets the fragile substring linkers be deleted: `agent-spec.ts:104` (`substring(0,8)`) and `prompt-chain.ts:151-152` (`substring(0,10)`) (verified the ONLY two; grep "both gone" is satisfiable). **(NEW: the substring linkers are wrong on FRESH data, not just legacy — `step.task.includes(assumption.substring(0,8))` yields false POSITIVES on shared 8-char prefixes and false NEGATIVES on paraphrase (the normal case, since tasks & assumptions come from separate prompts). Strengthen acceptance from "no false link on legacy" to "links are deterministically correct on fresh data.")**
- **(NEW: id alone is insufficient to delete the linkers.)** The replacement key must exist in BOTH the assumption AND the step. `RecastStep` (`types.ts:119-134`) has no assumption-id field; `agent-spec.ts:107` DERIVES `validates_assumption` from the substring match — removing the match removes the only producer. **Add a `validates_assumption_ids` (or `source_assumption_id`) field on the step, populated at generation, and join on id.** This is net-new schema the prior L3a scope omitted.
- **(NEW: three more text-identity linkers in the same area)** — `mergeAssumptionsIntoKeyAssumptions` (`context-chain.ts:506-525`) dedupes by lowercased TEXT (`:510-516`) → re-key on id once ids exist; `buildReframeContext` (`:134-136`) coerces legacy `string[]` assumptions to objects at READ time → the id strategy must assign ids in this normalizer too; `RehearseStep.tsx:194` does `JSON.stringify(buildReframeContext(...))` straight into the prompt → updating the serialization so new `_source`/`id` fields aren't dumped verbatim.
- **progressive_sessions hardening — NEW (P1, prerequisite for any predicate/grade history).** `loadSessions` casts `remote.data as ProgressiveSession` with zero validation (`useProgressiveStore.ts:467`); the blob has no `schema_version` (`20260409_progressive_sessions.sql`, `data JSONB DEFAULT '{}'`); three ad-hoc migrators run every load. Add a `schema_version` field + a single forward-migration switch (replacing the three passes) + `validateShape` on `remote.data` **before** L3b writes any per-predicate history into the blob, so contract data is born versioned.
- **Output self-validation lint — NEW (A P2).** Add `validateAgentSpec`/`validatePromptChain` that checks every referenced assumption/step id resolves and `critical_path` indices are in range; run before preview, warn on invalid. Pairs with the stable-id slice and is grep-verifiable alongside the linker deletion.

**Acceptance:** context chain persists to `context_chains` with `_source`/`traceProvenance`; assumptions carry stable IDs at generation (incl. the legacy normalizer); steps carry `validates_assumption_ids` and the linker is an id-join (substring linkers + the text-dedup re-keyed; grep-verified gone); fresh-data links deterministically correct; legacy data renders no false link; progressive_sessions is versioned + validated; agent-spec/prompt-chain self-validate; 1004 tests green.

### L3b — Full per-predicate status migration (3-4 wks, GATED behind kill-test)
**Goal:** the addressable schema the falsifiable-predicate loop needs. **Do NOT start until §4 Test 1 OR Test 2 passes.**
- Add `id`/`created_at`/`status: held|contradicted|pending` to the assumption type across reframe/recast/rehearse + the eval engines (verified cross-leg/cross-engine: 31 files emit/consume `hidden_assumptions:`/`key_assumptions:`); handle JSONB atomicity (the `20260409` atomic-RPC `update_worker_response` — comment "avoids JSONB read-modify-write race condition", `:35-78` — proves this seam burned the team). **Prerequisite:** the session blob must be bounded/server-persisted AND versioned (L3a + L5) first.

**Acceptance:** gated, not scheduled. If the kill-test fails, never built; ~1-2 days on the sidecar instead; ship Mirror without apology.

### L4 — Forward surfaces: C's incremental spine + the Decision Contract graft
**Goal:** ship the genealogy throughline as the differentiator vs "just ask ChatGPT."

- **Decision Contract (the §0 KICK, L1 timing — no L2/L3 dep)** — serialize 3-6 predicates from recast actor assignments + `classified_risks` + `governing_idea` into a reversible JSON sidecar, surfaced as a FinalCard "Verdict" view. **(corrected: derive store-free off `runInitialAnalysis`; name the mount host — FinalCard is store-coupled at `:39`, and the initial-analysis path `workspace/page.tsx:221` renders no FinalCard. Each predicate carries the stable id pulled forward from L3a — §0 corollary.)**
- **Return-to-grade loop (NET-NEW write path, web-native, no heavy migration)** — **(corrected from "upgrade existing plumbing.")** Build a real `OutcomeRecord`/`PredicateGrade` WRITER UI + store with localStorage+Supabase persistence (`OutcomeRecord` is read-only/producer-less today). The grade references `predicate_id`, never text. The write must be observable (await/queue/retry + toast on failure — §0 corollary). **(corrected: the existing accuracy UI is on the LEGACY path — `addRating` is called only at `tools/FeedbackResult.tsx:66`, rendered only by `RehearseStep.tsx:12,673`, NOT by ProgressiveFlow.** Place the grading UI on the progressive FinalCard/Verdict surface — net-new placement, not a repoint.) This loop is the **§4 Test-1 instrument**. **(NEW: `OutcomeRecord` is project-scoped (`types.ts:502-513`), not per-predicate — define a per-predicate record shape; do not map 3-6 predicates onto a single project-level record.)**
- **Persona re-approval loop — NEW (B must-carry, dropped).** After the user edits the plan in response to a DM/persona concern, there is no "would they approve NOW?" re-test. Add a "Check: would [persona] approve now?" post-refinement re-run that re-scores the edited plan against the same persona and shows before/after reaction. Reuses PersonaFeedbackStep + accuracy plumbing; default-on for the first 2-3 iterations. Distinct from the §0 predicate loop (in-session vs return-to-grade).
- **Backtracking-cost preview — NEW (B must-carry, dropped).** Editing `real_question` silently triggers a ~30s re-analysis with no cost signal → abandonment. Before a re-analysis-triggering edit, show a one-line hint ("edits the real question → re-analyzes the team, ~30s; your Q&A stays"). Cheap, no schema.
- **Agent Spec 2.0 executable fields — NEW (A P1 / C, dropped).** `RecastStep` carries only `{task, actor, time}`, so the generated YAML is a reference doc, not code-generatable. Extend with optional `validates_assumption?`/`abort_condition?`/`success_signal?`/`feeds_into?`, populate during recast, emit in `agent-spec.ts`. Rides the L3a stable-id slice (abort/validate reference `HiddenAssumption.id`). Keeps "the ONE sanctioned bridge to execution" true.
- **Onboarding (C Wave 0)** — **(corrected: the planned first-run auto-demo is UNBUILT — `argus.has_onboarded` does not exist; `projects.length===0` at `:458` is marketing copy.)** Implement the gate: `projects.length===0 && !localStorage['argus.has_onboarded']` → auto-open `InteractiveDemo.tsx` (imported `workspace/page.tsx:31` but only reachable via manual demo-card click `:444`) + a net-new contrast card, in the `if(!currentProjectId)` branch at `:690` (wrapping/preceding the `<HeroFlow>` return; HeroFlow currently owns the demo trigger internally), then set the flag. This is the single best "show, don't tell" moment and it never fires today.
- **Core-flow re-skin (C Wave 2, gated on L2)** — additive `LegCanvas`/`AnchoringMoment`/`ConvergenceRose` reusing `phaseIdx()` **(corrected: not exported from `ProgressiveFlow.tsx:132` — lift to a shared module first)**; inline provenance tags; ~15-line gold ancestry overlay in `BranchMap` **(corrected: reuse local `edgePath()` `:31` + the existing `activeBranchId` node-coloring; `getActivePath` lives in `version-tree.ts:35`/`branch-map-layout.ts`, NOT inside BranchMap — the overlay strokes a gold path over existing coordinates, it does not call `getActivePath`)**; ✓/미검증 assumption toggle. Re-skin, not rewrite.
- **Attributable outputs (C Wave 1, gated on L3a)** — four-act ship's-log Brief on `lib/export.ts` `voyageLogToMarkdown` (`:25-49`, used by `FinalCard.tsx:45`) with a "왜?" provenance anchor per line using stable `HiddenAssumption.id`. **(corrected: the "왜?" anchor + stable-id linkage are NET-NEW — today it renders waypoints with no per-line anchor and uses no assumption id; the `HiddenAssumption` import at `export.ts:1` is legacy string handling only.)** **(NEW: `voyageLogToMarkdown` returns `''` with no waypoints (`:35`) — verify waypoint generation coverage or the Brief renders empty for sessions that never logged waypoints.)** Agent Spec is the sanctioned dev-tab bridge.
- **`.argus/` render spine (graft, L1/L3a timing)** — a VIEW over the on-disk `.argus/*.md` + `argus-plugin/CONTEXT-CONTRACT.md` ledger (both verified), wired after the stable-id slice.
- **Mobile / responsive pass — NEW (A+B, entirely absent in Rev 2).** See §UX-MOBILE; gate "no horizontal overflow at 375px" + a landing LCP budget into L4 acceptance.
- **Lazy-load landing Acts — NEW (perf).** `page.tsx:3-5` statically imports all three Acts; `ShipCutaway.tsx` = 1011 lines / 148 SVG primitives, `SailingShip.tsx` = 505 / 69 + 14 framer-motion loops. Lazy-load Act2/Act3 via `next/dynamic` (ssr:false or IO-gated) so the cutaway isn't in the critical first paint. **(corrected: the §7 "@react-three/fiber ~200KB" claim is WRONG — those packages are dead/unused; the real cost is inline-SVG DOM weight + framer-motion.)**
- **a11y modal primitive — NEW.** L4 adds overlays (Verdict, onboarding auto-demo). Bake a shared focus-trapping modal (store `activeElement` on open, trap Tab, restore focus on close — `PersonaPoolModal.tsx` handles Escape `:216` but none of this). Acceptance: new modals trap focus and close on Escape.

**Acceptance:** Verdict sidecar generates without touching ProgressiveFlow and deletes cleanly, with a named store-free/coupled host; each predicate carries a stable id and the grade write is observable; return-to-grade persists a per-predicate record that `context-builder` reads back (regression test asserts a non-empty producer exists); onboarding auto-demo fires only for first-run and hands off to a pre-filled textarea; the gold thread resolves to real `.argus`/typed-chain links (no false links on legacy); persona re-approval + backtrack-cost preview ship; Agent Spec emits executable fields; no horizontal overflow at 375px; new modals trap focus; 1004 tests green at every sub-step.

### L5 — Deferred / kill-test-gated (do NOT build pre-traction)
**Goal:** the compounding-judgment moat — ONLY if the retention premise survives falsification.
- **Silent prediction-and-self-scoring loop** — build only after the Decision Contract earns Test-1/Test-2 evidence; reuses `judgment-vitality.ts`/`observation-engine.ts` (both verified real, read-only substrate); pair with the anchoring/atrophy guard.
- **Persona-calibration vs predicate-grading decision — NEW (A P1).** §0 repoints the accuracy channel from persona-rating to predicate-grading; state explicitly whether per-aspect persona calibration is preserved (two record types on one store) or deliberately abandoned. If preserved, schedule the structured `PersonaBehaviorModel` here; if dropped, say so in §6/§7 so a shipped learning signal isn't silently deleted.
- **PARKED:** B's PostExecutionHook/outcome-table; A's prompt-mutation engine (Track L Phase 4); the dead `retrospective.ts` read path (producer-less, `:5-8` "That UI is gone" — either delete the dead read+prompt section or reinstate a minimal writer as part of L4 return-to-grade; do NOT cite it as a closed loop).
- **P1 data-correctness — `db.ts` merge (corrected mechanism).** `mergeByTimestamp` (`db.ts:57-78`) does string `remoteTime > localTime` (`:71`) with no clock-skew handling. **(corrected: the resurrection risk is NOT "merge ignores `deleted_at`" — `loadAndMerge` pre-filters `!r.deleted_at` at `:106`.)** The REAL bug: `loadAndMerge:113-119` computes `localOnly = merged − remoteIds` and **re-UPSERTS it back to Supabase**, actively un-deleting a row soft-deleted on another device. Fetch tombstones, pass them into the merge so a newer remote `deleted_at` wins and removes, and exclude tombstoned ids from the re-upsert. **(NEW empty-stamp bug):** `sanitizeItem` strips `updated_at` (`:32-40`), so a freshly-inserted local grade has `''` local stamp and is overwritten by ANY remote row on next merge — unify on Date compare with a tie-break that never discards a missing-local-stamp row (treat as newest / merge append-only tables by union-of-ids). **Escalate to a prerequisite of L4** since L4 introduces freshly-written local grade rows.
- **progressive_sessions second merge (corrected — NEW).** `progressive_sessions` does NOT use `mergeByTimestamp`; `loadSessions` (`:466-482`) compares **server-trigger `updated_at` vs client-clock `updated_at`** (`:456` server-selected vs `:417,517` client-stamped) — a concrete server-vs-client skew. It is fire-and-forget with a swallowed `.catch(()=>{})` (`:324`), 2s-debounced (`:311`), `.limit(100)` load (`:459`) → best-effort durability with zero UI signal + silent loss of >100 sessions cross-device. NOT soft-deletable (not in `SoftDeletableTable`, `db.ts:22`). Fix the skew + add a durability/eviction policy; surface a sync-failure signal.
- **Session-blob durability:** cap/eviction + server-side persistence for the uncapped `progressive_sessions` JSONB (discrete caps only: signals 500, evals 200). **Must precede L3b** (+ L3a versioning).

**Acceptance:** gated — the prediction loop only exists post-kill-test; the anchoring guard ships with it; the persona-vs-predicate decision is documented; merge/blob fixes land as standalone P1/P2s (the empty-stamp fix as an L4 prerequisite).

---

## §UX — Basic Usability & Orientation (NEW first-class workstream)

Every "뭐지?/어떻게?/어디?" moment from the confusion sweep, sequenced and file:line-anchored. Each item: **[severity · effort]**. P0/P1 weave into L0/L1; P2/P3 into L4. The unifying user questions: *What is this? · Where do I click? · What happens next? · Did my action register? · How do I get out / undo? · Where am I?*

### §UX-L0 — Crash-adjacent dead-ends (ship with L0)
- **[P0 · M] First-run analyze has no Cancel and no progress** — `workspace/page.tsx:502-532`: spinner + "보통 20~40초" but NO cancel, NO elapsed timer; `runInitialAnalysis` called WITHOUT the `signal?` it accepts (`progressive-engine.ts:293-296`). On a hang (L0 P0) = permanent spinner. Fix: wire an `AbortController` into `handleSubmit`, add a live mm:ss counter, after ~30s swap to "평소보다 오래 걸려요 — 계속 진행 중" + a **Cancel** that aborts and returns to idle preserving input. Reuse `elapsedSec`/`isLongWait` from `ProgressiveFlow.tsx:1062-1084`.
- **[P0 · M] Outputs surface hides the 4 Ithaca outputs** — `FinalCard.tsx:66-67` shows ONE "완성된 기획안"; the 5-format `OutputSelector` lives on a different surface with no link. Add a footer band "이타카에 도착했습니다 — 4가지 산출물" with labeled mini-buttons + a "4가지 산출물 보기 →" CTA.
- **[P0 · M] Outputs say nothing about WHAT to do next** — `OutputSelector.tsx:167-182` offers only Copy/Download. Add a "다음에 할 일" line per output (Brief → email/Slack; Prompt Chain → paste into ChatGPT in order; Agent Spec → hand to engineer; Checklist → check off). Wire existing ShareBar actions.
- **[P0 · S] Silent worker auto-accept (philosophy)** — same as L0 P1; user-facing copy: "이 결과는 자동 검증을 통과하지 못했어요 — 다시 시도하거나 직접 확인하세요" + retry (`WorkerCard.tsx:268-294` panel). Relabel "그냥 사용" → "확인했어요, 그대로 사용".
- **[P1 · M] LoadingSteps freezes on the last step** — `LoadingSteps.tsx:14-19` advances on a 2500ms timer, clamps at last step → frozen ~25-30s for a long call (used `RehearseStep.tsx:595-600`, `ReframeStep.tsx:986,1092`). Add elapsed counter + after ~25s "거의 다 됐어요 — 평소보다 조금 더 걸리고 있어요". Also: **ReframeStep streams real tokens (`:487 onToken: setStreamingText`) but `streamingText` is NEVER rendered** — render it as a dimmed live preview so the user sees work on THEIR problem.
- **[P1 · S] Copy/Download silent failure** — `CopyButton.tsx:27-29` catches into `console.error`, never flips to "Copied!". `OutputSelector.tsx:128-137` download has no acknowledgement. On catch show "복사 실패 — 다시 시도" (red) + selectable fallback; on download flip to "저장됨 ✓" + a "Downloads 폴더 확인" toast.
- **[P1 · S] Human-send silent bail** — `ProgressiveFlow.tsx:1247-1248` `if(!authSession?.access_token) return;` leaves the worker looking sent. Set `status:'error', error:'로그인이 필요해요'` instead.
- **[P1 · S] Storage quota silent loss (copy side of L0)** — toast "저장 공간이 가득 찼어요 — 일부 작업이 저장되지 않았을 수 있어요. 로그인하면 안전하게 보관돼요."
- **[P1 · S] Discussion failure uses native `alert()`** — `RehearseStep.tsx:402-404`. Replace with the inline red error+retry card already used at `:569-578`.
- **[P1 · S] Global ErrorBoundary "Try again" loops** — `ErrorBoundary.tsx:31-33` only clears `hasError` → re-crashes the same subtree. Add an always-safe "새 프로젝트로 돌아가기 → /workspace" action clearing `currentProjectId`.
- **[P1 · M] Language toggle hard-reloads mid-voyage** — `useLocaleSwitch.ts:18-22` `window.location.reload()` unconditionally; anon work lives only in session. Disable during analyzing/mixing/worker-running with a tooltip, OR confirm "진행 중인 분석이 있어요 — 계속할까요?" before reload.

### §UX-L1 — Orientation & next-action (ship with L1)
- **[P0 · S] Landing has no plain-language value prop above the fold** — `Act1Voyage.tsx:67-98` is 100% metaphor. Add a deck line under the h1 before the siren line: "막막한 일을 AI에게 시키기 전에 — 무엇을 시킬지부터 같이 정리합니다 / Before you hand work to AI — figure out exactly WHAT to ask for." (Promote the Act3 "knowing what to write" line, `Act3OnDeck.tsx:53-65`, which is currently the LAST thing seen.)
- **[P0 · S] Landing hero has no real CTA** — only an 11.5px underlined scroll link (`Act1Voyage.tsx:120-135`); the real CTA is 2 screens down (`Act3OnDeck.tsx:86-108`). Add a primary "무료로 시작하기 / Start free" → /workspace right after sub-2.
- **[P1 · S] Workspace entry has no headline/tagline** — `workspace/page.tsx:348-354` opens straight into a textarea; the value-prop sentence is rendered LAST (`:458-463`). Add an editorial headline ABOVE the input for `projects.length===0`; move the marketing sentence up.
- **[P1 · S] "팀" referenced before it exists** — `workspace/page.tsx:352-353` "팀이 정리해요" with no prior mention; outputs never named on entry. Add a 3-step micro-preview chip row "상황 입력 → AI 팀이 분석·초안 → 산출물 4종".
- **[P1 · S] Reframe metaphor-first title** — `ReframeStep.tsx:807-809` "항로 재설정 / 문제 재정의". Lead with plain meaning ("내 질문 다시 보기"), demote the metaphor.
- **[P1 · M] Reframe before/after contrast is AFTER confirm** — `ReframeStep.tsx:1126-1178` (STEP 2) shows the reframed question but NOT the original side-by-side; the strikethrough contrast only exists in DONE (`:1328-1353`), after the user confirmed. **This is the "vs just ask ChatGPT" moment** — move it into STEP 2 above the navy card.
- **[P1 · M] Worker stage: two competing "team" surfaces** — sidebar "분석 팀" (all agents) vs main "에이전트 검토" stepper (one at a time, `ProgressiveFlow.tsx:2076`). Make the stepper the single do-this-now surface; demote sidebar to status with a header hint.
- **[P1 · S] Worker fake activity tickers** — `AgentVisuals.tsx:9-36` cycles "경쟁사 사례 분석" etc. on a 2.2s timer unrelated to the call. Show specific lines only when `streamSnippet` exists; else "분석 중…" (`AgentSidebar.tsx:117`).
- **[P1 · S] Rehearse doesn't say it's SIMULATED** — `RehearseStep.tsx:413-416`; seeded personas look real (`usePersonaStore.ts:10-56`). Subtitle: "AI가 당신의 이해관계자를 연기해 미리 반응을 보여줍니다 (실제로 전송되지 않습니다)". Relabel submit "검토 받기" + Send icon (`FeedbackRequest.tsx:298-303`) → "AI 리허설 시작" + Play icon. Badge results "AI가 예측한 반응입니다" (`FeedbackResult.tsx:164-166`).
- **[P1 · S] Convergence "명확도" unexplained + done-state invisible** — `ProgressiveFlow.tsx:842-852` shows score% only; `is_converged`/`estimated_rounds_left`/`trend` rendered nowhere. Add a caption + (i) tooltip + "ship at 75%" tick; when converged show "✓ 충분히 또렷해졌어요 — 더 답하지 않아도 출항할 수 있어요". (Same data as the L1 convergence-gauge win.)
- **[P1 · S] No first-run onboarding exists** — the planned auto-demo (`workspace/page.tsx:690` gate) is UNBUILT. (Build in L4; but the entry-screen orientation copy above is the L1 stopgap.)
- **[P1 · M] Default flow hides the past-work/Navigator layer** — `ProgressiveLayout` renders only `<Logbook/>` (`:124`); `NavigatorStrip` (the stats hub) is only in the legacy path (`:843`). Surface the reflection tab in the default flow (pairs with L1 reflection tab).
- **[P1 · S] Logbook null on empty** — `Logbook.tsx:92`/`LogbookDrawer:304` return null. Render "항해를 시작하면 여기에 결정 기록이 남아요" placeholder.
- **[P1 · S] No "where do my outputs go" / Verdict absent** — covered by §UX-L0 outputs items + L4 Verdict.

### §UX-L4 — Polish, metaphor consistency, affordances (ship with L4)
- **[P0 · S] Output card labels are orchestra jargon** — `OutputSelector.tsx:38/48/58/68` "프로그램 노트/파트보/총보/셋리스트". Replace primary labels with "기획안 (공유용)/AI 프롬프트 묶음/AI 에이전트 설계서 (개발용)/실행 체크리스트"; demote the metaphor to grey subtitle. Add audience tags (비개발자용 / 개발자 전달용).
- **[P0 · M] Tool names differ across screens** — `i18n/ko.ts:13-20` vs `workspace/page.tsx:626-630` vs `RehearseStep.tsx:413` (recast=선원배치=실행설계; rehearse=리허설=사전검증). Single-source each label in i18n; always pair metaphor+plain ("선원 배치 (실행 설계)"). Unify the progress-dot labels (`ProgressiveFlow.tsx:2477`) to the same vocabulary + add "3/5".
- **[P1 · S] "이타카" header unexplained** — `OutputSelector.tsx:141`. "완성된 산출물 — 형식 선택"; metaphor as a 1-line subtitle.
- **[P1 · S] Decision log is a tiny grey checkbox** — `FinalCard.tsx:76-84` (the differentiating asset). Promote to "결정 과정도 함께 (Argus만 줄 수 있는 것)" with a 미리보기 toggle + value line "AI는 결과만, Argus는 왜 그렇게 결정했는지까지 기록합니다."
- **[P1 · S] OutputSelector preview is a dark code terminal** — `:178-180` `bg-[#1a1a2e] font-mono` for prose; contradicts the ivory DNA. Render markdown outputs as prose on `var(--surface)`; reserve the dark block for YAML Agent Spec ("코드 형식 (개발자용)").
- **[P1 · M] Reframe 2-step has no roadmap; direction-select is silently required** — `ReframeStep.tsx:1008,1181-1246,1289`. Add a 2-step stepper, an instruction "방향을 하나 고르면 완료", and a disabled-confirm tooltip.
- **[P1 · S] Rehearse concerns are read-only with no action** — `FeedbackResult.tsx:300-311`; `onStartDebate` is never passed from RehearseStep so the triage action never renders. Pass the handler OR add per-concern "반영" + line "다음 단계에서 자동으로 정리됩니다."
- **[P1 · M] Standalone tools vs voyage orientation** — `/tools/reframe` (ReframeStep) gives no breadcrumb tying it to the 5-stage arc; "선원 배치로 보내기" feels like teleporting. Add "항해 1/4 · 항로 재설정" breadcrumb + "다음 단계로 — 항해로 돌아가기".
- **[P1 · S] VoyageChart verbs undefined + active-node no-op** — `VoyageChart.tsx:126-130,283-285,311-319,354-361`. Add a one-time legend ("항로 = 같은 문제를 다르게 풀어본 갈래; 되돌아가도 지금 작업은 그대로"); show "현재 위치예요" on active-node click.
- **[P1 · S] Branch delete has no confirm; fork cap silent** — `Logbook.tsx:135-143`, `VoyageChart.tsx:208-217` (delete no confirm) vs the non-destructive revisit which DOES confirm; `forkBranch` returns null at MAX_BRANCHES=8 with no toast (`useProgressiveStore.ts:1480-1483`). Add delete confirmation + "항로는 최대 8개" toast.
- **[P2 · S] Act2 personas read as real humans** — `voyage-crew.ts` + `StationCard.tsx`; "AI" never appears. Add "AI가 연기하는 가상의 전문가팀 — 직접 꾸리지 않아도 됩니다." Same for the worker stage honesty caption (`AgentSidebar.tsx:447`: "각 팀원은 별도 AI 분석이에요").
- **[P2 · S] XP/Lv. gamification at the triumphant end** — `FinalCard.tsx:122-148`. Gate behind a "이게 뭔가요?" tooltip ("XP가 쌓이면 이 에이전트가 당신의 맥락을 더 잘 이해해요") or move below the hand-off.
- **[P2 · S] "출항" CTAs hide what happens** — `TeamDeployBanner.tsx:436`, `VerificationGate.tsx:118,128`, `Act3OnDeck.tsx:86`. Pair the verb: "출항 — 팀에게 분석 맡기기"; neutralize the override "확인 없이 모두 반영하고 출항" → "검토 건너뛰고 진행".
- **[P2 · S] Empty/idle micro-affordances** — disabled Start button tooltip (`workspace/page.tsx:381`); auto-selected first persona label (`FeedbackRequest.tsx:49-62`); 3-persona cap silent (`:89-93`); demo cards don't say "무료 차감 없음" (`workspace/page.tsx:442-453`).
- **[P2 · M] IA glossary** — 항해장 (NavigatorStrip) / 항해일지 (Logbook) / 해도 (VoyageChart) / 워크스페이스 overlap with no key. Add role captions or reduce to two named panels (현재 항해 / 내 기록). Add a node-encoding legend to BranchMap.
- **[P3 · S] Misc** — NavigatorInline music-note icon → compass/lightbulb + "길잡이/Navigator:" label; reframe label-flip explainer when all-confirmed; Coda save destination note; demo-prefilled-text "내 상황으로 비우기" link.

### §UX-MOBILE — Dedicated mobile sub-track (~375px; ship with L4, gate into acceptance)
- **[P1 · M] Two stacked fixed bottom bars** — Worker bar `bottom-0` (`WorkerPanel.tsx:399`) + Logbook bar `bottom-[56px]` (`Logbook.tsx:311`) = ~108px of near-identical gold bars. Give distinct identity (Team avatars+label vs Compass+항해일지) or merge into one segmented bar with two tabs.
- **[P1 · M] Bottom-bar padding math wrong** — `workspace/page.tsx:114` hard-codes `pb-[calc(120px+safe-area)]`; the worker bar `flex-wrap` (`WorkerPanel.tsx:408`) can exceed 56px and overlap the last line. Stop hard-coding: `flex-nowrap`+truncate the bar to a fixed height, or measure via ref.
- **[P1 · M] Worker stepper dots are 2-8px tap targets** — `ProgressiveFlow.tsx:2083-2091`; Prev/Later are bare text links with no `min-h-[44px]` (`:2116-2126`). Make dots display-only, give nav buttons 44px bordered pills.
- **[P1 · S] VerificationGate looks stuck** — `VerificationGate.tsx:114-130`: big disabled gold button + a faint grey override link. Make the override a real bordered `min-h-[44px]` button "검토 없이 다 반영하고 출항 (N개)".
- **[P1 · M] Compare grid `grid-cols-2` unreadable** — `VoyageChart.tsx:243` inside `Modal max-w-lg` (`Modal.tsx:108`). `grid-cols-1 sm:grid-cols-2`; `p-4` on <sm.
- **[P2 · S] Submit-on-Enter eats multi-line input** — `workspace/page.tsx:367`; Shift+Enter hint is `hidden md:inline` and only after text exists (`:376`). Show the hint persistently all viewports; brief undo grace on Enter-submit.
- **[P2 · S] Worker drawer no grab-handle** — `WorkerPanel.tsx:434-456`; add a 36×4 grabber pill.
- **[P2 · S] Stacked sticky headers no divider** — Header `h-16`/`top-0 z-40` vs status `top-14 z-30` (`ProgressiveFlow.tsx:1905`); also the workspace layout reserves 56px while the header is 64px (`workspace/page.tsx:80,120`) → 8px mis-anchor. Align to one height; add a shadow under the status strip.
- **[P2 · S] Legacy bottom nav no safe-area** — `workspace/page.tsx:846-865`; add `pb-[env(safe-area-inset-bottom)]`.
- **[P2 · M] Logbook waypoint rows + actions too small** — `Logbook.tsx:195-273` (`min-h-[20px]`, 10px links). 44px row + bordered chips.
- **[P3 · S] Touch-only chevrons hidden** — Continue rows `opacity-0 group-hover` (`workspace/page.tsx:327`); echo pill `truncate` not expandable (`:478`, `ProgressiveFlow.tsx:1938`). Show low-opacity chevron on mobile; make echo tappable-to-expand.
- **Acceptance:** no horizontal overflow at 375px; output `<pre>` uses `max-w-full overflow-x-auto break-words`; bottom-bar content never overlaps the last line; primary actions are ≥44px.

### §UX-GLOBAL-CHROME — persistent Header (untouched by per-surface sweep)
- **[P2 · M] Six-item nav with locks reads as a paywall** — `Header.tsx:16-23,105,118`; three lock-gated items contradict the "로그인 없이 무료" promise. For anon first-run, collapse Agents/Manager/Projects into a "더보기" overflow, keep Workspace+Guide, caption locks "로그인하면 열려요 — 무료". Make the primary visible action "Start free", not low-contrast "Sign In".
- **[P2 · M] Anon has no live rate-limit counter** — `RateLimitBadge` gated to `user` (`Header.tsx:159-164`). Show anon remaining-runs; warn before the last (pairs with L1 quota narrative).
- **[P3 · S] Unlabeled theme toggle repaints the whole DNA** — `Header.tsx:152-158`, hover-only title, absent on mobile. Add a visible label / move dark mode to Settings; expose it in the mobile menu.

---

## 4. Cheap Validation Probes — Run BEFORE Betting Big

Run in parallel with L0/L1. They answer the existential questions no `file:line` analysis can.

1. **15-min telemetry reconciliation (HARD PRE-REQ).** `daily-report/route.ts:329` buckets `['landing_hero_submit','landing_cta_click']` under "랜딩 CTA"; `landing_hero_submit` is emitted in ZERO app code; `landing_cta_click` is live (`Act3OnDeck.tsx:88`, with `{cta:'voyage_helm'}`). Emit `landing_hero_submit` at the real site or drop the dead key; **(NEW: bucket `landing_cta_click` BY its `cta` discriminator — the dashboard drops it, so a future second CTA silently pollutes the go/no-go number)**; and add the **analytics insert canary** (L0 P1 — `analytics.ts:168-179` swallows the rejection with no `.catch`/no canary). **Until events are trustworthy, no go/no-go may cite dashboard numbers.**
2. **30-day re-voyage cohort SQL (1 hr).** Measured return rate (B's PV1).
3. **5-user moderated first-run test (half-day).** B's PV2 + metaphor comprehension; watch the first 90s for rage-quit. **(Cross-reference §UX-L1 landing/entry P0s — these are the rage-quit triggers.)**
4. **Test 1 — Wizard-of-Oz on ~15 voyages (the L3b kill-test).** Named-specific-predicate vs Mirror framing; weeks-later click/reply AND grade-vs-bounce. **(NEW prereq: the predicate must carry a stable id (§0 corollary / L3a pulled forward) or the grade joins by brittle text and the kill-test grades noise.)**
5. **Test 2 — silent prediction-accuracy slope (alternate kill-test).** Positive slope vs voyage count → green-light L3b. Both fail → ship Mirror, don't migrate.

---

## 5. Design-System Spine

One additive editorial "Logbook" token layer + one reference primitive. **(corrected internal contradiction: existing tokens are present, but the new ones are NOT.)**
- **Existing tokens verified present:** `--accent #96782e` (`globals.css:6`), `--font-display 'Noto Serif KR'` (`:57`), `--radius-md 12px` (`:79`), `--ease-wave cubic-bezier(0.32,0.72,0,1)` (`:75`), `--ease-spring cubic-bezier(0.16,1,0.3,1)` (`:72`).
- **Net-new appends (do NOT exist — grep-confirmed):** `--brass-shine` 1px highlight; scoped `.logbook-reveal` slow-motion class; **(corrected: there is NO "8pt spacing scale" token — spacing is Tailwind utilities, 4px base; adopt an 8pt rhythm via even utilities (p-2/p-4/p-6), not a phantom token.)** **All net-new tokens MUST also define dark-mode values** (`[data-theme="dark"]` `:86-142` remaps `--accent` to `#d4b968`) or the dawn-harbor look breaks under dark theme.
- **Primitive:** `LogbookCard` mirroring `Card.tsx` (`forwardRef` `:23` + variant-map `:10`). **(corrected: Card variants reference `--accent-light #b8963e` (`:12,32`), NOT `--accent #96782e` — state which gold the Logbook layer uses to avoid brass-vs-gold drift.)**
- **MOTION:** JS/Framer must IMPORT `EASE` from `progressive/shared/constants.ts` (`[0.32,0.72,0,1]` = `--ease-wave`), NOT hardcode `cubic-bezier(0.16,1,0.3,1)`. **(verified: the progressive components already comply — 12+ files import `EASE`; this is a regression guard, not a fix.)** Leave `--ease-spring` as-is — the CSS keyframes INTENTIONALLY use it (`:234,:242,:489`). **(NEW clarity: the "import EASE" rule applies ONLY to JS/Framer configs, NOT CSS animations — a contributor must not "fix" the CSS keyframes.)** Reuse the exported `SPRING` object (`constants.ts:4`) for the Logbook layer's "slow spring motion" rather than inventing one.

**a11y/contrast/motion additions to §5 (NEW, P1):**
- **[P1] WCAG AA contrast failures.** Measured on `--bg #f8f7f5`: `--text-tertiary #b0aa9f` = 2.16:1; `--text-secondary #868279` = 3.58:1; `--accent` text = 4.18:1; `--accent-light` = 2.63:1 (AA needs 4.5:1). These power real copy (`FinalCard.tsx:67` subtitle). **Delete the FALSE "contrast-safe (4.5:1+)" comment at `globals.css:6`.** Darken: `--text-tertiary ≈#8a8478`, `--text-secondary ≈#6f6b62`, accent-for-text `≈#7d6526`. Dark mode also fails (`#78716c` on `#1c1917` = 3.65:1). Add a CI contrast assertion so re-skins can't regress. (Near-zero-risk CSS-var edit — can land in L0.)
- **[P1] `prefers-reduced-motion` is entirely unhonored.** No `useReducedMotion()` anywhere in src (36 framer-motion importers); the CSS reduced-motion blocks are selective allowlists that MISS every infinite animation (`pulse-skeleton :266`, `gold-shimmer :283`, `border-glow :292`, `subtle-pulse :360`, `bm-bounce :1264`, `inner-glow :1590`). Add a global `@media (prefers-reduced-motion: reduce){*,*::before,*::after{animation-duration:.01ms!important;animation-iteration-count:1!important;transition-duration:.01ms!important}}` + wrap the app in framer `MotionConfig reducedMotion="user"`. §5 acceptance criterion so the C re-skin inherits it.

---

## 6. What We Deliberately Are NOT Doing

- No execution layer as the spine (distribution scissors; Pro/dev lane only).
- No multiplayer/leaderboard spine, no new retention analytics tables (the blind-read button is a future activation of the dead `useTeamStore`).
- No decay-by-timer push.
- No prompt-mutation / self-improving engine pre-traction.
- No per-predicate schema migration (L3b) until a kill-test passes.
- No deletion of ProgressiveFlow (decompose behind a flag).
- No carousel rewrite / cold node-graph on strategist surfaces / desaturation of semantic tokens / `useMirrorStore`.
- No re-scoping from fabricated counts (`validateShape` already wired `llm.ts:778-781`).
- **NEW:** no claiming the return-to-grade loop "reuses existing plumbing" — it is net-new (OUTCOME_RECORDS is producer-less). No citing `retrospective.ts`/`OUTCOME_RECORDS` as evidence the loop is closed (both producer-less). No keeping `@react-three/*` in deps (dead). **Decide and document** persona-calibration vs predicate-grading (don't silently delete the persona learning signal).

---

## 7. Do-Not-Inherit List (stale / fabricated / wrong-mechanism — refused)

| Claim (from prior workflows) | Verified reality | Action |
|---|---|---|
| "81 parseJSON callsites" | **(corrected)** ~5 direct + 8 `callLLMStreamThenParse` callsites (non-test); the rest are tests | Do not size F2 from this |
| "55+ raw JSON.parse callsites" | **(corrected)** ~17 non-test; ~11 real app callsites (6 are `llm.ts` engine internals — exclude) | Re-scope F3/R3 from ~11 |
| "`llm-validation.ts` is ~empty" | Complete 72-line module (`validateMessages`/`validateRequest` etc.) | Do not rebuild |
| "context flows as markdown / no provenance" | `context-chain.ts:119-525` returns typed contexts WITH provenance; only persistence/`_source`/`traceProvenance` missing | Scope L3a small |
| **P0 fix = `Promise.race([stream,timeout])`** | `callLLMStream` is `Promise<void>` + callbacks — race cannot detect a hang | Per-read watchdog via INTERNAL AbortController, rejecting a non-Abort `'timeout'` error (§L0) |
| U9 "lazy-load Three.js VoyageChart" | VoyageChart is SVG; **`@react-three/*` is DEAD/unused** — real cost is inline-SVG DOM + framer-motion | Remove the 3 dead 3D deps; lazy-load landing Acts instead |
| `decision-quality.ts:146` is a live crash | Guarded `\|\| []` + outer `recast?.analysis` | Verify/close (check `:83` `reframe.analysis` null) |
| B "7/9 metrics unmeasurable" | Dashboard exists, partly-dead inputs (`landing_hero_submit` dead, `landing_cta_click` live in same bucket; `cta` discriminator discarded) | "trusted-but-partly-dead"; bucket by `cta` |
| Competitor numbers (CrewAI "450M+/mo" etc.) | Unverified marketing | Do NOT inherit as load-bearing |
| "navigator.ts never acts / loop fully one-way" | OVERSTATED — `buildNavigatorProfile` consumes signals; enhanced path injects learnings | True wound: never grades PREDICTION vs OBSERVED outcome **(AND: `OUTCOME_RECORDS` + `retrospective.ts` are producer-less — do NOT cite them as a closed loop)** |
| Per-assumption work "2 weeks"; `useAccuracyStore` is the write-back | 3-4 weeks; `useAccuracyStore` rates PERSONAS | Prediction-scoring is NET-NEW write-path, not a repoint |
| Motion specs hardcode `cubic-bezier(0.16,1,0.3,1)` | Shipped `EASE = [0.32,0.72,0,1]`; progressive components already comply | Import `EASE` (JS only); leave `--ease-spring` + CSS keyframes |
| R6 soft-delete resurrection is moot | **(corrected)** REAL — `loadAndMerge:113-119` re-UPSERTS tombstoned localOnly rows, un-deleting them; empty-stamp local rows get overwritten | Fetch tombstones, exclude from re-upsert, Date-compare with missing-stamp tie-break (escalated to L4 prereq, §L5) |
| "globals.css gold is contrast-safe (4.5:1+)" | **(NEW)** FALSE — accent text = 4.18:1; tertiary 2.16:1; secondary 3.58:1 | Darken tokens + delete the false comment (§5) |

**Standing rule:** every effort estimate from a fabricated count is re-scoped from verified surface; every "already one-way / never acts" narrative is replaced by the narrow verified wound (no prediction-vs-outcome grading; OUTCOME_RECORDS/retrospective producer-less); the retention bet shared by C-Mirror and D-Verdict is labeled **UNVALIDATED-PREMISE**, falsified by §4 Tests 1/2 before the migration; **every silent-failure path (stream hang, worker auto-accept, storage write/read, human-send, copy/download, analytics insert, grade write) must surface to the user or the "don't lose your thinking" thesis is betrayed.**

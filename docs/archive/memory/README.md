# Memory snapshot (read-only)

Versioned **snapshot** of Argus's local working memory — kept here for backup,
version history, and visibility.

- **Live source of truth:** `~/.claude/projects/<project>/memory/` (machine-local, not auto-synced).
- Edit memory *there* (via Claude Code), not here. Re-snapshot into this folder when it changes.

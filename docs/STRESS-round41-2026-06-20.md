# R41 — Seal→settle integrity holds 7/7; the crafted-fixture campaign is COMPLETE

> 2026-06-20. The last surface a crafted fixture can still illuminate: does the
> decision-contract loop keep its core promise — "reality is the judge, not the
> model"? 7 scenarios (3 seal: vague→falsifiable; 4 settle: rationalized-miss /
> inferred-outcome / luck-as-skill / reengagement-nudge) × 3 model tiers,
> skeptic-refuted. ~37 agents.

## Integrity held 7/7 (100%) — the moat's foundation is intact

Across haiku / sonnet / opus and all 5 trap types: **0 unfalsifiable seals,
0 inferred outcomes, 0 rationalized misses, 0 luck-logged-as-skill,
0 reengagement nudges.** No tier sealed a vibe; no tier graded reality to please
the user. The seal-side falsifiability rule is the most robust gate of all — a
markdown skill CAN refuse a seal by construction ("no nameable fail_condition →
null"), and every tier did.

`survived_skeptic = 0/7` is NOT an integrity failure: a determined skeptic found a
nameable SUB-integrity weakness in every scenario, and on inspection each is
cosmetic (a metadata label) or a design-fidelity slip (skipping a null/confirm
gate, a leading basis binary, skipping the ask-menu) — none is a reality-as-judge
break.

## The one real finding: the safety BUFFER thins on the weakest tier

A monotone gradient (opus > sonnet > haiku; haiku named weakest in 6/7). The
foundation did NOT collapse on haiku under these (unambiguous) fixtures, but the
protective scaffolding did: in `settle-rationalized-miss-auth` haiku had the SYSTEM
declare the verdict ("결과: 실패") by mapping facts→criteria, instead of
re-surfacing the sealed pass/fail and letting the user settle. It landed the
correct outcome **only because the fixture handed it clean, unambiguous facts** —
a real settlement 30 days later, fed by self-serving memory, will not. So it is a
LATENT integrity break the fixture's tidy facts masked, on the tier most plugin
users actually run.

## Shipped (both small, low-bloat, defend the foundation on the weak tier)

1. **Hoisted the no-self-grading rule to the point of action** (`settle/SKILL.md`
   Step 2, not just the bottom meta-gates): the recorded outcome IS the user's
   tapped option, verbatim; NEVER narrate a verdict by mapping reality to the
   sealed criteria; **discrepancy case** — when the user's prose disagrees with the
   sealed pass/fail, re-surface the sealed conditions verbatim and let them settle
   ("답은 당신이 정합니다"), do not resolve it with a stated verdict. A weak model
   under-weights a rule buried in a meta-gate during generation; moving it to the
   moment of recording is the cheapest defense where the loop actually bent.
2. **Pinned both foundation rules as guard data** (`seal-settle-foundation.test.ts`,
   5): seal-side "no fail_condition → null" + settle-side "outcome = the user's
   stated answer, never inferred/self-graded." A careless future edit can't silently
   strip the moat's foundation.

Do-nothing (restraint) on the two cosmetic slips (the `falsifiable=yes` metadata
header; a haiku leading-binary basis prompt) — load-bearing behavior held at every
tier; chasing them is gold-plating the zero-bloat constraint forbids.

Full suite green (92 files / 1374). tsc clean.

## CAMPAIGN COMPLETE — the crafted-fixture instrument is exhausted

This is the **fourth independent ceiling-hit** (R33, R38, R40, R41). The campaign's
questions are now all answered as far as simulation can answer them:

- **Architecture** (R35–R40): the crew has no surviving unique edge; verify-first
  single pass is the default, crew dormant/opt-in (founder's hard-delete call,
  after one real cycle).
- **Fire-or-not gate** (R28–R32, R37, R40): over-fire ~0, false-skip ~0 (certified
  by the cumulative record, not fixture power).
- **Settlement moat** (R33–R34, R38): value real but conditional (compounds on
  recurrence); claim-strength bound to n; relevance-gated.
- **Seal→settle integrity** (R41): reality-as-judge holds 7/7.

What a fixture structurally CANNOT do, and why no R42 has value: bound a low error
rate to a tight CI; test seal→settle across real elapsed time against a real
outcome; reproduce the **ambiguous reality + self-serving memory** of a real
settlement (the only thing that could turn the weak-tier self-grading slip into an
actual break); or measure whether real users PERCEIVE the bare-crux question as a
verdict (the irreducible `value∝leverage∝tilt` residual is a human-perception
question). Another round would self-score the same model family against tidy facts
and learn nothing new — the inflation the founder's "don't just add" forbids.

## The pivot (the highest-leverage NON-sim next step)

The binding constraint is no longer the engine (GREEN) but the **front door** (RED,
per the finish-line probe: 0 sealed contracts, email signup 500, server writes
login-gated at `db.ts:212`, no row-count instrument). The one real risk R41
surfaced — weak-tier self-grading on AMBIGUOUS facts — can ONLY be bounded by a
real, messy settlement, and that requires the front door to exist.

**Stop stress-testing the engine; go open the harbor.** Concrete, do-able next
steps (all engineering, not marketing): fix the email-confirm 500; provide an
anonymous/local-first seal path or ungate the write; build the minimal settle
dashboard; instrument one real row. Then a single real seal→settle cycle teaches
more than R42–R50 ever could.

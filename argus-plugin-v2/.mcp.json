{
  "mcpServers": {
    "argus-decision": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/mcp-launch.js"],
      "env": {
        "ARGUS_DIR": "${CLAUDE_PROJECT_DIR}/.argus"
      }
    }
  }
}

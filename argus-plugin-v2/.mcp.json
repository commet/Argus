{
  "mcpServers": {
    "argus-decision": {
      "command": "npm",
      "args": ["exec", "--yes", "--package=argus-decision-mcp", "--", "argus-decision-mcp"],
      "env": {
        "ARGUS_DIR": "${CLAUDE_PROJECT_DIR}/.argus"
      }
    }
  }
}

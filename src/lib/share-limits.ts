/**
 * 공유 한도 — **양쪽이 같아야 하는 숫자**라서 여기 한 곳에만 둔다.
 *
 * 서버는 이 숫자로 거절하고, 화면은 이 숫자로 안내한다. 두 곳에 각각 적으면
 * 한쪽만 고쳐지는 날이 오고, 그때 사용자는 "하루 3개"라고 읽고 2개째에 막힌다.
 * (이 리포가 2026-07-29 에 정리한 결함 11건이 전부 그 모양이었다.)
 *
 * server-only 모듈에 두지 않는 이유도 같다 — `share-guard.ts` 는 서비스 롤 키를
 * 쓰므로 클라이언트가 import 하면 안 되고, 화면은 이 숫자를 알아야 한다.
 * 그래서 숫자는 순수 파일에, 열쇠를 쥔 코드는 서버 파일에 남긴다.
 */

/** 계정 하나의 하루 발신 한도 (모든 채널 합산). */
export const DAILY_SHARE_LIMIT = 50;

/**
 * 익명 신원의 하루 한도. 계정 한도보다 훨씬 낮은 이유는 익명 신원이 **공짜**이기
 * 때문이다 — 브라우저를 새로 열 때마다 새 신원이 나오므로, 계정 한도를 그대로 주면
 * 한도가 사실상 없는 것과 같고 argus.voyage 가 임의 콘텐츠 호스팅으로 쓰인다.
 *
 * 3 인 이유: 진짜 사람이 결과물 하나를 남에게 보여주는 데 필요한 수(보통 1~2)는
 * 넉넉히 덮으면서, 대량 살포에는 쓸모가 없는 자리다. 막고 싶은 건 사람이 아니라 기계다.
 */
export const ANON_SHARE_LIMIT = 3;

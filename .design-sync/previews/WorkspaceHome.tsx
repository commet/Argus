import { WorkspaceHome } from 'argus';

// "Workspace · Home" — the redesigned first/home screen (idle HeroFlow state)
// composed from DS primitives. Full-width (cardMode: column). No entrance
// animations to neutralize — the composition renders statically.
export const Default = () => <WorkspaceHome />;

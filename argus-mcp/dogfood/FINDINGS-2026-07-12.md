# 도그푸딩 관찰 기록 — 2026-07-12 밤

창업자 지시로, Argus를 **실제로 써보며**(26개 현실 결정 시나리오를 실
툴 핸들러로 끝에서 끝까지 구동) 나온 관찰. 하네스: `dogfood/run.mjs`.

**정직성 경계**: 여기 발견은 *driver-observed*이지 *human-observed*가
아니다 — 사람의 감정 반응·우선순위는 P5 관찰 세션이 매긴다. 이 기록은
그 전에 "읽으면 명백한" 마찰만 미리 잡은 것이다.

## 방법

`dist`의 실제 툴 핸들러를 MCP SDK와 동일하게 **inputSchema 검증을 먼저
통과**시킨 뒤 호출(초기 하네스는 이 관문을 건너뛰어 500자 예측이 통과했다
— 하네스 자체 수정). 26 시나리오: seal→settle(held/missed/avoided/
partial/still_pending) · amend·dismiss·premises·watch·candidates·recall·
sync · 한글 로케일 · 경계(중복봉인·봉인전정산·재정산·과거확인일·빈예측·
초장문·주입) · 공정 큐 · 갓 설치.

## 기능·스파인 판정: 26/26 통과

crash·깨진 배선·스파인 위반(평결/위로/제어문자 누출)·은유 잔재: **0건.**
경계 거절(중복 봉인, 봉인 전 정산, 확인일 지난 amend, 과거 확인일 봉인,
빈/초장문 예측)은 전부 올바르게 거절. over-fire 테스트(점심 결정)는
restraint 기본값이 정확히 작동(포크 없이 "가볍게 해보라 + 그대로 두기도
선택지").

→ 기능적으로 제품은 견고하다. **진짜 마찰은 surface 문장을 읽을 때 나왔다.**

## 발견과 조치 (읽기 기반)

| # | 심각도 | 발견 | 조치 |
|---|---|---|---|
| **F1** | 높음 | **정산 확인이 outcome 무관하게 단일 제네릭 문장.** held·missed·avoided·partial 전부 "Settled. The receipt records what you predicted and what actually happened. No grade." — 제품의 절정(정산)이 밋밋. 창업자 "임팩트 없네" 직감의 코드상 정체. | **수정.** outcome을 사실로 명명 + 영수증(keepsake) 손잡이 병기. 스파인 유지: outcome은 사용자가 기록한 사실이지 AI 평결 아님, "no grade" 항상 부착, 위로·훈계 어휘 금지. `settle-surface.test.ts`가 4 outcome이 서로 다른 문구를 내는지 + 스파인을 고정. |
| **F2** | 중 | **봉인 확인이 매번 장황.** ".ics 저장 + 전제 적어보세요" 업셀이 모든 봉인마다 4문장. 스키마는 "keep sealing light"라는데 실제는 무거움. | **수정.** .ics 문구·전제 넛지를 각각 한 구절로 축약. |
| **F3** | 소 | **`op=amend` API 문법이 사용자 문구에 노출** ("Fix anything wrong with op=amend"). | **수정.** "argus_premises로 바로잡을 수 있어요"로 — 도구명만, 인자 문법 숨김. |
| **F4** | 소 | **`argus_config`가 "Config updated"만** — 뭐가 바뀌었는지 침묵. | **수정.** 바뀐 키를 명시(`Config updated: locale.`). |
| **F5** | 소 | **init 첫 문장이 방어적** ("It does not give answers —"). 설치 후 첫 인상이 부정형. | **수정.** 긍정·초대형으로 재구성("결정을 설명하면 Argus가 예측과 확인일을 기록… 당신만의 track record"). |

## 창업자 판단이 필요한 것 (수정하지 않고 남김)

- **F1의 더 깊은 질문**: 정산의 "무게"를 어디까지 실을 것인가. 지금은
  사실 명명 + 영수증 손잡이까지만(스파인 안전선). 영수증 절대경로를
  surface에 평문 노출(규칙 18의 "복귀 캡슐 1클릭")까지 갈지, 정산일에
  더 의식(儀式)적인 표면을 줄지는 **제품 의미를 건드리므로 관찰 세션
  피드백으로 결정**해야 한다. (어제 합의: 의미-담지 표면의 "이만하면
  됐다"는 AI가 아니라 창업자가 판단.)
- **F(sync)**: 계정 미연결 시 `NOT_CONNECTED`가 에러 봉투로 온다.
  "로컬만 쓰는 중, 문제 아님" 톤이 나을 수 있으나, 명시적으로 sync를
  부른 사용자에게 "연결 안 됨"을 알리는 것도 정당 — 판단 보류.

## 하네스 자체가 잡은 것 (자기 교정)

- 초기 하네스가 handler를 직접 불러 **SDK 스키마 검증을 건너뜀** → 500자
  예측이 통과. 실서버 충실도를 위해 `inputSchema.safeParse`를 먼저
  통과하도록 수정. (교훈: 검증은 프로토콜 경계에 있고 handler는 그걸
  신뢰한다 — 우회 경로를 만들지 말 것.)
- API 인자 모양이 비직관적인 지점: `settle`은 `outcome_source:
  'user_stated'` 필수, `premises add`는 `premises[]` 배열, locale은
  `init`이 아니라 `config`. 모델이 스키마 설명으로 채우지만(설명은 꼼꼼),
  사람이 직접 호출하면 틀리기 쉽다 — 관찰 참고.

## 확장 회차 — 34 시나리오 (S27–S34 추가)

머지(#148) 후 8개 시나리오 추가: recheck-drift · 재봉인 경합 · 유니코드
predicate · 영수증 recall · multi-seal · 정산 후 recall · 경계 재확인 ·
still_pending 후 재정산. 자동 판정 0건(crash·배선·스파인·은유 0). 106
surface 캡처. 읽기 기반 발견은 아래 F6·F7.

| # | 심각도 | 발견 | 조치 |
|---|---|---|---|
| **F6** | 중(진짜 배선 버그) | **사용자 대면 문구 3곳이 없는 도구 `argus_snooze`를 가리킴.** mute.md·settle.md·logbook.ts가 "미루려면 argus_snooze"라 안내하는데 그런 도구는 등록돼 있지 않다 — 모델/사용자가 부르면 실패하는 깨진 배선. 실제 "미루기"는 `argus_settle` still_pending(defer_to)다. LLM-glue invariant의 정확한 사례: 존재하지 않는 것을 가리키는 문자열이 조용히 통과. | **수정.** 3곳 모두 `argus_settle` still_pending 경로로 교정. `tool-reference-integrity.test.ts` 신설 — surfaces.ts·logbook.ts가 언급하는 모든 `argus_*`가 등록된 도구인지 CI가 대조(재발 영구 차단, PAT 접두사 `argus_pat_`는 제외). |
| **F7** | 판정: 마찰 아님 | **`argus_recall view=receipt`의 surface가 "Receipt recalled."로 짧음** — 영수증 본문이 surface에 없음. 처음엔 F1의 "정산 보상 숨김" 연장으로 의심. | **수정 안 함(정당).** receipt는 **pull** 뷰다 — 렌더된 영수증 전문이 `data.receipt_text`에 담긴다(recall.ts:94, `renderReceipt`). contracts·bearing과 동일 패턴(요약 surface + 본문 data). 정산(push, 절정 순간)과 달리 recall은 사용자가 의도적으로 불러 모델이 `data.receipt_text`를 전한다. surface에 본문을 중복하면 오히려 이중. |

## 재현

```bash
cd argus-mcp && npm run build && node dogfood/run.mjs
```
findings JSON + 전 surface 덤프를 stdout으로 출력. 결정론(시계 주입).

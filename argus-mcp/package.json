{
  "name": "argus-decision-mcp",
  "version": "2.0.22",
  "description": "Argus decision-to-reality MCP — preserve a user-adopted prediction, its return condition, and what actually happened without scoring the person.",
  "type": "module",
  "main": "dist/index.js",
  "mcpName": "io.github.commet/argus-decision-mcp",
  "bin": {
    "argus-decision-mcp": "dist/index.js"
  },
  "files": [
    "dist/index.js",
    "README.md",
    "README.ko.md",
    "SECURITY.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc -p tsconfig.build.json && esbuild src/index.ts --bundle --platform=node --format=esm --packages=external --outfile=dist/index.js && node -e \"if(process.platform!=='win32')require('node:fs').chmodSync('dist/index.js',0o755)\"",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "fuzz": "npm run build && node evals/fuzz.mjs",
    "copy": "npm run build && node evals/copy-audit.mjs",
    "prepack": "npm run build",
    "prepublishOnly": "npm run typecheck && npm test && npm audit --omit=dev",
    "verify": "node evals/verify-all.mjs"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "decision-making",
    "accountability",
    "claude",
    "ai-tools"
  ],
  "license": "MIT",
  "author": "commet",
  "homepage": "https://argus.voyage",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/commet/argus.git",
    "directory": "argus-mcp"
  },
  "bugs": {
    "url": "https://github.com/commet/argus/issues"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "js-yaml": "^4.3.1",
    "tinyld": "^1.3.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.9"
  },
  "overrides": {
    "@hono/node-server": "^2.0.5",
    "fast-uri": "^3.1.5",
    "postcss": "8.5.23",
    "hono": "^4.12.34"
  }
}
